[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-avatar-backend"
version = "0.1.0"
description = "AI Avatar Platform - Backend API"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "sqlalchemy[asyncio]>=2.0.35",
    "aiosqlite>=0.20.0",
    "alembic>=1.13.0",
    "pydantic>=2.0",
    "pydantic-settings>=2.5.0",
    "python-jose[cryptography]>=3.3.0",
    "passlib[bcrypt]>=1.7.4",
    "bcrypt<4.1",
    "httpx>=0.27.0",
    "python-multipart>=0.0.9",
    "websockets>=13.0",
    "sse-starlette>=2.0.0",
    "slowapi>=0.1.10",
    "openai>=1.50.0",
    "anthropic>=0.40.0",
    "openpyxl>=3.1.0",
    "aiofiles>=23.0.0",
    "azure-identity>=1.17.0",
    "azure-storage-blob>=12.20.0",
    "azure-ai-projects>=2.3.0",  # v2.3+ required: Skills API create_from_files (create_from_package removed), top-level client.toolboxes
    "python-pptx>=1.0.0",
    "python-docx>=1.1.0",
    "pdfplumber>=0.10.0",
    "mcp>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "pytest-timeout>=2.2.0",
    "pytest-cov>=5.0.0",
    "pytest-html>=4.1.0",
    "ruff>=0.6.0",
]
postgresql = [
    "asyncpg>=0.29.0",
    "psycopg2-binary>=2.9.0",
]
voice = [
    "azure-cognitiveservices-speech>=1.48.0",
    # TEMPORARY: GA 1.3.0 not yet on PyPI as of 2026-07-19; api_version="2026-07-15" is
    # explicitly passed at every connect() call site to override the beta default —
    # see Plan 29-02/29-06/29-07.
    "azure-ai-voicelive[aiohttp]==1.3.0b1",
]
all = [
    "ai-avatar-backend[dev,postgresql,voice]",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
timeout = 60
# TODO: raise to 95 per project testing standard once coverage improves (see 29-10-SUMMARY.md for baseline)
addopts = "-m 'not integration' --cov=app --cov-fail-under=89"
markers = [
    "integration: mark test as requiring real Azure credentials (deselect with '-m \"not integration\"')",
]

[tool.coverage.run]
concurrency = ["greenlet", "thread"]

[tool.ruff]
target-version = "py311"
line-length = 100
exclude = ["alembic/"]

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP"]
ignore = []

[tool.ruff.lint.isort]
known-first-party = ["app"]

[tool.ruff.format]
quote-style = "double"

[tool.pyright]
pythonVersion = "3.11"
pythonPlatform = "All"
venvPath = "."
venv = ".venv"
include = ["app", "tests", "scripts"]
typeCheckingMode = "basic"
