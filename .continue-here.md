# Continue Here — Phase 17 Agent Knowledge Base (Foundry IQ)

**Paused at**: 2026-04-10
**Phase**: 17 — Agent Knowledge Base Foundry IQ Integration
**Branch**: main

---

## Current Position

All Phase 17 backend + frontend + docs work is **complete and pushed**. The critical fix (MCPTool vs AzureAISearchTool) was just committed and pushed.

## What Was Completed

1. **KB listing** — Uses `/knowledgebases?api-version=2025-11-01-preview` API (not `/indexes`)
2. **KB connect dialog** — Shows only KB name (no long markdown description)
3. **KB UI placement** — Inside Voice & Avatar tab's "知识库与工具" section (not separate tab)
4. **Agent sync** — `build_search_tools()` creates MCPTool pointing to KB MCP endpoint:
   - `server_url`: `{search_endpoint}/knowledgebases/{kb_name}/mcp?api-version=2025-11-01-preview`
   - `allowed_tools`: `MCPToolFilter(tool_names=["knowledge_base_retrieve"])`
   - `require_approval`: `"never"`
5. **Tests** — 38 tests passing in `test_knowledge_base.py`
6. **Documentation** — `docs/microsoft-agent-framework/06-agent-tools-and-knowledge-grounding.md` rewritten

## Key Discovery (CRITICAL — do not revert)

- `AzureAISearchTool` → appears in Portal **Tools** section (wrong)
- `MCPTool` + KB MCP endpoint → appears in Portal **Knowledge** section (correct)
- Verified via Agent version history: v31 (portal Knowledge) = MCPTool, v32 (our AzureAISearchTool) = Tools

## Pending Verification

- [ ] User needs to save an HCP Profile in UI to trigger agent resync
- [ ] Check AI Foundry Portal → Agent → Knowledge section shows the connected KB
- [ ] If Knowledge section still empty, investigate MCPTool auth (KB MCP endpoint may need API key via RemoteTool connection)

## Key Files Changed

| File | Change |
|------|--------|
| `backend/app/services/knowledge_base_service.py` | `build_search_tools()` → MCPTool instead of AzureAISearchTool |
| `backend/tests/test_knowledge_base.py` | Updated test for MCPTool format |
| `docs/microsoft-agent-framework/06-agent-tools-and-knowledge-grounding.md` | Complete rewrite with correct mechanism |

## Potential Blocker

The KB MCP endpoint may require authentication. Portal uses a **RemoteTool connection** (with embedded API key) as intermediary. Our MCPTool uses `server_url` directly. If the endpoint rejects unauthenticated requests, we may need to:
1. Use `MCPTool(project_connection_id="kb-omada-product-param-e88p4")` referencing existing RemoteTool connection
2. Or create RemoteTool connections programmatically (data-plane API doesn't support creation — needs ARM API or portal)

## Resume Command

```bash
cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding
# Ask user to verify Knowledge section in Portal first, then address any remaining issues
```
