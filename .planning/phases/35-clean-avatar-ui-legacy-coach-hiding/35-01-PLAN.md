---
phase: 35-clean-avatar-ui-legacy-coach-hiding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/anonymous-avatar-qa.spec.ts
  - .planning/phases/35-clean-avatar-ui-legacy-coach-hiding/35-E2E-BASELINE.md
autonomous: true
requirements: [AVUI-01]

must_haves:
  truths:
    - "Visiting / with no auth state renders no <nav> element anywhere on the page (no coach/admin chrome leaks into the avatar experience)"
    - "The existing answer/citation structural separation (transcript vs SourcesPanel) still passes unchanged -- this phase verifies, does not rearchitect"
    - "A fresh full E2E baseline (pass/skip/fail counts + failing test titles) is captured and recorded before any AVUI-02 code change lands in Plan 35-02"
  artifacts:
    - path: "frontend/e2e/anonymous-avatar-qa.spec.ts"
      provides: "New chrome-absence negative assertion (D-06): expect(page.locator('nav')).toHaveCount(0) on /"
    - path: ".planning/phases/35-clean-avatar-ui-legacy-coach-hiding/35-E2E-BASELINE.md"
      provides: "Pre-change full E2E baseline snapshot (D-10) used as the zero-new-failures diff target for Plan 35-02"
  key_links:
    - from: "frontend/e2e/anonymous-avatar-qa.spec.ts"
      to: "frontend/src/pages/avatar-page.tsx"
      via: "page.locator(\"nav\") count assertion against the rendered AvatarPage DOM"
      pattern: "page\\.locator\\(\"nav\"\\)\\.toHaveCount\\(0\\)"
---

<objective>
Lock in AVUI-01 ("avatar page shows only digital human + document-links panel, voice content and sources visually separate") as a regression-proof, automatically-verified fact, and capture the pre-change full E2E baseline that Plan 35-02 will diff against.

Per CONTEXT.md D-05, `frontend/src/pages/avatar-page.tsx` already structurally satisfies AVUI-01 (standalone route, not wrapped by `UserLayout`/`AdminLayout`; `handleSend` never concatenates `data.answer` into `SourcesPanel` or vice versa; `avatar-page.test.tsx` already proves the data-separation contract). This plan does NOT touch `avatar-page.tsx` or any component under it -- it adds the one missing regression guard (a *visual chrome-absence* E2E assertion, per D-06) and captures the baseline required by D-10 before any subsequent code change in this phase.

Purpose: Close the one Wave 0 gap RESEARCH.md/35-VALIDATION.md identified for AVUI-01 (no test currently asserts chrome-absence) and produce the artifact Plan 35-02's zero-new-failures gate depends on.
Output: Extended `anonymous-avatar-qa.spec.ts` with a chrome-absence test; `35-E2E-BASELINE.md` recording the current full E2E pass/skip/fail state.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-clean-avatar-ui-legacy-coach-hiding/35-CONTEXT.md
@.planning/phases/35-clean-avatar-ui-legacy-coach-hiding/35-RESEARCH.md
@.planning/phases/35-clean-avatar-ui-legacy-coach-hiding/35-UI-SPEC.md
@.planning/phases/34-spanish-es-i18n/deferred-items.md
@frontend/e2e/anonymous-avatar-qa.spec.ts
@frontend/src/pages/avatar-page.tsx
</context>

<interfaces>
<!-- No new interfaces created or consumed by this plan -- it is test-only. -->
<!-- Reference: avatar-page.tsx's header (lines 254-257) renders a <header> containing a <span> and (conditionally) a <Badge>/<Button> -- it never renders a <nav> element. UserLayout (untouched by this plan) is the only component in the app that renders <nav>. This is why `page.locator("nav")` count 0 on `/` is a true structural assertion, not an implementation-detail-coupled one (per UI-SPEC's resolution). -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-change full E2E baseline</name>
  <files>.planning/phases/35-clean-avatar-ui-legacy-coach-hiding/35-E2E-BASELINE.md</files>
  <action>
Run the full Playwright E2E suite exactly as-is (no code changes yet in this phase) and record the result before touching any file, per CONTEXT.md D-10 and RESEARCH.md Pitfall 4.

1. From `frontend/`, run: `npm run test:e2e -- --reporter=list 2>&1 | tee /tmp/35-e2e-baseline-raw.txt`
2. Extract the final summary line (e.g. "415 passed, 9 skipped, 35 failed") and the full list of failing test titles (grep `✘`/`failed` lines from the raw output).
3. Write `.planning/phases/35-clean-avatar-ui-legacy-coach-hiding/35-E2E-BASELINE.md` containing:
   - Captured date/time
   - Total pass/skip/fail counts
   - The complete list of failing test titles (file:line + test name), one per line
   - A one-line note: "This baseline was captured before any Phase 35 code change. Plan 35-02's regression gate must show zero tests newly failing relative to this list (pre-existing failures below are out of scope to fix per CLAUDE.md one-requirement-at-a-time rule)."
4. Sanity-check the captured counts are in the same order of magnitude as the previously-documented ~415/9/35 split (per `.planning/phases/34-spanish-es-i18n/deferred-items.md` addendum) -- if wildly different (e.g. E2E suite errors out entirely), stop and report rather than writing a broken baseline.
  </action>
  <verify>
    <automated>test -f .planning/phases/35-clean-avatar-ui-legacy-coach-hiding/35-E2E-BASELINE.md && grep -q "passed" .planning/phases/35-clean-avatar-ui-legacy-coach-hiding/35-E2E-BASELINE.md</automated>
  </verify>
  <done>35-E2E-BASELINE.md exists with pass/skip/fail counts and the full list of currently-failing test titles, captured before any Phase 35 code change.</done>
</task>

<task type="auto">
  <name>Task 2: Add chrome-absence E2E assertion and verify AVUI-01 compliance</name>
  <files>frontend/e2e/anonymous-avatar-qa.spec.ts</files>
  <action>
Extend the existing spec (per UI-SPEC's resolution: extend `anonymous-avatar-qa.spec.ts` rather than create a new file) with one new test inside the `test.describe("Anonymous avatar Q&A", ...)` block that proves no nav/sidebar/coach chrome renders on `/`.

Add, after the existing "visiting / with no auth state..." test:

```typescript
test("the avatar page renders no nav, sidebar, or coach chrome -- only the avatar experience", async ({
  page,
}) => {
  await mockSession(page);
  await mockWebrtcSessionFailure(page);

  await page.goto("/");
  await dismissMicDialogIfOpen(page);

  // AVUI-01: AvatarPage is a standalone route, never wrapped by UserLayout or
  // AdminLayout -- neither of which is reachable from an anonymous session.
  // UserLayout/AdminLayout are the only components in the app that render a
  // <nav> element, so this is a true structural assertion, not an
  // implementation-detail-coupled one (per 35-UI-SPEC.md's resolution).
  await expect(page.locator("nav")).toHaveCount(0);

  // The only chrome on / is the avatar's own minimal header (title span +
  // login button/badge) -- confirm the input/transcript/sources surfaces are
  // present alongside the chrome-absence, so this isn't a false-positive
  // from a broken/blank page.
  await expect(page.getByRole("textbox")).toBeVisible();
});
```

Do NOT modify `avatar-page.tsx`, `sources-panel.tsx`, or any other production component -- per D-05, this task is verification-only. If the new assertion fails, that is a signal to investigate (report as a blocker), not a license to add layout wrapping.

After adding the test, run the verification commands below to confirm both the new assertion and the pre-existing AVUI-01 data-separation contract (ANON-03/PERS-02 guard tests in `avatar-page.test.tsx`) remain green.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/avatar-page.test.tsx && npx playwright test anonymous-avatar-qa.spec.ts --config=e2e/playwright.config.ts</automated>
  </verify>
  <done>anonymous-avatar-qa.spec.ts contains the new chrome-absence test and it passes; avatar-page.test.tsx's existing structural-separation and PERS-02 guard tests remain green (AVUI-01 verified, not modified).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Anonymous visitor -> `/` (AvatarPage) | Unchanged this plan -- existing public route, no auth required (ANON-01). This plan only adds a test-side assertion against the already-rendered DOM; it introduces no new client-server boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-35-01-01 | Elevation of Privilege (misconception risk) | `anonymous-avatar-qa.spec.ts` new assertion | accept | Test-only change, zero production code touched. The assertion does not create or alter any access-control boundary -- it verifies UI composition (chrome-absence), which is unrelated to authorization. No new attack surface. |
| T-35-01-02 | Information Disclosure | `35-E2E-BASELINE.md` (planning artifact) | accept | Contains only test-name/pass-fail metadata already visible in CI logs; no secrets, tokens, or PII are captured by a Playwright summary run. |
</threat_model>

<verification>
1. `cd frontend && npx vitest run src/pages/avatar-page.test.tsx` -- existing AVUI-01/ANON-03/PERS-02 tests remain green (verification, not regression).
2. `cd frontend && npx playwright test anonymous-avatar-qa.spec.ts --config=e2e/playwright.config.ts` -- all tests in the extended spec pass, including the new chrome-absence assertion.
3. `.planning/phases/35-clean-avatar-ui-legacy-coach-hiding/35-E2E-BASELINE.md` exists and contains a full pass/skip/fail snapshot with failing-test titles, captured before this plan's own commit (Task 1 must run first, against a clean pre-Phase-35 tree).
</verification>

<success_criteria>
- AVUI-01 is now regression-proof: a dedicated automated E2E assertion (`toHaveCount(0)` on `nav`) fails loudly if any future change reintroduces coach/admin chrome onto `/`.
- No production code was modified -- `avatar-page.tsx` and its subcomponents are untouched, honoring D-05.
- A committed, timestamped E2E baseline exists for Plan 35-02 to diff against, satisfying D-10's "zero newly-failing tests" acceptance bar.
- AVUI-01 requirement can be marked complete in REQUIREMENTS.md/ROADMAP.md traceability once this plan's tests are green.
</success_criteria>

<output>
After completion, create `.planning/phases/35-clean-avatar-ui-legacy-coach-hiding/35-01-SUMMARY.md`
</output>
