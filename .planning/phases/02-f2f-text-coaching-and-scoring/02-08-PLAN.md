---
phase: 02-f2f-text-coaching-and-scoring
plan: 08
type: execute
wave: 4
depends_on: ["02-03", "02-04", "02-06", "02-07"]
files_modified:
  - frontend/src/router/index.tsx
  - frontend/src/components/layouts/admin-layout.tsx
  - backend/app/api/azure_config.py
  - backend/app/api/__init__.py
  - backend/app/main.py
autonomous: false
requirements:
  - PLAT-03
  - COACH-01
  - SCORE-01

must_haves:
  truths:
    - "All new pages are accessible via router (admin HCP profiles, scenarios, azure config; user scoring feedback)"
    - "Admin sidebar shows links to HCP Profiles, Scenarios, and Azure Config pages"
    - "Azure config API endpoint exists for connection testing (PLAT-03)"
    - "Full flow works: login -> select scenario -> chat -> end session -> see scores"
    - "Frontend builds without errors"
    - "Backend starts without errors"
  artifacts:
    - path: "frontend/src/router/index.tsx"
      provides: "Updated router with all Phase 2 routes"
      contains: "hcp-profiles"
    - path: "backend/app/api/azure_config.py"
      provides: "Azure config API endpoint"
      contains: "router = APIRouter"
  key_links:
    - from: "frontend/src/router/index.tsx"
      to: "frontend/src/pages/admin/hcp-profiles.tsx"
      via: "route definition"
      pattern: "hcp-profiles.*HcpProfilesPage"
    - from: "frontend/src/router/index.tsx"
      to: "frontend/src/pages/user/scoring-feedback.tsx"
      via: "route definition"
      pattern: "scoring.*ScoringFeedback"
---

<objective>
Wire all Phase 2 pages into the router, update admin sidebar navigation, create Azure config backend endpoint (PLAT-03), and verify the full end-to-end flow.

Purpose: Connect all the pieces built in Plans 01-07 into a working application. Final integration and verification.
Output: Updated router, admin layout, Azure config API, verified full flow.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-f2f-text-coaching-and-scoring/02-CONTEXT.md
@.planning/phases/02-f2f-text-coaching-and-scoring/02-UI-SPEC.md
@.planning/phases/02-f2f-text-coaching-and-scoring/02-03-SUMMARY.md
@.planning/phases/02-f2f-text-coaching-and-scoring/02-04-SUMMARY.md
@.planning/phases/02-f2f-text-coaching-and-scoring/02-06-SUMMARY.md
@.planning/phases/02-f2f-text-coaching-and-scoring/02-07-SUMMARY.md

<interfaces>
From frontend/src/router/index.tsx (current):
```typescript
export const router = createBrowserRouter([
  // GuestRoute -> AuthLayout -> LoginPage
  // ProtectedRoute -> UserLayout -> dashboard, training
  // ProtectedRoute -> TrainingSession (outside UserLayout, full-screen)
  // AdminRoute -> AdminLayout -> AdminDashboard
]);
```

From frontend/src/components/layouts/admin-layout.tsx:
```typescript
// Admin sidebar with navigation links
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update router, admin sidebar, and create Azure config API</name>
  <files>
    frontend/src/router/index.tsx
    frontend/src/components/layouts/admin-layout.tsx
    backend/app/api/azure_config.py
    backend/app/api/__init__.py
    backend/app/main.py
  </files>
  <read_first>
    frontend/src/router/index.tsx
    frontend/src/components/layouts/admin-layout.tsx
    frontend/src/pages/admin/hcp-profiles.tsx
    frontend/src/pages/admin/scenarios.tsx
    frontend/src/pages/admin/azure-config.tsx
    frontend/src/pages/user/scoring-feedback.tsx
    backend/app/api/__init__.py
    backend/app/api/config.py
    backend/app/main.py
    backend/app/config.py
    backend/app/dependencies.py
  </read_first>
  <action>
**frontend/src/router/index.tsx** — Add all Phase 2 routes:

Add imports at top:
```typescript
import HcpProfilesPage from "@/pages/admin/hcp-profiles";
import ScenariosPage from "@/pages/admin/scenarios";
import AzureConfigPage from "@/pages/admin/azure-config";
import ScoringFeedback from "@/pages/user/scoring-feedback";
```

Add routes inside the existing structure:
- Under ProtectedRoute > UserLayout children (after training):
  ```typescript
  { path: "scoring/:sessionId", element: <ScoringFeedback /> },
  ```
  Or if using search params: keep the route as `{ path: "scoring", element: <ScoringFeedback /> }` and read sessionId from URL search params.

- Under AdminRoute > AdminLayout children (after dashboard):
  ```typescript
  { path: "hcp-profiles", element: <HcpProfilesPage /> },
  { path: "scenarios", element: <ScenariosPage /> },
  { path: "azure-config", element: <AzureConfigPage /> },
  ```

The final router should include all of these route paths:
- `/login` (existing)
- `/user/dashboard` (existing)
- `/user/training` (existing, enhanced in Plan 07)
- `/user/training/session` (existing, enhanced in Plan 07)
- `/user/scoring` or `/user/scoring/:sessionId` (new)
- `/admin/dashboard` (existing)
- `/admin/hcp-profiles` (new)
- `/admin/scenarios` (new)
- `/admin/azure-config` (new)

**frontend/src/components/layouts/admin-layout.tsx** — Add sidebar nav items:

Read the current admin layout sidebar navigation. Add 3 new nav items after the dashboard link:
- HCP Profiles: icon `Users` (from lucide-react), path `/admin/hcp-profiles`, label via `t("nav:hcpProfiles")` or fallback "HCP Profiles"
- Scenarios: icon `BookOpen` or `Target`, path `/admin/scenarios`, label "Scenarios"
- Azure Config: icon `Cloud` or `Settings`, path `/admin/azure-config`, label "Azure Config"

Add corresponding i18n keys if not already present in nav.json.

**backend/app/api/azure_config.py** — Azure service configuration endpoint for PLAT-03:

```python
router = APIRouter(prefix="/azure-config", tags=["azure-config"])
```

Admin-only endpoints:
- `GET /services` — List all Azure services with their current config status (connected/not configured). Returns service name, status (based on whether endpoint and key are set in settings), and masked key (last 4 chars).
- `PUT /services/{service_name}` — Update service config (endpoint, api_key, model, region). Accepts JSON body. For MVP, store in settings or a config table. For simplest approach: write to `.env` file or a separate config store.
- `POST /services/{service_name}/test` — Test connection to a specific Azure service. For Azure OpenAI: make a simple API call. For mock/MVP: return success if endpoint and key are non-empty strings matching expected format (starts with `https://`).

For MVP implementation, the simplest approach:
- Read current config from `get_settings()` (env vars)
- Test endpoint does a basic HTTP health check or OpenAI list-models call
- Update endpoint stores in a JSON config file (since env vars can't be hot-reloaded easily)
- OR: just validate the format and return success for demo purposes

**backend/app/api/__init__.py** — Add azure_config_router:
```python
from app.api.azure_config import router as azure_config_router
```
Add to `__all__`.

**backend/app/main.py** — Register azure_config_router:
```python
app.include_router(azure_config_router, prefix=settings.api_prefix)
```

Note: Merge with existing router registrations from Plans 03 and 04. The final main.py should include ALL routers: auth, config, hcp_profiles, scenarios, sessions, scoring, azure_config.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc --noEmit 2>&1 | head -20 && cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend && python -c "from app.main import app; routes = [r.path for r in app.routes]; print('Backend routes:', len(routes), 'routes'); print([r for r in routes if 'azure' in r or 'hcp' in r or 'session' in r or 'scoring' in r])"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/router/index.tsx contains `hcp-profiles` route path
    - frontend/src/router/index.tsx contains `scenarios` route path under admin
    - frontend/src/router/index.tsx contains `azure-config` route path
    - frontend/src/router/index.tsx contains `scoring` route path under user
    - frontend/src/router/index.tsx imports HcpProfilesPage, ScenariosPage, AzureConfigPage, ScoringFeedback
    - frontend/src/components/layouts/admin-layout.tsx contains nav link for "hcp-profiles"
    - frontend/src/components/layouts/admin-layout.tsx contains nav link for "scenarios"
    - backend/app/api/azure_config.py contains `router = APIRouter(prefix="/azure-config"`
    - backend/app/api/azure_config.py contains `require_role("admin")`
    - backend/app/main.py contains `azure_config_router`
    - `npx tsc --noEmit` passes
    - Backend app loads without error
  </acceptance_criteria>
  <done>All routes registered. Admin sidebar updated with Phase 2 nav links. Azure config API created. Router wires all pages together.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 2 end-to-end flow</name>
  <files>none</files>
  <action>
Human verification of the complete Phase 2 implementation. All automated work is done in Task 1 and prior plans. This checkpoint confirms the full flow works visually and functionally.

Steps for human verifier:
1. Start backend: `cd backend && python scripts/seed_phase2.py && uvicorn app.main:app --reload --port 8000`
2. Start frontend: `cd frontend && npm run dev`
3. Login as admin (admin/admin123 or per seed data)
4. Navigate to Admin > HCP Profiles — verify list/edit form with personality sliders
5. Navigate to Admin > Scenarios — verify table with scoring weight sliders totaling 100%
6. Navigate to Admin > Azure Config — verify service cards render
7. Login as user (user/user123)
8. Navigate to Training — verify scenario cards from backend API
9. Click "Start Training" on a scenario — verify 3-column coaching page loads
10. Type a message and send — verify HCP response streams in via SSE
11. Check left panel: key message checklist should update
12. Check right panel: coaching hints should appear
13. Click "End Session" — verify scoring page loads with radar chart and feedback cards
14. Check scoring page has dimension bars with color coding (green/orange/red)
15. Verify "Back to Dashboard" button works
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npm run build && cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend && ruff check . && python -c "from app.main import app; print('Backend OK')"</automated>
  </verify>
  <done>Human has verified: admin CRUD for HCPs and scenarios works, F2F coaching chat streams correctly, scoring feedback page displays radar chart and dimension feedback. Full Phase 2 flow operational.</done>
</task>

</tasks>

<verification>
- Frontend builds: `cd frontend && npm run build` succeeds
- Backend starts: `cd backend && uvicorn app.main:app --port 8000` without errors
- All API routes registered: `curl http://localhost:8000/api/health` returns 200
- TypeScript compiles: `cd frontend && npx tsc --noEmit` passes
- Ruff passes: `cd backend && ruff check .` passes
</verification>

<success_criteria>
Full Phase 2 flow operational: admin configures HCPs and scenarios, user selects scenario, has text-based coaching conversation with AI HCP via SSE, receives multi-dimensional scoring feedback with radar chart visualization. All 18 requirements addressed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-f2f-text-coaching-and-scoring/02-08-SUMMARY.md`
</output>
