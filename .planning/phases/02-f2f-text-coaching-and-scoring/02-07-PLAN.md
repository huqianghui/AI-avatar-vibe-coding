---
phase: 02-f2f-text-coaching-and-scoring
plan: 07
type: execute
wave: 3
depends_on: ["02-02", "02-05"]
files_modified:
  - frontend/src/components/coach/scenario-card.tsx
  - frontend/src/components/coach/scenario-panel.tsx
  - frontend/src/components/coach/chat-area.tsx
  - frontend/src/components/coach/chat-message.tsx
  - frontend/src/components/coach/typing-indicator.tsx
  - frontend/src/components/coach/key-messages.tsx
  - frontend/src/components/coach/hints-panel.tsx
  - frontend/src/components/coach/message-tracker.tsx
  - frontend/src/components/coach/session-timer.tsx
  - frontend/src/components/scoring/radar-chart.tsx
  - frontend/src/components/scoring/dimension-bars.tsx
  - frontend/src/components/scoring/feedback-card.tsx
  - frontend/src/components/scoring/score-summary.tsx
  - frontend/src/pages/user/training.tsx
  - frontend/src/pages/user/training-session.tsx
  - frontend/src/pages/user/scoring-feedback.tsx
autonomous: true
requirements:
  - COACH-01
  - COACH-02
  - COACH-03
  - COACH-08
  - UI-03
  - UI-05
  - SCORE-03
  - SCORE-04

must_haves:
  truths:
    - "User sees scenario card grid with HCP info, difficulty badges, and Start Training buttons"
    - "F2F coaching page shows 3-column layout with collapsible left/right panels"
    - "Chat messages stream in real-time word-by-word via SSE"
    - "Key message checklist updates automatically during conversation"
    - "Coaching hints appear in right panel during conversation"
    - "Scoring feedback page shows radar chart, dimension bars, and per-dimension feedback cards"
  artifacts:
    - path: "frontend/src/pages/user/training.tsx"
      provides: "Scenario selection page"
      contains: "ScenarioCard"
    - path: "frontend/src/pages/user/training-session.tsx"
      provides: "F2F coaching session page"
      contains: "ChatArea"
    - path: "frontend/src/pages/user/scoring-feedback.tsx"
      provides: "Scoring feedback page"
      contains: "RadarChart"
    - path: "frontend/src/components/coach/chat-area.tsx"
      provides: "Chat area with SSE streaming"
      contains: "useSSEStream"
    - path: "frontend/src/components/scoring/radar-chart.tsx"
      provides: "Recharts radar chart"
      contains: "ResponsiveContainer"
  key_links:
    - from: "frontend/src/pages/user/training-session.tsx"
      to: "frontend/src/hooks/use-sse.ts"
      via: "useSSEStream hook"
      pattern: "useSSEStream"
    - from: "frontend/src/pages/user/training-session.tsx"
      to: "frontend/src/hooks/use-session.ts"
      via: "useSession, useEndSession hooks"
      pattern: "useSession|useEndSession"
    - from: "frontend/src/components/scoring/radar-chart.tsx"
      to: "recharts"
      via: "RadarChart import"
      pattern: "from 'recharts'"
---

<objective>
Build the core user-facing coaching UI: scenario selection page (card grid with filters), F2F coaching session page (3-column live chat with SSE streaming, key messages, hints), and scoring feedback page (radar chart, dimension bars, feedback cards). Replace existing mock pages with fully functional versions.

Purpose: This is the primary user experience — selecting scenarios, having AI-powered coaching conversations, and reviewing scores.
Output: 10 coaching components, 4 scoring components, 3 page rewrites.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-f2f-text-coaching-and-scoring/02-CONTEXT.md
@.planning/phases/02-f2f-text-coaching-and-scoring/02-UI-SPEC.md
@.planning/phases/02-f2f-text-coaching-and-scoring/02-02-SUMMARY.md
@.planning/phases/02-f2f-text-coaching-and-scoring/02-05-SUMMARY.md

<interfaces>
From frontend/src/hooks/use-sse.ts:
```typescript
export function useSSEStream(callbacks: SSECallbacks): {
  sendMessage: (sessionId: string, message: string) => Promise<void>;
  isStreaming: boolean;
  streamedText: string;
  error: string | null;
}
```

From frontend/src/hooks/use-session.ts:
```typescript
export function useCreateSession() { ... }
export function useSession(id) { ... }
export function useEndSession() { ... }
export function useSessionMessages(sessionId) { ... }
```

From frontend/src/hooks/use-scenarios.ts:
```typescript
export function useActiveScenarios(params?) { ... }
```

From frontend/src/hooks/use-scoring.ts:
```typescript
export function useTriggerScoring() { ... }
export function useSessionScore(sessionId) { ... }
```

From frontend/src/types/session.ts:
```typescript
export interface KeyMessageStatus { message: string; delivered: boolean; detected_at: string | null; }
export interface CoachingHint { content: string; metadata?: Record<string, unknown>; }
export interface SessionMessage { role: "user" | "assistant"; content: string; ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build scenario selection page and coaching session page with SSE chat</name>
  <files>
    frontend/src/components/coach/scenario-card.tsx
    frontend/src/components/coach/scenario-panel.tsx
    frontend/src/components/coach/chat-area.tsx
    frontend/src/components/coach/chat-message.tsx
    frontend/src/components/coach/typing-indicator.tsx
    frontend/src/components/coach/key-messages.tsx
    frontend/src/components/coach/hints-panel.tsx
    frontend/src/components/coach/message-tracker.tsx
    frontend/src/components/coach/session-timer.tsx
    frontend/src/pages/user/training.tsx
    frontend/src/pages/user/training-session.tsx
  </files>
  <read_first>
    frontend/src/pages/user/training.tsx
    frontend/src/pages/user/training-session.tsx
    frontend/src/components/coach/left-panel.tsx
    frontend/src/components/coach/center-panel.tsx
    frontend/src/components/coach/right-panel.tsx
    frontend/src/components/shared/chat-bubble.tsx
    frontend/src/components/shared/chat-input.tsx
    frontend/src/components/shared/hcp-profile-card.tsx
    frontend/src/components/ui/card.tsx
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/checkbox.tsx
    frontend/src/components/ui/scroll-area.tsx
    frontend/src/components/ui/dialog.tsx
    frontend/src/components/ui/badge.tsx
    frontend/src/components/ui/switch.tsx
    frontend/src/components/ui/avatar.tsx
    frontend/src/components/ui/textarea.tsx
    frontend/src/components/shared/empty-state.tsx
    frontend/src/hooks/use-sse.ts
    frontend/src/hooks/use-session.ts
    frontend/src/hooks/use-scenarios.ts
    frontend/src/types/session.ts
    frontend/src/types/scenario.ts
    frontend/public/locales/en-US/coach.json
  </read_first>
  <action>
**IMPORTANT:** The existing `training.tsx`, `training-session.tsx`, and `coach/` components were built in Phase 01.1 with mock data. Phase 2 replaces them with fully functional versions connected to real backend APIs. Read the existing files first to preserve any useful patterns, then rewrite with API integration.

**frontend/src/components/coach/scenario-card.tsx** — Per UI-SPEC Page 1:
- Props: `scenario: Scenario`, `onStart: (scenarioId: string) => void`
- Card: `bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden hover:shadow-md transition-shadow`
- Card header: `h-48 bg-gradient-to-br from-blue-500 to-blue-600` with centered PlayCircle icon (w-16 h-16 text-white opacity-80)
- Card body: `p-6`
- Title: `text-lg font-semibold text-gray-900 mb-2` — `scenario.name`
- Description: `text-sm text-gray-600 mb-4` — `scenario.description`
- Metadata row: `flex items-center gap-4 mb-4 text-sm text-gray-600`
  - Clock icon + estimated duration
  - Star icon + difficulty
- HCP info: Avatar + name + specialty (if hcp_profile loaded)
- Difficulty badge: `px-3 py-1 rounded-full text-sm font-semibold` with colors: Easy=`bg-blue-100 text-blue-700`, Medium=`bg-orange-100 text-orange-700`, Hard=`bg-red-100 text-red-700`
- Start button: `px-4 py-2 bg-blue-600 text-white rounded-lg text-base font-semibold hover:bg-blue-700 transition-colors`

**frontend/src/pages/user/training.tsx** — Rewrite scenario selection page:
- Uses `useActiveScenarios()` hook instead of mock data
- Page: `p-4 lg:p-8 max-w-7xl mx-auto`
- Title: `text-3xl font-semibold text-gray-900 mb-8` via `t("coach:scenarioSelection.title")`
- Filter bar: Tabs ("F2F Training" / "Conference Training"), Product dropdown, Difficulty dropdown, Search input — `flex items-center gap-4 mb-6`
- Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
- Each scenario renders as ScenarioCard
- On "Start Training" click: call `createSession(scenarioId)`, then navigate to `/user/training/session?id={sessionId}`
- Empty state: EmptyState component when no scenarios
- Loading state: Skeleton cards

**frontend/src/components/coach/scenario-panel.tsx** — Left panel content per UI-SPEC Page 2:
- Props: `scenario: Scenario`, `keyMessagesStatus: KeyMessageStatus[]`, `isCollapsed: boolean`, `onToggle: () => void`
- Sections (each a Card with `mb-4`):
  1. Scenario Briefing: product, therapeutic_area, difficulty
  2. HCP Profile: Avatar 60px, name, specialty, personality_type
  3. Key Messages: KeyMessages component
  4. Scoring Criteria: weight percentages displayed as key-value pairs

**frontend/src/components/coach/key-messages.tsx** — Key message checklist per COACH-03:
- Props: `messages: KeyMessageStatus[]`
- Renders checkbox + label for each key message
- Checked state driven by `delivered` field (read-only, updated via SSE)
- Delivered: `text-green-700 line-through`, icon Check green
- Pending: `text-slate-700`, icon empty circle

**frontend/src/components/coach/chat-message.tsx** — Chat bubble per UI-SPEC:
- Props: `message: SessionMessage | { role: string; content: string }`, `isStreaming?: boolean`
- HCP (assistant): `max-w-[70%] rounded-lg px-4 py-2.5 bg-blue-500 text-white`
- MR (user): `max-w-[70%] rounded-lg px-4 py-2.5 bg-slate-200 text-slate-900`
- Text: `text-sm leading-relaxed`
- Timestamp: `text-sm opacity-70 mt-1 block`
- Self-aligns: HCP left, MR right

**frontend/src/components/coach/typing-indicator.tsx** — Bouncing dots:
- 3 dots: `w-2 h-2 bg-white rounded-full animate-bounce` with staggered delays (0ms, 150ms, 300ms)
- Container: `bg-blue-500 text-white rounded-lg px-4 py-2.5`
- `aria-label` per accessibility: "HCP is typing"

**frontend/src/components/coach/chat-area.tsx** — Center panel chat area:
- Props: `sessionId: string`, `messages: SessionMessage[]`, `onSendMessage: (msg: string) => void`, `isStreaming: boolean`, `streamingText: string`, `onEndSession: () => void`, `sessionStatus: SessionStatus`
- Top bar: `h-14 border-b border-slate-200 px-6 flex items-center justify-between`
  - SessionTimer component
  - End Session: `Button variant="destructive" size="sm"` with AlertCircle icon
- Avatar display area: `h-[240px] bg-slate-900` with HCP avatar (use existing pattern from Phase 01.1)
- Chat messages: `ScrollArea className="flex-1 p-6"`, messages in `space-y-4 max-w-4xl mx-auto`
  - Render existing messages from `messages` prop
  - If `isStreaming`, show current streaming text in a HCP bubble + TypingIndicator
- Auto-scroll to bottom on new messages
- Input area: `border-t border-slate-200 p-4 bg-slate-50`
  - Textarea + Send button (`h-11 w-11 rounded-full bg-blue-600`)
  - Keyboard: Enter sends, Shift+Enter newline
  - Send button disabled while streaming or no text
  - Mic button placeholder (disabled in Phase 2 per deferred voice)

**frontend/src/components/coach/hints-panel.tsx** — Right panel per UI-SPEC:
- Props: `hints: CoachingHint[]`, `keyMessagesStatus: KeyMessageStatus[]`, `sessionStats: { duration: number; wordCount: number; messageCount: number }`, `isCollapsed: boolean`, `onToggle: () => void`
- Sections:
  1. AI Avatar Hints: `Card className="mb-4 bg-yellow-50 border-yellow-200"`, latest hint text
  2. Message Tracker: MessageTracker component
  3. Session Stats: duration, word count, messages — key-value pairs

**frontend/src/components/coach/message-tracker.tsx** — Per UI-SPEC:
- Props: `messages: KeyMessageStatus[]`
- Status icons: delivered=`Check h-4 w-4 text-green-600`, in-progress=`div h-2 w-2 bg-blue-600 rounded-full`, pending=`Circle h-4 w-4 text-slate-400`
- Text color: delivered=`text-green-700`, in-progress=`text-blue-700`, pending=`text-slate-500`

**frontend/src/components/coach/session-timer.tsx** — Timer:
- Props: `startedAt: string | null`
- Displays elapsed time in `MM:SS` format using `font-mono`
- Updates every second via `useEffect` + `setInterval`
- Clock icon `h-4 w-4`

**frontend/src/pages/user/training-session.tsx** — Full F2F coaching page rewrite:
- Reads `sessionId` from URL search params (`?id=xxx`)
- Uses `useSession(sessionId)` to load session data
- Uses `useSessionMessages(sessionId)` for message history
- Uses `useSSEStream(callbacks)` for real-time chat
- State management:
  - `messages: SessionMessage[]` — accumulated from API + SSE
  - `keyMessagesStatus: KeyMessageStatus[]` — updated via SSE key_messages events
  - `hints: CoachingHint[]` — accumulated from SSE hint events
  - `sessionStats` — computed from messages
  - `leftCollapsed`, `rightCollapsed` — panel collapse state
- SSE callbacks:
  - `onText`: append to streaming buffer for current HCP message
  - `onHint`: add to hints array
  - `onKeyMessages`: update keyMessagesStatus
  - `onDone`: finalize current HCP message, add to messages array, refetch messages
- End Session flow (D-06):
  - Show confirmation dialog: "Are you sure you want to end?"
  - Call `useEndSession()` mutation
  - On success: navigate to scoring page with session ID
- Layout: `h-screen w-screen overflow-hidden flex bg-white`
  - Left: ScenarioPanel (280px / 48px collapsed)
  - Center: ChatArea (flex-1)
  - Right: HintsPanel (260px / 48px collapsed)
- Panel collapse: instant toggle, chevron buttons with `aria-expanded`
- Responsive per UI-SPEC: auto-collapse panels at smaller breakpoints
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/user/training.tsx contains `useActiveScenarios`
    - frontend/src/pages/user/training.tsx contains `ScenarioCard`
    - frontend/src/pages/user/training.tsx contains `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
    - frontend/src/pages/user/training-session.tsx contains `useSSEStream`
    - frontend/src/pages/user/training-session.tsx contains `useSession`
    - frontend/src/pages/user/training-session.tsx contains `useEndSession`
    - frontend/src/pages/user/training-session.tsx contains `h-screen`
    - frontend/src/components/coach/chat-area.tsx contains `ScrollArea`
    - frontend/src/components/coach/chat-message.tsx contains `bg-blue-500` (HCP bubble)
    - frontend/src/components/coach/chat-message.tsx contains `bg-slate-200` (MR bubble)
    - frontend/src/components/coach/typing-indicator.tsx contains `animate-bounce`
    - frontend/src/components/coach/key-messages.tsx contains `KeyMessageStatus`
    - frontend/src/components/coach/session-timer.tsx contains `font-mono`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Scenario selection page with API-connected card grid. F2F coaching session with live SSE chat, key message tracking, coaching hints, and session timer. All per UI-SPEC layout specs.</done>
</task>

<task type="auto">
  <name>Task 2: Build scoring feedback page with radar chart and dimension feedback</name>
  <files>
    frontend/src/components/scoring/radar-chart.tsx
    frontend/src/components/scoring/dimension-bars.tsx
    frontend/src/components/scoring/feedback-card.tsx
    frontend/src/components/scoring/score-summary.tsx
    frontend/src/pages/user/scoring-feedback.tsx
  </files>
  <read_first>
    frontend/src/components/shared/score-card.tsx
    frontend/src/components/shared/dimension-bar.tsx
    frontend/src/components/ui/card.tsx
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/badge.tsx
    frontend/src/hooks/use-scoring.ts
    frontend/src/hooks/use-session.ts
    frontend/src/types/score.ts
    frontend/public/locales/en-US/scoring.json
    frontend/src/styles/index.css
  </read_first>
  <action>
**frontend/src/components/scoring/radar-chart.tsx** — Recharts radar chart per UI-SPEC Page 3:
- Props: `currentScores: { dimension: string; score: number }[]`, `previousScores?: { dimension: string; score: number }[]`
- Uses recharts: `ResponsiveContainer`, `RadarChart`, `PolarGrid`, `PolarAngleAxis`, `PolarRadiusAxis`, `Radar`
- Container height: `h-80` (320px)
- Grid stroke: `#E5E7EB`
- Angle axis tick: `fill: '#64748B', fontSize: 14`
- Radius axis: domain [0, 100], tick `fill: '#64748B', fontSize: 14`
- Current session radar: `stroke="#1E40AF" fill="#1E40AF" fillOpacity={0.5}`
- Previous session radar (if data exists): `stroke="#475569" strokeDasharray="5 5" fill="none"`
- Dimension names from i18n: `scoring.dimensions.keyMessage`, etc.

**frontend/src/components/scoring/dimension-bars.tsx** — Score bars per UI-SPEC:
- Props: `details: ScoreDetail[]`
- Stack of bars, each showing:
  - Label + score: `flex items-center justify-between`, label `text-sm`, score `text-sm font-semibold`
  - Bar track: `w-full h-2 bg-accent rounded-full overflow-hidden`
  - Bar fill: `h-full rounded-full transition-all duration-500`
  - Color by score: >= 80 `bg-green-500` / `bg-strength`, 60-79 `bg-orange-500` / `bg-weakness`, < 60 `bg-red-500` / `bg-destructive`
  - `role="progressbar"` with `aria-valuenow`, `aria-valuemin="0"`, `aria-valuemax="100"`

**frontend/src/components/scoring/feedback-card.tsx** — Per-dimension feedback card:
- Props: `detail: ScoreDetail`
- Card component with:
  - Header: Dimension name + score (colored by threshold)
  - Strengths section: `text-green-600` with Check icon, each strength shows text + quoted conversation excerpt in italics
  - Areas to Improve section: `text-orange-600` with X icon, each weakness shows text + quote
  - Suggestions section: `text-purple-600` with Lightbulb icon, bulleted list
- Labels from i18n: `scoring.strengths`, `scoring.areasToImprove`, `scoring.suggestions`

**frontend/src/components/scoring/score-summary.tsx** — Overall score display:
- Props: `overallScore: number`, `passed: boolean`, `trend?: number` (positive/negative)
- Large score number: `text-3xl font-semibold`
- Grade badge: Excellent (>=80, green), Good (60-79, orange), Needs Improvement (<60, red)
- Pass/Fail badge
- Trend indicator: `flex items-center gap-1 text-sm` with TrendingUp/TrendingDown icon, colored green/red

**frontend/src/pages/user/scoring-feedback.tsx** — Scoring feedback page:
- Reads `sessionId` from URL search params
- Uses `useSessionScore(sessionId)` — if no score yet, shows loading + triggers scoring
- Uses `useTriggerScoring()` — called when session status is "completed" but not "scored"
- Layout: Standard page within user layout shell
  - `p-4 lg:p-8 max-w-7xl mx-auto`
- Top section: ScoreSummary with overall score
- Two-column layout: `grid grid-cols-1 lg:grid-cols-2 gap-8`
  - Left: RadarChart + DimensionBars
  - Right: ScrollArea with FeedbackCard per dimension
- Bottom action bar: `flex items-center justify-end gap-4 mt-8 pt-6 border-t`
  - "Try Again" `Button variant="outline"` — navigates back to training page
  - "Export PDF" `Button variant="outline"` disabled (Phase 3+)
  - "Share with Manager" `Button variant="outline"` disabled (future)
  - "Back to Dashboard" `Button variant="default"` — navigates to `/user/dashboard`
- Loading state: while scoring in progress, show a centered spinner with "Scoring your session..." text
- All text via `useTranslation("scoring")`
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/scoring/radar-chart.tsx contains `from 'recharts'`
    - frontend/src/components/scoring/radar-chart.tsx contains `ResponsiveContainer`
    - frontend/src/components/scoring/radar-chart.tsx contains `#1E40AF` (primary blue for current session)
    - frontend/src/components/scoring/radar-chart.tsx contains `strokeDasharray` (previous session overlay)
    - frontend/src/components/scoring/dimension-bars.tsx contains `role="progressbar"`
    - frontend/src/components/scoring/dimension-bars.tsx contains `aria-valuenow`
    - frontend/src/components/scoring/feedback-card.tsx contains `scoring.strengths` or `strengths`
    - frontend/src/components/scoring/score-summary.tsx contains `text-3xl font-semibold`
    - frontend/src/pages/user/scoring-feedback.tsx contains `useSessionScore`
    - frontend/src/pages/user/scoring-feedback.tsx contains `useTriggerScoring`
    - frontend/src/pages/user/scoring-feedback.tsx contains `RadarChart`
    - frontend/src/pages/user/scoring-feedback.tsx contains `Back to Dashboard` or `scoring.backToDashboard`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Scoring feedback page with recharts radar chart, color-coded dimension bars, per-dimension feedback cards with conversation quotes, and action bar. Accessible with ARIA attributes.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- `cd frontend && npm run build` succeeds
- All coach component files exist in src/components/coach/
- All scoring component files exist in src/components/scoring/
</verification>

<success_criteria>
Complete user-facing coaching flow: scenario selection -> F2F chat with SSE -> scoring feedback with radar chart. All pages connected to real backend APIs via hooks.
</success_criteria>

<output>
After completion, create `.planning/phases/02-f2f-text-coaching-and-scoring/02-07-SUMMARY.md`
</output>
