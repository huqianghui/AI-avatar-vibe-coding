---
phase: 02-f2f-text-coaching-and-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/hcp_profile.py
  - backend/app/models/scenario.py
  - backend/app/models/session.py
  - backend/app/models/message.py
  - backend/app/models/score.py
  - backend/app/models/__init__.py
  - backend/app/schemas/hcp_profile.py
  - backend/app/schemas/scenario.py
  - backend/app/schemas/session.py
  - backend/app/schemas/score.py
  - backend/app/schemas/__init__.py
  - backend/alembic/env.py
  - backend/pyproject.toml
autonomous: true
requirements:
  - HCP-01
  - HCP-02
  - HCP-03
  - HCP-04
  - HCP-05
  - COACH-09
  - SCORE-01
  - SCORE-05

must_haves:
  truths:
    - "All Phase 2 database models exist and can create tables without errors"
    - "Pydantic schemas validate HCP profile, scenario, session, and score data"
    - "Alembic migration generates and applies cleanly"
    - "Scoring weights on Scenario model default to 5 dimensions totaling 100"
    - "Session status lifecycle is enforced: created -> in_progress -> completed -> scored"
  artifacts:
    - path: "backend/app/models/hcp_profile.py"
      provides: "HcpProfile ORM model"
      contains: "class HcpProfile"
    - path: "backend/app/models/scenario.py"
      provides: "Scenario ORM model"
      contains: "class Scenario"
    - path: "backend/app/models/session.py"
      provides: "CoachingSession ORM model"
      contains: "class CoachingSession"
    - path: "backend/app/models/message.py"
      provides: "SessionMessage ORM model"
      contains: "class SessionMessage"
    - path: "backend/app/models/score.py"
      provides: "SessionScore and ScoreDetail ORM models"
      contains: "class SessionScore"
    - path: "backend/app/schemas/hcp_profile.py"
      provides: "HCP profile request/response schemas"
      exports: ["HcpProfileCreate", "HcpProfileUpdate", "HcpProfileResponse"]
    - path: "backend/app/schemas/scenario.py"
      provides: "Scenario request/response schemas"
      exports: ["ScenarioCreate", "ScenarioUpdate", "ScenarioResponse"]
  key_links:
    - from: "backend/app/models/__init__.py"
      to: "all 5 new model files"
      via: "import and __all__ re-export"
      pattern: "from app.models.hcp_profile import HcpProfile"
    - from: "backend/alembic/env.py"
      to: "app.models"
      via: "import for autogenerate detection"
      pattern: "from app.models import.*HcpProfile.*Scenario.*CoachingSession"
---

<objective>
Create all Phase 2 backend data models (HcpProfile, Scenario, CoachingSession, SessionMessage, SessionScore, ScoreDetail), corresponding Pydantic v2 schemas, and Alembic migration. Install sse-starlette dependency.

Purpose: Establish the data foundation that all backend API routers and frontend hooks depend on. No APIs or UI can be built without these models and schemas.
Output: 6 ORM models, 4 schema modules, 1 Alembic migration, updated pyproject.toml with sse-starlette.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-f2f-text-coaching-and-scoring/02-CONTEXT.md
@.planning/phases/02-f2f-text-coaching-and-scoring/02-RESEARCH.md

<interfaces>
<!-- Existing models and patterns the executor must follow -->

From backend/app/models/base.py:
```python
class Base(DeclarativeBase):
    pass

class TimestampMixin:
    id: Mapped[str] = mapped_column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now(), nullable=False)
    updated_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now(), onupdate=func.now(), nullable=False)
```

From backend/app/models/user.py:
```python
class User(Base, TimestampMixin):
    __tablename__ = "users"
    username: Mapped[str] = mapped_column(String(100), unique=True, nullable=False, index=True)
    email: Mapped[str] = mapped_column(String(255), unique=True, nullable=False)
    hashed_password: Mapped[str] = mapped_column(String(255), nullable=False)
    full_name: Mapped[str] = mapped_column(String(255), default="")
    role: Mapped[str] = mapped_column(String(20), default="user")
    is_active: Mapped[bool] = mapped_column(Boolean, default=True)
    preferred_language: Mapped[str] = mapped_column(String(10), default="zh-CN")
```

From backend/app/schemas/auth.py:
```python
class UserResponse(BaseModel):
    model_config = ConfigDict(from_attributes=True)
```

From backend/app/models/__init__.py:
```python
from app.models.base import Base, TimestampMixin
from app.models.user import User
__all__ = ["Base", "TimestampMixin", "User"]
```

From backend/alembic/env.py:
```python
from app.models import Base, User  # noqa: F401
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all Phase 2 ORM models and Pydantic schemas</name>
  <files>
    backend/app/models/hcp_profile.py
    backend/app/models/scenario.py
    backend/app/models/session.py
    backend/app/models/message.py
    backend/app/models/score.py
    backend/app/models/__init__.py
    backend/app/schemas/hcp_profile.py
    backend/app/schemas/scenario.py
    backend/app/schemas/session.py
    backend/app/schemas/score.py
    backend/app/schemas/__init__.py
  </files>
  <read_first>
    backend/app/models/base.py
    backend/app/models/user.py
    backend/app/models/__init__.py
    backend/app/schemas/auth.py
    backend/app/schemas/__init__.py
  </read_first>
  <action>
Create 5 new ORM model files following the exact pattern from `user.py` (inherit from `Base, TimestampMixin`, use `Mapped[T]` with `mapped_column()`):

**backend/app/models/hcp_profile.py** — `HcpProfile` model per D-07, HCP-01, HCP-02:
- `__tablename__ = "hcp_profiles"`
- `name: Mapped[str] = mapped_column(String(255), nullable=False)`
- `specialty: Mapped[str] = mapped_column(String(100), nullable=False)`
- `hospital: Mapped[str] = mapped_column(String(255), default="")`
- `title: Mapped[str] = mapped_column(String(100), default="")`
- `avatar_url: Mapped[str] = mapped_column(String(500), default="")`
- `personality_type: Mapped[str] = mapped_column(String(50), default="friendly")` — personality types: friendly, skeptical, busy, analytical, cautious
- `emotional_state: Mapped[int] = mapped_column(default=50)` — 0=calm/neutral to 100=resistant/hostile
- `communication_style: Mapped[int] = mapped_column(default=50)` — 0=very direct to 100=very indirect
- `expertise_areas: Mapped[str] = mapped_column(Text, default="[]")` — JSON array of strings
- `prescribing_habits: Mapped[str] = mapped_column(Text, default="")`
- `concerns: Mapped[str] = mapped_column(Text, default="")`
- `objections: Mapped[str] = mapped_column(Text, default="[]")` — JSON array of strings (HCP-02)
- `probe_topics: Mapped[str] = mapped_column(Text, default="[]")` — JSON array of strings
- `difficulty: Mapped[str] = mapped_column(String(20), default="medium")` — easy/medium/hard
- `is_active: Mapped[bool] = mapped_column(Boolean, default=True)`
- `created_by: Mapped[str] = mapped_column(String(36), ForeignKey("users.id"), nullable=False)`
- Add `relationship("Scenario", back_populates="hcp_profile")` for FK navigation
- Add helper method `def to_prompt_dict(self) -> dict` that returns all personality/knowledge fields as a dict for system prompt construction

**backend/app/models/scenario.py** — `Scenario` model per HCP-03, HCP-04, HCP-05, SCORE-05:
- `__tablename__ = "scenarios"`
- `name: Mapped[str] = mapped_column(String(255), nullable=False)`
- `description: Mapped[str] = mapped_column(Text, default="")`
- `product: Mapped[str] = mapped_column(String(255), nullable=False)`
- `therapeutic_area: Mapped[str] = mapped_column(String(255), default="")`
- `mode: Mapped[str] = mapped_column(String(20), default="f2f")` — f2f / conference
- `difficulty: Mapped[str] = mapped_column(String(20), default="medium")`
- `status: Mapped[str] = mapped_column(String(20), default="draft")` — draft / active
- `hcp_profile_id: Mapped[str] = mapped_column(String(36), ForeignKey("hcp_profiles.id"), nullable=False)` (HCP-04)
- `key_messages: Mapped[str] = mapped_column(Text, default="[]")` — JSON array of strings
- `weight_key_message: Mapped[int] = mapped_column(default=30)` (D-08)
- `weight_objection_handling: Mapped[int] = mapped_column(default=25)`
- `weight_communication: Mapped[int] = mapped_column(default=20)`
- `weight_product_knowledge: Mapped[int] = mapped_column(default=15)`
- `weight_scientific_info: Mapped[int] = mapped_column(default=10)`
- `pass_threshold: Mapped[int] = mapped_column(default=70)` (HCP-05)
- `created_by: Mapped[str] = mapped_column(String(36), ForeignKey("users.id"), nullable=False)`
- Add `relationship("HcpProfile", back_populates="scenarios")` for FK navigation
- Add helper method `def get_scoring_weights(self) -> dict` returning `{"key_message": self.weight_key_message, ...}`

**backend/app/models/session.py** — `CoachingSession` model per COACH-01, COACH-09:
- `__tablename__ = "coaching_sessions"`
- `user_id: Mapped[str] = mapped_column(String(36), ForeignKey("users.id"), nullable=False)`
- `scenario_id: Mapped[str] = mapped_column(String(36), ForeignKey("scenarios.id"), nullable=False)`
- `status: Mapped[str] = mapped_column(String(20), default="created")` — created/in_progress/completed/scored (COACH-09: immutable once completed)
- `started_at: Mapped[datetime | None] = mapped_column(DateTime, nullable=True)`
- `completed_at: Mapped[datetime | None] = mapped_column(DateTime, nullable=True)`
- `duration_seconds: Mapped[int | None] = mapped_column(nullable=True)`
- `key_messages_status: Mapped[str] = mapped_column(Text, default="[]")` — JSON: [{message, delivered, detected_at}]
- `overall_score: Mapped[float | None] = mapped_column(nullable=True)` — filled after scoring
- `passed: Mapped[bool | None] = mapped_column(nullable=True)`
- Add relationships to Scenario and User

**backend/app/models/message.py** — `SessionMessage` model:
- `__tablename__ = "session_messages"`
- `session_id: Mapped[str] = mapped_column(String(36), ForeignKey("coaching_sessions.id"), nullable=False)`
- `role: Mapped[str] = mapped_column(String(20), nullable=False)` — "user" or "assistant"
- `content: Mapped[str] = mapped_column(Text, nullable=False)`
- `message_index: Mapped[int] = mapped_column(nullable=False)` — ordering within session

**backend/app/models/score.py** — `SessionScore` and `ScoreDetail` models per SCORE-01:
- SessionScore `__tablename__ = "session_scores"`:
  - `session_id: Mapped[str] = mapped_column(String(36), ForeignKey("coaching_sessions.id"), unique=True)`
  - `overall_score: Mapped[float] = mapped_column(nullable=False)`
  - `passed: Mapped[bool] = mapped_column(nullable=False)`
  - `feedback_summary: Mapped[str] = mapped_column(Text, default="")`
- ScoreDetail `__tablename__ = "score_details"`:
  - `score_id: Mapped[str] = mapped_column(String(36), ForeignKey("session_scores.id"), nullable=False)`
  - `dimension: Mapped[str] = mapped_column(String(50), nullable=False)`
  - `score: Mapped[float] = mapped_column(nullable=False)`
  - `weight: Mapped[int] = mapped_column(nullable=False)`
  - `strengths: Mapped[str] = mapped_column(Text, default="[]")` — JSON array of {text, quote}
  - `weaknesses: Mapped[str] = mapped_column(Text, default="[]")` — JSON array of {text, quote}
  - `suggestions: Mapped[str] = mapped_column(Text, default="[]")` — JSON array of strings

**backend/app/models/__init__.py** — Update to re-export all new models:
```python
from app.models.base import Base, TimestampMixin
from app.models.user import User
from app.models.hcp_profile import HcpProfile
from app.models.scenario import Scenario
from app.models.session import CoachingSession
from app.models.message import SessionMessage
from app.models.score import SessionScore, ScoreDetail

__all__ = ["Base", "TimestampMixin", "User", "HcpProfile", "Scenario", "CoachingSession", "SessionMessage", "SessionScore", "ScoreDetail"]
```

**Pydantic Schemas** — Create 4 schema files following the `auth.py` pattern (`model_config = ConfigDict(from_attributes=True)` on response models):

**backend/app/schemas/hcp_profile.py**:
- `HcpProfileCreate(BaseModel)`: all required fields (name, specialty, created_by), optional fields with defaults
- `HcpProfileUpdate(BaseModel)`: all fields Optional (for partial updates)
- `HcpProfileResponse(BaseModel)`: all fields + id + created_at + updated_at, `model_config = ConfigDict(from_attributes=True)`
- `HcpProfileListResponse(BaseModel)`: items: list[HcpProfileResponse], total: int

**backend/app/schemas/scenario.py**:
- `ScenarioCreate(BaseModel)`: name, product, hcp_profile_id required; key_messages as list[str]; weight fields with defaults totaling 100; pass_threshold default 70
- Add `@field_validator` on ScenarioCreate to validate weights sum to 100 (Pitfall 3)
- `ScenarioUpdate(BaseModel)`: all fields Optional, same weight validator
- `ScenarioResponse(BaseModel)`: all fields + id + timestamps, `ConfigDict(from_attributes=True)`

**backend/app/schemas/session.py**:
- `SessionCreate(BaseModel)`: scenario_id required
- `SendMessageRequest(BaseModel)`: message: str
- `SessionResponse(BaseModel)`: all session fields + ConfigDict
- `MessageResponse(BaseModel)`: id, session_id, role, content, message_index, created_at

**backend/app/schemas/score.py**:
- `ScoreDetailResponse(BaseModel)`: dimension, score, weight, strengths (list[dict]), weaknesses (list[dict]), suggestions (list[str])
- `SessionScoreResponse(BaseModel)`: overall_score, passed, feedback_summary, details: list[ScoreDetailResponse], session_id

**backend/app/schemas/__init__.py** — Update to re-export key schemas.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend && python -c "from app.models import Base, HcpProfile, Scenario, CoachingSession, SessionMessage, SessionScore, ScoreDetail; from app.schemas.hcp_profile import HcpProfileCreate, HcpProfileResponse; from app.schemas.scenario import ScenarioCreate, ScenarioResponse; from app.schemas.session import SessionCreate, SendMessageRequest; from app.schemas.score import SessionScoreResponse; print('All models and schemas import successfully')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/models/hcp_profile.py contains `class HcpProfile(Base, TimestampMixin):`
    - backend/app/models/scenario.py contains `class Scenario(Base, TimestampMixin):`
    - backend/app/models/scenario.py contains `weight_key_message: Mapped[int] = mapped_column(default=30)`
    - backend/app/models/session.py contains `class CoachingSession(Base, TimestampMixin):`
    - backend/app/models/session.py contains `status: Mapped[str] = mapped_column(String(20), default="created")`
    - backend/app/models/message.py contains `class SessionMessage(Base, TimestampMixin):`
    - backend/app/models/score.py contains `class SessionScore(Base, TimestampMixin):`
    - backend/app/models/score.py contains `class ScoreDetail(Base, TimestampMixin):`
    - backend/app/models/__init__.py contains `from app.models.hcp_profile import HcpProfile`
    - backend/app/models/__init__.py contains all 8 model names in __all__
    - backend/app/schemas/scenario.py contains `@field_validator` for weight validation
    - backend/app/schemas/hcp_profile.py contains `model_config = ConfigDict(from_attributes=True)`
    - Python import command exits 0
  </acceptance_criteria>
  <done>All 5 model files and 4 schema files created. Models follow TimestampMixin pattern. Schemas use Pydantic v2 ConfigDict. Scoring weight validator ensures sum=100.</done>
</task>

<task type="auto">
  <name>Task 2: Create Alembic migration, update dependencies, and update env.py</name>
  <files>
    backend/alembic/env.py
    backend/pyproject.toml
  </files>
  <read_first>
    backend/alembic/env.py
    backend/pyproject.toml
    backend/app/models/__init__.py
  </read_first>
  <action>
**1. Update backend/alembic/env.py** — Import all new models for autogenerate detection (Gotcha #7, Pitfall 6):
Change the import line from:
```python
from app.models import Base, User  # noqa: F401
```
to:
```python
from app.models import Base, User, HcpProfile, Scenario, CoachingSession, SessionMessage, SessionScore, ScoreDetail  # noqa: F401
```

**2. Update backend/pyproject.toml** — Add `sse-starlette` to dependencies:
Add `"sse-starlette>=2.0.0",` to the `dependencies` list (after `"websockets>=13.0"`).

**3. Install sse-starlette:**
Run `cd backend && pip install sse-starlette>=2.0.0`

**4. Generate Alembic migration:**
Run `cd backend && alembic revision --autogenerate -m "add_phase2_models_hcp_scenario_session_score"`

**5. Apply migration:**
Run `cd backend && alembic upgrade head`

Verify the migration file contains `create_table` operations for all 5 new tables: hcp_profiles, scenarios, coaching_sessions, session_messages, session_scores, score_details (6 tables total).
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend && python -c "import asyncio; from app.database import engine; from app.models.base import Base; asyncio.run((lambda: None)())" && alembic current && pip show sse-starlette | head -2</automated>
  </verify>
  <acceptance_criteria>
    - backend/alembic/env.py contains `from app.models import Base, User, HcpProfile, Scenario, CoachingSession, SessionMessage, SessionScore, ScoreDetail`
    - backend/pyproject.toml contains `sse-starlette>=2.0.0`
    - A migration file exists in backend/alembic/versions/ containing `create_table` for `hcp_profiles`
    - `alembic current` shows head revision applied
    - `pip show sse-starlette` outputs version info
  </acceptance_criteria>
  <done>Alembic migration created and applied. sse-starlette installed. All 6 new database tables exist. env.py imports all models.</done>
</task>

</tasks>

<verification>
- `cd backend && python -c "from app.models import HcpProfile, Scenario, CoachingSession, SessionMessage, SessionScore, ScoreDetail; print('OK')"` prints OK
- `cd backend && python -c "from app.schemas.scenario import ScenarioCreate; s = ScenarioCreate(name='test', product='drug', hcp_profile_id='uuid', created_by='uuid'); print(s.weight_key_message)"` prints 30
- `cd backend && alembic current` shows latest migration applied
- `pip show sse-starlette` shows installed version
</verification>

<success_criteria>
All 6 ORM models importable from app.models. All Pydantic schemas validate correctly. Alembic migration applied. sse-starlette installed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-f2f-text-coaching-and-scoring/02-01-SUMMARY.md`
</output>
