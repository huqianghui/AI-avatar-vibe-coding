---
phase: 02-f2f-text-coaching-and-scoring
plan: 06
type: execute
wave: 3
depends_on: ["02-02", "02-05"]
files_modified:
  - frontend/src/components/admin/hcp-editor.tsx
  - frontend/src/components/admin/hcp-list.tsx
  - frontend/src/components/admin/scenario-table.tsx
  - frontend/src/components/admin/scenario-editor.tsx
  - frontend/src/components/admin/scoring-weights.tsx
  - frontend/src/components/admin/personality-sliders.tsx
  - frontend/src/components/admin/objection-list.tsx
  - frontend/src/components/admin/test-chat-dialog.tsx
  - frontend/src/components/admin/service-config-card.tsx
  - frontend/src/pages/admin/hcp-profiles.tsx
  - frontend/src/pages/admin/scenarios.tsx
  - frontend/src/pages/admin/azure-config.tsx
autonomous: true
requirements:
  - HCP-01
  - HCP-02
  - HCP-03
  - HCP-04
  - HCP-05
  - SCORE-05
  - PLAT-03

must_haves:
  truths:
    - "Admin can see HCP profile list with search, select a profile, and edit in right panel form"
    - "HCP editor form has personality sliders, knowledge fields, and editable objection list"
    - "Admin can see scenario table with sortable columns and status filters"
    - "Scenario editor has linked scoring weight sliders that total 100%"
    - "Test Chat dialog opens a mini-chat to test HCP personality"
    - "Azure config page shows service cards with connection test buttons"
  artifacts:
    - path: "frontend/src/pages/admin/hcp-profiles.tsx"
      provides: "HCP profile management page"
      contains: "HcpList"
    - path: "frontend/src/pages/admin/scenarios.tsx"
      provides: "Scenario management page"
      contains: "ScenarioTable"
    - path: "frontend/src/components/admin/scoring-weights.tsx"
      provides: "Linked slider component"
      contains: "adjustWeights"
    - path: "frontend/src/pages/admin/azure-config.tsx"
      provides: "Azure service config page"
      contains: "ServiceConfigCard"
  key_links:
    - from: "frontend/src/pages/admin/hcp-profiles.tsx"
      to: "frontend/src/hooks/use-hcp-profiles.ts"
      via: "useHcpProfiles hook"
      pattern: "useHcpProfiles"
    - from: "frontend/src/pages/admin/scenarios.tsx"
      to: "frontend/src/hooks/use-scenarios.ts"
      via: "useScenarios hook"
      pattern: "useScenarios"
    - from: "frontend/src/components/admin/scoring-weights.tsx"
      to: "frontend/src/components/ui/slider.tsx"
      via: "Slider import"
      pattern: "import.*Slider.*from.*ui/slider"
---

<objective>
Build all admin pages: HCP profile management (list + editor form), scenario management (table + editor dialog), and Azure service configuration. Includes scoring weight sliders (D-08), personality sliders, objection list editor, and Test Chat dialog (D-09).

Purpose: Enable admin to configure HCP profiles and training scenarios — required before users can select scenarios and start coaching sessions.
Output: 9 admin components, 3 admin pages.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-f2f-text-coaching-and-scoring/02-CONTEXT.md
@.planning/phases/02-f2f-text-coaching-and-scoring/02-UI-SPEC.md
@.planning/phases/02-f2f-text-coaching-and-scoring/02-02-SUMMARY.md
@.planning/phases/02-f2f-text-coaching-and-scoring/02-05-SUMMARY.md

<interfaces>
From frontend/src/hooks/use-hcp-profiles.ts:
```typescript
export function useHcpProfiles(params?) { ... }
export function useHcpProfile(id) { ... }
export function useCreateHcpProfile() { ... }
export function useUpdateHcpProfile() { ... }
export function useDeleteHcpProfile() { ... }
```

From frontend/src/hooks/use-scenarios.ts:
```typescript
export function useScenarios(params?) { ... }
export function useCreateScenario() { ... }
export function useUpdateScenario() { ... }
export function useDeleteScenario() { ... }
export function useCloneScenario() { ... }
```

From frontend/src/types/hcp.ts:
```typescript
export interface HcpProfile {
  personality_type: "friendly" | "skeptical" | "busy" | "analytical" | "cautious";
  emotional_state: number;
  communication_style: number;
  objections: string[];
  ...
}
```

From frontend/src/types/scenario.ts:
```typescript
export interface ScoringWeights {
  key_message: number; objection_handling: number; communication: number;
  product_knowledge: number; scientific_info: number;
}
```

From frontend/src/components/ui/slider.tsx:
```typescript
export { Slider }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin HCP profile management components and page</name>
  <files>
    frontend/src/components/admin/hcp-editor.tsx
    frontend/src/components/admin/hcp-list.tsx
    frontend/src/components/admin/personality-sliders.tsx
    frontend/src/components/admin/objection-list.tsx
    frontend/src/components/admin/test-chat-dialog.tsx
    frontend/src/pages/admin/hcp-profiles.tsx
  </files>
  <read_first>
    frontend/src/components/ui/card.tsx
    frontend/src/components/ui/input.tsx
    frontend/src/components/ui/select.tsx
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/dialog.tsx
    frontend/src/components/ui/form.tsx
    frontend/src/components/ui/label.tsx
    frontend/src/components/ui/slider.tsx
    frontend/src/components/ui/avatar.tsx
    frontend/src/components/ui/scroll-area.tsx
    frontend/src/components/ui/textarea.tsx
    frontend/src/components/shared/empty-state.tsx
    frontend/src/hooks/use-hcp-profiles.ts
    frontend/src/types/hcp.ts
    frontend/src/lib/utils.ts
    frontend/public/locales/en-US/admin.json
  </read_first>
  <action>
**frontend/src/components/admin/hcp-list.tsx** — Left sidebar HCP list per UI-SPEC Page 4:
- Props: `profiles: HcpProfile[]`, `selectedId: string | undefined`, `onSelect: (id: string) => void`, `onCreateNew: () => void`, `searchQuery: string`, `onSearchChange: (q: string) => void`
- Container: `w-[300px] border-r border-slate-200 flex flex-col h-full`
- Search Input at top with `p-4`
- ScrollArea for list, each item shows: Avatar (40px) + name + specialty
- Active item: `bg-blue-50 border-l-2 border-blue-600`
- Bottom: "Create New HCP" button, `Button variant="outline"` full-width
- Use `useTranslation("admin")` for all text
- Empty state when no profiles using EmptyState component

**frontend/src/components/admin/personality-sliders.tsx** — Personality configuration per D-07:
- Props: `personalityType: string`, `emotionalState: number`, `communicationStyle: number`, `onPersonalityTypeChange`, `onEmotionalStateChange`, `onCommunicationStyleChange`
- Personality Type: Select dropdown with options "friendly", "skeptical", "busy", "analytical", "cautious"
- Emotional State: Slider 0-100 with labels "Calm/Neutral" (0) and "Resistant/Hostile" (100)
- Communication Style: Slider 0-100 with labels "Very Direct" (0) and "Very Indirect" (100)
- Each slider shows current numeric value
- Layout: Card with section title "Personality", `space-y-4`

**frontend/src/components/admin/objection-list.tsx** — Editable objection list per HCP-02:
- Props: `items: string[]`, `onChange: (items: string[]) => void`, `label: string`, `addLabel: string`
- Renders list of items, each with Input + delete (X) button
- "Add" button at bottom
- Empty item auto-focuses
- Reusable for both objections and probe_topics

**frontend/src/components/admin/test-chat-dialog.tsx** — Quick test chat per D-09:
- Props: `profileId: string`, `profileName: string`, `open: boolean`, `onOpenChange: (open: boolean) => void`
- Dialog with mini-chat interface
- Send messages and get mock responses
- Uses simple fetch to `/api/v1/sessions` to create a test session, then SSE for messages
- Or for MVP: simulated local responses based on personality type (no backend needed)
- Close button in dialog header

**frontend/src/components/admin/hcp-editor.tsx** — Right panel editor form per D-07, UI-SPEC Page 4:
- Props: `profile: HcpProfile | null`, `onSave: (data: HcpProfileCreate | HcpProfileUpdate) => void`, `onTestChat: () => void`, `onDiscard: () => void`, `isNew: boolean`
- Uses react-hook-form + zod for validation
- Form sections using Card components with `mb-6`:
  1. Portrait: Avatar 120px circle (just initials for now, no upload)
  2. Identity Card: Name (required), Specialty (Select), Hospital, Title — `grid grid-cols-2 gap-4`
  3. Personality Card: PersonalitySliders component
  4. Knowledge Card: Expertise Areas (tag-like input or comma-separated), Prescribing Habits (Textarea), Concerns (Textarea)
  5. Interaction Rules Card: ObjectionList for objections, ObjectionList for probe_topics, Difficulty radio (easy/medium/hard)
- Bottom actions: `flex items-center gap-3`
  - "Save Profile" `Button variant="default"` (primary)
  - "Test Chat" `Button variant="outline"` — opens TestChatDialog
  - "Discard Changes" `Button variant="ghost"`
- All labels via `useTranslation("admin")`

**frontend/src/pages/admin/hcp-profiles.tsx** — Full HCP management page:
- Two-panel layout: HcpList (left 300px) + HcpEditor (right flex-1)
- State: `selectedProfileId`, `searchQuery`, `isCreatingNew`
- Uses `useHcpProfiles({ search: searchQuery })` hook
- Uses `useHcpProfile(selectedProfileId)` for editor
- Uses `useCreateHcpProfile()`, `useUpdateHcpProfile()`, `useDeleteHcpProfile()` mutations
- On save success: toast notification via sonner, invalidate queries
- On delete: confirmation dialog, then delete
- Container: `flex h-full` within admin layout
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/admin/hcp-profiles.tsx contains `useHcpProfiles`
    - frontend/src/pages/admin/hcp-profiles.tsx contains `HcpList` and `HcpEditor`
    - frontend/src/components/admin/hcp-list.tsx contains `w-[300px]`
    - frontend/src/components/admin/hcp-list.tsx contains `bg-blue-50 border-l-2 border-blue-600`
    - frontend/src/components/admin/hcp-editor.tsx contains `useForm` or `react-hook-form`
    - frontend/src/components/admin/personality-sliders.tsx contains `Slider` import
    - frontend/src/components/admin/personality-sliders.tsx contains options for "friendly", "skeptical", "busy", "analytical", "cautious"
    - frontend/src/components/admin/objection-list.tsx contains `onChange` prop handling
    - frontend/src/components/admin/test-chat-dialog.tsx contains `Dialog`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>HCP profile management page complete with list sidebar, editor form, personality sliders, objection list, and test chat dialog. All i18n-enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Create admin scenario management page, scoring weights, and Azure config page</name>
  <files>
    frontend/src/components/admin/scenario-table.tsx
    frontend/src/components/admin/scenario-editor.tsx
    frontend/src/components/admin/scoring-weights.tsx
    frontend/src/components/admin/service-config-card.tsx
    frontend/src/pages/admin/scenarios.tsx
    frontend/src/pages/admin/azure-config.tsx
  </files>
  <read_first>
    frontend/src/components/ui/dialog.tsx
    frontend/src/components/ui/badge.tsx
    frontend/src/components/ui/select.tsx
    frontend/src/components/ui/dropdown-menu.tsx
    frontend/src/components/ui/switch.tsx
    frontend/src/components/ui/slider.tsx
    frontend/src/components/shared/status-badge.tsx
    frontend/src/hooks/use-scenarios.ts
    frontend/src/hooks/use-hcp-profiles.ts
    frontend/src/types/scenario.ts
    frontend/src/types/hcp.ts
    frontend/public/locales/en-US/admin.json
  </read_first>
  <action>
**frontend/src/components/admin/scoring-weights.tsx** — Linked sliders totaling 100% per D-08:
- Props: `weights: { key_message: number; objection_handling: number; communication: number; product_knowledge: number; scientific_info: number }`, `onChange: (weights) => void`
- Renders 5 Slider components with labels and percentage display
- Implements `adjustWeights` algorithm per RESEARCH.md Pattern 4:
  When one slider changes, remaining 4 decrease proportionally to maintain total = 100
  `oldValue = weights[changed]`, `diff = newValue - oldValue`, distribute -diff across others proportional to their current values
  Fix rounding errors to ensure sum === 100
- Default: 30/25/20/15/10 (matches backend model defaults)
- Labels from i18n: `admin.scenarios.keyMessageDelivery`, `admin.scenarios.objectionHandling`, etc.
- Each slider shows `{value}%` next to label
- Total display: `100%` (always, as validation ensures it)

**frontend/src/components/admin/scenario-table.tsx** — Scenario management table per UI-SPEC Page 5:
- Props: `scenarios`, `onEdit`, `onDelete`, `onClone`, `onStatusChange`
- Table with columns: Name, Product, HCP (avatar + name), Mode (Badge: F2F/Conference), Difficulty (Badge with color), Status (StatusBadge: Active/Draft), Actions (DropdownMenu)
- Sortable columns: Name, Product, Difficulty (client-side sort)
- Difficulty badge colors: Easy = `bg-blue-100 text-blue-700`, Medium = `bg-orange-100 text-orange-700`, Hard = `bg-red-100 text-red-700`
- Actions dropdown: Edit, Clone, Delete
- Pagination: `flex items-center justify-between mt-4`

**frontend/src/components/admin/scenario-editor.tsx** — Scenario editor dialog per UI-SPEC Page 5:
- Props: `scenario: Scenario | null`, `open: boolean`, `onOpenChange`, `onSave`, `isNew: boolean`
- Dialog modal with form fields:
  - Name (Input, required)
  - Description (Textarea)
  - Product (Select)
  - Therapeutic Area (Select)
  - Assigned HCP (Select with avatar preview, fetches from useHcpProfiles)
  - Mode (radio: F2F / Conference)
  - Key Messages (editable checklist using ObjectionList pattern — reuse or create similar)
  - Scoring Weights (ScoringWeights component with linked sliders)
  - Pass Threshold (Input type number, default 70, range 0-100)
- Uses react-hook-form + zod with weight sum validation
- Actions: "Save Scenario" primary, "Clone Scenario" outline (only if editing existing)

**frontend/src/components/admin/service-config-card.tsx** — Azure service config card per UI-SPEC Page 6, PLAT-03:
- Props: `service: { name, description, icon, status }`, `config: { endpoint, apiKey, model, region }`, `onSave`, `onTestConnection`
- Expandable accordion pattern
- Header: Icon (48px bg `bg-primary/10`), name, status dot (green=active, gray=inactive, red=error), description
- Expanded: Form fields for endpoint, API key (password input), model, region + "Test Connection" button
- Test button shows loading spinner, then success/error toast
- Status transitions: inactive (gray) -> testing (spinner) -> active (green) / error (red)

**frontend/src/pages/admin/scenarios.tsx** — Scenario management page:
- Top: Title + "Create Scenario" primary button + status filter (Select: All/Active/Draft)
- ScenarioTable with data from `useScenarios({ status: filterStatus })`
- ScenarioEditor dialog for create/edit
- On delete: confirmation dialog
- On clone: calls `useCloneScenario()`, shows toast
- All text via `useTranslation("admin")`

**frontend/src/pages/admin/azure-config.tsx** — Azure config page per PLAT-03:
- Standard admin page with stacked ServiceConfigCard components
- 5 services: Azure OpenAI, Azure Speech (STT), Azure Speech (TTS), Azure AI Avatar, Azure Content Understanding
- Each card reads/writes config via existing config API or new endpoint
- Test Connection button calls backend endpoint to validate credentials
- For MVP: just show the form UI, backend test endpoint can be stubbed
- All text via `useTranslation("admin")`
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/admin/scoring-weights.tsx contains `adjustWeights` function
    - frontend/src/components/admin/scoring-weights.tsx contains `Slider` import from ui
    - frontend/src/components/admin/scoring-weights.tsx ensures weights total 100 (rounding fix logic present)
    - frontend/src/components/admin/scenario-table.tsx contains `DropdownMenu`
    - frontend/src/components/admin/scenario-table.tsx contains difficulty badge styling
    - frontend/src/components/admin/scenario-editor.tsx contains `Dialog`
    - frontend/src/components/admin/scenario-editor.tsx contains `ScoringWeights`
    - frontend/src/components/admin/service-config-card.tsx contains `testConnection` or `onTestConnection`
    - frontend/src/pages/admin/scenarios.tsx contains `useScenarios`
    - frontend/src/pages/admin/azure-config.tsx contains `ServiceConfigCard`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Scenario management page with table, editor dialog, linked scoring weight sliders. Azure config page with service cards. All i18n-enabled.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- `cd frontend && npm run build` succeeds
- All admin page files exist in src/pages/admin/
- All admin component files exist in src/components/admin/
</verification>

<success_criteria>
All admin pages functional: HCP profile CRUD with personality config, scenario CRUD with scoring weights, Azure service config. All UI matches Figma spec layout.
</success_criteria>

<output>
After completion, create `.planning/phases/02-f2f-text-coaching-and-scoring/02-06-SUMMARY.md`
</output>
