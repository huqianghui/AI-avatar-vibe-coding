---
phase: 20-skill-dry-run-simulation-ai-skill
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/dry-run.ts
  - frontend/src/api/dry-runs.ts
  - frontend/src/hooks/use-dry-runs.ts
  - frontend/public/locales/en-US/skill.json
  - frontend/public/locales/zh-CN/skill.json
autonomous: true
requirements:
  - DR-08

must_haves:
  truths:
    - "TypeScript types match the backend DryRun schemas exactly"
    - "API client provides typed functions for all dry run endpoints"
    - "TanStack Query hooks follow skillKeys factory pattern for cache invalidation"
    - "i18n keys cover all copywriting from UI-SPEC in both en-US and zh-CN"
  artifacts:
    - path: "frontend/src/types/dry-run.ts"
      provides: "TypeScript interfaces for Dry Run domain"
      contains: "interface DryRun"
    - path: "frontend/src/api/dry-runs.ts"
      provides: "API client functions"
      contains: "export async function"
    - path: "frontend/src/hooks/use-dry-runs.ts"
      provides: "TanStack Query hooks"
      contains: "export function useDryRuns"
  key_links:
    - from: "frontend/src/api/dry-runs.ts"
      to: "frontend/src/types/dry-run.ts"
      via: "type imports"
      pattern: "import.*from.*types/dry-run"
    - from: "frontend/src/hooks/use-dry-runs.ts"
      to: "frontend/src/api/dry-runs.ts"
      via: "API function calls"
      pattern: "import.*from.*api/dry-runs"
---

<objective>
Create the frontend data layer for Dry Runs: TypeScript types, API client, TanStack Query hooks, and all i18n translations.

Purpose: Provide the typed data foundation that the report page (Plan 04) and editor integration (Plan 05) will build upon.
Output: Types, API client, hooks with query-key factory, complete i18n for en-US and zh-CN.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-skill-dry-run-simulation-ai-skill/20-UI-SPEC.md

<interfaces>
<!-- Existing frontend patterns -->

From frontend/src/types/skill.ts:
```typescript
export type SkillStatus = "draft" | "review" | "published" | "archived" | "failed";
export interface Skill extends SkillListItem { ... }
```

From frontend/src/api/skills.ts:
```typescript
import apiClient from "./client";
// Pattern: const { data } = await apiClient.get<T>("/skills/...", { params });
```

From frontend/src/hooks/use-skills.ts:
```typescript
export const skillKeys = {
  all: ["skills"] as const,
  lists: () => [...skillKeys.all, "list"] as const,
  detail: (id: string) => [...skillKeys.details(), id] as const,
  evaluation: (id: string) => [...skillKeys.detail(id), "evaluation"] as const,
};
```

From frontend/src/components/shared/chat-bubble.tsx:
```typescript
interface ChatBubbleProps {
  sender: "hcp" | "mr";
  text: string;
  timestamp: Date;
  speakerName?: string;
  speakerColor?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeScript types, API client, and TanStack Query hooks</name>
  <files>frontend/src/types/dry-run.ts, frontend/src/api/dry-runs.ts, frontend/src/hooks/use-dry-runs.ts</files>
  <read_first>
    - frontend/src/types/skill.ts
    - frontend/src/api/skills.ts
    - frontend/src/hooks/use-skills.ts
    - frontend/src/api/client.ts
  </read_first>
  <action>
**Create `frontend/src/types/dry-run.ts`:**

```typescript
export type DryRunStatus = "pending" | "running" | "completed" | "failed" | "cancelled";
export type CoverageStatus = "covered" | "partial" | "not_covered";
export type IssueSeverity = "warning" | "error";

export interface DryRunMessage {
  id: string;
  dry_run_id: string;
  sequence_number: number;
  role: "mr" | "hcp";
  content: string;
  sop_step_id: string | null;
  sop_step_name: string | null;
  created_at: string;
}

export interface SopStepCoverage {
  step_id: string;
  step_name: string;
  status: CoverageStatus;
  matched_message_ids: string[];
  details: string;
}

export interface DryRunIssue {
  severity: IssueSeverity;
  step_id: string | null;
  description: string;
  suggestion: string;
}

export interface DryRunListItem {
  id: string;
  skill_id: string;
  run_number: number;
  status: DryRunStatus;
  executability_score: number | null;
  coverage_percent: number | null;
  total_sop_steps: number;
  covered_sop_steps: number;
  issues_count: number;
  duration_seconds: number | null;
  created_at: string;
}

export interface DryRun extends DryRunListItem {
  partial_sop_steps: number;
  sop_coverage: SopStepCoverage[];
  issues: DryRunIssue[];
  error_message: string;
  messages: DryRunMessage[];
  created_by: string;
}

export interface DryRunStatusResponse {
  status: DryRunStatus;
  covered_sop_steps: number;
  total_sop_steps: number;
  coverage_percent: number | null;
}

export interface PaginatedDryRuns {
  items: DryRunListItem[];
  total: number;
  page: number;
  page_size: number;
  total_pages: number;
}
```

**Create `frontend/src/api/dry-runs.ts`:**

```typescript
import apiClient from "./client";
import type { DryRun, DryRunListItem, DryRunStatusResponse, PaginatedDryRuns } from "@/types/dry-run";

export async function createDryRun(skillId: string): Promise<DryRun> {
  const { data } = await apiClient.post<DryRun>(`/skills/${skillId}/dry-runs`);
  return data;
}

export async function getDryRuns(skillId: string, params?: { page?: number; page_size?: number }): Promise<PaginatedDryRuns> {
  const { data } = await apiClient.get<PaginatedDryRuns>(`/skills/${skillId}/dry-runs`, { params });
  return data;
}

export async function getDryRun(skillId: string, runId: string): Promise<DryRun> {
  const { data } = await apiClient.get<DryRun>(`/skills/${skillId}/dry-runs/${runId}`);
  return data;
}

export async function getDryRunStatus(skillId: string, runId: string): Promise<DryRunStatusResponse> {
  const { data } = await apiClient.get<DryRunStatusResponse>(`/skills/${skillId}/dry-runs/${runId}/status`);
  return data;
}

export async function cancelDryRun(skillId: string, runId: string): Promise<DryRun> {
  const { data } = await apiClient.post<DryRun>(`/skills/${skillId}/dry-runs/${runId}/cancel`);
  return data;
}
```

**Create `frontend/src/hooks/use-dry-runs.ts`:**

```typescript
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { createDryRun, cancelDryRun, getDryRun, getDryRuns, getDryRunStatus } from "@/api/dry-runs";
import { skillKeys } from "./use-skills";

// Query-key factory extending skillKeys pattern
export const dryRunKeys = {
  all: (skillId: string) => [...skillKeys.detail(skillId), "dry-runs"] as const,
  list: (skillId: string, params?: Record<string, unknown>) => [...dryRunKeys.all(skillId), "list", params] as const,
  detail: (skillId: string, runId: string) => [...dryRunKeys.all(skillId), runId] as const,
  status: (skillId: string, runId: string) => [...dryRunKeys.detail(skillId, runId), "status"] as const,
};

export function useDryRuns(skillId: string | undefined, params?: { page?: number; page_size?: number }) {
  return useQuery({
    queryKey: dryRunKeys.list(skillId ?? "", params),
    queryFn: () => getDryRuns(skillId!, params),
    enabled: !!skillId,
  });
}

export function useDryRun(skillId: string | undefined, runId: string | undefined) {
  return useQuery({
    queryKey: dryRunKeys.detail(skillId ?? "", runId ?? ""),
    queryFn: () => getDryRun(skillId!, runId!),
    enabled: !!skillId && !!runId,
  });
}

export function useDryRunStatus(skillId: string, runId: string, enabled: boolean) {
  return useQuery({
    queryKey: dryRunKeys.status(skillId, runId),
    queryFn: () => getDryRunStatus(skillId, runId),
    enabled,
    refetchInterval: enabled ? 3000 : false,  // Poll every 3s per UI-SPEC
  });
}

export function useCreateDryRun() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (skillId: string) => createDryRun(skillId),
    onSuccess: (_data, skillId) => {
      queryClient.invalidateQueries({ queryKey: dryRunKeys.all(skillId) });
    },
  });
}

export function useCancelDryRun() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (args: { skillId: string; runId: string }) => cancelDryRun(args.skillId, args.runId),
    onSuccess: (_data, variables) => {
      queryClient.invalidateQueries({ queryKey: dryRunKeys.all(variables.skillId) });
    },
  });
}
```
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/types/dry-run.ts contains interfaces DryRun, DryRunListItem, DryRunMessage, SopStepCoverage, DryRunIssue, DryRunStatusResponse, PaginatedDryRuns
    - frontend/src/api/dry-runs.ts contains exported functions createDryRun, getDryRuns, getDryRun, getDryRunStatus, cancelDryRun
    - frontend/src/hooks/use-dry-runs.ts contains exported dryRunKeys factory, useDryRuns, useDryRun, useDryRunStatus, useCreateDryRun, useCancelDryRun
    - useDryRunStatus has `refetchInterval: enabled ? 3000 : false` for polling
    - All types import from "@/types/dry-run"
    - TypeScript compilation succeeds with zero errors
  </acceptance_criteria>
  <done>Frontend data layer complete: typed interfaces match backend schemas, API client wraps all endpoints, hooks provide reactive data with 3s polling for progress</done>
</task>

<task type="auto">
  <name>Task 2: Add all Dry Run i18n translations to skill namespace</name>
  <files>frontend/public/locales/en-US/skill.json, frontend/public/locales/zh-CN/skill.json</files>
  <read_first>
    - frontend/public/locales/en-US/skill.json
    - frontend/public/locales/zh-CN/skill.json
    - .planning/phases/20-skill-dry-run-simulation-ai-skill/20-UI-SPEC.md
  </read_first>
  <action>
Add the following keys to both `en-US/skill.json` and `zh-CN/skill.json`, extending the existing content (do NOT overwrite existing keys). All keys go under a new `"dryRun"` top-level key within the skill namespace, matching the UI-SPEC Copywriting Contract exactly.

**en-US/skill.json** — add at the end of the JSON object, before the closing `}`:

```json
"dryRun": {
  "button": "Dry Run",
  "startTitle": "Start Dry Run Simulation",
  "startBody": "The system will simulate a complete MR-HCP training conversation using AI agents to validate this Skill's SOP. Estimated time: 1-3 minutes.",
  "startButton": "Start Simulation",
  "goBack": "Go Back",
  "cancelRun": "Cancel Run",
  "cancelConfirm": "Cancel this simulation? Progress will be lost.",
  "progressTitle": "Dry Run in Progress",
  "progressStep": "Step {{current}} of {{total}} SOP steps covered",
  "progressSimulating": "Simulating conversation...",
  "reportTitle": "Dry Run Report",
  "scoreLabel": "Executability Score",
  "coverageLabel": "SOP Coverage",
  "issuesLabel": "Issues Found",
  "tabConversation": "Conversation",
  "tabCoverage": "SOP Coverage",
  "tabIssues": "Issues",
  "stepCovered": "Covered",
  "stepPartial": "Partial",
  "stepNotCovered": "Not Covered",
  "historyTitle": "Dry Run History",
  "viewAll": "View All",
  "startNewRun": "Start New Run",
  "emptyTitle": "No Dry Runs yet",
  "emptyBody": "Run a simulation to validate your Skill's SOP before publishing.",
  "errorRunFailed": "Simulation failed. Please check the SOP content and try again.",
  "errorLoadFailed": "Failed to load Dry Run results.",
  "toastCompleted": "Dry Run completed. View the report for details.",
  "toastCancelled": "Dry Run cancelled.",
  "tooltipDisabled": "Save the skill content first to run a simulation.",
  "speakerMr": "AI-MR",
  "speakerHcp": "AI-HCP",
  "durationLabel": "Duration",
  "severityWarning": "Warning",
  "severityError": "Error",
  "suggestionPrefix": "Suggestion:",
  "noIssues": "No issues found. The SOP drives a complete and effective conversation.",
  "backToEditor": "Back to Editor",
  "run": "Run #{{number}}",
  "score": "Score",
  "coverage": "Coverage",
  "issues": "Issues",
  "warnings": "warnings",
  "noConversationData": "No conversation data available",
  "noSopSteps": "No SOP steps detected in skill content",
  "errorStartFailed": "Failed to start simulation"
}
```

**zh-CN/skill.json** — add the corresponding Chinese translations:

```json
"dryRun": {
  "button": "模拟测试",
  "startTitle": "启动模拟测试",
  "startBody": "系统将使用 AI Agent 模拟完整的 MR-HCP 培训对话，以验证此 Skill 的 SOP 可执行性。预计耗时：1-3 分钟。",
  "startButton": "开始模拟",
  "goBack": "返回",
  "cancelRun": "取消测试",
  "cancelConfirm": "取消此模拟测试？进度将丢失。",
  "progressTitle": "模拟测试进行中",
  "progressStep": "已覆盖 {{current}}/{{total}} 个 SOP 步骤",
  "progressSimulating": "正在模拟对话...",
  "reportTitle": "模拟测试报告",
  "scoreLabel": "可执行性评分",
  "coverageLabel": "SOP 覆盖率",
  "issuesLabel": "发现问题",
  "tabConversation": "对话记录",
  "tabCoverage": "SOP 覆盖率",
  "tabIssues": "问题列表",
  "stepCovered": "已覆盖",
  "stepPartial": "部分覆盖",
  "stepNotCovered": "未覆盖",
  "historyTitle": "模拟测试历史",
  "viewAll": "查看全部",
  "startNewRun": "启动新测试",
  "emptyTitle": "暂无模拟测试",
  "emptyBody": "在发布前运行模拟测试以验证 Skill 的 SOP 可执行性。",
  "errorRunFailed": "模拟测试失败，请检查 SOP 内容后重试。",
  "errorLoadFailed": "加载模拟测试结果失败。",
  "toastCompleted": "模拟测试已完成，请查看报告了解详情。",
  "toastCancelled": "模拟测试已取消。",
  "tooltipDisabled": "请先保存 Skill 内容后再运行模拟测试。",
  "speakerMr": "AI-MR",
  "speakerHcp": "AI-HCP",
  "durationLabel": "耗时",
  "severityWarning": "警告",
  "severityError": "错误",
  "suggestionPrefix": "建议:",
  "noIssues": "未发现问题。SOP 能驱动完整有效的培训对话。",
  "backToEditor": "返回编辑器",
  "run": "测试 #{{number}}",
  "score": "评分",
  "coverage": "覆盖率",
  "issues": "问题",
  "warnings": "个警告",
  "noConversationData": "无对话数据",
  "noSopSteps": "未在 Skill 内容中检测到 SOP 步骤",
  "errorStartFailed": "启动模拟测试失败"
}
```

Ensure the JSON files remain valid (no trailing commas, proper nesting). The new "dryRun" key should be at the same level as existing "hub" and "editor" keys.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && node -e "JSON.parse(require('fs').readFileSync('public/locales/en-US/skill.json','utf8')); console.log('en-US OK')" && node -e "JSON.parse(require('fs').readFileSync('public/locales/zh-CN/skill.json','utf8')); console.log('zh-CN OK')" && node -e "const j=JSON.parse(require('fs').readFileSync('public/locales/en-US/skill.json','utf8')); console.log('dryRun keys:', Object.keys(j.dryRun).length)"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/public/locales/en-US/skill.json is valid JSON (node -e parse succeeds)
    - frontend/public/locales/zh-CN/skill.json is valid JSON (node -e parse succeeds)
    - en-US skill.json contains key "dryRun" with subkeys matching UI-SPEC copywriting
    - zh-CN skill.json contains corresponding "dryRun" key with Chinese translations
    - Both files contain at least 35 keys under "dryRun"
    - Existing keys ("hub", "editor", "quality", etc.) are preserved unchanged
  </acceptance_criteria>
  <done>Complete i18n coverage for Dry Run feature in both en-US and zh-CN locales, all copywriting matching UI-SPEC contract</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Frontend data layer — no trust boundaries crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-09 | Information Disclosure | API responses | accept | All data is admin-visible skill content, no PII |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` — zero TypeScript errors
2. `node -e "JSON.parse(require('fs').readFileSync('public/locales/en-US/skill.json'))"` — valid JSON
3. `node -e "JSON.parse(require('fs').readFileSync('public/locales/zh-CN/skill.json'))"` — valid JSON
</verification>

<success_criteria>
- All TypeScript types match backend schemas
- API client wraps all 5 dry run endpoints with proper typing
- TanStack Query hooks follow query-key factory pattern
- useDryRunStatus polls every 3 seconds when enabled
- i18n has 35+ keys in both locales matching UI-SPEC copywriting
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/20-skill-dry-run-simulation-ai-skill/20-03-SUMMARY.md`
</output>
