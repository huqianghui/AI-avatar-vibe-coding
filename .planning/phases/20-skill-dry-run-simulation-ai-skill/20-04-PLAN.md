---
phase: 20-skill-dry-run-simulation-ai-skill
plan: 04
type: execute
wave: 2
depends_on: ["20-01", "20-02", "20-03"]
files_modified:
  - frontend/src/components/shared/dry-run-conversation.tsx
  - frontend/src/components/shared/sop-coverage-map.tsx
  - frontend/src/components/shared/coverage-ring-chart.tsx
  - frontend/src/components/shared/dry-run-score-summary.tsx
  - frontend/src/components/shared/dry-run-issue-card.tsx
  - frontend/src/components/shared/dry-run-comparison-chart.tsx
  - frontend/src/pages/admin/dry-run-report.tsx
  - frontend/src/router/index.tsx
autonomous: true
requirements:
  - DR-05
  - DR-06

must_haves:
  truths:
    - "Admin can view a complete Dry Run report with conversation transcript, SOP coverage map, and issues list"
    - "Report page shows 3 summary metric cards (score, coverage, issues) as focal point"
    - "Conversation tab reuses ChatBubble with AI-MR (purple) and AI-HCP (blue) speaker colors"
    - "SOP Coverage tab shows ring chart + clickable step rows with coverage status icons"
    - "Issues tab shows issue cards with severity badges and suggestions"
    - "Route is registered at /admin/skills/:id/dry-run/:runId"
  artifacts:
    - path: "frontend/src/pages/admin/dry-run-report.tsx"
      provides: "Full Dry Run report page"
      contains: "export default function DryRunReportPage"
      min_lines: 100
    - path: "frontend/src/components/shared/dry-run-conversation.tsx"
      provides: "Read-only simulated conversation transcript"
      contains: "export function DryRunConversation"
    - path: "frontend/src/components/shared/sop-coverage-map.tsx"
      provides: "SOP step coverage visualization"
      contains: "export function SopCoverageMap"
    - path: "frontend/src/components/shared/coverage-ring-chart.tsx"
      provides: "Circular percentage chart for coverage"
      contains: "export function CoverageRingChart"
  key_links:
    - from: "frontend/src/pages/admin/dry-run-report.tsx"
      to: "frontend/src/hooks/use-dry-runs.ts"
      via: "useDryRun hook"
      pattern: "useDryRun"
    - from: "frontend/src/router/index.tsx"
      to: "frontend/src/pages/admin/dry-run-report.tsx"
      via: "route registration"
      pattern: "dry-run.*DryRunReportPage"
---

<objective>
Build the Dry Run Report page and all its component dependencies: conversation transcript, SOP coverage map, coverage ring chart, score summary cards, issue cards, and comparison chart.

Purpose: Provide the visual report that admins use to evaluate Dry Run results and identify SOP design issues.
Output: Full report page at /admin/skills/:id/dry-run/:runId with 3 sub-tabs and 6 new shared components.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-skill-dry-run-simulation-ai-skill/20-UI-SPEC.md
@.planning/phases/20-skill-dry-run-simulation-ai-skill/20-01-SUMMARY.md
@.planning/phases/20-skill-dry-run-simulation-ai-skill/20-03-SUMMARY.md

<interfaces>
From frontend/src/types/dry-run.ts (Plan 03):
```typescript
export interface DryRun extends DryRunListItem {
  partial_sop_steps: number;
  sop_coverage: SopStepCoverage[];
  issues: DryRunIssue[];
  error_message: string;
  messages: DryRunMessage[];
  created_by: string;
}
export interface DryRunMessage { id, dry_run_id, sequence_number, role: "mr"|"hcp", content, sop_step_id, sop_step_name, created_at }
export interface SopStepCoverage { step_id, step_name, status: CoverageStatus, matched_message_ids, details }
export interface DryRunIssue { severity: IssueSeverity, step_id, description, suggestion }
```

From frontend/src/hooks/use-dry-runs.ts (Plan 03):
```typescript
export function useDryRun(skillId: string | undefined, runId: string | undefined)
export function useDryRuns(skillId: string | undefined, params?)
```

From frontend/src/components/shared/chat-bubble.tsx:
```typescript
export function ChatBubble({ sender, text, timestamp, speakerName?, speakerColor? }: ChatBubbleProps)
```

From frontend/src/components/ui: Button, Card, CardContent, CardHeader, Badge, Tabs, TabsList, TabsTrigger, TabsContent, ScrollArea, Skeleton, Separator, Tooltip
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 6 shared Dry Run components</name>
  <files>frontend/src/components/shared/dry-run-conversation.tsx, frontend/src/components/shared/sop-coverage-map.tsx, frontend/src/components/shared/coverage-ring-chart.tsx, frontend/src/components/shared/dry-run-score-summary.tsx, frontend/src/components/shared/dry-run-issue-card.tsx, frontend/src/components/shared/dry-run-comparison-chart.tsx</files>
  <read_first>
    - frontend/src/components/shared/chat-bubble.tsx
    - frontend/src/components/shared/quality-radar-chart.tsx
    - frontend/src/components/shared/quality-score-card.tsx
    - frontend/src/components/ui/badge.tsx
    - frontend/src/components/ui/card.tsx
    - frontend/src/components/ui/scroll-area.tsx
    - frontend/src/types/dry-run.ts
    - .planning/phases/20-skill-dry-run-simulation-ai-skill/20-UI-SPEC.md
  </read_first>
  <action>
Create 6 components following the UI-SPEC exactly:

**1. `dry-run-conversation.tsx` — DryRunConversation:**
- Props: `{ messages: DryRunMessage[] }`
- Renders a ScrollArea (max-h-[500px]) containing ChatBubble list
- role="log" aria-label={t("skill:dryRun.tabConversation")}
- Each message renders as ChatBubble:
  - `sender={msg.role}` ("mr" or "hcp")
  - `text={msg.content}`
  - `timestamp={new Date(msg.created_at)}`
  - MR messages: `speakerName={t("skill:dryRun.speakerMr")}` speakerColor="#A855F7" (var(--improvement))
  - HCP messages: `speakerName={t("skill:dryRun.speakerHcp")}` speakerColor="#1E40AF" (var(--primary))
- Each message also has an optional SOP step badge on the right side if `sop_step_id` is present:
  `<Badge variant="outline" className="text-xs ml-2">{msg.sop_step_name}</Badge>`
- Empty state: centered muted text `t("skill:dryRun.noConversationData")`
- Uses `useTranslation("skill")` for i18n

**2. `sop-coverage-map.tsx` — SopCoverageMap:**
- Props: `{ coverage: SopStepCoverage[], onStepClick?: (stepId: string) => void }`
- Renders a list of SopStepRow items (NOT a separate component file -- inline within this file)
- Each row: h-[28px], flex items-center, gap-2, px-3, hover:bg-muted/50 transition cursor-pointer
  - Step ID in mono font: `<span className="font-mono text-sm w-16 shrink-0">{step.step_id}</span>`
  - Step name: `<span className="text-sm flex-1 truncate">{step.step_name}</span>`
  - Status icon right-aligned:
    - "covered": Check icon (lucide-react) in text-strength (#22C55E)
    - "partial": AlertTriangle icon in text-weakness (#F97316)
    - "not_covered": X icon in text-destructive (#EF4444)
  - Status text: Badge with variant appropriate to status
- Clicking a row calls `onStepClick(step.step_id)` if provided
- Row has role="button" aria-expanded={false} (expandable state managed by parent)
- Empty state: `t("skill:dryRun.noSopSteps")`

**3. `coverage-ring-chart.tsx` — CoverageRingChart:**
- Props: `{ percent: number, covered: number, total: number, size?: number }`
- Pure SVG donut chart (no recharts dependency for this simple visualization)
- SVG viewBox="0 0 120 120", default size prop = 120
- Background circle: stroke var(--muted) strokeWidth=12
- Foreground arc: stroke color based on percent (>= 80: var(--strength), 50-79: var(--weakness), < 50: var(--destructive))
- StrokeDasharray/offset calculation for percentage fill
- Center text: percent number in text-2xl font-semibold, "%" in text-sm
- aria-label: `SOP coverage ${percent} percent, ${covered} of ${total} steps covered`
- CSS transition on stroke-dashoffset: 600ms ease-out (per UI-SPEC animation)

**4. `dry-run-score-summary.tsx` — DryRunScoreSummary:**
- Props: `{ score: number | null, coveragePercent: number | null, coveredSteps: number, totalSteps: number, issuesCount: number }`
- Grid layout: grid-cols-1 md:grid-cols-3 gap-4
- Three Card components, each centered content:
  - **Score card**: Label `t("skill:dryRun.scoreLabel")`, number in text-4xl font-semibold, color-coded: >= 70 text-strength, 50-69 text-weakness, < 50 text-destructive. "/100" in text-sm text-muted-foreground
  - **Coverage card**: CoverageRingChart at 64x64, percent text, label `t("skill:dryRun.coverageLabel")`, secondary text "{covered}/{total}"
  - **Issues card**: Count in text-4xl, label `t("skill:dryRun.issuesLabel")`, secondary text `t("skill:dryRun.warnings")`
- Each card: min-w-[140px] p-6, aria-label with full description
- Responsive: 1 col mobile, 3 cols md+

**5. `dry-run-issue-card.tsx` — DryRunIssueCard:**
- Props: `{ issue: DryRunIssue }`
- Card with border-l-4:
  - severity "error": border-l-destructive bg-destructive/5
  - severity "warning": border-l-weakness bg-weakness/5
- AlertTriangle icon (warning) or XCircle icon (error)
- Step reference if step_id present: `Step: {step_id}`
- Description text
- Suggestion text (if not empty): italic, prefixed with `t("skill:dryRun.suggestionPrefix")`

**6. `dry-run-comparison-chart.tsx` — DryRunComparisonChart:**
- Props: `{ runs: DryRunListItem[], onRunClick?: (runId: string) => void }`
- Uses recharts LineChart (already installed in project)
- Height: 200px, responsive container
- X-axis: run_number, Y-axis: 0-100
- Two lines:
  - executability_score: stroke var(--primary), name=t("skill:dryRun.score")
  - coverage_percent: stroke var(--chart-2), name=t("skill:dryRun.coverage")
- Tooltip shows: "Run #N: Score X, Coverage Y%"
- Click point triggers onRunClick(runId)
- Renders only when runs.length >= 2

All components use `useTranslation("skill")` and `cn()` from `@/lib/utils`.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/shared/dry-run-conversation.tsx contains `export function DryRunConversation` that renders ChatBubble components
    - frontend/src/components/shared/sop-coverage-map.tsx contains `export function SopCoverageMap` with step rows using Check/AlertTriangle/X icons
    - frontend/src/components/shared/coverage-ring-chart.tsx contains `export function CoverageRingChart` with SVG donut chart
    - frontend/src/components/shared/dry-run-score-summary.tsx contains `export function DryRunScoreSummary` with 3 Card grid
    - frontend/src/components/shared/dry-run-issue-card.tsx contains `export function DryRunIssueCard` with severity-colored border
    - frontend/src/components/shared/dry-run-comparison-chart.tsx contains `export function DryRunComparisonChart` using recharts LineChart
    - All components import from "@/types/dry-run" for props typing
    - All components use useTranslation("skill") for i18n
    - TypeScript compilation succeeds with zero errors
  </acceptance_criteria>
  <done>6 shared Dry Run components created matching UI-SPEC layout, color, typography, and accessibility specs</done>
</task>

<task type="auto">
  <name>Task 2: Create Dry Run Report page and register route</name>
  <files>frontend/src/pages/admin/dry-run-report.tsx, frontend/src/router/index.tsx</files>
  <read_first>
    - frontend/src/pages/admin/skill-editor.tsx
    - frontend/src/router/index.tsx
    - frontend/src/components/shared/dry-run-conversation.tsx
    - frontend/src/components/shared/sop-coverage-map.tsx
    - frontend/src/components/shared/coverage-ring-chart.tsx
    - frontend/src/components/shared/dry-run-score-summary.tsx
    - frontend/src/components/shared/dry-run-issue-card.tsx
    - frontend/src/hooks/use-dry-runs.ts
    - .planning/phases/20-skill-dry-run-simulation-ai-skill/20-UI-SPEC.md
  </read_first>
  <action>
**Create `frontend/src/pages/admin/dry-run-report.tsx`:**

Full report page following UI-SPEC "Dry Run Report Page" layout:

```typescript
import { useParams, useNavigate } from "react-router-dom";
import { useTranslation } from "react-i18next";
// ... other imports

export default function DryRunReportPage() {
  const { id: skillId, runId } = useParams<{ id: string; runId: string }>();
  const navigate = useNavigate();
  const { t } = useTranslation("skill");
  const { data: dryRun, isLoading, isError } = useDryRun(skillId, runId);
```

Page structure:
1. **Header row**: 
   - Back button (ArrowLeft icon) linking to `/admin/skills/${skillId}/edit`
   - Page title: `t("skill:dryRun.reportTitle")`
   - "Start New Run" button (primary variant) — calls useCreateDryRun mutation then navigates to new run report
2. **Metadata row**: 
   - Run #{run_number} | date (formatted) | Duration: {duration}
   - Text-sm text-muted-foreground
3. **Summary cards**: DryRunScoreSummary component with score, coverage, issues from dryRun data
4. **Sub-tabs** (Tabs component): Conversation | SOP Coverage | Issues
   - **Conversation tab**: DryRunConversation with dryRun.messages
   - **SOP Coverage tab**: CoverageRingChart centered (size 120) + SopCoverageMap below with dryRun.sop_coverage
   - **Issues tab**: 
     - If dryRun.issues.length === 0: success card with Check icon and `t("skill:dryRun.noIssues")`
     - Otherwise: list of DryRunIssueCard for each issue

**Loading state**: 3 Skeleton summary cards + Skeleton conversation area (per UI-SPEC State Specifications)

**Error state**: EmptyState card with AlertTriangle icon + `t("skill:dryRun.errorLoadFailed")` + Retry button

**Update `frontend/src/router/index.tsx`:**

Add a new route inside the admin layout children array, AFTER the skills/:id/edit route (around line 99):
```typescript
{ path: "skills/:id/dry-run/:runId", element: <SuspensePage><DryRunReportPage /></SuspensePage> },
```

Add the lazy import at top of file:
```typescript
const DryRunReportPage = lazy(() => import("@/pages/admin/dry-run-report"));
```
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc --noEmit --pretty 2>&1 | head -30 && node -e "const fs=require('fs'); const r=fs.readFileSync('src/router/index.tsx','utf8'); console.log('Route registered:', r.includes('dry-run/:runId'))"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/admin/dry-run-report.tsx contains `export default function DryRunReportPage`
    - Page uses useDryRun hook to fetch data
    - Page renders DryRunScoreSummary, DryRunConversation, SopCoverageMap, CoverageRingChart, DryRunIssueCard
    - Page has Tabs with 3 sub-tabs (Conversation, SOP Coverage, Issues)
    - Page has loading skeleton state and error state
    - frontend/src/router/index.tsx contains route path "skills/:id/dry-run/:runId"
    - Router imports DryRunReportPage lazily
    - TypeScript compilation succeeds with zero errors
  </acceptance_criteria>
  <done>Complete Dry Run Report page with all sub-tabs, summary cards, and route registered for navigation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Frontend rendering — data already validated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-10 | Information Disclosure | conversation content | accept | Content is admin-created skill SOP, no user PII |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` — zero TypeScript errors
2. `cd frontend && npm run build` — build succeeds
3. Route path "skills/:id/dry-run/:runId" exists in router
</verification>

<success_criteria>
- 6 shared components match UI-SPEC visual and accessibility specs
- Report page renders all 3 sub-tabs with proper data binding
- Coverage ring chart uses SVG with color-coded percentage
- Comparison chart uses recharts LineChart for multi-run trends
- Route accessible at /admin/skills/:id/dry-run/:runId
- All components compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/20-skill-dry-run-simulation-ai-skill/20-04-SUMMARY.md`
</output>
