---
phase: 30-unified-training-pinned-foundry-hcp-agent-kb-retrieval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/a34a_add_session_agent_pin.py
  - backend/app/models/session.py
  - backend/app/schemas/session.py
  - frontend/src/types/session.ts
  - backend/tests/test_phase30_session_pin_foundation.py
autonomous: true
requirements: [R1]

must_haves:
  truths:
    - "A new session record can store one immutable Foundry Prompt Agent name/version snapshot and one internal Responses continuation ID"
    - "Legacy sessions remain representable with null pins, but no historical Agent version is guessed"
    - "Session API output can report the pinned Agent name/version for audit without exposing the internal continuation ID or accepting identity from SessionCreate"
  artifacts:
    - path: "backend/alembic/versions/a34a_add_session_agent_pin.py"
      provides: "Nullable session pin and continuation columns descending from z33a_drop_hcp_voice_fields"
      contains: "down_revision = \"z33a_drop_hcp_voice_fields\""
    - path: "backend/app/models/session.py"
      provides: "CoachingSession agent_name, agent_version, and agent_response_id persistence"
      contains: "agent_response_id"
    - path: "backend/app/schemas/session.py"
      provides: "Read-only session pin response fields with unchanged client-owned create contract"
      contains: "agent_version"
  key_links:
    - from: "backend/app/models/session.py"
      to: "backend/alembic/versions/a34a_add_session_agent_pin.py"
      via: "matching nullable column names and lengths"
      pattern: "agent_(name|version|response_id)"
    - from: "backend/app/schemas/session.py"
      to: "frontend/src/types/session.ts"
      via: "matching nullable response-only pin fields"
      pattern: "agent_(name|version)"
---

<objective>
Establish the persistence and transport contract for a session-owned, immutable Foundry HCP Prompt Agent pin.

Purpose: D-02 requires the Agent name and authoritative version to be snapshotted once; D-03 requires legacy null rows to fail closed later rather than being guessed or backfilled. This foundation deliberately contains no interaction logic.
Output: One reversible Alembic revision, matching ORM fields, response-only API/TypeScript fields, and focused foundation tests.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-CONTEXT.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-RESEARCH.md
@backend/alembic/versions/z33a_drop_hcp_inline_voice_fields.py
@backend/app/models/session.py
@backend/app/schemas/session.py
@frontend/src/types/session.ts

<interfaces>
Existing client-owned create contract (must remain identity-free):
```python
class SessionCreate(BaseModel):
    scenario_id: str
    mode: Literal[...] = "text"
```

New persistence contract created by this plan:
```python
CoachingSession.agent_name: str | None
CoachingSession.agent_version: str | None
CoachingSession.agent_response_id: str | None  # internal only
```

New response contract:
```python
SessionResponse.agent_name: str | None
SessionResponse.agent_version: str | None
# agent_response_id is intentionally absent
```
</interfaces>
</context>

<commit_policy>
D-09 and the CLAUDE.md one-requirement rule require exactly one commit/push only after all Phase 30 tests and real-Azure acceptance pass. Do not commit or push in this plan; leave scoped changes for Plan 30-06. Never use git clean/reset/stash and never touch or stage the unrelated debug files or backend/storage/db-backups/ (D-10).
</commit_policy>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the reversible nullable session pin migration and ORM fields</name>
  <files>backend/alembic/versions/a34a_add_session_agent_pin.py, backend/app/models/session.py, backend/tests/test_phase30_session_pin_foundation.py</files>
  <behavior>
    - Test 1: the revision has revision `a34a_session_agent_pin`, down_revision `z33a_drop_hcp_voice_fields`, and adds exactly nullable `agent_name`, `agent_version`, and `agent_response_id` columns to `coaching_sessions`
    - Test 2: ORM-created legacy-style sessions permit all three fields to be null
    - Test 3: ORM round-trip preserves distinct Agent name/version and continuation ID values
    - Test 4: downgrade removes only the three Phase 30 columns
  </behavior>
  <action>
    Per D-02/D-03, create `a34a_add_session_agent_pin.py` using `op.batch_alter_table("coaching_sessions")` for SQLite compatibility. Add nullable `agent_name` (`String(100)`), `agent_version` (`String(50)`), and `agent_response_id` (`String(255)`) with no server defaults and no data backfill. Downgrade drops those columns in reverse order. Mirror those exact nullable columns on `CoachingSession`. Do not add an HCP FK, do not derive values in model defaults, and do not reuse `focus_instruction` for identity. Add focused migration/ORM tests; execute upgrade against a temporary SQLite schema or inspect resulting columns after Alembic upgrade rather than merely grepping source.
  </action>
  <verify>
    <automated>cd backend && .venv/Scripts/python.exe -m pytest tests/test_phase30_session_pin_foundation.py -x -v</automated>
  </verify>
  <done>The current Alembic head descends from `z33a_drop_hcp_voice_fields`, legacy rows remain null, new rows can preserve all three values, and downgrade is reversible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose audit pins as server-owned response fields only</name>
  <files>backend/app/schemas/session.py, frontend/src/types/session.ts, backend/tests/test_phase30_session_pin_foundation.py</files>
  <behavior>
    - Test 1: `SessionResponse` serializes nullable `agent_name` and `agent_version`
    - Test 2: `SessionResponse` never serializes `agent_response_id`
    - Test 3: `SessionCreate` rejects/ignores no Agent identity by contract because it declares neither Agent field
  </behavior>
  <action>
    Add nullable `agent_name` and `agent_version` to `SessionResponse` and `CoachingSession` TypeScript output. Keep `SessionCreate` exactly browser-owned scenario/mode input: do not add Agent name, Agent version, response ID, Skill context, or additional instructions. Keep `agent_response_id` internal to backend persistence. This implements D-02 while protecting the client-to-API trust boundary in D-03.
  </action>
  <verify>
    <automated>cd backend && .venv/Scripts/python.exe -m pytest tests/test_phase30_session_pin_foundation.py -x -v && cd ../frontend && npx tsc -b</automated>
  </verify>
  <done>API/TypeScript outputs expose only the two audit pins, create input remains identity-free, and the continuation ID cannot leak in session JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> session create API | Scenario/mode are untrusted; Agent identity must never be accepted from the browser |
| Legacy database row -> interaction services | Null session pins are historical unknowns, not permission to infer the latest HCP Agent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01-01 | Tampering | `SessionCreate` | mitigate | Keep all Agent identity fields absent from the request schema; add a contract test |
| T-30-01-02 | Information Disclosure | `SessionResponse` | mitigate | Expose audit name/version only; never serialize `agent_response_id` |
| T-30-01-03 | Elevation of Privilege | Legacy null rows | mitigate | Nullable migration with no guessed backfill; interaction fail-closed behavior is implemented in Plan 30-02 |
</threat_model>

<verification>
Run the focused test file, `alembic upgrade head`, `alembic downgrade z33a_drop_hcp_voice_fields`, and `alembic upgrade head` against a disposable database, then run backend Ruff and frontend typecheck for touched files.
</verification>

<success_criteria>
The schema has a reversible, nullable, no-backfill Agent pin foundation; server responses expose only audit pins; browser input cannot choose Agent identity; Requirement 2 remains untouched (D-01).
</success_criteria>

<output>
After completion, create `.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-01-SUMMARY.md`. Do not commit it yet; Plan 30-06 owns the single requirement commit/push.
</output>
