---
phase: 30-unified-training-pinned-foundry-hcp-agent-kb-retrieval
plan: 06
type: execute
wave: 6
depends_on: [1, 2, 3, 4, 5]
files_modified:
  - backend/tests/integration/test_unified_training_foundry_kb.py
  - frontend/e2e/unified-training-pinned-agent.spec.ts
  - .planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-ACCEPTANCE.md
autonomous: true
requirements: [R1]
user_setup:
  - service: azure-foundry
    why: "Release-gated proof that the exact session-pinned Prompt Agent version has authenticated Foundry IQ MCP retrieval and answers a KB-exclusive question"
    env_vars:
      - name: AZURE_FOUNDRY_ENDPOINT
        source: "Existing backend Azure Foundry project configuration"
      - name: AZURE_FOUNDRY_DEFAULT_PROJECT
        source: "Existing backend Azure Foundry project configuration"
      - name: UNIFIED_TRAINING_HCP_PROFILE_ID
        source: "Local configured HCP profile already synced to the target hosted Agent"
      - name: UNIFIED_TRAINING_SCENARIO_ID
        source: "Active F2F scenario bound to that HCP profile"
      - name: UNIFIED_TRAINING_KB_QUESTION
        source: "Operator-supplied question whose answer exists only in the bound KB"
      - name: UNIFIED_TRAINING_KB_EXPECTED_MARKER
        source: "Stable unique substring expected in the grounded answer"
    dashboard_config:
      - task: "Ensure the target Agent version has an authenticated RemoteTool MCP connection and only knowledge_base_retrieve is allowed"
        location: "Microsoft Foundry Agent / Knowledge configuration"

must_haves:
  truths:
    - "Playwright executes the core Unified Training create/text/voice-boundary story and proves the browser never submits Agent identity"
    - "A credential-gated real Azure test inspects the exact session-pinned Agent version and finds authenticated MCP tooling restricted to knowledge_base_retrieve"
    - "The same pinned Agent answers the operator's KB-exclusive question with the expected marker"
    - "All backend, frontend, Playwright, migration, lint, format, build, and 100% changed-code coverage gates pass before one commit and one push"
    - "Unrelated untracked debug files and backend/storage/db-backups remain byte-for-byte untouched and unstaged"
  artifacts:
    - path: "backend/tests/integration/test_unified_training_foundry_kb.py"
      provides: "Credential-gated exact-version Foundry IQ acceptance"
      contains: "knowledge_base_retrieve"
    - path: "frontend/e2e/unified-training-pinned-agent.spec.ts"
      provides: "Core browser user-story coverage"
      contains: "session_id"
    - path: ".planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-ACCEPTANCE.md"
      provides: "Nonsecret release evidence and exact gate results"
      min_lines: 30
  key_links:
    - from: "backend/tests/integration/test_unified_training_foundry_kb.py"
      to: "client.agents.get_version"
      via: "session.agent_name + session.agent_version exact lookup"
      pattern: "get_version"
    - from: "backend/tests/integration/test_unified_training_foundry_kb.py"
      to: "backend/app/services/agent_chat_service.py"
      via: "same production chat service and exact pin"
      pattern: "chat_with_agent"
    - from: "frontend/e2e/unified-training-pinned-agent.spec.ts"
      to: "Unified Training UI"
      via: "session creation, SSE response, and WS first frame"
      pattern: "session_id"
---

<objective>
Prove Requirement 1 end-to-end, enforce every repository/release gate, record nonsecret evidence, then create and push exactly one allowlisted commit.

Purpose: D-08 requires real—not mocked—Foundry IQ retrieval through the exact pinned Agent version. D-09 requires unit + Playwright + real Azure + all quality gates before release. D-10 requires preservation of unrelated work.
Output: Integration acceptance test, Playwright user story, acceptance evidence, full green verification, and one commit pushed to `feat/0616_shuning`.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-CONTEXT.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-RESEARCH.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-01-SUMMARY.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-02-SUMMARY.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-03-SUMMARY.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-04-SUMMARY.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-05-SUMMARY.md
@backend/tests/test_agent_chat_service.py
@backend/app/services/knowledge_base_service.py
@frontend/e2e/voice-live-proxy.spec.ts
@frontend/e2e/playwright.config.ts

<interfaces>
Verified installed SDK exact-version inspection API:
```python
client.agents.get_version(agent_name: str, agent_version: str) -> AgentVersionDetails
```

Real acceptance must use:
```python
session = await create_session(real_db_transaction, scenario_id, owner_id, mode="text")
agent = client.agents.get_version(session.agent_name, session.agent_version)
result = await chat_with_agent(real_db_transaction, session.agent_name, session.agent_version, question)
```
The transaction must roll back; the test must not create/delete/modify customer Agents, KBs, connections, or documents.
</interfaces>
</context>

<commit_policy>
This plan is the only plan allowed to commit/push. There must be exactly one new Phase 30 commit, created only after every offline and real-Azure gate passes without skip, then pushed once to `origin feat/0616_shuning`. Use explicit allowlist staging. Never use `git add -A`, `git add .`, git clean/reset/stash, or modify/stage `.planning/debug/*` and `backend/storage/db-backups/` (D-09/D-10).
</commit_policy>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Playwright Unified Training pinned-Agent user story</name>
  <files>frontend/e2e/unified-training-pinned-agent.spec.ts</files>
  <behavior>
    - Test 1: user selects/creates a Unified Training session and the POST body contains scenario/mode only—no Agent name/version or Skill temporary context
    - Test 2: returned session exposes pinned name/version and text entry renders streamed HCP text plus existing key-message/hint behavior
    - Test 3: starting Voice Live sends a first WS `session.update` containing only session_id for trusted identity
    - Test 4: a later HCP profile version change does not alter the already-created session pin observed from GET/session behavior
  </behavior>
  <action>
    Create one deterministic Playwright spec for the primary user story. Use existing auth/setup and API helpers. Mock only UI-facing network responses where deterministic rendering requires it; do not claim mocked browser traffic proves Azure routing—the backend unit and real integration tests own that proof. Assert create/request payloads never contain `agent_name`, `agent_version`, `agent_response_id`, `additional_instructions`, or `focus_instruction`. Exercise the Unified Training page's text SSE flow and inspect the Voice Live first WS frame for only `session_id`, following `voice-live-proxy.spec.ts`. Ensure every test actually runs (no `skip`, `fixme`, conditional pass, or error-swallow fallback).
  </action>
  <verify>
    <automated>cd frontend && npx playwright test --config=e2e/playwright.config.ts unified-training-pinned-agent.spec.ts --project=chromium --reporter=line</automated>
  </verify>
  <done>The core browser story executes with zero skipped tests and proves identity-minimal HTTP/WS transport plus preserved text UX.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove exact-version authenticated Foundry IQ retrieval against real Azure</name>
  <files>backend/tests/integration/test_unified_training_foundry_kb.py, .planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-ACCEPTANCE.md</files>
  <behavior>
    - Test 1: absent required env inputs causes an explicit normal-suite skip, but the release command fails unless exactly one test passes and zero skip
    - Test 2: configured scenario/HCP matches, create_session snapshots its exact hosted Agent name/version, and exact `agents.get_version` succeeds
    - Test 3: exact Agent version definition contains at least one authenticated MCP tool whose allowed tool names equal only `[knowledge_base_retrieve]`
    - Test 4: production `chat_with_agent()` invoked with exact session pins returns expected marker and a nonempty response ID
    - Test 5: test rolls back local session state and creates/deletes no Azure resources
  </behavior>
  <action>
    Add `@pytest.mark.integration` acceptance using the operator env inputs and a separate real application DB engine/transaction (not the autouse in-memory fixture). Load the active scenario and HCP, assert `UNIFIED_TRAINING_HCP_PROFILE_ID` matches, use an existing valid owner ID, call production `create_session()`, and keep the transaction rollback-only. Build the existing project client and call `client.agents.get_version(agent_name=session.agent_name, agent_version=session.agent_version)`. Serialize the exact version definition and assert an MCP tool has nonempty `project_connection_id`, an HTTPS server URL whose host is recorded, and `allowed_tools.tool_names` exactly `['knowledge_base_retrieve']` (no broader tool set). Ask `UNIFIED_TRAINING_KB_QUESTION` through production `chat_with_agent()` with the same exact pin; compare marker case-insensitively and require response ID. Do not print secrets/full sensitive question; print/hash the question, session ID, pins, MCP label/host/allowed tool, response ID, marker matched, UTC timestamp. Never create/delete Agent/KB/connection/document resources (D-08).

    Run the explicit release command with integration selection and inspect pytest's terminal summary. A skip is acceptable only in routine offline suites; for this task/release it is a hard failure. Write the nonsecret output and command/result to `30-ACCEPTANCE.md`.
  </action>
  <verify>
    <automated>cd backend && .venv/Scripts/python.exe -m pytest tests/integration/test_unified_training_foundry_kb.py -m integration -x -v -rs | Tee-Object -FilePath phase30-real-azure.log; if (Select-String -Path phase30-real-azure.log -Pattern "skipped|SKIPPED" -Quiet) { exit 1 }; if (-not (Select-String -Path phase30-real-azure.log -Pattern "1 passed" -Quiet)) { exit 1 }</automated>
  </verify>
  <done>The exact pinned Agent version is inspected, authenticated/restricted Foundry IQ MCP tooling is proven, and the KB-exclusive marker is returned with one real test passed and zero skipped.</done>
</task>

<task type="auto">
  <name>Task 3: Run all quality/release gates, verify 100% changed-code coverage, then one allowlisted commit and push</name>
  <files>.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-ACCEPTANCE.md</files>
  <action>
    First capture `git status --short` and SHA-256 hashes for the three unrelated `.planning/debug/*.md` files plus a recursive name/size/hash manifest of `backend/storage/db-backups/`. Verify branch is exactly `feat/0616_shuning` and record pre-release HEAD.

    Run in strict sequence, stopping on any failure:
    1. Disposable DB migration round trip: `alembic upgrade head`, `alembic downgrade z33a_drop_hcp_voice_fields`, `alembic upgrade head` (never point this at customer/prod DB).
    2. Backend `ruff check .`, `ruff format --check .`, and full `pytest -v --cov=app --cov-report=term-missing --cov-report=xml` (normal integration skips allowed here only).
    3. Install/use `diff-cover` in the active dev environment without adding a runtime dependency, then `diff-cover coverage.xml --compare-branch=origin/main --fail-under=100`; this is the D-09 full changed-Python-code coverage gate, not the repository's 89% global baseline.
    4. Frontend `npx tsc -b`, `npm run test:coverage`, `npm run build`; run `diff-cover coverage/lcov.info --compare-branch=origin/main --fail-under=100` for changed executable TypeScript. Type-only lines are validated by `tsc`.
    5. Playwright `npx playwright test --config=e2e/playwright.config.ts unified-training-pinned-agent.spec.ts --project=chromium`; require nonzero passed, zero failed, zero skipped.
    6. Re-run Task 2 real-Azure command; require exactly one passed and zero skipped.
    7. Scope sweeps: no Phase 30 Agent path contains `additional_instructions`; session text no longer references generic registry/CoachRequest/prompt builder/update_sop_progress; WS session path no longer composes focus; exact `agent_version` appears in Responses, WS connect, and WebRTC signaling tests.

    Append exact command summaries, coverage percentages, Playwright count, real-Azure evidence reference, branch/HEAD, and scope-sweep results to `30-ACCEPTANCE.md`. Recompute protected-path hashes/manifests and require exact equality. Confirm `git diff --check` and inspect `git status --short`.

    Stage only an explicit allowlist of Phase 30 production/test files and `.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/`; never use broad add. Confirm `git diff --cached --name-only` contains no `.planning/debug/` or `backend/storage/db-backups/` and only expected Phase 30 paths. Create exactly one commit with message `feat: pin unified training Foundry HCP agent`. Confirm `git rev-list --count <pre-release-head>..HEAD` is exactly 1, then `git push origin HEAD:feat/0616_shuning` once. Verify remote branch resolves to final HEAD and protected unrelated paths remain unstaged/unmodified. If push authentication/network fails, stop and report—do not create another commit.
  </action>
  <verify>
    <automated>git rev-list --count 3a68cbe22c075d425fa63136e8f929537944b55d..HEAD; git status --short; git rev-parse HEAD; git ls-remote origin refs/heads/feat/0616_shuning</automated>
  </verify>
  <done>All gates—including real Azure with zero skip and 100% diff coverage—are recorded green; protected untracked work is unchanged; exactly one allowlisted commit exists and remote `feat/0616_shuning` points to it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local configured DB -> Azure Agent | Test reads real session/HCP identity but must roll back and never mutate customer Azure resources |
| Foundry Agent -> MCP KB | External tool definition/content must be authenticated, restricted, and treated as untrusted output |
| Test results -> release decision | Skips, weak coverage, or mocked evidence must not be accepted as release proof |
| Git workspace -> remote branch | Only requirement files may be staged; unrelated debug/backups must be preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06-01 | Spoofing | Real Agent version acceptance | mitigate | Resolve pins via production session creation and call exact `agents.get_version(name, version)` |
| T-30-06-02 | Tampering | MCP tool scope | mitigate | Require authenticated connection and allowed tools exactly `[knowledge_base_retrieve]` |
| T-30-06-03 | Information Disclosure | Acceptance logs | mitigate | Hash question; record no keys/tokens/full sensitive text |
| T-30-06-04 | Repudiation | Skipped/mock acceptance | mitigate | Explicit zero-skip parser, nonsecret evidence file, exact command outputs |
| T-30-06-05 | Tampering | Git staging | mitigate | Hash protected paths, explicit allowlist staging, cached-name audit, one-commit count |
</threat_model>

<verification>
Every listed command must pass in order. `30-ACCEPTANCE.md` must contain exact nonsecret evidence. Real Azure must show one passed/zero skipped. Diff coverage must be 100%. Playwright must execute with zero skipped. Remote branch SHA must equal local final SHA, with exactly one commit after the recorded baseline.
</verification>

<success_criteria>
Requirement 1 alone is delivered and proven across creation, text, WS/avatar, future WebRTC, and real Foundry IQ retrieval; Requirement 2 is absent; all tests/gates pass; unrelated work is preserved; one commit is pushed.
</success_criteria>

<output>
Create `.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-06-SUMMARY.md` with links to `30-ACCEPTANCE.md`, final commit SHA, remote SHA, test counts, changed-code coverage, and protected-path hash verification. If the executor creates this summary after the final commit, leave it uncommitted rather than amending or creating a second commit; report that explicitly.
</output>
