---
phase: 30-unified-training-pinned-foundry-hcp-agent-kb-retrieval
plan: 04
type: execute
wave: 4
depends_on: [3]
files_modified:
  - backend/app/services/voice_live_websocket.py
  - backend/tests/test_voice_live_session_context.py
  - backend/tests/test_voice_live_websocket.py
autonomous: true
requirements: [R1]

must_haves:
  truths:
    - "Session-bound Voice Live WebSocket resolves identity only from the owned session pin and accepts every supported Unified Training voice/avatar mode"
    - "The SDK connect call receives exact agent_name, agent_version, and project_name, with avatar remaining a modality on that same Agent connection"
    - "Client HCP/prompt/instance/avatar identity overrides and the HCP profile's latest Agent version cannot alter the session Agent pin"
    - "Missing/invalid pins, closed/foreign sessions, missing project, or upstream Agent failure close with structured errors and never fall back to model mode"
  artifacts:
    - path: "backend/app/services/voice_live_websocket.py"
      provides: "Session-owned pinned-Agent Voice Live WebSocket resolution and connect wiring"
      contains: "agent_version"
    - path: "backend/tests/test_voice_live_session_context.py"
      provides: "Owned-session identity, mode, avatar, and fail-closed boundary coverage"
    - path: "backend/tests/test_voice_live_websocket.py"
      provides: "Exact SDK connect kwargs and no-fallback transport tests"
  key_links:
    - from: "backend/app/services/voice_live_websocket.py:_resolve_training_session_context"
      to: "backend/app/services/session_service.py:resolve_pinned_agent"
      via: "owned session pin resolution"
      pattern: "resolve_pinned_agent"
    - from: "backend/app/services/voice_live_websocket.py"
      to: "azure.ai.voicelive.aio.connect"
      via: "agent_name + agent_version + project_name on one Agent connection"
      pattern: "agent_version=.*"
---

<objective>
Rewire session-bound Voice Live WebSocket/avatar from forced model + Skill instructions to the exact session-pinned Prompt Agent version.

Purpose: D-06 requires voice and avatar to use the same immutable Agent identity as text; D-03 forbids model fallback, HCP-latest substitution, and client identity overrides.
Output: Session-pin context resolution, exact SDK kwargs, retained standalone playground behavior, and complete WS tests.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-CONTEXT.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-RESEARCH.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-03-SUMMARY.md
@backend/app/services/voice_live_websocket.py
@backend/tests/test_voice_live_session_context.py
@backend/tests/test_voice_live_websocket.py

<interfaces>
Verified SDK 1.3.0b1 Agent call:
```python
connect(
    endpoint=...,
    credential=...,
    api_version=settings.voice_live_api_version,
    agent_name=pinned.name,
    agent_version=pinned.version,
    project_name=master.default_project,
)
```

Session-bound first browser frame remains identity-minimal:
```json
{"type":"session.update","session":{"session_id":"..."}}
```
Standalone HCP/VL-instance admin playground inputs remain non-session test paths.
</interfaces>
</context>

<commit_policy>
No commit/push in this plan; Plan 30-06 performs the only requirement commit after every gate (D-09). Never alter/stage unrelated untracked debug files or backend/storage/db-backups/ (D-10).
</commit_policy>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve session-owned Voice Live identity, modality, and config boundaries</name>
  <files>backend/app/services/voice_live_websocket.py, backend/tests/test_voice_live_session_context.py</files>
  <behavior>
    - Test 1: owned active F2F voice and digital-human modes return trusted HCP config ID plus exact pinned name/version and correct avatar flag
    - Test 2: both `_model` and `_agent` legacy mode labels are accepted as transport choices but identity always comes from the pin
    - Test 3: foreign/closed/non-F2F sessions and missing/blank/classic pins fail with structured errors
    - Test 4: focus_instruction absence/content is irrelevant and never returned/composed for Agent mode
    - Test 5: HCP latest Agent name/version can differ without changing resolved session identity
  </behavior>
  <action>
    Replace `MODEL_SESSION_MODES`/Skill-focus gating in `_resolve_training_session_context()` with a supported Unified Training voice-mode gate that resolves `PinnedAgentReference` from the already ownership-checked session. Return HCP profile ID only for voice/avatar configuration, exact pin for identity, and avatar flag from session mode. Remove session-bound use of `_compose_session_instructions`, `focus_instruction`, and `force_model_mode` semantics; do not delete standalone playground instruction support that is outside persisted Unified Training. Ensure interaction validation occurs before profile voice config or Azure credential work (D-01/D-03/D-06).
  </action>
  <verify>
    <automated>cd backend && .venv/Scripts/python.exe -m pytest tests/test_voice_live_session_context.py -x -v</automated>
  </verify>
  <done>The session context contains exact pins and modality only, with no Requirement 2 focus transport and no HCP-latest identity dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Connect Voice Live and avatar to the exact pinned Agent version</name>
  <files>backend/app/services/voice_live_websocket.py, backend/tests/test_voice_live_websocket.py, backend/tests/test_voice_live_session_context.py</files>
  <behavior>
    - Test 1: session-bound connect uses exact agent_name/version/project_name/api_version and no model kwarg
    - Test 2: avatar mode adds `Modality.AVATAR` to the same Agent connection; voice-only omits it
    - Test 3: malicious client HCP/system_prompt/VL-instance/avatar fields and changed HCP latest identity are ignored for Agent identity
    - Test 4: invalid pin/project/config/upstream failure emits structured error and connect/model fallback is never attempted
    - Test 5: standalone HCP/VL-instance playground call patterns continue to behave as before
  </behavior>
  <action>
    Load voice/avatar settings from the session scenario HCP/VL instance, but explicitly overwrite/construct connection identity from the trusted training context. Resolve `project_name` server-side from master config and fail closed if blank. In the session branch always choose Agent mode and invoke SDK `connect()` with `agent_name`, `agent_version`, and `project_name`; never supply session Skill instructions or a model fallback. Keep RequestSession voice/audio/avatar settings, transcript proxying, and avatar on the same connection. Include pinned name/version in safe structured logs/proxy metadata if useful, but never credentials. Preserve non-session admin/playground behavior (D-06).
  </action>
  <verify>
    <automated>cd backend && .venv/Scripts/python.exe -m pytest tests/test_voice_live_websocket.py tests/test_voice_live_session_context.py -x -v</automated>
  </verify>
  <done>Voice and avatar connect once to the exact pinned Agent version and every bypass/fallback path is test-proven closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser WS frame -> backend | Client session ID is untrusted; all other supplied identity/prompt fields are ignored on session path |
| Session DB -> Voice Live SDK | Pins are validated and combined with server-owned project/config |
| HCP/VL config -> Agent connect | HCP contributes modality settings, never mutable Agent identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04-01 | Spoofing | Initial `session.update` | mitigate | Resolve owned session server-side and ignore client identity fields |
| T-30-04-02 | Tampering | HCP latest Agent version | mitigate | Use HCP only for voice/avatar config; overwrite identity from session pin |
| T-30-04-03 | Elevation of Privilege | Model fallback | mitigate | Session branch is Agent-only and tests assert no model `connect()` retry |
| T-30-04-04 | Information Disclosure | WS errors/logs | mitigate | Structured codes and nonsecret pins only; never expose credentials |
</threat_model>

<verification>
Run both focused WS suites; assert SDK kwarg equality including version, avatar same-connection modality, no model fallback, client override rejection, HCP-latest mismatch proof, and standalone playground regression coverage. Ruff check/format touched files.
</verification>

<success_criteria>
Unified Training WS voice and avatar use the exact session-pinned Agent name/version/project; all ownership/pin/config failures close; Requirement 2 focus context is absent from the Agent connection.
</success_criteria>

<output>
After completion, create `.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-04-SUMMARY.md`. Do not commit it yet.
</output>
