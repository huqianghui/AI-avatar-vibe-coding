---
phase: 30-unified-training-pinned-foundry-hcp-agent-kb-retrieval
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - backend/app/services/session_service.py
  - backend/tests/test_session_service.py
  - backend/tests/test_sessions_api.py
autonomous: true
requirements: [R1]

must_haves:
  truths:
    - "Creating a new F2F Unified Training session copies the scenario HCP's hosted Agent name and authoritative version exactly once"
    - "Missing HCP, non-synced status, classic asst_* identity, blank name, or blank version rejects creation with a structured error"
    - "Changing the HCP profile's Agent/version after creation does not change the session pin"
    - "Every downstream interaction can resolve and validate identity from the owned session without consulting the HCP's latest Agent version"
  artifacts:
    - path: "backend/app/services/session_service.py"
      provides: "Session creation snapshot and reusable fail-closed pinned-Agent resolver"
      contains: "resolve_pinned_agent"
    - path: "backend/tests/test_session_service.py"
      provides: "Complete success/failure branch coverage for snapshot and immutable resolution"
  key_links:
    - from: "backend/app/services/session_service.py:create_session"
      to: "Scenario.hcp_profile"
      via: "eager-loaded authoritative HCP identity at creation only"
      pattern: "selectinload.*hcp_profile"
    - from: "backend/app/services/session_service.py:resolve_pinned_agent"
      to: "CoachingSession.agent_name/agent_version"
      via: "session-owned validation with no HcpProfile lookup"
      pattern: "session\.agent_(name|version)"
---

<objective>
Implement the single snapshot boundary and reusable fail-closed resolver that all text/voice transports will consume.

Purpose: D-02 freezes historical Agent identity at creation; D-03 prevents classic, blank, unsynced, legacy, client-selected, or latest-profile fallback identities from crossing an interaction boundary.
Output: Deterministic creation pinning, structured validation errors, and unit/API tests covering every branch.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-CONTEXT.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-RESEARCH.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-01-SUMMARY.md
@backend/app/services/session_service.py
@backend/app/models/hcp_profile.py
@backend/app/models/scenario.py
@backend/tests/test_session_service.py

<interfaces>
```python
class HcpProfile:
    agent_id: str
    agent_version: str
    agent_sync_status: str  # authoritative only at creation

@dataclass(frozen=True)
class PinnedAgentReference:
    name: str
    version: str

async def resolve_pinned_agent(session: CoachingSession) -> PinnedAgentReference:
    """Validate fields on the session only; never query/substitute HcpProfile."""
```

Use project structured errors (`AppException`) with distinct codes for missing pin, invalid/classic pin, and non-synced creation source. Exact wording is discretionary, per CONTEXT.
</interfaces>
</context>

<commit_policy>
Do not commit or push in this plan. Plan 30-06 performs the only Phase 30 commit/push after all gates pass (D-09). Never clean, reset, stash, modify, or stage unrelated debug files or backend/storage/db-backups/ (D-10).
</commit_policy>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Snapshot the authoritative hosted Agent during session creation</name>
  <files>backend/app/services/session_service.py, backend/tests/test_session_service.py, backend/tests/test_sessions_api.py</files>
  <behavior>
    - Test 1: synced hosted `agent_id` + nonblank `agent_version` are copied to `session.agent_name/version`; continuation starts null
    - Test 2: later mutation of `HcpProfile.agent_id/version/status` leaves the existing session pin unchanged while a new session snapshots the new values
    - Test 3: missing HCP, status other than synced, empty/whitespace name, `asst_*` name, and empty/whitespace version each fail creation before a session row is flushed
    - Test 4: API returns structured status/code and never permits request Agent fields to override the server snapshot
  </behavior>
  <action>
    Eager-load `Scenario.hcp_profile` in the existing active-scenario query. Before Skill focus processing or `CoachingSession` construction, validate the profile's current sync state and normalize only surrounding whitespace; reject rather than repair/substitute. Persist `profile.agent_id` as `agent_name`, `profile.agent_version` as `agent_version`, and null `agent_response_id`. Never call `resync_classic_agent`, never default version to `"1"`, and never accept pins from `SessionCreate` (D-02/D-03). Preserve existing key-message, mode, Skill audit, and focus snapshot behavior without adding Requirement 2 logic (D-01).
  </action>
  <verify>
    <automated>cd backend && .venv/Scripts/python.exe -m pytest tests/test_session_service.py tests/test_sessions_api.py -x -v</automated>
  </verify>
  <done>Only a fully synced hosted Prompt Agent can create a new Unified Training session, and its exact identity is persisted before any interaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a session-only pinned-Agent resolver for every transport</name>
  <files>backend/app/services/session_service.py, backend/tests/test_session_service.py</files>
  <behavior>
    - Test 1: resolver returns an immutable name/version reference for a valid pin
    - Test 2: null/blank name, null/blank version, and asst_* name produce distinguishable structured errors
    - Test 3: resolver result remains the original value after latest HCP identity is changed or relationship is absent
    - Test 4: resolver performs no HCP query/resync and cannot silently substitute profile values
  </behavior>
  <action>
    Add a small immutable `PinnedAgentReference` contract and `resolve_pinned_agent()` helper that reads only `CoachingSession.agent_name` and `agent_version`. It must strip/check but return the persisted exact values selected by the agreed normalization policy, reject classic `asst_*`, and raise `AppException` with structured pin-specific codes. Do not make status/ownership decisions in this helper; callers first obtain owned sessions and apply their transport lifecycle gate. This separation lets text, WS, WebRTC, and acceptance tests share one non-bypassable identity validation boundary (D-03).
  </action>
  <verify>
    <automated>cd backend && .venv/Scripts/python.exe -m pytest tests/test_session_service.py -x -v -k "pin or agent or create"</automated>
  </verify>
  <done>All transports have one tested session-only resolver, including proof that latest-profile substitution and auto-resync are impossible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scenario/HCP state -> new session | Mutable latest HCP state becomes one immutable historical snapshot |
| Session row -> Azure transport | Persisted values are accepted only after format/presence validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02-01 | Spoofing | Session creation | mitigate | Source identity server-side from eager-loaded scenario HCP; request has no identity fields |
| T-30-02-02 | Tampering | `resolve_pinned_agent()` | mitigate | Validate null/blank/classic values and never read mutable HCP state |
| T-30-02-03 | Repudiation | Historical session identity | mitigate | Persist and expose immutable name/version audit fields at creation |
</threat_model>

<verification>
Focused service/API tests must cover every success/failure branch and demonstrate old-session immutability plus new-session uptake after HCP version changes. Ruff check/format touched Python files.
</verification>

<success_criteria>
Session creation is the only point where HCP Agent identity is consulted; every later resolver is session-only and fail-closed; Requirement 2 behavior is neither added nor injected.
</success_criteria>

<output>
After completion, create `.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-02-SUMMARY.md`. Do not commit it yet.
</output>
