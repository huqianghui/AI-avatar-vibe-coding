---
phase: 30-unified-training-pinned-foundry-hcp-agent-kb-retrieval
plan: 05
type: execute
wave: 5
depends_on: [4]
files_modified:
  - backend/app/api/voice_live.py
  - backend/app/services/voice_live_webrtc.py
  - backend/app/schemas/voice_live.py
  - backend/tests/test_voice_live_webrtc.py
  - frontend/src/api/voice-live.ts
  - frontend/src/types/voice-live.ts
  - frontend/src/api/api-clients.test.ts
autonomous: true
requirements: [R1]

must_haves:
  truths:
    - "The WebRTC broker accepts session_id and resolves the owned session's exact Agent pin server-side"
    - "Session-bound signaling includes exact Agent name, Agent version, project, and configured API version, and never accepts client Agent identity"
    - "Missing/invalid/foreign/closed session pins fail before token exchange and cannot fall back to HCP latest or model signaling"
    - "Existing HCP/VL-instance standalone playground requests remain separate non-session paths"
  artifacts:
    - path: "backend/app/services/voice_live_webrtc.py"
      provides: "Owned-session pinned-Agent WebRTC broker and exact version signaling"
      contains: "session_id"
    - path: "backend/app/schemas/voice_live.py"
      provides: "WebRTC response audit `agent_version`"
      contains: "agent_version"
    - path: "frontend/src/api/voice-live.ts"
      provides: "Optional sessionId transport without client Agent identity"
      contains: "session_id"
  key_links:
    - from: "backend/app/api/voice_live.py"
      to: "backend/app/services/voice_live_webrtc.py"
      via: "authenticated user ID plus session_id"
      pattern: "session_id"
    - from: "backend/app/services/voice_live_webrtc.py"
      to: "backend/app/services/session_service.py:resolve_pinned_agent"
      via: "owned session resolution before signaling/token exchange"
      pattern: "resolve_pinned_agent"
---

<objective>
Close the future WebRTC bypass by adding an explicit authenticated session-bound broker contract that signals the same exact pinned Agent version.

Purpose: D-07 requires WebRTC parity even though current Unified Training uses backend WebSocket; D-03 requires server-side identity and fail-closed behavior.
Output: Backend broker/API/schema changes, matching frontend transport types, and exhaustive unit/API tests while preserving playground calls.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-CONTEXT.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-RESEARCH.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-04-SUMMARY.md
@backend/app/api/voice_live.py
@backend/app/services/voice_live_webrtc.py
@backend/app/schemas/voice_live.py
@backend/tests/test_voice_live_webrtc.py
@frontend/src/api/voice-live.ts
@frontend/src/types/voice-live.ts

<interfaces>
Target broker contract:
```python
async def create_webrtc_session_config(
    db: AsyncSession,
    hcp_profile_id: str | None = None,
    vl_instance_id: str | None = None,
    *,
    session_id: str | None = None,
    user_id: str | None = None,
) -> WebRTCSessionResponse: ...
```

Frontend call remains identity-free:
```typescript
fetchWebRTCSession(hcpProfileId?, vlInstanceId?, sessionId?)
```

For session-bound calls, `session_id` is authoritative and HCP/VL inputs are ignored or rejected; no Agent name/version request parameter is introduced.
</interfaces>
</context>

<commit_policy>
Do not commit/push. Plan 30-06 owns the one final requirement commit/push (D-09). Preserve all unrelated untracked files/backups (D-10).
</commit_policy>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add authenticated session-bound exact-version WebRTC signaling</name>
  <files>backend/app/api/voice_live.py, backend/app/services/voice_live_webrtc.py, backend/app/schemas/voice_live.py, backend/tests/test_voice_live_webrtc.py</files>
  <behavior>
    - Test 1: session_id + owner produces agent signaling with exact pinned name/version/project/api-version and response audit fields
    - Test 2: HCP latest differs but signaling remains pinned; client HCP/VL inputs cannot override session identity
    - Test 3: foreign/closed/non-F2F/missing/blank/classic pin and missing project reject before bearer-token exchange
    - Test 4: no session user context rejects; no Agent-to-model fallback occurs
    - Test 5: legacy standalone HCP and VL-instance paths still pass their existing tests
  </behavior>
  <action>
    Add optional `session_id` query input to the authenticated route and pass `user.id` into the service. When session_id is present, call the same owned-session/pin boundary used by WS (reuse a neutral helper or session service; do not duplicate weaker validation), derive HCP only for voice settings, derive project server-side, and build Agent signaling containing the exact name/version. Use the Azure parameter names verified by the Phase 30 research/SDK endpoint; URL-encode every query value with `urlencode` rather than interpolation. Return `agent_version` alongside existing `agent_id`/project audit metadata. All session validation occurs before `_exchange_api_key_for_bearer_token`. Keep non-session HCP/VL playground behavior separate and unchanged. Never accept Agent identity or Skill instructions from the client (D-01/D-03/D-07).
  </action>
  <verify>
    <automated>cd backend && .venv/Scripts/python.exe -m pytest tests/test_voice_live_webrtc.py -x -v</automated>
  </verify>
  <done>WebRTC has an authenticated, exact-pin, versioned session path with no identity override/fallback and no regression to standalone test paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry session_id through the typed browser broker client</name>
  <files>frontend/src/api/voice-live.ts, frontend/src/types/voice-live.ts, frontend/src/api/api-clients.test.ts</files>
  <behavior>
    - Test 1: optional sessionId serializes only as `session_id` alongside existing optional playground parameters
    - Test 2: no Agent name/version request parameter exists
    - Test 3: WebRTC response type contains optional `agent_version`
    - Test 4: calls without sessionId preserve existing query shape
  </behavior>
  <action>
    Extend `fetchWebRTCSession()` with optional `sessionId` and add `session_id` only when supplied; do not wire the currently unused preview hook into Unified Training (D-07 explicitly records WS as current transport). Add `agent_version?: string` to `WebRTCSessionConfig`. Add API-client unit tests for session-bound and standalone query serialization. Do not add Skill context, Agent identity inputs, UI controls, or a WebRTC transport switch.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/api/api-clients.test.ts && npx tsc -b</automated>
  </verify>
  <done>The browser API can request the secure future session path without choosing Agent identity, while current Unified Training remains WS-based.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser HTTP -> WebRTC broker | session_id is untrusted and must be ownership/lifecycle validated |
| Broker -> signaling URL | Pins/project/version must be URL-encoded server-owned values |
| Broker -> STS | Token exchange must happen only after all authorization/pin checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05-01 | Spoofing | `session_id` query | mitigate | Authenticate user and resolve owned session before signaling |
| T-30-05-02 | Tampering | Signaling query | mitigate | Server-owned exact pins plus `urlencode`; no client Agent parameters |
| T-30-05-03 | Information Disclosure | Bearer token broker | mitigate | Reject invalid session before STS; retain raw-key-never-in-response test |
| T-30-05-04 | Elevation of Privilege | HCP/model fallback | mitigate | Session path ignores HCP identity and cannot choose model signaling |
</threat_model>

<verification>
Backend WebRTC suite covers exact URL params, ownership/lifecycle/pin/project failures before token exchange, no fallback, and standalone regression. Frontend API unit test/typecheck covers optional session_id and response version.
</verification>

<success_criteria>
Any future Unified Training WebRTC caller can supply only session_id and receive exact pinned Agent signaling; current WS use is unchanged; all bypasses fail closed.
</success_criteria>

<output>
After completion, create `.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-05-SUMMARY.md`. Do not commit it yet.
</output>
