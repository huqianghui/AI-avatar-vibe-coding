---
phase: 30-unified-training-pinned-foundry-hcp-agent-kb-retrieval
plan: 03
type: execute
wave: 3
depends_on: [2]
files_modified:
  - backend/app/services/agent_chat_service.py
  - backend/app/api/sessions.py
  - backend/tests/test_agent_chat_service.py
  - backend/tests/test_unified_training_pinned_text.py
autonomous: true
requirements: [R1]

must_haves:
  truths:
    - "POST /sessions/{id}/message streams the exact session-pinned Agent through Responses API and never invokes the generic LLM adapter"
    - "Subsequent text turns pass the prior successful response ID while retaining the same pinned Agent name/version"
    - "Existing SSE text, key_messages, hint, error, and done behavior remains observable"
    - "Upstream failure/cancellation does not persist a partial assistant message or a continuation ID, and there is no prompt/model/Skill fallback"
  artifacts:
    - path: "backend/app/services/agent_chat_service.py"
      provides: "Non-blocking async stream bridge for synchronous Foundry Responses streaming"
      contains: "agent_reference"
    - path: "backend/app/api/sessions.py"
      provides: "Pinned Agent SSE orchestration, persistence, key-message and coaching-hint preservation"
      contains: "resolve_pinned_agent"
    - path: "backend/tests/test_unified_training_pinned_text.py"
      provides: "Transport-level exact-pin, continuity, SSE, failure, and no-fallback tests"
  key_links:
    - from: "backend/app/api/sessions.py"
      to: "backend/app/services/agent_chat_service.py"
      via: "async iterator called with session pin and agent_response_id"
      pattern: "agent_(name|version|response_id)"
    - from: "backend/app/services/agent_chat_service.py"
      to: "Foundry Responses API"
      via: "extra_body.agent_reference plus previous_response_id"
      pattern: "agent_reference"
---

<objective>
Replace Unified Training text's generic adapter/prompt path with a non-blocking, exact-version Foundry Prompt Agent Responses stream while preserving the user-visible SSE contract.

Purpose: D-04 mandates the pinned Prompt Agent for every text turn; D-05 mandates Responses conversation continuity; D-03 forbids fallback or latest-version substitution.
Output: Async Agent stream service, rewired session message route, and exhaustive unit/transport tests.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-CONTEXT.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-RESEARCH.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-02-SUMMARY.md
@backend/app/services/agent_chat_service.py
@backend/app/api/sessions.py
@backend/tests/test_agent_chat_service.py
@frontend/src/hooks/use-sse.ts

<interfaces>
Existing completed-call contract:
```python
async def chat_with_agent(db, agent_name, agent_version, message, previous_response_id=None) -> dict
```

Streaming contract to add (exact event class/name discretionary):
```python
@dataclass(frozen=True)
class AgentResponseEvent:
    kind: Literal["text", "completed"]
    text: str = ""
    response_id: str | None = None

async def stream_agent_response(
    db: AsyncSession,
    agent_name: str,
    agent_version: str,
    message: str,
    previous_response_id: str | None = None,
) -> AsyncIterator[AgentResponseEvent]: ...
```

SSE event names consumed by frontend must stay: `text`, `hint`, `key_messages`, `error`, `done`.
</interfaces>
</context>

<commit_policy>
Do not commit or push. Plan 30-06 owns the single commit/push after all offline, E2E, and real-Azure gates pass (D-09). Preserve unrelated untracked paths exactly (D-10).
</commit_policy>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a strict non-blocking Responses stream for exact Agent references</name>
  <files>backend/app/services/agent_chat_service.py, backend/tests/test_agent_chat_service.py</files>
  <behavior>
    - Test 1: stream request contains exact `agent_reference.name/version/type`, model, input, and optional previous_response_id
    - Test 2: blank/classic name and blank version fail before client invocation; no version `"1"` default remains in `chat_with_agent()` or stream path
    - Test 3: text delta events preserve order and terminal event supplies the actual response ID
    - Test 4: synchronous iterator executes off the event loop; cancellation closes upstream when supported
    - Test 5: upstream exceptions are translated to one typed service failure without yielding a false completion
  </behavior>
  <action>
    Add an async iterator around `openai_client.responses.create(..., stream=True)` using `asyncio.to_thread` plus an `asyncio.Queue` (or an equally non-blocking bridge). Parse actual OpenAI Responses stream event shapes for output-text deltas and the terminal completed response ID; do not fabricate IDs. Ensure worker exceptions/cancellation reach the consumer exactly once and close the upstream stream when available. Factor strict Agent-reference validation shared by existing `chat_with_agent()` and the new stream, deleting the current `agent_version or "1"` fallback. Keep model/project client resolution unchanged. Never add `additional_instructions`, `focus_instruction`, local HCP prompts, or generic model fallback (D-01/D-03/D-04).
  </action>
  <verify>
    <automated>cd backend && .venv/Scripts/python.exe -m pytest tests/test_agent_chat_service.py -x -v</automated>
  </verify>
  <done>The service streams exact pinned Agent events without blocking, defaults, partial-success completion, or Requirement 2 context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire session text SSE and persist successful continuity atomically</name>
  <files>backend/app/api/sessions.py, backend/tests/test_unified_training_pinned_text.py</files>
  <behavior>
    - Test 1: valid first turn saves user + complete assistant, streams text/key_messages/hints/done, and stores returned response ID
    - Test 2: second turn uses stored response ID and the unchanged session name/version even after HCP latest version changes
    - Test 3: missing/invalid pin or closed/foreign session rejects before saving the user message or calling Azure
    - Test 4: upstream error/cancel emits one structured SSE error, no done, no partial assistant, and preserves prior continuation ID
    - Test 5: generic registry adapter, prompt builder, SOP progress/focus injection, and HCP latest identity are never called
  </behavior>
  <action>
    Resolve the owned session and lifecycle, then call `resolve_pinned_agent()` before any message side effect. Save the MR message only after preflight succeeds. Replace `registry.get()/CoachRequest/build_hcp_system_prompt/update_sop_progress` execution with `stream_agent_response(session.agent_name, session.agent_version, request.message, session.agent_response_id)`. Accumulate assistant text; only on a genuine terminal completion save the assistant and assign/flush `agent_response_id`, then run existing key-message detection and coaching suggestion generation and emit `done`. Preserve scoring-rubric lookup required by suggestions, not Agent prompting. On service failure/cancellation, do not save assistant or alter continuation state; emit one JSON structured SSE error (`code`, `message`) and terminate. No adapter/model/prompt fallback is permitted (D-03/D-04/D-05).
  </action>
  <verify>
    <automated>cd backend && .venv/Scripts/python.exe -m pytest tests/test_unified_training_pinned_text.py tests/test_sessions_api.py tests/test_agent_chat_service.py -x -v</automated>
  </verify>
  <done>Text Unified Training preserves its complete SSE UX while every turn uses and continues the exact session pin, with safe failure persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session API -> Foundry Responses | User text is untrusted; Agent identity is server-owned session state |
| Synchronous SDK stream -> async SSE | Worker events/exceptions/cancellation cross concurrency boundary |
| Partial stream -> database | Partial output must not become successful conversation state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03-01 | Spoofing | Responses `agent_reference` | mitigate | Construct only from `resolve_pinned_agent()`, never request/HCP latest fields |
| T-30-03-02 | Tampering | `previous_response_id` | mitigate | Read/write internal session column only; never expose in request schema |
| T-30-03-03 | Denial of Service | Sync SDK iterator | mitigate | Worker-thread queue bridge and cancellation/upstream close handling |
| T-30-03-04 | Repudiation | Partial response persistence | mitigate | Persist assistant and continuation only after terminal completion; test error/cancel branches |
</threat_model>

<verification>
Focused tests prove every SSE event, persistence transition, exact request kwarg, two-turn continuity, cancellation/error branch, and negative assertion that generic adapter/prompt/Skill injection paths are never called. Run Ruff on touched files.
</verification>

<success_criteria>
Text turns use the exact pinned Prompt Agent and Responses continuation ID; user-visible SSE behavior remains; partial/upstream failures fail closed with no fallback or corrupted state.
</success_criteria>

<output>
After completion, create `.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-03-SUMMARY.md`. Do not commit it yet.
</output>
