---
phase: 23-complete-training-session-with-digital-human-full-implementa
plan: 03
type: execute
wave: 2
depends_on: ["23-02"]
files_modified:
  - frontend/src/pages/user/unified-session.tsx
  - frontend/src/components/session/unified-session-layout.tsx
  - frontend/src/components/session/mode-switch-bar.tsx
  - frontend/src/components/session/left-panel/voice-panel.tsx
  - frontend/src/components/session/left-panel/text-panel.tsx
  - frontend/src/components/session/right-panel/chat-transcript.tsx
  - frontend/src/components/session/guidance-cards.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-07
  - D-14

must_haves:
  truths:
    - "Unified session page renders with voice-dominant 2-panel layout (D-02)"
    - "Left panel shows avatar/waveform in voice mode, HCP info in text mode (D-02, D-03)"
    - "Right panel shows unified chat transcript for both modes"
    - "Mode switch bar allows switching between text/voice/digital_human (D-04)"
    - "Inline guidance cards appear and dismiss on user progress (D-07)"
    - "All text uses t() function with i18n keys (D-14)"
  artifacts:
    - path: "frontend/src/pages/user/unified-session.tsx"
      provides: "Main unified session page component"
      min_lines: 80
    - path: "frontend/src/components/session/unified-session-layout.tsx"
      provides: "Voice-dominant 2-panel layout container"
      min_lines: 40
    - path: "frontend/src/components/session/mode-switch-bar.tsx"
      provides: "In-session mode switcher buttons"
      min_lines: 30
    - path: "frontend/src/components/session/left-panel/voice-panel.tsx"
      provides: "Left panel for voice/digital-human mode (avatar + controls)"
      min_lines: 40
    - path: "frontend/src/components/session/left-panel/text-panel.tsx"
      provides: "Left panel for text mode (HCP info + key messages)"
      min_lines: 40
    - path: "frontend/src/components/session/right-panel/chat-transcript.tsx"
      provides: "Unified conversation display"
      min_lines: 40
    - path: "frontend/src/components/session/guidance-cards.tsx"
      provides: "Inline tutorial prompt cards"
      min_lines: 30
  key_links:
    - from: "frontend/src/pages/user/unified-session.tsx"
      to: "frontend/src/hooks/use-unified-session.ts"
      via: "useUnifiedSession hook call"
      pattern: "useUnifiedSession"
    - from: "frontend/src/components/session/unified-session-layout.tsx"
      to: "frontend/src/components/session/left-panel/voice-panel.tsx"
      via: "conditional rendering based on mode"
      pattern: "mode.*voice|digital_human"
---

<objective>
Build the unified training session page with voice-dominant layout, mode switching, and guidance cards.

Purpose: This is the core UI deliverable of Phase 23 — a single page (D-01) that composes existing voice/text components into a voice-dominant 2-panel layout (D-02) with real-time mode switching (D-04) and inline guidance (D-07). Replaces the separate training-session.tsx and voice-session.tsx pages.

Output: Unified session page, layout container, mode switch bar, left panels (voice + text), right panel (chat transcript), and guidance cards. All with full i18n.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-complete-training-session-with-digital-human-full-implementa/23-CONTEXT.md
@.planning/phases/23-complete-training-session-with-digital-human-full-implementa/23-RESEARCH.md
@.planning/phases/23-complete-training-session-with-digital-human-full-implementa/23-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 outputs -->
From frontend/src/types/unified-session.ts:
```typescript
export type UnifiedSessionMode = "text" | "voice" | "digital_human";
export type VoiceConnectionState = "idle" | "connecting" | "connected" | "error" | "disconnecting";
export interface GuidanceCard { id: string; messageKey: string; triggerCondition: string; dismissed: boolean; }
```

From frontend/src/hooks/use-unified-session.ts:
```typescript
export function useUnifiedSession(options?: UseUnifiedSessionOptions): UnifiedSessionControls;
// Returns: { mode, switchMode, degradeToText, isSwitching, voiceConnectionState, setVoiceConnectionState }
```

From frontend/src/hooks/use-audio-recorder.ts:
```typescript
export function useAudioRecorder(): { state, startRecording, pauseRecording, resumeRecording, stopAndGetBlob, reset };
```

<!-- From existing codebase -->
From frontend/src/components/voice/avatar-view.tsx:
```typescript
export function AvatarView({ videoRef, isPlaying, className }: AvatarViewProps): JSX.Element;
```

From frontend/src/components/voice/voice-controls.tsx:
```typescript
export function VoiceControls({ isMuted, isConnected, onToggleMute, onEndSession, ... }): JSX.Element;
```

From frontend/src/components/coach/chat-area.tsx:
```typescript
export function ChatArea({ messages, isStreaming, onSendMessage, ... }): JSX.Element;
```

From frontend/src/components/coach/hints-panel.tsx:
```typescript
export function HintsPanel({ hints, keyMessagesStatus }): JSX.Element;
```

From frontend/src/components/coach/key-messages.tsx:
```typescript
export function KeyMessages({ status }: { status: KeyMessageStatus[] }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unified session layout container + mode switch bar + guidance cards</name>
  <files>frontend/src/components/session/unified-session-layout.tsx, frontend/src/components/session/mode-switch-bar.tsx, frontend/src/components/session/guidance-cards.tsx</files>
  <read_first>
    - frontend/src/pages/user/training-session.tsx
    - frontend/src/components/voice/voice-session.tsx
    - frontend/src/pages/user/voice-session.tsx
    - frontend/src/types/unified-session.ts
    - frontend/src/hooks/use-unified-session.ts
    - frontend/public/locales/en-US/voice.json
    - frontend/public/locales/zh-CN/voice.json
  </read_first>
  <action>
1. Create frontend/src/components/session/unified-session-layout.tsx:
   - Voice-dominant 2-panel full-screen layout (no UserLayout wrapper, same pattern as voice-session)
   - Layout: header (top bar with session info + mode switch + end button), then 2-column grid
   - Left column: 45% width on desktop, full-height. Renders VoicePanel or TextPanel based on mode
   - Right column: 55% width on desktop. Renders ChatTranscript + HintsPanel in vertical stack
   - Mobile: stacked layout with left panel on top (shorter), right panel below
   - Background: bg-background with full viewport height (h-screen)
   - Props: `{ mode, leftPanel, rightPanel, header, guidanceCards }`

2. Create frontend/src/components/session/mode-switch-bar.tsx:
   - Horizontal button group showing 3 mode options: Text, Voice, Digital Human (D-04)
   - Current mode highlighted with primary accent
   - Icons: MessageSquare (text), Mic (voice), User (digital human) from lucide-react
   - onClick calls `switchMode(mode)` from useUnifiedSession
   - While switching, show loading spinner on target button (`isSwitching` state)
   - All labels via t() function with i18n keys: `session.mode.text`, `session.mode.voice`, `session.mode.digitalHuman`
   - Props: `{ currentMode, onSwitchMode, isSwitching, disabled? }`

3. Create frontend/src/components/session/guidance-cards.tsx:
   - Renders a stack of guidance prompt cards that appear at key moments (D-07)
   - Cards: "Click microphone to start talking" (initial voice mode), "Switch modes anytime" (first switch), etc.
   - Each card has dismiss button (X icon), dismissed state stored in localStorage per session
   - Cards auto-dismiss after 10 seconds
   - Uses framer-motion-style fade or simple CSS transition for appearance/disappearance
   - Props: `{ mode, isConnected, modeTransitions, sessionId }`
   - All card text via t() with keys: `session.guidance.startTalking`, `session.guidance.switchModes`, `session.guidance.endSession`
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc --noEmit src/components/session/unified-session-layout.tsx src/components/session/mode-switch-bar.tsx src/components/session/guidance-cards.tsx 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/session/unified-session-layout.tsx contains `export function UnifiedSessionLayout`
    - frontend/src/components/session/unified-session-layout.tsx contains `h-screen` (full viewport height)
    - frontend/src/components/session/mode-switch-bar.tsx contains `export function ModeSwitchBar`
    - frontend/src/components/session/mode-switch-bar.tsx contains `session.mode.text` (i18n key)
    - frontend/src/components/session/mode-switch-bar.tsx contains `session.mode.voice` (i18n key)
    - frontend/src/components/session/guidance-cards.tsx contains `export function GuidanceCards`
    - frontend/src/components/session/guidance-cards.tsx contains `localStorage`
    - frontend/src/components/session/guidance-cards.tsx contains `session.guidance`
  </acceptance_criteria>
  <done>Layout container renders full-screen 2-panel grid, mode switch bar shows 3 modes with i18n labels and loading state, guidance cards appear/dismiss with localStorage persistence</done>
</task>

<task type="auto">
  <name>Task 2: Left panels (voice + text) + right panel (chat transcript) + unified session page</name>
  <files>frontend/src/components/session/left-panel/voice-panel.tsx, frontend/src/components/session/left-panel/text-panel.tsx, frontend/src/components/session/right-panel/chat-transcript.tsx, frontend/src/pages/user/unified-session.tsx</files>
  <read_first>
    - frontend/src/components/voice/avatar-view.tsx
    - frontend/src/components/voice/voice-controls.tsx
    - frontend/src/components/voice/waveform-viz.tsx
    - frontend/src/components/voice/audio-orb.tsx
    - frontend/src/components/coach/chat-area.tsx
    - frontend/src/components/coach/hints-panel.tsx
    - frontend/src/components/coach/key-messages.tsx
    - frontend/src/components/coach/scenario-panel.tsx
    - frontend/src/hooks/use-unified-session.ts
    - frontend/src/hooks/use-audio-recorder.ts
    - frontend/src/hooks/use-sse.ts
    - frontend/src/hooks/use-voice-live.ts
    - frontend/src/hooks/use-session.ts
    - frontend/src/hooks/use-scenarios.ts
    - frontend/src/types/unified-session.ts
    - frontend/src/components/session/unified-session-layout.tsx
    - frontend/src/components/session/mode-switch-bar.tsx
    - frontend/src/components/session/guidance-cards.tsx
  </read_first>
  <action>
1. Create frontend/src/components/session/left-panel/voice-panel.tsx (D-02):
   - Displays AvatarView when digital_human mode and avatar is connected
   - Displays AudioOrb/WaveformViz when voice mode (no avatar)
   - VoiceControls below the visual (mute/unmute mic, end session)
   - Connection status indicator (connecting spinner, connected dot, error icon)
   - Props: `{ mode, videoRef, isAvatarPlaying, voiceConnectionState, isMuted, onToggleMute, onEndSession, avatarCharacter }`
   - Layout: centered vertically, avatar area takes ~70% height, controls bottom 30%

2. Create frontend/src/components/session/left-panel/text-panel.tsx (D-03):
   - Displays HCP avatar placeholder image (circle with initials), HCP name, specialty
   - Scenario description section below
   - Key Messages checklist at bottom (reuse KeyMessages component)
   - Props: `{ hcpName, hcpSpecialty, scenarioDescription, keyMessagesStatus }`
   - Layout: padded panel with vertical stack, subtle border-right separator
   - All text labels via t(): `session.textPanel.hcpInfo`, `session.textPanel.keyMessages`, `session.textPanel.scenario`

3. Create frontend/src/components/session/right-panel/chat-transcript.tsx:
   - Unified conversation display that works for BOTH text and voice modes (D-04)
   - Renders SessionMessage[] with ChatBubble pattern (role-based alignment)
   - Shows typing indicator when AI is responding
   - Text mode: direct message display from useSSEStream
   - Voice mode: transcripts converted to message format (already persisted via persistTranscriptMessage)
   - Auto-scroll to bottom on new message
   - Props: `{ messages, isStreaming, inputMode, onSendTextMessage }`
   - In text mode: shows text input bar at bottom (reuse ChatInput component)
   - In voice mode: no text input (voice controls on left panel)

4. Create frontend/src/pages/user/unified-session.tsx (D-01, D-05):
   - Full-screen page (no UserLayout), reads session params from URL search params (?id=xxx)
   - Fetches session + scenario data using existing useSession/useScenario hooks
   - Initializes useUnifiedSession with defaultMode="voice" (D-05)
   - Initializes useAudioRecorder for voice capture
   - Composes UnifiedSessionLayout with:
     - Header: VoiceSessionHeader (session title + timer + end button)
     - ModeSwitchBar in header area
     - LeftPanel: VoicePanel or TextPanel based on mode
     - RightPanel: ChatTranscript + HintsPanel (collapsible tabs)
     - GuidanceCards overlay
   - Mode switch handler:
     - text -> voice: request mic permission, start audio recorder, connect voice live
     - voice -> text: pause audio recorder, keep voice transcripts in message list
     - digital_human -> text: pause recorder, disconnect avatar
   - On session end: stop recorder, upload audio blob, navigate to scoring page
   - Error states: loading spinner, error message with back button (reuse voice-session.tsx pattern)
   - All text via t() with "session" and "voice" namespaces
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/session/left-panel/voice-panel.tsx contains `export function VoicePanel`
    - frontend/src/components/session/left-panel/voice-panel.tsx contains `AvatarView` or `AudioOrb`
    - frontend/src/components/session/left-panel/text-panel.tsx contains `export function TextPanel`
    - frontend/src/components/session/left-panel/text-panel.tsx contains `KeyMessages` or `keyMessagesStatus`
    - frontend/src/components/session/right-panel/chat-transcript.tsx contains `export function ChatTranscript`
    - frontend/src/components/session/right-panel/chat-transcript.tsx contains `auto-scroll` or `scrollIntoView` or `scrollTo`
    - frontend/src/pages/user/unified-session.tsx contains `export default function UnifiedSession`
    - frontend/src/pages/user/unified-session.tsx contains `useUnifiedSession`
    - frontend/src/pages/user/unified-session.tsx contains `useAudioRecorder`
    - frontend/src/pages/user/unified-session.tsx contains `defaultMode.*voice` or `"voice"`
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>Unified session page fully composed with voice-dominant layout, mode-conditional left panels, unified chat transcript, guidance cards, audio recording integration, and end-session audio upload flow</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> microphone | Audio stream captured from user device |
| UI state -> navigation | Mode transitions affect what user sees and hears |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08 | Information Disclosure | voice-panel audio stream | mitigate | Stream tracks stopped on component unmount and session end |
| T-23-09 | Denial of Service | guidance cards localStorage | accept | Low risk — local storage only, no server impact |
| T-23-10 | Tampering | mode switch state | accept | UI-only state; all data operations authenticated via existing JWT |
</threat_model>

<verification>
1. `cd frontend && npx tsc -b` passes
2. `cd frontend && npm run build` succeeds
3. All components export named functions
4. No `any` types used
5. All user-visible strings use t() function
</verification>

<success_criteria>
- Unified session page renders full-screen with 2-panel voice-dominant layout (D-02)
- Left panel conditionally renders VoicePanel (voice/digital_human) or TextPanel (text) based on mode (D-02, D-03)
- ModeSwitchBar shows 3 mode buttons with active state highlighting (D-04)
- GuidanceCards appear and dismiss with localStorage persistence (D-07)
- Voice is default mode on page load (D-05)
- Chat transcript works for both text messages and voice transcripts in unified list
- All text externalized to i18n (D-14)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-complete-training-session-with-digital-human-full-implementa/23-03-SUMMARY.md`
</output>
