---
phase: 01-foundation-auth-and-design-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/styles/index.css
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/label.tsx
  - frontend/src/components/ui/checkbox.tsx
  - frontend/src/components/ui/avatar.tsx
  - frontend/src/components/ui/dropdown-menu.tsx
  - frontend/src/components/ui/select.tsx
  - frontend/src/components/ui/separator.tsx
  - frontend/src/components/ui/tooltip.tsx
  - frontend/src/components/ui/sheet.tsx
  - frontend/src/components/ui/dialog.tsx
  - frontend/src/components/ui/switch.tsx
  - frontend/src/components/ui/skeleton.tsx
  - frontend/src/components/ui/badge.tsx
  - frontend/src/components/ui/sonner.tsx
  - frontend/src/components/ui/form.tsx
  - frontend/src/components/ui/index.ts
  - frontend/package.json
  - frontend/package-lock.json
autonomous: true
requirements:
  - ARCH-04
  - UI-01

must_haves:
  truths:
    - "Design tokens from Figma Make theme.css are the source of truth for all colors, radii, and typography"
    - "All shadcn/ui primitives needed by Phase 1 are adapted and exported from components/ui/index.ts"
    - "Every UI component imports cn from @/lib/utils (not from ./utils or any relative path)"
    - "Tailwind classes like bg-primary, text-foreground, bg-card work correctly with the new theme"
    - "TypeScript strict mode passes for all UI components (npx tsc -b)"
  artifacts:
    - path: "frontend/src/styles/index.css"
      provides: "Figma Make design tokens as CSS custom properties + Tailwind @theme inline"
      contains: "--primary"
    - path: "frontend/src/components/ui/button.tsx"
      provides: "Button component with cva variants"
      contains: "buttonVariants"
    - path: "frontend/src/components/ui/index.ts"
      provides: "Barrel export for all UI components"
      min_lines: 15
  key_links:
    - from: "frontend/src/components/ui/button.tsx"
      to: "frontend/src/lib/utils.ts"
      via: "cn import"
      pattern: "from \"@/lib/utils\""
    - from: "frontend/src/styles/index.css"
      to: "Figma Make theme.css"
      via: "CSS custom properties"
      pattern: "--primary: #1E40AF"
---

<objective>
Replace existing design tokens with Figma Make theme system (per D-04) and adapt all Phase 1 shadcn/ui components from figma-make/ into frontend/src/components/ui/ (per D-01, D-02).

Purpose: Creates the shared component library (ARCH-04, UI-01) that all pages and layouts will use. Design tokens must be correct before any UI is built.
Output: Complete frontend/src/components/ui/ directory with 17 adapted components, updated design tokens, all new npm dependencies installed.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth-and-design-system/01-UI-SPEC.md

Source files for adaptation:
@figma-make/Design System for SaaS/src/styles/theme.css
@figma-make/Design Login and Layout Shell/src/styles/theme.css
@frontend/src/styles/index.css
@frontend/src/lib/utils.ts
@frontend/package.json
@frontend/tsconfig.json

<interfaces>
<!-- Existing utility the executor needs -->

From frontend/src/lib/utils.ts:
```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

From frontend/tsconfig.json:
```json
{
  "compilerOptions": {
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noUncheckedIndexedAccess": true,
    "paths": { "@/*": ["./src/*"] }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and replace design tokens with Figma Make theme</name>
  <files>
    frontend/package.json,
    frontend/package-lock.json,
    frontend/src/styles/index.css
  </files>
  <read_first>
    frontend/package.json,
    frontend/src/styles/index.css,
    figma-make/Design Login and Layout Shell/src/styles/theme.css,
    figma-make/Design Login and Layout Shell/src/styles/fonts.css,
    figma-make/Design System for SaaS/src/styles/theme.css
  </read_first>
  <action>
    **Step 1: Install all new npm dependencies required by Phase 1 components.**

    Run in frontend/:
    ```bash
    npm install react-i18next i18next i18next-browser-languagedetector i18next-http-backend
    npm install @radix-ui/react-slot @radix-ui/react-dropdown-menu @radix-ui/react-dialog @radix-ui/react-avatar @radix-ui/react-label @radix-ui/react-separator @radix-ui/react-tooltip @radix-ui/react-navigation-menu @radix-ui/react-checkbox @radix-ui/react-select @radix-ui/react-switch
    npm install class-variance-authority sonner vaul
    npm install react-hook-form @hookform/resolvers zod
    ```

    **Step 2: Replace `frontend/src/styles/index.css` entirely.**

    The new file MUST:
    1. Start with `@import "tailwindcss";`
    2. Include the Figma Make `theme.css` content (`:root` vars + `.dark` vars + `@theme inline` block + `@layer base` block)
    3. Override `--primary` from `#030213` (Figma default dark) to `#1E40AF` (project primary blue per D-04 and UI-SPEC)
    4. Override `--primary-foreground` to `#FFFFFF` (white text on blue)
    5. Override `--destructive` from `#d4183d` to `#EF4444` (project destructive red per UI-SPEC)
    6. Override `--sidebar` to `#1E293B` (dark blue admin sidebar per D-05 and UI-SPEC)
    7. Override `--sidebar-primary` to `#1E40AF` (active sidebar item per UI-SPEC)
    8. Override `--sidebar-primary-foreground` to `#FFFFFF`
    9. Add font-family in `@theme inline`: `--font-sans: 'Inter', 'Noto Sans SC', sans-serif;`
    10. Add `--font-mono: 'JetBrains Mono', 'Fira Code', monospace;`
    11. Add Google Fonts import for Inter and Noto Sans SC at the top (after tailwindcss import)
    12. Add scoring semantic colors in `:root`: `--strength: #22C55E; --weakness: #F97316; --improvement: #A855F7;`
    13. Preserve the `--input-background: #F3F3F5` and `--switch-background: #CBCED4` tokens
    14. Preserve the `--font-weight-medium: 500` and `--font-weight-normal: 400` tokens
    15. Preserve the `@layer base` typography rules for h1-h4, label, button, input

    The `@theme inline` block MUST map all CSS vars to `--color-*` tokens so Tailwind classes like `bg-primary`, `text-foreground`, `bg-card`, `border-border` work:
    ```css
    @theme inline {
      --color-background: var(--background);
      --color-foreground: var(--foreground);
      --color-primary: var(--primary);
      --color-primary-foreground: var(--primary-foreground);
      /* ... all other mappings from Figma Make theme.css ... */
      --color-sidebar: var(--sidebar);
      --color-sidebar-foreground: var(--sidebar-foreground);
      /* ... */
      --radius-sm: calc(var(--radius) - 4px);
      --radius-md: calc(var(--radius) - 2px);
      --radius-lg: var(--radius);
      --radius-xl: calc(var(--radius) + 4px);
      --font-sans: 'Inter', 'Noto Sans SC', sans-serif;
      --font-mono: 'JetBrains Mono', 'Fira Code', monospace;
    }
    ```

    CRITICAL: The `@custom-variant dark (&:is(.dark *));` line from Figma Make theme.css MUST be included for dark mode variant support.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && node -e "const pkg = require('./package.json'); const deps = {...pkg.dependencies, ...pkg.devDependencies}; const required = ['react-i18next','i18next','@radix-ui/react-slot','@radix-ui/react-dropdown-menu','class-variance-authority','sonner']; const missing = required.filter(d => !deps[d]); if(missing.length) { console.error('Missing:', missing); process.exit(1); } else { console.log('All deps present'); }"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json contains "react-i18next" in dependencies
    - frontend/package.json contains "i18next" in dependencies
    - frontend/package.json contains "@radix-ui/react-slot" in dependencies
    - frontend/package.json contains "@radix-ui/react-dropdown-menu" in dependencies
    - frontend/package.json contains "class-variance-authority" in dependencies
    - frontend/package.json contains "sonner" in dependencies
    - frontend/package.json contains "vaul" in dependencies
    - frontend/src/styles/index.css contains "--primary: #1E40AF"
    - frontend/src/styles/index.css contains "--sidebar: #1E293B"
    - frontend/src/styles/index.css contains "--destructive: #EF4444"
    - frontend/src/styles/index.css contains "--color-primary: var(--primary)"
    - frontend/src/styles/index.css contains "--font-sans:"
    - frontend/src/styles/index.css contains "@import"
    - frontend/src/styles/index.css contains "@theme inline"
    - frontend/src/styles/index.css contains "@custom-variant dark"
    - frontend/src/styles/index.css contains "--strength: #22C55E"
    - frontend/node_modules/@radix-ui/react-slot directory exists
  </acceptance_criteria>
  <done>All Phase 1 npm dependencies installed. Design tokens replaced with Figma Make theme + project overrides. Tailwind utility classes map to correct colors.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt all Phase 1 shadcn/ui components from figma-make/ into components/ui/</name>
  <files>
    frontend/src/components/ui/button.tsx,
    frontend/src/components/ui/card.tsx,
    frontend/src/components/ui/input.tsx,
    frontend/src/components/ui/label.tsx,
    frontend/src/components/ui/checkbox.tsx,
    frontend/src/components/ui/avatar.tsx,
    frontend/src/components/ui/dropdown-menu.tsx,
    frontend/src/components/ui/select.tsx,
    frontend/src/components/ui/separator.tsx,
    frontend/src/components/ui/tooltip.tsx,
    frontend/src/components/ui/sheet.tsx,
    frontend/src/components/ui/dialog.tsx,
    frontend/src/components/ui/switch.tsx,
    frontend/src/components/ui/skeleton.tsx,
    frontend/src/components/ui/badge.tsx,
    frontend/src/components/ui/sonner.tsx,
    frontend/src/components/ui/form.tsx,
    frontend/src/components/ui/index.ts
  </files>
  <read_first>
    figma-make/Design System for SaaS/src/app/components/ui/button.tsx,
    figma-make/Design System for SaaS/src/app/components/ui/card.tsx,
    figma-make/Design System for SaaS/src/app/components/ui/input.tsx,
    figma-make/Design System for SaaS/src/app/components/ui/label.tsx,
    figma-make/Design System for SaaS/src/app/components/ui/checkbox.tsx,
    figma-make/Design System for SaaS/src/app/components/ui/avatar.tsx,
    figma-make/Design System for SaaS/src/app/components/ui/dropdown-menu.tsx,
    figma-make/Design System for SaaS/src/app/components/ui/select.tsx,
    figma-make/Design System for SaaS/src/app/components/ui/separator.tsx,
    figma-make/Design System for SaaS/src/app/components/ui/tooltip.tsx,
    figma-make/Design System for SaaS/src/app/components/ui/sheet.tsx,
    figma-make/Design System for SaaS/src/app/components/ui/dialog.tsx,
    figma-make/Design System for SaaS/src/app/components/ui/switch.tsx,
    figma-make/Design System for SaaS/src/app/components/ui/skeleton.tsx,
    figma-make/Design System for SaaS/src/app/components/ui/badge.tsx,
    figma-make/Design System for SaaS/src/app/components/ui/sonner.tsx,
    figma-make/Design System for SaaS/src/app/components/ui/form.tsx,
    frontend/src/lib/utils.ts,
    frontend/tsconfig.json
  </read_first>
  <action>
    For EACH of the 17 components listed in UI-SPEC Component Inventory, follow the Figma Make Component Adaptation pattern (RESEARCH.md Pattern 6):

    1. **Copy** the `.tsx` file from `figma-make/Design System for SaaS/src/app/components/ui/` to `frontend/src/components/ui/`
    2. **Replace all imports** of `cn` from relative paths (`"./utils"`, `"../utils"`, `"../../lib/utils"`) to `"@/lib/utils"`
    3. **Remove any unused imports** (TypeScript strict mode: `noUnusedLocals`, `noUnusedParameters`)
    4. **Verify** the component uses Tailwind classes that reference the correct CSS variable names from the updated `index.css` (e.g., `bg-primary`, `text-foreground`, `bg-card`)
    5. **Prefix unused function parameters** with underscore if needed (e.g., `_event` for event handlers not used)

    Specific adaptations per component:
    - `button.tsx`: Must use `class-variance-authority` (`cva`) for variants. Must export `buttonVariants` and `Button`. Variants: default, destructive, outline, secondary, ghost, link. Sizes: default, sm, lg, icon.
    - `card.tsx`: Export `Card`, `CardHeader`, `CardFooter`, `CardTitle`, `CardDescription`, `CardContent`.
    - `input.tsx`: Must use `bg-input-background` for input field background per UI-SPEC.
    - `sonner.tsx`: Imports from `sonner` library (not a Radix primitive).
    - `form.tsx`: Imports from `react-hook-form`. Exports `Form`, `FormField`, `FormItem`, `FormLabel`, `FormControl`, `FormDescription`, `FormMessage`.
    - `sheet.tsx`: Uses `@radix-ui/react-dialog` internally (same as Dialog but with slide-in animation). Used for mobile nav drawer.

    Create `frontend/src/components/ui/index.ts` barrel export file that re-exports ALL components:
    ```typescript
    export { Button, buttonVariants } from "./button";
    export { Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent } from "./card";
    export { Input } from "./input";
    export { Label } from "./label";
    export { Checkbox } from "./checkbox";
    export { Avatar, AvatarImage, AvatarFallback } from "./avatar";
    export { DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator, DropdownMenuLabel } from "./dropdown-menu";
    export { Select, SelectTrigger, SelectValue, SelectContent, SelectItem } from "./select";
    export { Separator } from "./separator";
    export { Tooltip, TooltipTrigger, TooltipContent, TooltipProvider } from "./tooltip";
    export { Sheet, SheetTrigger, SheetContent, SheetHeader, SheetFooter, SheetTitle, SheetDescription } from "./sheet";
    export { Dialog, DialogTrigger, DialogContent, DialogHeader, DialogFooter, DialogTitle, DialogDescription } from "./dialog";
    export { Switch } from "./switch";
    export { Skeleton } from "./skeleton";
    export { Badge, badgeVariants } from "./badge";
    // sonner and form have specific imports, export separately
    ```

    IMPORTANT: Do NOT include any `utils.ts` file in `components/ui/`. The project uses `@/lib/utils` exclusively.

    After all components are created, run `npx tsc -b` from frontend/ to verify TypeScript compilation passes with strict mode.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ui/button.tsx exists and contains `from "@/lib/utils"`
    - frontend/src/components/ui/button.tsx contains `buttonVariants`
    - frontend/src/components/ui/card.tsx exists and contains `from "@/lib/utils"`
    - frontend/src/components/ui/input.tsx exists and contains `from "@/lib/utils"`
    - frontend/src/components/ui/label.tsx exists
    - frontend/src/components/ui/checkbox.tsx exists
    - frontend/src/components/ui/avatar.tsx exists
    - frontend/src/components/ui/dropdown-menu.tsx exists
    - frontend/src/components/ui/select.tsx exists
    - frontend/src/components/ui/separator.tsx exists
    - frontend/src/components/ui/tooltip.tsx exists
    - frontend/src/components/ui/sheet.tsx exists
    - frontend/src/components/ui/dialog.tsx exists
    - frontend/src/components/ui/switch.tsx exists
    - frontend/src/components/ui/skeleton.tsx exists
    - frontend/src/components/ui/badge.tsx exists
    - frontend/src/components/ui/sonner.tsx exists
    - frontend/src/components/ui/form.tsx exists
    - frontend/src/components/ui/index.ts exists and contains `export { Button`
    - No file in frontend/src/components/ui/ contains `from "./utils"` or `from "../utils"`
    - `cd frontend && npx tsc -b` exits 0
  </acceptance_criteria>
  <done>All 17 Phase 1 shadcn/ui components adapted from Figma Make into components/ui/. All imports use @/lib/utils. TypeScript strict mode compilation passes. Barrel export file created.</done>
</task>

</tasks>

<verification>
```bash
cd frontend
npx tsc -b                    # TypeScript strict mode passes
npm run build                 # Vite build succeeds (may need index.html -- note for plan 04)
# Verify no relative utils imports
grep -r "from.*\.\/utils" src/components/ui/ || echo "No relative utils imports found (good)"
# Verify all components use @/lib/utils
grep -r "from.*@/lib/utils" src/components/ui/ | wc -l  # Should be >= 15
```
</verification>

<success_criteria>
- frontend/src/styles/index.css uses Figma Make design token system with project color overrides
- All 17 UI components exist in frontend/src/components/ui/
- All components import cn from "@/lib/utils"
- Barrel export frontend/src/components/ui/index.ts exists
- npx tsc -b passes (TypeScript strict mode)
- All required npm dependencies installed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth-and-design-system/01-02-SUMMARY.md`
</output>
