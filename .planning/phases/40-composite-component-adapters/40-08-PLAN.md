---
phase: 40-composite-component-adapters
plan: 08
type: execute
wave: 8
depends_on: ["40-07"]
files_modified: []
autonomous: true
requirements: [COMP-08]

must_haves:
  truths:
    - "ScrollArea remains on @radix-ui/react-scroll-area — zero migration to Fluent, per the user's 2026-08-05 decision (confirmed in 39-CONTEXT.md and REQUIREMENTS.md's Out of Scope table)"
    - "@radix-ui/react-scroll-area remains listed as a dependency in frontend/package.json — it is NOT removed, and Phase 42's dependency-cleanup (CLEAN-01) must explicitly exclude it from the 'radix packages to uninstall' grep/removal pass"
    - "scroll-area.tsx's existing 2 exports (ScrollArea, ScrollBar) are unchanged — confirmed via direct read this session that the file is already untouched/no-op for this phase"
  artifacts:
    - path: "frontend/src/components/ui/scroll-area.tsx"
      provides: "Unchanged Radix-backed implementation (verification-only, zero code edits) — confirmed via git diff showing no changes"
  key_links:
    - from: "frontend/src/components/ui/scroll-area.tsx"
      to: "@radix-ui/react-scroll-area"
      via: "unchanged direct dependency, explicitly retained per user decision"
      pattern: "@radix-ui/react-scroll-area"
---

<objective>
Formally verify and document that ScrollArea (COMP-08) is explicitly EXCLUDED from the Fluent UI migration and remains on `@radix-ui/react-scroll-area`, per the user's 2026-08-05 decision (confirmed in `39-CONTEXT.md`'s deferred-decisions log and `REQUIREMENTS.md`'s Out-of-Scope table: "Fluent 无等价物，保留 Radix + 自定义滚动条视觉"). This is a verification-only plan with zero code changes — its purpose is to satisfy the "every requirement ID must appear in at least one plan" validation rule while creating a durable, auditable checkpoint that Phase 42's dependency-cleanup work does not accidentally remove `@radix-ui/react-scroll-area` when uninstalling other now-unused Radix packages.

Purpose: Without an explicit plan for COMP-08, there is a real risk that Phase 42's blanket "grep confirms @radix-ui packages are zero-hit, uninstall them all" cleanup step accidentally removes `@radix-ui/react-scroll-area` too, breaking ScrollArea. This plan creates the paper trail and a permanent code comment marking the exception.
Output: A documented, git-committed confirmation (via a code comment addition, not a functional change) that ScrollArea intentionally remains on Radix, plus a verification pass confirming zero regressions from Phase 40's other 7 composite migrations.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-fluent-infrastructure-leaf-components/39-CONTEXT.md
@.planning/REQUIREMENTS.md

<interfaces>
Current `scroll-area.tsx` (confirmed via direct read, 47 lines, 2 exports):
```ts
export { ScrollArea, ScrollBar };
// Both backed by @radix-ui/react-scroll-area, zero Fluent dependency
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit retention comment to scroll-area.tsx and run full-suite regression verification</name>
  <files>frontend/src/components/ui/scroll-area.tsx</files>
  <read_first>
    frontend/src/components/ui/scroll-area.tsx
    frontend/package.json
  </read_first>
  <action>
    Add a single doc comment at the top of `scroll-area.tsx` (the ONLY change in this plan) stating clearly:
    ```ts
    // COMP-08 / Phase 40: ScrollArea intentionally remains on @radix-ui/react-scroll-area.
    // Fluent UI v9 has no equivalent primitive with matching custom-scrollbar visuals.
    // User decision 2026-08-05 (see .planning/phases/39-fluent-infrastructure-leaf-components/39-CONTEXT.md).
    // Phase 42 dependency cleanup (CLEAN-01) MUST exclude @radix-ui/react-scroll-area from removal.
    ```

    Confirm via `grep -n "@radix-ui/react-scroll-area" frontend/package.json` that the dependency is still present (it should be — no package.json change needed in this plan).

    Run the FULL vitest suite and FULL Playwright E2E suite as the final regression gate for all of Phase 40's 7 code-changing plans (40-01 through 40-07) combined, since this is the last plan in the phase's sequential chain: `cd frontend && npx vitest run && npx playwright test`. Fix any cross-component regressions discovered here that individual plans' scoped E2E runs may have missed (e.g. interaction effects between migrated Dialog and migrated Sheet on the same page, or Select/DropdownMenu nested inside a migrated Dialog).

    If any regression is found, determine which prior plan's component is responsible and fix it in a NEW commit scoped to that component only (do not bundle multiple component fixes into one commit, even at this late verification stage — CLAUDE.md's one-thing-per-commit rule still applies).

    Commit the single comment addition: `git add frontend/src/components/ui/scroll-area.tsx` then `git commit -m "docs(40-08): confirm ScrollArea retained on Radix per COMP-08"`, then `git push`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "COMP-08" frontend/src/components/ui/scroll-area.tsx` present
    - `grep -n "@radix-ui/react-scroll-area" frontend/package.json` present (unchanged)
    - `cd frontend && npx vitest run` exits 0 (full suite, all components from 40-01 through 40-07 combined)
    - `cd frontend && npx playwright test` exits 0 (full E2E suite)
    - `cd frontend && npx tsc -b` exits 0
    - `cd frontend && npm run build` exits 0
    - `git log -1 --name-only` shows exactly scroll-area.tsx, commit message `docs(40-08): confirm ScrollArea retained on Radix per COMP-08`
  </acceptance_criteria>
  <done>ScrollArea's exclusion from the Fluent migration is documented in code with a durable comment referencing the user's decision and Phase 42's cleanup constraint; the full unit + E2E suite passes as the final regression gate for all 8 Phase 40 composites combined; committed and pushed independently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Documentation-only change; no functional code path affected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-40-15 | Tampering (future accidental dependency removal) | Phase 42 dependency cleanup | mitigate | Explicit code comment + this plan's existence create a durable, grep-able marker preventing accidental @radix-ui/react-scroll-area removal in Phase 42 |
</threat_model>

<verification>
1. `cd frontend && npx vitest run` — full suite green, covering all Phase 40 composites.
2. `cd frontend && npx playwright test` — full E2E suite green.
3. `cd frontend && npx tsc -b` clean.
4. `cd frontend && npm run build` succeeds.
5. Exactly one commit, pushed.
</verification>

<success_criteria>
- ScrollArea's Radix retention is explicitly documented in code and traceable to the user's decision.
- Full unit + E2E suite passes as the final Phase 40 regression gate across all 8 composites.
- @radix-ui/react-scroll-area confirmed still present in package.json.
- One independent commit, pushed.
</success_criteria>

<output>
After completion, create `.planning/phases/40-composite-component-adapters/40-08-SUMMARY.md`
</output>
