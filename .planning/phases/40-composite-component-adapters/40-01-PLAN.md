---
phase: 40-composite-component-adapters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/dialog.tsx
  - frontend/src/components/ui/dialog.test.tsx
  - frontend/src/components/ui/_shim-as-child.ts
autonomous: true
requirements: [COMP-01]

must_haves:
  truths:
    - "All 10 existing named exports (Dialog, DialogClose, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogOverlay, DialogPortal, DialogTitle, DialogTrigger) still exist with unchanged prop signatures — every consumer (assign-hcp-dialog.tsx, connect-kb-dialog.tsx, and other Dialog consumers) compiles and renders without modification"
    - "Opening a Dialog renders a role='dialog' element portaled to document.body with the app's active Fluent theme/brand (not Microsoft default blue) applied to its surface"
    - "DialogTitle's text is exposed via aria-labelledby on the dialog surface; if DialogDescription is present its text is exposed via aria-describedby"
    - "Clicking the close (X) button or pressing Escape closes the dialog"
  artifacts:
    - path: "frontend/src/components/ui/dialog.tsx"
      provides: "Fluent DialogSurface/DialogBody-backed adapter preserving all 10 named exports and all 10 data-slot values"
    - path: "frontend/src/components/ui/dialog.test.tsx"
      provides: "New unit test file (none existed before) covering render, open/close, aria-labelledby/describedby wiring, data-slot preservation"
    - path: "frontend/src/components/ui/_shim-as-child.ts"
      provides: "Shared isSingleRefCapableElement guard + cloneElement helper, extracted from button.tsx's Phase 39 pattern, reused by DialogTrigger and by Sheet/DropdownMenu triggers in later Phase 40 plans"
  key_links:
    - from: "frontend/src/components/ui/dialog.tsx"
      to: "@fluentui/react-components DialogSurface/DialogBody"
      via: "DialogContent composes DialogSurface > DialogBody > children, per research Pattern 1"
      pattern: "DialogSurface"
    - from: "frontend/src/components/ui/dialog.tsx"
      to: "frontend/src/components/ui/_shim-as-child.ts"
      via: "DialogTrigger imports isSingleRefCapableElement for its asChild-equivalent behavior"
      pattern: "_shim-as-child"
---

<objective>
Migrate Dialog (COMP-01) — the FIRST composite in Phase 40 — to be internally backed by Fluent UI's Dialog/DialogSurface/DialogBody, preserving all 10 existing named exports, all 10 `data-slot` values, and Radix-equivalent ARIA wiring (`aria-labelledby`/`aria-describedby`). This plan also extracts the `isSingleRefCapableElement` `asChild`-shim guard (Phase 39's `button.tsx` precedent) into a new shared utility file so every subsequent Phase 40 overlay component (Sheet, DropdownMenu) reuses one implementation instead of copy-pasting it 3-4 times (research Pitfall 5 / Assumption A2).

Purpose: Dialog is deliberately executed FIRST among the 8 composites because it establishes the Fluent-portal-inherits-app-theme pattern (Pitfall 6) that every subsequent portal-based composite (Sheet's OverlayDrawer, Select/DropdownMenu's popovers, Tooltip's positioned surface) depends on. Getting this wrong here means it's wrong everywhere.
Output: `dialog.tsx` Fluent-backed with unchanged export surface; `dialog.test.tsx` created (no prior test file existed — confirmed via direct `ls` this session); `_shim-as-child.ts` extracted as a new shared utility.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-composite-component-adapters/40-RESEARCH.md
@.planning/phases/39-fluent-infrastructure-leaf-components/39-CONTEXT.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md

<interfaces>
Current `dialog.tsx` full export list (confirmed via direct read this session, 134 lines):
```ts
export {
  Dialog,             // DialogPrimitive.Root wrapper, data-slot="dialog"
  DialogClose,        // DialogPrimitive.Close wrapper, data-slot="dialog-close"
  DialogContent,       // composes Portal>Overlay+Content, data-slot="dialog-content"
  DialogDescription,   // DialogPrimitive.Description, data-slot="dialog-description"
  DialogFooter,        // plain <div>, data-slot="dialog-footer"
  DialogHeader,        // plain <div>, data-slot="dialog-header"
  DialogOverlay,       // DialogPrimitive.Overlay, data-slot="dialog-overlay"
  DialogPortal,        // DialogPrimitive.Portal, data-slot="dialog-portal"
  DialogTitle,         // DialogPrimitive.Title, data-slot="dialog-title"
  DialogTrigger,       // DialogPrimitive.Trigger, data-slot="dialog-trigger"
};
```
All 10 data-slot values to preserve exactly: `dialog`, `dialog-close`, `dialog-content`, `dialog-description`, `dialog-footer`, `dialog-header`, `dialog-overlay`, `dialog-portal`, `dialog-title`, `dialog-trigger`.

Fluent UI v9 imports needed (verify exact shape against installed `.d.ts` in `node_modules/@fluentui/react-components` before writing — do not assume, per Phase 39's probe-before-implementing precedent):
```ts
import {
  Dialog as FluentDialog,
  DialogTrigger as FluentDialogTrigger,
  DialogSurface,
  DialogBody,
  DialogTitle as FluentDialogTitle,
  DialogContent as FluentDialogContent,
  DialogActions,
  mergeClasses,
} from "@fluentui/react-components";
```
**Naming collision warning (research Anti-Pattern, confirmed critical):** Fluent's own `DialogContent`/`DialogTitle` are INNER sub-slots inside `DialogBody`, NOT the all-in-one surface wrapper our exported `DialogContent`/`DialogTitle` need to be. Alias Fluent's imports (`as FluentDialogContent`, `as FluentDialogTitle`) to avoid confusing them with this file's own exports of the same name.

Phase 39's `isSingleRefCapableElement` guard to extract (from `frontend/src/components/ui/button.tsx` lines 54-67, read this session):
```ts
function isSingleRefCapableElement(
  children: React.ReactNode,
): children is React.ReactElement<{ className?: string }> {
  if (!React.isValidElement(children)) return false;
  if (children.type === React.Fragment) return false;
  return true;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract asChild shim util + migrate Dialog to Fluent UI</name>
  <files>frontend/src/components/ui/_shim-as-child.ts, frontend/src/components/ui/dialog.tsx, frontend/src/components/ui/dialog.test.tsx</files>
  <read_first>
    frontend/src/components/ui/dialog.tsx
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/index.ts
  </read_first>
  <behavior>
    - Test 1: `<Dialog open><DialogContent><DialogTitle>T</DialogTitle></DialogContent></Dialog>` renders an element with `role="dialog"` visible in the document
    - Test 2: the rendered dialog surface has `aria-labelledby` pointing to the `DialogTitle` element's `id`, and the title text is visible
    - Test 3: when `DialogDescription` is present, the dialog surface's `aria-describedby` points to its `id` and its text is visible
    - Test 4: clicking the rendered close (X) button (or calling `onOpenChange(false)`, whichever the adapter wires) results in the dialog surface no longer being in the document
    - Test 5: `data-slot` is present and correct on `Dialog`, `DialogTrigger`, `DialogContent`, `DialogHeader`, `DialogFooter`, `DialogTitle`, `DialogDescription` (7 of the 10 slots directly testable via render; `DialogPortal`/`DialogOverlay`/`DialogClose` covered by usage inside Test 1/4)
    - Test 6: consumer `className` passed to `DialogContent` is present in the rendered surface's class list (via `mergeClasses`)
  </behavior>
  <action>
    Create `frontend/src/components/ui/_shim-as-child.ts` exporting `isSingleRefCapableElement` (verbatim logic from `button.tsx` lines 54-67, add a one-line doc comment noting it is the shared Phase 40 copy — do not modify `button.tsx` itself, it is out of this plan's `files_modified` scope and Phase 39 already shipped/committed it).

    Rewrite `dialog.tsx`:
    - `Dialog` → thin wrapper around Fluent's `Dialog` root (`data-slot="dialog"`), preserving `open`/`onOpenChange`/`modalType` passthrough.
    - `DialogTrigger` → wraps Fluent's `DialogTrigger` (Fluent's own `DialogTrigger` already clones its single child by default per its own documented behavior — verify this against the installed `.d.ts`/source before deciding whether `_shim-as-child`'s guard is additionally needed here; if Fluent's own trigger sufficiently clones onClick/aria props onto an arbitrary child element without a separate shim, use it directly and note this finding in the SUMMARY; only fall back to the `_shim-as-child` guard if Fluent's trigger does NOT natively support passing a custom child the way Radix's `asChild` did).
    - `DialogContent` → per research Pattern 1: `<DialogSurface data-slot="dialog-content" className={mergeClasses(className)}><DialogBody>{children}<button data-slot="dialog-close" aria-label="Close" onClick={...}><XIcon/></button></DialogBody></DialogSurface>`, wrapped in `DialogPortal`/no-portal-needed-if-Fluent-Dialog-portals-internally (verify: Fluent's `Dialog` typically portals its `DialogSurface` automatically — confirm via rendered DOM location in Test 1, do not assume). `DialogPortal`/`DialogOverlay` become thin pass-through/no-op wrappers preserving their exported names and `data-slot` values for any consumer that still imports them directly, since Fluent's Dialog manages its own overlay/portal internally.
    - `DialogTitle`/`DialogDescription` → use Fluent's own `DialogTitle` (aliased) for `DialogTitle` to get `aria-labelledby` wiring "for free" per Fluent's documented context-based association; for `DialogDescription`, if Fluent has no equivalent auto-wiring, manually generate an `id` via `React.useId()` and thread it into the surrounding `DialogBody`'s `aria-describedby` — verify empirically via Test 3 which approach actually produces the correct `aria-describedby` linkage, do not assume.
    - `DialogHeader`/`DialogFooter`/`DialogClose` remain simple styled wrappers (`DialogHeader`/`DialogFooter` stay plain `<div>` with `mergeClasses` instead of `cn`; `DialogClose` wraps Fluent's own close-trigger mechanism or a plain button with `onClick={() => onOpenChange?.(false)}` sourced from Dialog's context, whichever Fluent's API supports — verify against `.d.ts`).
    - Preserve the `XIcon` close-button icon from `lucide-react` unchanged (icon replacement is Phase 41 scope, not this phase).
    - Use `mergeClasses()` from `@fluentui/react-components` instead of `cn()` for all internal className composition, appending consumer `className` last (Phase 39 convention).

    Create `dialog.test.tsx` (no prior file existed) with the 6 tests above.

    Run the FULL existing Playwright E2E suite before committing (research Pitfall 7 — not just new specs): `cd frontend && npx playwright test` (or the targeted subset touching Dialog consumers if the full suite is too slow to run in this session — grep `e2e/` for `Dialog`/`assign-hcp-dialog`/`connect-kb-dialog` usage first to scope it). Fix any newly-broken ARIA-selector assertions as part of THIS component's commit (Pitfall 7).

    After all unit tests + the scoped/full E2E pass, stage and commit: `git add frontend/src/components/ui/dialog.tsx frontend/src/components/ui/dialog.test.tsx frontend/src/components/ui/_shim-as-child.ts` then `git commit -m "feat(40-01): migrate Dialog to Fluent UI"`, then `git push`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ui/dialog.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/ui/dialog.test.tsx` exits 0, all 6 tests passing
    - `cd frontend && npx tsc -b` exits 0
    - `grep -c "data-slot=\"dialog" frontend/src/components/ui/dialog.tsx` returns at least 7 (7+ distinct data-slot literal occurrences across the file)
    - `grep -n "export {" -A 12 frontend/src/components/ui/dialog.tsx` lists all 10 original export names unchanged
    - `grep -n "isSingleRefCapableElement" frontend/src/components/ui/_shim-as-child.ts` present
    - `cd frontend && npm run build` exits 0
    - `git log -1 --name-only` shows exactly the 3 files (dialog.tsx, dialog.test.tsx, _shim-as-child.ts), commit message `feat(40-01): migrate Dialog to Fluent UI`
  </acceptance_criteria>
  <done>Dialog is Fluent-backed, all 10 exports/data-slots preserved, ARIA labelling/describedby verified via tests, shared asChild-shim util extracted for reuse by Sheet/DropdownMenu, full E2E regression checked, committed and pushed independently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure UI component internals swap; Dialog renders only consumer-supplied children, same as before |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-40-01 | Tampering (functional correctness) | Dialog ARIA labelling/describedby wiring | mitigate | Tests 2/3 explicitly assert `aria-labelledby`/`aria-describedby` linkage, not just visual render |
| T-40-02 | Information Disclosure (theme leak) | DialogSurface portal theme inheritance | mitigate | Manual check: portaled dialog surface renders with the app's active Fluent brand token, not Microsoft default blue (Pitfall 6) — verify by rendering with a non-default accent selected |
</threat_model>

<verification>
1. `cd frontend && npx vitest run src/components/ui/dialog.test.tsx` all green.
2. `cd frontend && npx tsc -b` clean.
3. `cd frontend && npm run build` succeeds.
4. Full or Dialog-scoped Playwright E2E suite passes with zero new failures.
5. Exactly one commit, pushed.
</verification>

<success_criteria>
- Dialog fully Fluent-backed with all 10 named exports and data-slot values preserved.
- ARIA labelledby/describedby wiring empirically verified, not assumed.
- Shared `_shim-as-child.ts` utility created for reuse by Sheet (40-05) and DropdownMenu (40-07).
- Zero new Playwright E2E failures.
- One independent commit, pushed.
</success_criteria>

<output>
After completion, create `.planning/phases/40-composite-component-adapters/40-01-SUMMARY.md`
</output>
</verification>
