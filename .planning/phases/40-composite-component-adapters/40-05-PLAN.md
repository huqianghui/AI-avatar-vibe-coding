---
phase: 40-composite-component-adapters
plan: 05
type: execute
wave: 5
depends_on: ["40-04"]
files_modified:
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/card.styles.ts
  - frontend/src/components/ui/card.test.tsx
autonomous: true
requirements: [COMP-06]

must_haves:
  truths:
    - "A go/no-go decision is made and documented: Option B (keep plain Tailwind divs + Griffel makeStyles token overrides) is selected over adopting Fluent's real Card component, because Fluent Card carries selectable-list semantics (hover/selected/interactive states, onSelectionChange) that are not part of this codebase's usage pattern and would require defensive prop-stripping on every one of the many Card consumers"
    - "All 7 existing named exports (Card, CardHeader, CardTitle, CardDescription, CardAction, CardContent, CardFooter) still exist with unchanged prop signatures and unchanged underlying element types (div/h4/p)"
    - "Card's visual tokens (border, background, shadow, radius) are sourced from Fluent design tokens via Griffel makeStyles, matching the design-token-alignment goal of the migration without adopting Fluent's Card component semantics"
  artifacts:
    - path: "frontend/src/components/ui/card.tsx"
      provides: "Unchanged plain-div structure (Option B), now importing Griffel-generated className from card.styles.ts merged with mergeClasses instead of pure Tailwind-only classes"
    - path: "frontend/src/components/ui/card.styles.ts"
      provides: "New Griffel makeStyles file providing Fluent-token-sourced border/background/shadow/radius overrides, mirroring button.styles.ts's Phase 39 pattern"
  key_links:
    - from: "frontend/src/components/ui/card.tsx"
      to: "frontend/src/components/ui/card.styles.ts"
      via: "Card imports useCardStyles() from card.styles.ts and merges its className with consumer className via mergeClasses, same wiring pattern as button.tsx + button.styles.ts"
      pattern: "useCardStyles"
---

<objective>
Resolve Card's (COMP-06) go/no-go decision — this plan formally selects Option B (retain plain Tailwind `<div>`/`<h4>`/`<p>` structure, layer Fluent design tokens via a new `card.styles.ts` Griffel `makeStyles` file) over adopting Fluent's real `Card` component, because Fluent's `Card` bakes in selectable-list semantics (`selected`, `onSelectionChange`, focus-ring-as-list-item) that doesn't match this codebase's usage (plain content containers, zero selection-list usage confirmed via the existing `card.tsx`'s pure-div implementation and its 7 tests). This keeps the export surface, element types, and prop signatures 100% unchanged while still achieving the migration's design-token-alignment goal via Griffel overrides mirroring the `button.styles.ts` precedent from Phase 39.

Purpose: Card is the lowest-risk composite requiring code changes in Phase 40 (zero Radix dependency, zero behavior change) — it is sequenced after Form and before the two highest-risk composites (Select, DropdownMenu) to keep momentum with an easy win before the hardest work.
Output: `card.tsx` unchanged structurally but now token-sourced via Griffel; new `card.styles.ts`; `card.test.tsx` extended with a token-className-presence check.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-composite-component-adapters/40-RESEARCH.md

<interfaces>
Current `card.tsx` full export list (confirmed via direct read, 93 lines, zero Radix dependency):
```ts
export { Card, CardHeader, CardTitle, CardDescription, CardAction, CardContent, CardFooter };
// Card = <div data-slot="card" className={cn("...", className)}>
// CardHeader = <div data-slot="card-header">
// CardTitle = <h4 data-slot="card-title">
// CardDescription = <p data-slot="card-description">
// CardAction = <div data-slot="card-action">
// CardContent = <div data-slot="card-content">
// CardFooter = <div data-slot="card-footer">
```

`button.styles.ts` pattern to mirror (confirmed via direct read this session — Phase 39's Griffel token-override precedent):
```ts
import { makeStyles, tokens } from "@fluentui/react-components";

export const useButtonStyles = makeStyles({
  destructive: {
    backgroundColor: "var(--destructive)",
    // color-mix(in oklab, var(--destructive) NN%, transparent) for opacity steps
    ":global(.dark)": {
      // dark-mode variant via ancestor-selector escape
    },
  },
});
```
Key convention: reference CSS custom properties (`var(--destructive)`, etc.) rather than hardcoded hex values, and use `:global(.dark)` for dark-mode variants, per Phase 39's established pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Document Card go/no-go decision and layer Fluent tokens via Griffel</name>
  <files>frontend/src/components/ui/card.tsx, frontend/src/components/ui/card.styles.ts, frontend/src/components/ui/card.test.tsx</files>
  <read_first>
    frontend/src/components/ui/card.tsx
    frontend/src/components/ui/card.test.tsx
    frontend/src/components/ui/button.styles.ts
  </read_first>
  <behavior>
    - Test 1 (existing, preserve intent): Card/CardHeader/CardTitle/CardDescription/CardContent/CardFooter/CardAction each render with correct data-slot and text/children
    - Test 2 (NEW): `Card`'s rendered className includes both the Griffel-generated token class (from `useCardStyles()`) and any consumer-supplied `className`, in that order (Griffel base, then consumer override, matching `mergeClasses` convention)
    - Test 3 (NEW): Card renders as a `<div>` element (not a Fluent `Card` component instance) — confirms Option B was actually implemented, not accidentally swapped to Fluent's real Card
  </behavior>
  <action>
    Create `frontend/src/components/ui/card.styles.ts` using Griffel's `makeStyles` (mirroring `button.styles.ts`'s pattern) to define token-sourced style rules for the Card's border, background, box-shadow, and border-radius — reference Fluent design tokens via the `tokens` import from `@fluentui/react-components` (e.g. `tokens.colorNeutralStroke1` for border, `tokens.colorNeutralBackground1` for background, `tokens.borderRadiusMedium` for radius — verify exact token names against the installed `@fluentui/react-components` type definitions before using them, do not guess names).

    Modify `card.tsx`: import `useCardStyles` from `./card.styles`, call it inside each exported component, and merge its output with the existing Tailwind className string via `mergeClasses(styles.root, className)` (or equivalent per-slot style hook if different slots need different token treatment — start with just `Card`'s root styling to keep scope minimal, since CardHeader/CardTitle/etc. are typography/spacing-only and likely don't need token overrides beyond what Tailwind already provides).

    Do NOT change any exported component's underlying element type (div/h4/p) or add any Fluent `Card`/`CardPreview`/`CardHeader` component imports — this is the explicit go/no-go decision (Option B) and must be visible in the diff as "additive Griffel styling only," not a structural rewrite.

    Extend `card.test.tsx` with the 2 new tests, preserving the 7 existing ones.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ui/card.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/ui/card.test.tsx` exits 0, 9 tests passing (7 original + 2 new)
    - `cd frontend && npx tsc -b` exits 0
    - `grep -n "makeStyles\|tokens\." frontend/src/components/ui/card.styles.ts` present
    - `grep -n "@fluentui/react-components.*Card\b" frontend/src/components/ui/card.tsx` returns no matches (confirms Fluent's real Card component was NOT imported — Option B honored)
    - `grep -n "export {" -A 8 frontend/src/components/ui/card.tsx` lists all 7 original export names unchanged
    - `cd frontend && npm run build` exits 0
    - `git log -1 --name-only` shows exactly card.tsx, card.styles.ts, card.test.tsx, commit message `feat(40-05): migrate Card to Fluent design tokens (Option B)`
  </acceptance_criteria>
  <done>Card's go/no-go decision resolved and documented (Option B: plain divs retained, Fluent tokens layered via Griffel), all 7 exports/element-types unchanged, new card.styles.ts created mirroring button.styles.ts's precedent, committed and pushed independently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure styling-token swap; Card renders only consumer-supplied children, same as before |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-40-09 | Tampering (structural regression) | Card go/no-go decision drift | mitigate | Test 3 explicitly asserts Card renders as a plain div, guarding against accidental future adoption of Fluent's real Card component with its selectable-list semantics |
</threat_model>

<verification>
1. `cd frontend && npx vitest run src/components/ui/card.test.tsx` all green (9 tests).
2. `cd frontend && npx tsc -b` clean.
3. `cd frontend && npm run build` succeeds.
4. Exactly one commit, pushed.
</verification>

<success_criteria>
- Card go/no-go decision resolved as Option B and documented in code/tests.
- All 7 exports and element types unchanged; Fluent tokens layered additively via Griffel.
- One independent commit, pushed.
</success_criteria>

<output>
After completion, create `.planning/phases/40-composite-component-adapters/40-05-SUMMARY.md`
</output>
