---
phase: 09-integration-testing-with-real-azure-services
plan: 05
type: execute
wave: 3
depends_on: ["09-03", "09-04"]
files_modified:
  - frontend/e2e/demo-flow.spec.ts
  - docs/SMOKE_TEST_CHECKLIST.md
autonomous: true
requirements: [COACH-04, COACH-05, COACH-06, COACH-07, PLAT-03, PLAT-05]
must_haves:
  truths:
    - "Playwright E2E test exercises the full demo pipeline from admin config through coaching sessions to scoring"
    - "Smoke test checklist covers all Azure service health checks, text/voice/avatar modes, fallback chain, and scoring parity"
    - "E2E test has extended timeout (120s) for voice/avatar WebRTC setup"
    - "E2E test uses admin and user authentication flows"
  artifacts:
    - path: "frontend/e2e/demo-flow.spec.ts"
      provides: "Full demo pipeline E2E test"
      contains: "demo"
    - path: "docs/SMOKE_TEST_CHECKLIST.md"
      provides: "Manual pre-demo smoke test checklist"
      contains: "Smoke Test"
  key_links:
    - from: "frontend/e2e/demo-flow.spec.ts"
      to: "frontend/e2e/auth.setup.ts"
      via: "Admin authentication state"
      pattern: "storageState|admin.json"
    - from: "frontend/e2e/demo-flow.spec.ts"
      to: "frontend/src/pages/admin/azure-config.tsx"
      via: "Admin Azure Config page navigation"
      pattern: "azure-config"
---

<objective>
Playwright E2E demo flow test and manual smoke test checklist

Purpose: Create a Playwright E2E test that exercises the complete demo scenario per D-15 (login -> admin AI Foundry config -> text session -> voice session -> avatar session -> scoring), and a manual smoke test checklist document per D-16 for pre-demo preparation.

Output: E2E test spec file and smoke test checklist markdown document
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-integration-testing-with-real-azure-services/09-CONTEXT.md
@.planning/phases/09-integration-testing-with-real-azure-services/09-UI-SPEC.md

<interfaces>
<!-- Playwright config and auth setup patterns -->

From frontend/e2e/playwright.config.ts:
```typescript
// timeout: 30000 (default), projects: setup + chromium, webServer: backend + frontend
// auth.setup.ts creates admin.json and user.json storage state files
```

From frontend/e2e/auth.setup.ts (pattern):
```typescript
// Creates .auth/admin.json and .auth/user.json with saved browser state
// Admin: admin/admin123, User: user/user123
```

From frontend/e2e/admin-azure-config.spec.ts (existing patterns):
```typescript
// Uses test.use({ storageState: join(authDir, "admin.json") })
// Navigates to /admin/azure-config
// Interacts with ServiceConfigCard components
```

From frontend/e2e/coaching-session.spec.ts (existing patterns):
```typescript
// Uses page.goto, page.getByText, page.getByRole
// Waits for API responses with page.waitForResponse
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright E2E demo-flow test for full pipeline</name>
  <files>
    frontend/e2e/demo-flow.spec.ts
  </files>
  <read_first>
    frontend/e2e/playwright.config.ts,
    frontend/e2e/auth.setup.ts,
    frontend/e2e/admin-azure-config.spec.ts,
    frontend/e2e/coaching-session.spec.ts,
    frontend/e2e/voice-session.spec.ts,
    frontend/e2e/tsconfig.json,
    .planning/phases/09-integration-testing-with-real-azure-services/09-CONTEXT.md,
    .planning/phases/09-integration-testing-with-real-azure-services/09-UI-SPEC.md
  </read_first>
  <action>
Create `frontend/e2e/demo-flow.spec.ts` implementing the full demo pipeline E2E test per D-15. This spec tests the complete BeiGene demo experience.

**Structure:** Use `test.describe("Full Demo Pipeline")` with `test.setTimeout(120_000)` (2 minutes per test for voice/avatar setup).

**Helper functions** at top of file (lightweight, not full POM classes):
```typescript
import { test, expect, type Page } from "@playwright/test";
import { join } from "path";

const authDir = join(__dirname, ".auth");

// Helper: navigate to admin Azure config and verify page loads
async function goToAzureConfig(page: Page) {
  await page.goto("/admin/azure-config");
  await page.waitForLoadState("networkidle");
}

// Helper: navigate to scenario selection
async function goToScenarios(page: Page) {
  await page.goto("/user/training");
  await page.waitForLoadState("networkidle");
}
```

**Test 1: "admin configures AI Foundry and tests connection"** (admin auth)
```
test.use({ storageState: join(authDir, "admin.json") });
```
- Navigate to /admin/azure-config
- Verify AI Foundry card is visible (look for text "Azure AI Foundry" or i18n equivalent)
- If endpoint field is empty, fill with placeholder values (this test validates UI interaction, not real Azure calls — real service testing is in backend integration tests)
- Click "Test All Services" button if present
- Verify toast notification appears (success or "not configured" is acceptable)
- Take screenshot for visual record: `await page.screenshot({ path: "test-results/demo-admin-config.png" })`

**Test 2: "user starts text coaching session and receives scoring"** (user auth)
```
test.use({ storageState: join(authDir, "user.json") });
```
- Navigate to /user/training (scenario selection)
- Verify at least one scenario card is visible
- Click the first scenario card or "Start Session" button
- Verify mode selector is visible with at least "Text" option
- Click "Text" mode if not already selected
- Wait for session page to load (chat area visible)
- Type a test message: "Hello doctor, I would like to discuss treatment options for hypertension."
- Submit the message
- Wait for AI response (wait for a chat bubble with role="assistant" or similar selector, timeout 15s)
- Verify AI response appears in the chat area
- Verify response is non-empty (has text content)
- If "End Session" button exists, click it
- If scoring report page loads, verify it contains score-related elements (radar chart, dimension scores, or report heading)
- Take screenshot: `await page.screenshot({ path: "test-results/demo-text-session.png" })`

**Test 3: "mode selector shows available modes"** (user auth)
- Navigate to scenario selection
- Click first scenario
- Verify mode selector has "Text" button with `data-testid="mode-text"`
- If Voice Live is configured, verify "Voice" button exists
- If Avatar is configured, verify "Digital Human" button exists
- Check that disabled modes show tooltip on hover

**Test 4: "scoring report renders after session"** (user auth)
- Navigate to session history (/user/history or similar)
- If completed sessions exist, click the first one
- Verify scoring report elements:
  - Overall score number is visible
  - At least one dimension score is visible
  - Strengths or weaknesses section exists
- Take screenshot: `await page.screenshot({ path: "test-results/demo-scoring-report.png" })`

**Important notes:**
- This E2E test works against the mock backend by default (no Azure credentials required for CI)
- When run with real Azure credentials configured via admin UI, it tests the full pipeline
- Use generous timeouts: `{ timeout: 15000 }` for API-dependent waits
- Use `expect(locator).toBeVisible({ timeout: 10000 })` for elements that may take time to appear
- Import `test` and `expect` from `@playwright/test`
- File must pass TypeScript compilation (`npx tsc -b` using e2e/tsconfig.json)

Run: `cd frontend && npx tsc -b --noEmit` to verify TypeScript compiles.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - frontend/e2e/demo-flow.spec.ts exists and is non-empty
    - frontend/e2e/demo-flow.spec.ts contains `test.describe("Full Demo Pipeline"`
    - frontend/e2e/demo-flow.spec.ts contains `storageState` (admin auth)
    - frontend/e2e/demo-flow.spec.ts contains `azure-config` (admin config navigation)
    - frontend/e2e/demo-flow.spec.ts contains `training` (scenario selection navigation)
    - frontend/e2e/demo-flow.spec.ts contains `screenshot` (visual record)
    - frontend/e2e/demo-flow.spec.ts contains `timeout` (extended timeouts)
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <done>E2E demo-flow spec covers admin config, text session, mode selector, and scoring report with extended timeouts and screenshot capture</done>
</task>

<task type="auto">
  <name>Task 2: Pre-demo smoke test checklist document</name>
  <files>
    docs/SMOKE_TEST_CHECKLIST.md
  </files>
  <read_first>
    .planning/phases/09-integration-testing-with-real-azure-services/09-CONTEXT.md,
    .planning/phases/09-integration-testing-with-real-azure-services/09-UI-SPEC.md,
    docs/requirements.md
  </read_first>
  <action>
Create `docs/SMOKE_TEST_CHECKLIST.md` — a manual pre-demo smoke test checklist per D-16. This is the document demo operators use before customer presentations to verify everything works.

**Content structure:**

```markdown
# Pre-Demo Smoke Test Checklist

> Use this checklist before each BeiGene customer demo to verify all Azure services and demo flow are operational.
> Last updated: [auto-fill date]

## Prerequisites

- [ ] Backend running: `cd backend && uvicorn app.main:app --port 8000`
- [ ] Frontend running: `cd frontend && npm run dev`
- [ ] Azure credentials configured (see below)
- [ ] Demo user accounts exist: admin/admin123, user/user123
- [ ] Test microphone available (for voice/avatar modes)

## 1. Azure Service Health

### AI Foundry Configuration
- [ ] Login as admin (admin/admin123)
- [ ] Navigate to Admin > Azure Config
- [ ] AI Foundry card shows endpoint URL: `https://ai-foundary-qiah-east-us2.cognitiveservices.azure.com/`
- [ ] AI Foundry card shows region: `eastus2`
- [ ] AI Foundry card shows API key hint (****xxxx)

### Per-Service Connection Tests
- [ ] Azure OpenAI: Click "Test Connection" -> green success toast within 15s
- [ ] Azure Speech STT: Click "Test Connection" -> green success toast
- [ ] Azure Speech TTS: Click "Test Connection" -> green success toast
- [ ] Azure Voice Live: Click "Test Connection" -> green success toast (region must be eastus2 or swedencentral)
- [ ] Azure AI Avatar: Click "Test Connection" -> green success toast
- [ ] "Test All Services" button: all configured services return success

## 2. Text Mode Demo

- [ ] Login as user (user/user123)
- [ ] Navigate to Training > select any scenario
- [ ] Mode selector shows "Text" as active
- [ ] Click "Start Session"
- [ ] Chat area loads with empty message list
- [ ] Type: "Hello doctor, I would like to discuss the treatment options" and send
- [ ] AI responds within 3 seconds (D-17)
- [ ] AI response is in character (not mock "Lorem ipsum" text)
- [ ] Send 2 more messages — conversation flows naturally
- [ ] Key message checklist in hints panel updates during conversation
- [ ] Click "End Session"
- [ ] Scoring report renders with:
  - [ ] Overall score (numeric value)
  - [ ] Per-dimension scores (radar chart or cards)
  - [ ] Strengths section (green highlights)
  - [ ] Weaknesses section (orange highlights)
  - [ ] Improvement suggestions (purple highlights)
  - [ ] Conversation quotes from the session

## 3. Voice Mode Demo

- [ ] Select scenario, choose "Voice" communication type
- [ ] Select "Realtime" engine (two-level selector)
- [ ] Click "Start Session"
- [ ] Connection status transitions: "Connecting..." -> "Connected" (green dot)
- [ ] Microphone button appears and is interactive
- [ ] Speak: "Hello, I am a medical representative from BeiGene"
- [ ] Transcript area shows real-time transcription of your speech
- [ ] AI responds with synthesized speech (audible)
- [ ] AI response transcript appears in transcript area
- [ ] Response latency feels conversational (< 3 seconds) (D-17)
- [ ] Click "End Session"
- [ ] Scoring report renders with same quality as text mode (D-19)

## 4. Avatar Mode Demo

- [ ] Select scenario, choose "Digital Human" communication type
- [ ] Select "Realtime" engine
- [ ] Click "Start Session"
- [ ] Avatar video area renders (WebRTC connection establishes)
- [ ] Avatar character appears (no freeze, no black screen)
- [ ] Speak into microphone
- [ ] Avatar lip-sync matches speech output (D-18)
- [ ] Avatar gestures/expressions look natural
- [ ] No video freezing or audio desync during conversation
- [ ] Click "End Session"
- [ ] Scoring report renders correctly (D-19)

## 5. Fallback Chain (D-13)

### Avatar -> Voice Fallback
- [ ] Admin: disable Avatar service (toggle off)
- [ ] User: select Digital Human mode, start session
- [ ] System shows fallback toast: "Avatar unavailable, switching to voice-only"
- [ ] Session continues in voice-only mode with waveform visualization

### Voice -> Text Fallback
- [ ] Admin: disable Voice Live service (toggle off)
- [ ] User: attempt voice mode
- [ ] System shows fallback toast: "Voice unavailable, switching to text mode"
- [ ] Session continues in text-only mode with ChatInput

## 6. Mode Selector Verification

- [ ] Text mode always visible and enabled
- [ ] Voice mode visible only when Voice Live is enabled
- [ ] Digital Human mode visible only when Avatar is enabled
- [ ] Pipeline engine visible when STT+TTS are enabled
- [ ] Realtime engine visible when Voice Live is enabled
- [ ] Agent engine visible when agent mode is configured
- [ ] Disabled modes show tooltip explaining why disabled

## 7. Scoring Parity (D-19)

Compare scoring reports across session modes:
- [ ] Text session scoring: dimensions shown, quotes present, suggestions meaningful
- [ ] Voice session scoring: same dimension count, same quality level
- [ ] Avatar session scoring: same dimension count, same quality level
- [ ] All three modes produce comparable score ranges for similar conversations

## 8. Performance Checklist (D-17, D-18)

- [ ] Text: AI first response < 3 seconds
- [ ] Voice: First transcription appears < 2 seconds after speaking
- [ ] Voice: AI spoken response starts < 3 seconds after user finishes
- [ ] Avatar: Video renders within 10 seconds of session start
- [ ] Avatar: No visible freezing during 5-minute conversation
- [ ] Page transitions: < 1 second between pages

## Quick Recovery Steps

| Problem | Recovery |
|---------|----------|
| "Connection test failed" | Check AI Foundry endpoint URL and API key. Verify Azure portal shows resource is active. |
| Avatar shows black screen | Refresh browser. Check WebRTC is not blocked by firewall/VPN. Try Chrome incognito. |
| Voice "Connecting..." stuck | Check region is eastus2 or swedencentral. Check microphone permissions in browser. |
| Scoring report empty | Check Azure OpenAI connection. Mock adapter does not produce real scoring. |
| "Mock HCP Response" text | Azure OpenAI not configured or connection failed. Check admin config page. |

---
*Checklist version: 1.0*
*Last verified: [date of last successful full run]*
```
  </action>
  <verify>
    <automated>test -f /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/docs/SMOKE_TEST_CHECKLIST.md && grep -c "\- \[ \]" /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/docs/SMOKE_TEST_CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/SMOKE_TEST_CHECKLIST.md exists
    - docs/SMOKE_TEST_CHECKLIST.md contains "Azure Service Health" section
    - docs/SMOKE_TEST_CHECKLIST.md contains "Text Mode Demo" section
    - docs/SMOKE_TEST_CHECKLIST.md contains "Voice Mode Demo" section
    - docs/SMOKE_TEST_CHECKLIST.md contains "Avatar Mode Demo" section
    - docs/SMOKE_TEST_CHECKLIST.md contains "Fallback Chain" section
    - docs/SMOKE_TEST_CHECKLIST.md contains "Scoring Parity" section
    - docs/SMOKE_TEST_CHECKLIST.md contains "Performance Checklist" section
    - docs/SMOKE_TEST_CHECKLIST.md contains "Quick Recovery Steps" section
    - docs/SMOKE_TEST_CHECKLIST.md has at least 40 checkbox items (grep count of "- [ ]")
  </acceptance_criteria>
  <done>Smoke test checklist covers all 8 sections with 40+ checkbox items, includes quick recovery steps for common demo issues</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc -b --noEmit` passes (E2E spec compiles)
- demo-flow.spec.ts has 4 test cases covering admin config, text session, mode selector, and scoring
- SMOKE_TEST_CHECKLIST.md has 8 sections with checkbox format
- Both files are new additions (no existing files modified)
</verification>

<success_criteria>
- Playwright E2E test exercises admin config -> text session -> scoring pipeline
- E2E test works with mock backend (no Azure credentials required)
- Smoke test checklist covers all demo scenarios per D-16
- Checklist includes performance thresholds, fallback chain, and recovery steps
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-testing-with-real-azure-services/09-05-SUMMARY.md`
</output>
