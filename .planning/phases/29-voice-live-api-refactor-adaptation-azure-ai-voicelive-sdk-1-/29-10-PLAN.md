---
phase: 29
plan: 10
type: execute
wave: 5
depends_on: [2, 3, 4, 5, 6, 7, 8, 9]
files_modified:
  - backend/pyproject.toml
  - frontend/vite.config.ts
  - .planning/phases/29-voice-live-api-refactor-adaptation-azure-ai-voicelive-sdk-1-/29-10-SUMMARY.md
autonomous: true
requirements: [D-16]

must_haves:
  truths:
    - "The full backend pytest suite (all phases' tests, not just Phase 29's new tests) passes with zero failures"
    - "Frontend tsc -b and vitest run pass with zero errors, including the new agent-foundation-model component/hook from Plan 08"
    - "Playwright E2E specs touching voice/HCP/agent-sync flows are actually executed (not merely present as files) and pass"
    - "A coverage gate is enforced (either the project's 95% standard, or a recorded-baseline no-regression gate if 95% is not yet reached) so future regressions are caught automatically"
    - "No source file (backend or frontend, not just docs) still references a preview Voice Live api-version string post-refactor"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "cov-fail-under gate reflecting measured baseline or the 95% project standard"
      contains: "cov-fail-under"
    - path: ".planning/phases/29-voice-live-api-refactor-adaptation-azure-ai-voicelive-sdk-1-/29-10-SUMMARY.md"
      provides: "Recorded full-suite pass evidence: backend pytest output tail, frontend tsc/vitest output tail, Playwright E2E run output tail, measured coverage percentages"
      min_lines: 20
  key_links:
    - from: "backend/pyproject.toml [tool.pytest.ini_options] addopts"
      to: "pytest-cov --cov-fail-under enforcement"
      via: "addopts string"
      pattern: "cov-fail-under"
    - from: "Playwright E2E run"
      to: "voice/HCP/agent-sync specs actually executing (not skipped)"
      via: "playwright test output showing pass counts > 0 for targeted specs"
      pattern: "passed"
---

<objective>
Close out Phase 29 with the cross-cutting verification sweep D-16 requires: run the FULL backend test suite (not just Phase-29-added tests — this catches regressions Plans 02-09 may have introduced in unrelated modules), the full frontend typecheck + unit test suite, and an ACTUAL Playwright E2E execution of the voice/HCP/agent-sync specs (per project standard "E2E must actually run", not just exist as written files). Enforce a coverage gate so this level of rigor is maintained automatically going forward, and perform a final repo-wide (not docs-only) grep sweep confirming no stale preview api-version literal survives in source code.

This plan explicitly does NOT re-implement or duplicate the specialized test coverage already owned by other chunks: Entra/API-key credential fallback tests (chunk A, Plan 02), `asst_*` auto-resync tests (chunk A), VL-required save-time validation tests (chunk B, Plan 06), and the foundation-model catalog endpoint's own dedicated test file (`backend/tests/test_agent_foundation_models.py`, created in Plan 08) — this plan's job is to confirm those all pass TOGETHER as part of one green full-suite run, and to add the coverage gate + E2E execution + final sweep that no single prior plan owns.

Purpose: Per CLAUDE.md's top-priority rule and the project's testing standard (95% coverage, E2E must actually run), no phase is complete until the full suite — not just the new tests — is verified green and a regression gate is in place.
Output: A green full backend + frontend + E2E run, a coverage gate committed in `backend/pyproject.toml`, and `29-10-SUMMARY.md` recording the verbatim evidence.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-voice-live-api-refactor-adaptation-azure-ai-voicelive-sdk-1-/29-VALIDATION.md
@backend/pyproject.toml
@frontend/package.json
</context>

<interfaces>
<!-- Current test infra facts confirmed via grep at plan-writing time — executor should re-confirm at execution time since Plans 02-09 may have added test files -->

```
backend/pyproject.toml [tool.pytest.ini_options]:
  addopts = "-m 'not integration'"   # integration tests excluded by default — do NOT change this scope for this plan
  [tool.coverage.run] concurrency = ["greenlet"]
  # No --cov-fail-under currently set — this plan adds one

frontend/package.json scripts:
  "test": "vitest run"
  "test:coverage": "vitest run --coverage"
  "test:e2e:coverage": "INSTRUMENT_COVERAGE=true playwright test --config=e2e/playwright.config.ts && nyc report ..."
  devDependency: "@vitest/coverage-v8": "^3.2.4"

Playwright config path (CLAUDE.md Gotcha #5): frontend/e2e/playwright.config.ts
  — ALWAYS invoke as: npx playwright test --config=e2e/playwright.config.ts (run from frontend/)

Relevant E2E specs (confirmed present via find, frontend/e2e/):
  hcp-editor-voice-tab.spec.ts, hcp-agent-sync.spec.ts, voice-live-management.spec.ts,
  admin-hcp-profiles.spec.ts, voice-session.spec.ts, admin-azure-config.spec.ts,
  vl-avatar-toggle.spec.ts, voice-avatar-real.spec.ts, voice-fallback.spec.ts, voice-live-proxy.spec.ts
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Full backend suite green + coverage gate</name>
  <files>backend/pyproject.toml</files>
  <read_first>
    - backend/pyproject.toml (grep current `[tool.pytest.ini_options]` and `[tool.coverage.run]` sections fresh — do not assume the interfaces block above is still accurate at execution time, other Phase 29 plans may have edited it)
    - .planning/phases/29-voice-live-api-refactor-adaptation-azure-ai-voicelive-sdk-1-/29-VALIDATION.md (Per-Task Verification Map — confirms which test files/patterns each D-XX decision maps to, for cross-checking the full run covers them all)
  </read_first>
  <action>
    Run `cd backend && .venv/bin/pytest -v --cov=app --cov-report=term-missing 2>&1 | tee /tmp/phase29-backend-full.log`. This MUST include every test file added by Plans 02-09 of this phase (Entra/API-key fallback, asst_* resync, classic-branch-removed rejection, VL-required validation, `test_agent_foundation_models.py`) plus every pre-existing test in `backend/tests/` — do not scope with `-k` or a subdirectory filter.

    If any test fails: this is a genuine regression from Phase 29's changes (Plans 02-09 depend on this plan running last per the wave/depends_on graph) — fix the regression at its source file (do not weaken assertions to force a pass) and re-run until green. Record which failures occurred and how they were fixed in the SUMMARY.

    Once green, read the final coverage percentage from the `TOTAL` line of the `--cov-report=term-missing` output. If the measured total is >= 95%: set `addopts` in `[tool.pytest.ini_options]` to append `--cov=app --cov-fail-under=95` (preserve the existing `-m 'not integration'` filter — append, do not replace). If the measured total is < 95%: set `--cov-fail-under=<measured integer, rounded down>` instead, and add a comment directly above the `addopts` line: `# TODO: raise to 95 per project testing standard once coverage improves (see 29-10-SUMMARY.md for baseline)` — this prevents the gate from silently blocking on a target that was never actually met pre-refactor, while still catching any future regression below today's measured baseline.

    Re-run `cd backend && .venv/bin/pytest -v` once more after the `pyproject.toml` edit to confirm the new `--cov-fail-under` gate itself passes (proves the threshold was set correctly, not above the actual measured value).
  </action>
  <acceptance_criteria>
    - `grep -c "cov-fail-under" backend/pyproject.toml` returns >= 1
    - The full pytest run's exit code is 0 (captured in `/tmp/phase29-backend-full.log`, tail shows `passed` with 0 `failed`/`error`)
    - `grep -c "test_agent_foundation_models\|asst_resync\|agent_forced_reject\|vl_required" /tmp/phase29-backend-full.log` returns >= 1 (confirms the cross-chunk test files actually ran as part of this suite, not skipped)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && .venv/bin/pytest -v 2>&1 | tail -20</automated>
  </verify>
  <done>Full backend suite passes with 0 failures, a `--cov-fail-under` gate is set in pyproject.toml at either 95 or the honestly-measured baseline, and the gate itself passes on re-run.</done>
</task>

<task type="auto">
  <name>Task 2: Full frontend typecheck + unit test suite green + coverage measurement</name>
  <files>frontend/vite.config.ts</files>
  <read_first>
    - frontend/package.json (confirm exact `test`/`test:coverage`/`build` script names fresh — do not assume unchanged from the interfaces block)
    - frontend/vite.config.ts (check for any existing `test.coverage` block before adding one)
  </read_first>
  <action>
    Run `cd frontend && npx tsc -b 2>&1 | tee /tmp/phase29-frontend-tsc.log` — must exit 0 with zero type errors, including the new `agent-foundation-model-select.tsx`, `use-agent-foundation-models.ts`, and any files touched by chunk B's Plan 07 consolidation (`agent-config-left-panel.tsx`).

    Run `cd frontend && npm run test:coverage 2>&1 | tee /tmp/phase29-frontend-vitest.log` — must exit 0. Read the coverage summary table output by `@vitest/coverage-v8`. Apply the same baseline-vs-95% logic as Task 1: if overall statement coverage >= 95%, add a `coverage.thresholds` block to `frontend/vite.config.ts`'s `test` config (`thresholds: { statements: 95, branches: 95, functions: 95, lines: 95 }`); if below 95%, set the thresholds to the measured integer values instead, with a matching `// TODO: raise to 95 per project testing standard` comment.

    Run `cd frontend && npm run build 2>&1 | tail -20` to confirm the production build itself succeeds (build-check is part of the project's pre-commit checklist per CLAUDE.md).

    Re-run `npm run test:coverage` once after the `vite.config.ts` edit to confirm the new threshold gate passes.
  </action>
  <acceptance_criteria>
    - `npx tsc -b` (from `frontend/`) exits 0
    - `npm run test:coverage` (from `frontend/`) exits 0 both before and after the `vite.config.ts` threshold edit
    - `grep -c "thresholds" frontend/vite.config.ts` returns >= 1
    - `npm run build` (from `frontend/`) exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run test 2>&1 | tail -20</automated>
  </verify>
  <done>tsc, vitest, and the production build all pass with 0 errors, and a coverage threshold gate is set in vite.config.ts at either 95 or the honestly-measured baseline.</done>
</task>

<task type="auto">
  <name>Task 3: Actual Playwright E2E execution + final repo-wide stale-reference sweep</name>
  <files>.planning/phases/29-voice-live-api-refactor-adaptation-azure-ai-voicelive-sdk-1-/29-10-SUMMARY.md</files>
  <read_first>
    - frontend/e2e/playwright.config.ts (confirm baseURL, webServer config — needed to know if backend+frontend must be running manually before E2E, or if the config auto-starts them)
    - frontend/e2e/hcp-agent-sync.spec.ts, voice-live-management.spec.ts, admin-hcp-profiles.spec.ts (skim each for `test.skip`/`test.fixme` markers that would silently no-op the run — flag any found, since D-16 explicitly requires E2E to ACTUALLY run, not be skipped)
  </read_first>
  <action>
    Ensure backend and frontend dev servers are running (start them if `playwright.config.ts`'s `webServer` block doesn't auto-start — check the config first per read_first). Run:
    `cd frontend && npx playwright test --config=e2e/playwright.config.ts hcp-editor-voice-tab.spec.ts hcp-agent-sync.spec.ts voice-live-management.spec.ts admin-hcp-profiles.spec.ts voice-session.spec.ts admin-azure-config.spec.ts vl-avatar-toggle.spec.ts voice-avatar-real.spec.ts voice-fallback.spec.ts voice-live-proxy.spec.ts 2>&1 | tee /tmp/phase29-e2e.log`

    This MUST show each spec file actually executing (non-zero test count per file in the Playwright summary) — a report of "10 skipped" is a FAILURE of this task's intent, not a pass. If any spec was written to assume the old classic-agent/inline-field behavior (pre-Phase-29), update the spec's assertions to match the new dual-path/VL-mandatory reality rather than skipping it — cross-reference `29-06-SUMMARY.md` / `29-07-SUMMARY.md` (chunk B's HCP-save-validation and VL-summary-card changes) for what the new UI states actually look like before rewriting assertions blind.

    After the E2E run passes, perform the final repo-wide (not docs-only — Plan 09 already swept `docs/`) stale-reference sweep:
    `grep -rn "2025-05-01-preview\|2026-01-01-preview\|2026-06-01-preview" backend/app backend/tests backend/scripts frontend/src frontend/e2e --include="*.py" --include="*.ts" --include="*.tsx" | grep -v "\.venv\|node_modules"`
    Any hit is a genuine leftover from the pre-1.3.0 SDK and must be fixed at its source (not just noted) before this task is done — these are the exact literal preview-version strings the whole phase exists to eliminate (D-02).

    Write `.planning/phases/29-voice-live-api-refactor-adaptation-azure-ai-voicelive-sdk-1-/29-10-SUMMARY.md` recording: the Playwright summary line (X passed, Y failed, Z skipped) per spec file, the final repo-wide grep sweep result (must be empty), the backend coverage % and gate value from Task 1, the frontend coverage % and gate value from Task 2, and a closing statement confirming Phase 29's full-suite verification is green.
  </action>
  <acceptance_criteria>
    - Playwright summary in `/tmp/phase29-e2e.log` shows 0 failed and 0 skipped across the 10 targeted spec files (a nonzero passed count per file)
    - `grep -rn "2025-05-01-preview\|2026-01-01-preview\|2026-06-01-preview" backend/app backend/tests backend/scripts frontend/src frontend/e2e --include="*.py" --include="*.ts" --include="*.tsx" 2>/dev/null | wc -l` returns 0
    - `.planning/phases/29-voice-live-api-refactor-adaptation-azure-ai-voicelive-sdk-1-/29-10-SUMMARY.md` exists with the required sections
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx playwright test --config=e2e/playwright.config.ts hcp-agent-sync.spec.ts voice-live-management.spec.ts admin-hcp-profiles.spec.ts --reporter=line 2>&1 | tail -30</automated>
  </verify>
  <done>All 10 targeted E2E specs actually execute and pass (zero skipped, zero failed), the final repo-wide preview-api-version grep sweep returns 0 hits, and 29-10-SUMMARY.md records the complete evidence trail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test suite -> merge/release decision | The coverage gate and E2E pass/fail determine whether Phase 29 is considered complete — a falsely-green run masks real regressions |
| E2E run -> real backend/Azure calls | Some specs (`voice-avatar-real.spec.ts`, `voice-live-proxy.spec.ts`) may exercise real or near-real Azure connectivity paths depending on test-mode configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-10-01 | Tampering | Coverage gate / test assertions | mitigate | Task 1/2 explicitly forbid weakening assertions to force a pass; any failure must be fixed at its source and the fix recorded in the SUMMARY for auditability |
| T-29-10-02 | Repudiation | "E2E must actually run" standard | mitigate | Task 3 explicitly checks Playwright's skipped-count is 0 for all 10 targeted specs — a skipped test does not count as executed, closing the "wrote the test but never ran it" gap the project's testing standard exists to prevent |
</threat_model>

<verification>
Full backend `pytest -v` exits 0 with the `--cov-fail-under` gate active; frontend `tsc -b` + `npm run test:coverage` + `npm run build` all exit 0 with a coverage threshold gate active; Playwright E2E run of the 10 targeted specs shows 0 skipped/0 failed; repo-wide preview-api-version grep sweep (excluding docs, already swept in Plan 09) returns 0 hits.
</verification>

<success_criteria>
Phase 29 has a single, actually-executed, green full-suite verification (backend + frontend + E2E) with an enforced coverage regression gate, and zero remaining preview-api-version literals anywhere in backend or frontend source code.
</success_criteria>

<output>
After completion, create `.planning/phases/29-voice-live-api-refactor-adaptation-azure-ai-voicelive-sdk-1-/29-10-SUMMARY.md` with the verbatim evidence described in Task 3's action (Playwright summary, grep sweep result, both coverage percentages/gates, closing sign-off statement).
</output>
