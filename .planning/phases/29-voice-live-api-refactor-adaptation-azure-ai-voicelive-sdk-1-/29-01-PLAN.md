---
phase: 29
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/scripts/poc_voice_live_1_3_0.py
autonomous: false
requirements: [D-03, D-04, D-14-probe]

must_haves:
  truths:
    - "Executor knows definitively whether azure-ai-voicelive>=1.3.0,<2.0 is installable at execution time, not just at research time (2026-07-19)"
    - "A real Agent connect + session.update round-trip has been exercised against whatever SDK version is actually installed, using the Entra-first pattern"
    - "The live shape of ModelDeployment.capabilities from AIProjectClient.deployments.list() has been captured so Plan 08's filter logic is not guessing"
  artifacts:
    - path: "backend/scripts/poc_voice_live_1_3_0.py"
      provides: "Standalone POC script proving SDK 1.3.0 Agent connect/auth/session-config, mirroring docs/microsoft-agent-framework/tests/test_agent_auth_v2.py"
      min_lines: 40
    - path: "backend/pyproject.toml"
      provides: "azure-ai-voicelive dependency pin reflecting the resolved version decision"
      contains: "azure-ai-voicelive"
  key_links:
    - from: "backend/scripts/poc_voice_live_1_3_0.py"
      to: "azure.ai.voicelive.aio.connect"
      via: "direct SDK call with DefaultAzureCredential first, AzureKeyCredential fallback"
      pattern: "connect\\("
---

<objective>
Resolve the single highest-risk unknown in Phase 29 before any production code changes: is `azure-ai-voicelive` 1.3.0 GA actually installable right now, and does an Agent connect against it (or whatever is installable) actually work with the Entra-first/API-key-fallback pattern D-01 requires? Also capture the live `ModelDeployment.capabilities` shape so Plan 08's Foundry model-catalog filter (D-14) is built from evidence, not assumption.

Purpose: D-04 locks the sequencing — "verify via standalone POC before full migration". Every other plan in this phase (`depends_on: [1]`) is blocked on this plan's outcome per the phase's critical-preflight rule. Getting this wrong means Plans 02-13 build on top of an SDK version/auth assumption that silently doesn't hold.
Output: `backend/scripts/poc_voice_live_1_3_0.py` (reusable POC script), a resolved and pinned `azure-ai-voicelive` version in `backend/pyproject.toml`, and a recorded `capabilities` dict shape from a live (or documented-as-unavailable) Foundry deployments call.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-voice-live-api-refactor-adaptation-azure-ai-voicelive-sdk-1-/29-CONTEXT.md
@.planning/phases/29-voice-live-api-refactor-adaptation-azure-ai-voicelive-sdk-1-/29-RESEARCH.md
@docs/microsoft-agent-framework/tests/test_agent_auth_v2.py
@backend/app/services/agent_sync_service.py
</context>

<interfaces>
<!-- Existing Entra-first/API-key-fallback pattern to mirror exactly (source: agent_sync_service.py ~L350-391) -->
```python
def _get_project_client(endpoint: str, api_key: str = ""):
    try:
        from azure.identity import DefaultAzureCredential
        credential = DefaultAzureCredential()
        credential.get_token("https://cognitiveservices.azure.com/.default")
        return AIProjectClient(endpoint=endpoint, credential=credential)
    except Exception:
        pass
    if api_key:
        from azure.core.credentials import AzureKeyCredential
        from azure.core.pipeline.policies import AzureKeyCredentialPolicy
        return AIProjectClient(
            endpoint=endpoint,
            credential=_ApiKeyTokenCredential(api_key),
            authentication_policy=AzureKeyCredentialPolicy(credential=AzureKeyCredential(api_key)),
        )
    raise RuntimeError("No valid credential available...")
```
The POC script must apply the same Entra-first/AzureKeyCredential-fallback shape to `azure.ai.voicelive.aio.connect(...)` (async context manager), not `AIProjectClient`.

Current backend/pyproject.toml voicelive line (grep before editing — exact current constraint string is unknown to this plan and MUST be read fresh, do not assume it matches `azure-ai-voicelive[aiohttp]>=1.2.0b5` or similar).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Live PyPI availability check + POC script for Agent connect and Foundry capabilities probe</name>
  <files>backend/scripts/poc_voice_live_1_3_0.py</files>
  <read_first>
    - docs/microsoft-agent-framework/tests/test_agent_auth_v2.py (exact POC pattern to mirror: Entra + API-key paths, agent connect flow)
    - backend/app/services/agent_sync_service.py lines ~330-395 (`_get_project_client`, `_ApiKeyTokenCredential`) — the credential-resolution shape to replicate for Voice Live
    - backend/app/config.py (current `azure_foundry_endpoint`, `azure_foundry_api_key`, `azure_foundry_default_project` field names — the POC script reads real settings via `get_settings()`, do not hardcode credentials)
  </read_first>
  <action>
    Run `backend/.venv/bin/pip index versions azure-ai-voicelive` and `curl -s https://pypi.org/pypi/azure-ai-voicelive/json | python3 -c "import json,sys; print(json.load(sys.stdin)['info']['version'])"` to get the live-at-execution-time answer (do not trust the 2026-07-19 research finding of "only 1.3.0b1 on PyPI" — re-verify).

    Create `backend/scripts/poc_voice_live_1_3_0.py` as a standalone script (runnable via `backend/.venv/bin/python scripts/poc_voice_live_1_3_0.py`) that:
    1. Prints the installed `azure-ai-voicelive` version (`importlib.metadata.version("azure-ai-voicelive")`).
    2. Implements `_resolve_credential(api_key: str)`: tries `azure.identity.DefaultAzureCredential().get_token("https://cognitiveservices.azure.com/.default")` first (Entra), logs which path was used, falls back to `azure.core.credentials.AzureKeyCredential(api_key)` if Entra probing raises or no token, raises `RuntimeError` if neither works. This is the exact D-01 shape — both branches must be exercised and both must print a clear PASS/FAIL line.
    3. Loads `azure_foundry_endpoint` / `azure_foundry_api_key` from `app.config.get_settings()` (import via `sys.path` adjustment or run with `PYTHONPATH=backend`).
    4. Attempts `async with azure.ai.voicelive.aio.connect(endpoint=..., credential=..., api_version="2026-07-15", agent_name=<a known-good hosted agent name from an existing synced HCP, or a placeholder that logs the exact error if none configured>, project_name=...) as conn: await conn.session.update(session=RequestSession(modalities=[Modality.TEXT]))` and prints PASS/FAIL with the exact exception text on failure (do not swallow — this is the load-bearing verification).
    5. Separately, using `azure.ai.projects.AIProjectClient(endpoint, credential).deployments.list()`, iterates deployments and prints `d.name, d.model_name, d.model_publisher, d.capabilities` for every entry (D-14 probe) — wrap in try/except and print "FOUNDRY_PROBE_UNAVAILABLE: <reason>" if no live project/credentials, do not fail the whole script on this part.
    6. Exits 0 if the Agent connect step (step 4) passed; exits 1 with a clear summary line `POC_RESULT: SDK_VERSION=<x> AGENT_CONNECT=<PASS|FAIL> ENTRA=<PASS|FAIL|SKIPPED> API_KEY_FALLBACK=<PASS|FAIL|SKIPPED>` otherwise.

    Run the script. Record its full stdout in the SUMMARY.md this plan produces (verbatim, not paraphrased) — the `POC_RESULT` line and the `capabilities` dict lines are load-bearing evidence for Plan 08.
  </action>
  <acceptance_criteria>
    - `backend/scripts/poc_voice_live_1_3_0.py` exists and is executable via `backend/.venv/bin/python backend/scripts/poc_voice_live_1_3_0.py`
    - Script output contains a line matching `POC_RESULT: SDK_VERSION=`
    - `grep -c "DefaultAzureCredential" backend/scripts/poc_voice_live_1_3_0.py` returns >= 1
    - `grep -c "AzureKeyCredential" backend/scripts/poc_voice_live_1_3_0.py` returns >= 1
    - `grep -c "2026-07-15" backend/scripts/poc_voice_live_1_3_0.py` returns >= 1
  </acceptance_criteria>
  <verify>
    <automated>cd backend && .venv/bin/python scripts/poc_voice_live_1_3_0.py; echo "exit=$?"</automated>
  </verify>
  <done>Script exists, runs to completion (exit 0 or 1, not a crash/traceback), and prints a POC_RESULT summary line plus whatever capabilities data (or unavailability reason) it could gather.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>
    SKIP this checkpoint entirely (do not pause, proceed directly to Task 3) if Task 1's `POC_RESULT` line shows `AGENT_CONNECT=PASS` AND the installed version reported by the script satisfies `azure-ai-voicelive>=1.3.0,<2.0` (i.e., is GA 1.3.0.x, not a beta/rc). Only surface this decision to the user if either condition fails.
  </decision>
  <context>
    Per D-04, D-02, and D-03: the plan's premise is that `azure-ai-voicelive` 1.3.0 GA (api-version `2026-07-15`) is installable. RESEARCH.md (dated 2026-07-19) found only `1.3.0b1` (beta, default api-version `2026-06-01-preview`) on PyPI, with GA confirmed to exist on GitHub source but not yet published. If Task 1 confirms this gap still exists, the user must choose how to proceed — do not silently substitute a fallback.
  </context>
  <options>
    <option id="wait">
      <name>Wait and re-check later</name>
      <pros>Guarantees D-02/D-03 land exactly as locked (GA package, GA default api-version)</pros>
      <cons>Blocks the entire phase indefinitely; publish timing is outside our control</cons>
    </option>
    <option id="pin-beta">
      <name>Install `azure-ai-voicelive[aiohttp]==1.3.0b1` with `--pre`, explicitly pass `api_version="2026-07-15"` to every `connect()` call (overriding the beta SDK's own default of `2026-06-01-preview`)</name>
      <pros>Unblocks the phase immediately; SDK's `connect()` accepts an arbitrary `api_version` string per its signature</pros>
      <cons>Unverified whether the live Voice Live service accepts `2026-07-15` from a beta-SDK client (RESEARCH.md Assumption A2, not verified against a live service)</cons>
    </option>
    <option id="github-source">
      <name>Install directly from GitHub source: `pip install "git+https://github.com/Azure/azure-sdk-for-python.git#subdirectory=sdk/voicelive/azure-ai-voicelive"`</name>
      <pros>Gets the actual GA 1.3.0 code (confirmed present on `main` per RESEARCH.md) without waiting for PyPI</pros>
      <cons>Not a normal pinned dependency; reproducibility risk for CI/other environments; `pyproject.toml` pin per D-03 (`>=1.3.0,<2.0`) would technically resolve to this only via a `[tool.uv.sources]`/git-URL override, which needs an explicit note in `pyproject.toml`</cons>
    </option>
  </options>
  <resume-signal>Select: wait, pin-beta, or github-source</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize dependency pin per resolved version and confirm import</name>
  <files>backend/pyproject.toml</files>
  <read_first>
    - backend/pyproject.toml (grep the exact current `azure-ai-voicelive` line before editing — do not assume its current text)
  </read_first>
  <action>
    If Task 1 passed cleanly (no checkpoint triggered): set the `azure-ai-voicelive` dependency line in `backend/pyproject.toml` to exactly `"azure-ai-voicelive[aiohttp]>=1.3.0,<2.0"` per D-03 (locked, non-negotiable string). Run `backend/.venv/bin/pip install -e ".[dev]"` (or the project's equivalent install command — check `backend/pyproject.toml` for the correct extras group first) to lock in the resolved version.

    If the checkpoint was triggered and the user selected `pin-beta`: set the line to `"azure-ai-voicelive[aiohttp]==1.3.0b1"` and add an inline comment `# TEMPORARY: GA 1.3.0 not yet on PyPI as of <date>; api_version="2026-07-15" is explicitly passed at every connect() call site to override the beta default — see Plan 29-02/29-06/29-07` directly above the dependency line.

    If the user selected `github-source`: set the line to the git URL form and add the same kind of explanatory comment, noting the reproducibility caveat.

    If the user selected `wait`: do NOT edit `pyproject.toml`; instead this task's `<done>` becomes "blocked — record in SUMMARY.md that Phase 29 execution is paused pending PyPI publish, re-run Task 1's pip check before resuming."

    After editing (any branch except `wait`), run `backend/.venv/bin/python -c "import azure.ai.voicelive; print(azure.ai.voicelive.__version__ if hasattr(azure.ai.voicelive, '__version__') else 'imported OK')"` to confirm the import succeeds post-install.
  </action>
  <acceptance_criteria>
    - `grep "azure-ai-voicelive" backend/pyproject.toml` shows the resolved dependency string matching the decision branch taken
    - `backend/.venv/bin/python -c "import azure.ai.voicelive"` exits 0 (unless `wait` branch was taken, in which case this is explicitly N/A and documented as such)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && .venv/bin/python -c "import azure.ai.voicelive" && echo IMPORT_OK</automated>
  </verify>
  <done>backend/pyproject.toml reflects the resolved version decision with a traceable comment where a non-GA path was chosen, and the SDK import succeeds in the venv.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| POC script -> PyPI / GitHub | Untrusted network responses inform a version-pin decision; script must not auto-install anything without the decision gate |
| POC script -> Azure Voice Live / Foundry | Live credential probing (Entra token acquisition, API key read from settings) — script must never print the raw API key or bearer token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-P1 | Information Disclosure | poc_voice_live_1_3_0.py | mitigate | Script prints credential *path taken* (Entra vs API-key) and PASS/FAIL only, never the key/token value itself — enforce via code review of the script before running in any shared log |
| T-29-P2 | Tampering | pyproject.toml pin | accept | Any pin change from this plan is reviewed via the plan's own diff before commit; not a runtime attack surface |
</threat_model>

<verification>
`backend/.venv/bin/python scripts/poc_voice_live_1_3_0.py` exits 0 or 1 with a `POC_RESULT:` line; `backend/.venv/bin/python -c "import azure.ai.voicelive"` succeeds after Task 3.
</verification>

<success_criteria>
The phase has a definitive, evidence-based answer (not a 2026-07-19 research-time assumption) on SDK installability, the Entra/API-key credential pattern works end-to-end against a real `connect()` call, and either a resolved `pyproject.toml` pin exists or the phase is explicitly documented as blocked pending PyPI publish.
</success_criteria>

<output>
After completion, create `.planning/phases/29-voice-live-api-refactor-adaptation-azure-ai-voicelive-sdk-1-/29-01-SUMMARY.md` including the verbatim POC script stdout (POC_RESULT line + capabilities probe output) for Plan 08 to consume.
</output>
