---
phase: 32-anonymous-grounded-avatar-q-a
plan: 05
type: execute
wave: 5
depends_on: [02, 03, 04]
files_modified:
  - frontend/e2e/anonymous-avatar-qa.spec.ts
  - frontend/e2e/anonymous-avatar-voice.spec.ts
  - backend/tests/test_avatar_interaction_log.py
autonomous: false

requirements: [ANON-01, ANON-02, ANON-03, ANON-04, ANON-05]

must_haves:
  truths:
    - "A Playwright test proves the full main user story end-to-end: anonymous visitor opens /, asks a text question, sees a grounded answer with a separate sources panel, without ever logging in"
    - "A Playwright test proves the refusal path: an unanswerable question produces the fixed refusal template and an empty/no-match sources panel, never a fabricated citation"
    - "Every anonymous chat turn across the E2E run produces a matching AvatarInteractionLog row — audit completeness is verified, not assumed"
    - "A human confirms the avatar actually speaks the answer aloud via WebRTC — the one behavior no automated test can assert"
  artifacts:
    - path: "frontend/e2e/anonymous-avatar-qa.spec.ts"
      provides: "Main user story E2E: no-login access, text Q&A, separate sources panel, refusal path"
    - path: "frontend/e2e/anonymous-avatar-voice.spec.ts"
      provides: "Voice path E2E: mic permission dialog, WebRTC connection establishes, avatar UI reflects speaking state"
    - path: "backend/tests/test_avatar_interaction_log.py"
      provides: "Audit-log completeness test across success/refusal turns"
  key_links:
    - from: "frontend/e2e/anonymous-avatar-qa.spec.ts"
      to: "backend/tests/test_avatar_interaction_log.py"
      via: "Both assert the same observable contract: one AvatarInteractionLog row per chat turn"
      pattern: "AvatarInteractionLog"
---

<objective>
Close out Phase 32 with end-to-end proof that the five success criteria hold together as one working system: Playwright coverage of the main anonymous user story (text Q&A + separate sources panel + refusal path) and the voice path (mic permission + WebRTC connect), a backend audit-log completeness test, and a final human checkpoint for the one behavior automated tests cannot assert — the avatar actually speaking.

Purpose: Every prior plan (01-04) built one subsystem in isolation with mocked boundaries; this plan is the only place the full anonymous-visitor journey is exercised together, catching integration gaps (auth-guard leftovers, wiring mistakes, silent regressions) that unit tests structurally cannot see.
Output: Two Playwright spec files, one backend audit test, a full green suite run, and a signed-off human voice-verification checkpoint.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-anonymous-grounded-avatar-q-a/32-VALIDATION.md
@.planning/phases/32-anonymous-grounded-avatar-q-a/32-CONTEXT.md
@.planning/phases/32-anonymous-grounded-avatar-q-a/32-02-PLAN.md
@.planning/phases/32-anonymous-grounded-avatar-q-a/32-04-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Playwright E2E (text Q&A + refusal path + voice-connect UI) and backend audit-log completeness test</name>
  <files>frontend/e2e/anonymous-avatar-qa.spec.ts, frontend/e2e/anonymous-avatar-voice.spec.ts, backend/tests/test_avatar_interaction_log.py</files>
  <read_first>
    frontend/e2e/playwright.config.ts
    frontend/e2e/health.spec.ts
    backend/tests/test_public_avatar_api.py
    backend/tests/test_avatar_service.py
  </read_first>
  <behavior>
    - E2E: visiting `/` with a fresh browser context (no auth cookie/localStorage token set) shows the avatar page directly — assert no navigation to `/login` occurs and the page URL stays at `/`.
    - E2E: typing a question known to match seeded/mocked KB content into the input bar and submitting it results in (a) a new assistant transcript bubble containing the answer text, and (b) the sources panel populated with at least one citation card containing a page badge and an `<a>` with `target="_blank"` — asserted as two separate DOM regions (query each independently, assert neither's text content bleeds into the other).
    - E2E: typing a question engineered to produce a no-match response (mock/stub the backend chat endpoint via Playwright route interception to return `is_refusal: true, citations: []`) results in the fixed refusal-template text in the transcript bubble AND the sources panel's "本次未找到匹配来源"/"No matching source this time" empty state — assert the bubble has no `text-destructive` class.
    - E2E: exceeding the rate limit (mock the chat endpoint to return 429 via route interception) shows the rate-limited toast and disables the send action while keeping the textarea itself enabled.
    - E2E (voice, separate spec file): mock `navigator.mediaDevices.getUserMedia` to resolve successfully and mock the WebRTC session endpoint response; assert the avatar view enters a "connected" visual state (per whatever connection-status indicator `AvatarView`/`use-anonymous-voice-live` already exposes) without requiring a real Azure Voice Live connection.
    - Backend: `test_avatar_interaction_log.py` drives 3 real (in-memory-SQLite, mocked-Azure) calls to `handle_anonymous_turn()` — one success with citations, one refusal, one where the Agent stream raises — and asserts exactly 3 `AvatarInteractionLog` rows exist afterward with `citation_count`/`is_refusal` matching each case (mirrors, at the backend layer, what the E2E specs prove at the UI layer).
  </behavior>
  <action>
    Create `frontend/e2e/anonymous-avatar-qa.spec.ts` using the existing `frontend/e2e/playwright.config.ts` conventions (read `health.spec.ts` first for the exact `test()`/`expect()` import style and any shared fixtures/base URL setup already established). Use Playwright's `page.route()` to intercept `**/public/avatar/session`, `**/public/avatar/chat` where deterministic mocked responses are needed (no-match and 429 cases) — but let the happy-path text-Q&A case hit the real dev backend if a seeded `PublicKnowledgeConfig`/test KB is available; if not, mock that path too and note the limitation in the SUMMARY (a fully mocked E2E is still a valid structural-separation and no-login proof, even if it can't prove live Foundry IQ grounding — that gap is covered by `backend/tests/test_avatar_service.py`'s existing unit coverage from Plan 02).

    Create `frontend/e2e/anonymous-avatar-voice.spec.ts` covering the voice-connect UI behavior above, using Playwright's permission/mock APIs (`context.grantPermissions(["microphone"])` where applicable) and `page.route()` for the webrtc-session endpoint.

    Create `backend/tests/test_avatar_interaction_log.py` covering the 3-call audit-completeness behavior above, reusing the mocking setup already established in Plan 02's `test_avatar_service.py`.

    Run the full project suite and fix any integration-level failures surfaced only by running everything together (e.g. router guard leftovers, missing i18n keys, TanStack Query provider not wrapping the new page) — this is expected integration-debugging work, not a sign the task scope is wrong.
  </action>
  <verify>
    <automated>cd backend && pytest -v && cd ../frontend && npx tsc -b && npm run build && npx playwright test --config=e2e/playwright.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/e2e/anonymous-avatar-qa.spec.ts` contains at least 4 `test(` blocks covering no-login/happy-path/refusal/rate-limit
    - `frontend/e2e/anonymous-avatar-voice.spec.ts` contains at least 1 `test(` block covering mic-permission + WebRTC connect UI state
    - `backend/tests/test_avatar_interaction_log.py` contains `assert len(rows) == 3` (or equivalent exact-count assertion)
    - `cd backend && pytest -v` exits 0 with `--cov-fail-under=89` maintained
    - `cd frontend && npx tsc -b && npm run build` exits 0
    - `npx playwright test --config=e2e/playwright.config.ts` exits 0, all specs pass
  </acceptance_criteria>
  <done>The full anonymous-visitor journey (text Q&A, structural citation separation, refusal handling, rate limiting, voice-connect UI) is proven end-to-end, and audit-log completeness is proven at the backend layer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of full anonymous voice+text avatar experience</name>
  <action>No code changes in this task — this is a manual verification checkpoint. Follow the steps in `<how-to-verify>` below against a running dev instance (backend + frontend) to confirm the behaviors automated tests structurally cannot assert: audible avatar speech, and the absence of any `Authorization: Bearer` header on the anonymous path.</action>
  <what-built>
    The complete Phase 32 anonymous avatar experience: `/` public page, text + voice question input, grounded answers with a separate sources panel, refusal handling, rate limiting, and an always-connected Voice Live digital human — all covered by automated unit/E2E tests that mock the real Azure Voice Live audio stream (which cannot be asserted in CI per 32-VALIDATION.md's Manual-Only Verifications table).
  </what-built>
  <how-to-verify>
    1. Run `cd backend && uvicorn app.main:app --reload --port 8000` and `cd frontend && npm run dev` (or the project's `docker-compose up`).
    2. Open the frontend URL in a browser with NO existing login session (use a fresh/incognito window). Confirm you land directly on the avatar page — no redirect to a login screen.
    3. Type a question related to the site's official content into the input bar and submit it. Confirm: the digital human speaks the answer aloud AND the on-screen transcript text updates in sync with the speech; a separate "参考来源"/"Sources" panel on the right shows 0–3 citation cards, visually and structurally separate from the answer bubble; clicking a citation opens its link in a new tab.
    4. Ask a question clearly unrelated to the site's content (e.g. something nonsensical). Confirm the avatar responds with the fixed refusal phrase ("抱歉，我目前只能回答与官网内容相关的问题。") and the sources panel shows the neutral "本次未找到匹配来源" empty state — not an error-styled panel.
    5. Deny microphone permission when prompted (or test on a device/browser with no mic). Confirm the guidance dialog appears and you can still ask questions by typing.
    6. Confirm the browser's network tab shows no `Authorization: Bearer` header on any `/public/avatar/*` request — only `X-Anon-Session`.
  </how-to-verify>
  <resume-signal>Type "approved" once all 6 checks pass, or describe which check failed and what you observed instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Full anonymous journey (this plan is verification-only, no new production trust boundary introduced) | Confirms the boundaries established in Plans 01-04 hold together under a real browser session, not just mocked unit tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-32-18 | Repudiation | Integration gap between frontend's claimed no-JWT behavior and actual network requests | mitigate | Human-verify checkpoint step 6 explicitly inspects the network tab for the absence of `Authorization: Bearer` on all `/public/avatar/*` calls, catching any accidental JWT leakage the unit-level mocks in Plans 02-04 could not observe |
| T-32-19 | Information Disclosure | E2E test fixtures accidentally seeding/exposing real production KB content or credentials | accept | E2E specs use route interception/mocked responses for all non-trivial data (per Task 1 action notes); no real Azure credentials are committed to test fixtures |

</threat_model>

<verification>
`cd backend && pytest -v && cd ../frontend && npx tsc -b && npm run build && npx playwright test --config=e2e/playwright.config.ts` — full suite green, followed by the human-verify checkpoint's 6-point manual pass before this phase is considered done.
</verification>

<success_criteria>
- All 5 phase success criteria are demonstrated together in one continuous, automated E2E run (except the audio itself)
- Audit-log completeness is proven, not assumed
- A human has confirmed the avatar actually speaks and no JWT leaks into the anonymous path
</success_criteria>

<output>
After completion, create `.planning/phases/32-anonymous-grounded-avatar-q-a/32-05-SUMMARY.md`
</output>
