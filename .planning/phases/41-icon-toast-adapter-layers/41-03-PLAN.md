---
phase: 41-icon-toast-adapter-layers
plan: 03
type: execute
wave: 3
depends_on: ["41-02"]
files_modified:
  - "frontend/src/components/admin/**/*.tsx"
  - "frontend/src/pages/admin/**/*.tsx"
autonomous: true
requirements: [ICON-03]

must_haves:
  truths:
    - "Every lucide-react icon import in components/admin/ and pages/admin/ is replaced with an equivalent import from @/components/icons"
    - "No file in components/admin/ or pages/admin/ imports from lucide-react after this batch"
    - "The LucideIcon type import in pages/admin/voice-live-management.tsx is swapped to FluentIcon from @fluentui/react-icons"
    - "All existing admin unit tests and type checks pass with zero visual/behavioral regression"
  artifacts:
    - path: "frontend/src/components/admin/"
      provides: "51-file admin+admin-pages icon batch (26 components/admin files + 25 pages/admin files), zero lucide-react imports remaining"
  key_links:
    - from: "frontend/src/components/admin/*.tsx, frontend/src/pages/admin/*.tsx"
      to: "frontend/src/components/icons/index.ts"
      via: "named import"
      pattern: "from \"@/components/icons\""
---

<objective>
Migrate all lucide-react icon imports in `components/admin/` (26 files) and `pages/admin/` (25 files) — 51 files total — to the `@/components/icons` adapter created in 41-02-PLAN.md. This is the ICON-03 "admin" batch, one of four directory-scoped batches, each its own commit per CLAUDE.md's one-batch-one-commit rule.

Purpose: complete the largest icon-consumer batch first, proving the adapter module works at scale in real call sites (dialogs, tables, cards) before the remaining three smaller batches.
Output: zero `lucide-react` imports remaining under `components/admin/` or `pages/admin/`; all tests green.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-icon-toast-adapter-layers/41-RESEARCH.md
@.planning/phases/41-icon-toast-adapter-layers/41-02-PLAN.md

<interfaces>
<!-- The adapter module created in 41-02-PLAN.md exports all 84 lucide-shaped names from a single path.
     Every icon import in this batch becomes: import { IconName1, IconName2 } from "@/components/icons";
     (same named-import syntax as before, only the source path changes from "lucide-react" to "@/components/icons"). -->
From frontend/src/components/icons/index.ts (created in 41-02):
```typescript
export { CheckmarkRegular as Check } from "@fluentui/react-icons";
export { EditRegular as Pencil } from "@fluentui/react-icons";
// ...82 more named re-exports, same lucide-shaped names the codebase already uses
```

EXCLUDED from this batch (Phase 40 territory — do not touch): `components/ui/dialog.tsx`, `dropdown-menu.tsx`, `select.tsx`, `sheet.tsx`. If any admin file imports icons FROM one of those 4 files (not from lucide-react directly), leave it untouched — only migrate direct `from "lucide-react"` imports.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate lucide-react imports to @/components/icons across admin batch</name>
  <files>frontend/src/components/admin/*.tsx, frontend/src/pages/admin/*.tsx</files>
  <action>
    Run `grep -rl 'from "lucide-react"' frontend/src/components/admin frontend/src/pages/admin` to get the authoritative current file list (expect ~51 files; the exact count may drift slightly from research's estimate — trust the live grep, not the estimate). For each file, replace `from "lucide-react"` with `from "@/components/icons"` in the import statement, keeping the exact same named-import list (all icon names are already re-exported under identical names by the adapter). Do NOT batch-sed blindly — open each file, confirm the imported names exist in the adapter's export list (from 41-02's index.ts), and only then apply the edit. For `frontend/src/pages/admin/voice-live-management.tsx`, additionally change `import type { LucideIcon } from "lucide-react"` to `import type { FluentIcon } from "@fluentui/react-icons"` and update any local type annotations using `LucideIcon` to `FluentIcon`. Do not touch any file under `components/ui/` (Phase 40 territory).
  </action>
  <verify>
    <automated>cd frontend && ! grep -rl 'from "lucide-react"' src/components/admin src/pages/admin && npx tsc -b</automated>
  </verify>
  <done>Zero remaining `from "lucide-react"` matches under components/admin/ and pages/admin/; tsc passes with zero new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Verify tests, record bundle-size note, commit</name>
  <files>frontend/src/components/admin/*.tsx, frontend/src/pages/admin/*.tsx</files>
  <action>
    Run the full frontend unit test suite scoped to admin (`npx vitest run src/components/admin src/pages/admin`) and confirm zero regressions. Run `npm run build` and capture the `dist/assets/*.js` total size before/after via `du -sh frontend/dist/assets` (compare against the last known build size if available from a prior CI artifact or simply note the current build size — this satisfies research's Open Question #2 on bundle-size reporting via the simplest available method, no new tooling). Stage only the files changed in Task 1 and commit with message `feat(41-icon-toast): migrate admin+admin-pages icons to Fluent adapter (ICON-03 admin batch)` including the bundle-size note in the commit body. Push.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/admin src/pages/admin</automated>
  </verify>
  <done>All admin-scoped unit tests pass; build succeeds; one commit created and pushed with bundle-size note in the message body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | Import-path-only changes to decorative icon components; no logic, data, or trust boundary altered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03 | — | components/admin/*.tsx, pages/admin/*.tsx | accept | Mechanical import-path swap only; no behavioral or data-handling change; existing test suite is the regression guard. |
</threat_model>

<verification>
```bash
cd frontend
grep -rl 'from "lucide-react"' src/components/admin src/pages/admin  # must return nothing
npx tsc -b
npx vitest run src/components/admin src/pages/admin
```
</verification>

<success_criteria>
- [ ] Zero `lucide-react` imports remain in components/admin/ and pages/admin/
- [ ] voice-live-management.tsx's LucideIcon type swapped to FluentIcon
- [ ] All admin-scoped tests pass, tsc clean, build succeeds
- [ ] One commit pushed with bundle-size note
</success_criteria>

<output>
After completion, create `.planning/phases/41-icon-toast-adapter-layers/41-03-SUMMARY.md`
</output>
