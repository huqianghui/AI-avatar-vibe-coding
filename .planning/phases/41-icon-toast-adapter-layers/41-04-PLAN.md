---
phase: 41-icon-toast-adapter-layers
plan: 04
type: execute
wave: 3
depends_on: ["41-02"]
files_modified:
  - "frontend/src/components/shared/*.tsx"
autonomous: true
requirements: [ICON-03]

must_haves:
  truths:
    - "Every lucide-react icon import in components/shared/ is replaced with an equivalent import from @/components/icons"
    - "No file in components/shared/ imports from lucide-react after this batch"
    - "The LucideIcon type imports in action-card.tsx and stat-card.tsx are swapped to FluentIcon"
    - "All existing shared-component unit tests and type checks pass with zero visual/behavioral regression"
  artifacts:
    - path: "frontend/src/components/shared/"
      provides: "23-file shared-components icon batch, zero lucide-react imports remaining"
  key_links:
    - from: "frontend/src/components/shared/*.tsx"
      to: "frontend/src/components/icons/index.ts"
      via: "named import"
      pattern: "from \"@/components/icons\""
---

<objective>
Migrate all lucide-react icon imports in `components/shared/` (23 files) to the `@/components/icons` adapter. This is the ICON-03 "shared" batch — one of four directory-scoped batches, each its own commit per CLAUDE.md's one-batch-one-commit rule. Runs in parallel with the admin and pages batches (disjoint file sets).

**Sequencing note (per plan-check B1):** Two of this batch's files (`dry-run-button.tsx`, `dry-run-progress.tsx`) overlap with `41-10` (the TOAST-02 shared+voice batch), which edits `sonner` import lines in those same two files. This is NOT a fully-disjoint relationship relative to 41-10 — 41-10 now explicitly `depends_on` this plan (41-04) and runs strictly after it. This plan itself has no new dependency as a result (it only edits icon-import lines, unrelated to 41-10's sonner-import lines) and its own wave/depends_on are unchanged. It remains fully parallel-safe relative to the admin (41-03) and pages (41-06) icon batches.

Purpose: complete the shared-components icon migration independently and reviewably.
Output: zero `lucide-react` imports remaining under `components/shared/`; all tests green.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-icon-toast-adapter-layers/41-RESEARCH.md
@.planning/phases/41-icon-toast-adapter-layers/41-02-PLAN.md

<interfaces>
From frontend/src/components/icons/index.ts (created in 41-02):
```typescript
export { CheckmarkRegular as Check } from "@fluentui/react-icons";
// ...84 total named re-exports, same lucide-shaped names the codebase already uses
```
Only change the import source path; named import lists stay identical.

EXCLUDED (Phase 40 territory): `components/ui/dialog.tsx`, `dropdown-menu.tsx`, `select.tsx`, `sheet.tsx` — do not touch.

**Downstream note:** `41-10` (TOAST-02 shared+voice batch) depends on this plan and will edit `sonner` import lines in `dry-run-button.tsx`/`dry-run-progress.tsx` after this plan's commit lands — that is expected and does not affect this plan's own scope.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate lucide-react imports to @/components/icons across shared batch</name>
  <files>frontend/src/components/shared/*.tsx</files>
  <action>
    Run `grep -rl 'from "lucide-react"' frontend/src/components/shared` for the authoritative file list (expect ~23 files). For each file, replace `from "lucide-react"` with `from "@/components/icons"`, keeping the same named-import list, confirming each imported name exists in the adapter (41-02's index.ts) before applying — no blind batch-sed. In `frontend/src/components/shared/action-card.tsx` and `frontend/src/components/shared/stat-card.tsx`, change `import type { LucideIcon } from "lucide-react"` to `import type { FluentIcon } from "@fluentui/react-icons"` and update local type usages accordingly.
  </action>
  <verify>
    <automated>cd frontend && ! grep -rl 'from "lucide-react"' src/components/shared && npx tsc -b</automated>
  </verify>
  <done>Zero remaining `from "lucide-react"` matches under components/shared/; tsc passes with zero new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Verify tests, record bundle-size note, commit</name>
  <files>frontend/src/components/shared/*.tsx</files>
  <action>
    Run `npx vitest run src/components/shared` and confirm zero regressions. Run `npm run build` and note `du -sh frontend/dist/assets` for the bundle-size comparison. Stage only the files changed in Task 1 and commit with message `feat(41-icon-toast): migrate shared components icons to Fluent adapter (ICON-03 shared batch)` including the bundle-size note in the commit body. Push.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/shared && cd .. && git log @{u}.. --oneline | wc -l | grep -qx 0</automated>
  </verify>
  <done>All shared-component unit tests pass; build succeeds; one commit created and pushed (local HEAD not ahead of upstream), with bundle-size note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | Import-path-only changes to decorative icon components; no logic, data, or trust boundary altered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04 | — | components/shared/*.tsx | accept | Mechanical import-path swap only; existing test suite is the regression guard. |
</threat_model>

<verification>
```bash
cd frontend
grep -rl 'from "lucide-react"' src/components/shared  # must return nothing
npx tsc -b
npx vitest run src/components/shared
```
</verification>

<success_criteria>
- [ ] Zero `lucide-react` imports remain in components/shared/
- [ ] action-card.tsx and stat-card.tsx's LucideIcon type swapped to FluentIcon
- [ ] All shared-scoped tests pass, tsc clean, build succeeds
- [ ] One commit pushed (verified via `git log @{u}..`) with bundle-size note
</success_criteria>

<output>
After completion, create `.planning/phases/41-icon-toast-adapter-layers/41-04-SUMMARY.md`
</output>
</content>
