---
phase: 41-icon-toast-adapter-layers
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/toast/toast-bridge.spike.test.tsx
autonomous: true
requirements: [TOAST-01]

must_haves:
  truths:
    - "dispatchToast's void return is confirmed empirically (not just via .d.ts reading) — calling it does not yield an id"
    - "Passing an explicit options.toastId to dispatchToast causes that same id to later successfully dismiss the toast via dismissToast"
    - "A counter-based id generator (no uuid dependency) produces unique, monotonically increasing ids suitable as toastId values"
  artifacts:
    - path: "frontend/src/lib/toast/toast-bridge.spike.test.tsx"
      provides: "Automated proof of the dispatchToast/dismissToast toastId round-trip contract that TOAST-02's bridge module depends on"
  key_links:
    - from: "frontend/src/lib/toast/toast-bridge.spike.test.tsx"
      to: "@fluentui/react-components useToastController/dispatchToast/dismissToast"
      via: "render + act, asserting call sequence and toast presence/absence in the DOM"
      pattern: "dispatchToast.*toastId"
---

<objective>
Run the TOAST-01 empirical spike: prove that `dispatchToast(content, options)` returns `void` (not an id, unlike sonner's `toast.loading()`), and that passing an explicit, self-generated `options.toastId` allows a later `dismissToast(toastId)` call to correctly dismiss that exact toast — the critical round-trip sonner's `toast.loading()` + `toast.dismiss(id)` pattern at `avatar-page.tsx:224-243` depends on.

This is Phase 41's entry gate for the toast track: TOAST-02 (bridge module + all migration batches) must not start until this spike confirms the id round-trip mechanism works as designed.

Purpose: eliminate the risk that the bridge's manually-assigned counter-based `toastId` doesn't actually control real dismiss/update behavior, before wiring 46 files onto it.
Output: A permanent, automated spike test whose passing result IS the go/no-go signal for TOAST-02.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-icon-toast-adapter-layers/41-RESEARCH.md
@.planning/phases/39-fluent-infrastructure-leaf-components/39-CONTEXT.md

<interfaces>
<!-- @fluentui/react-components toast API (confirmed via .d.ts + vanilla/dispatchToast.js per 41-RESEARCH.md Sources):
     - useToastController(toasterId: string): { dispatchToast(content: JSX.Element, options?: ToastOptions): void; dismissToast(toastId: string): void; updateToast(options): void }
     - ToastOptions includes optional `toastId?: string`; if omitted, Fluent generates its own internal id (which is NOT returned to the caller — hence the id-management problem this spike proves must be solved by the caller always supplying its own).
     - <Toaster toasterId="app-toaster" /> must be mounted in the tree for useToastController(same id) to have an effect; in tests this means rendering <Toaster toasterId="test-toaster"/> alongside the hook consumer. -->
From frontend/src/components/providers/fluent-theme-bridge.tsx (Phase 39 precedent for mount-once-capture-hook-into-module-ref pattern, reference only — not imported by this spike):
```tsx
export function FluentThemeBridge({ children }: { children: React.ReactNode }) {
  const { mode, accent } = useThemeStore();
  const theme = useMemo(() => getFluentTheme(mode, accent), [mode, accent]);
  return <FluentProvider theme={theme} className="contents" style={{ background: "transparent", color: "inherit" }}>{children}</FluentProvider>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spike dispatchToast void-return and toastId round-trip</name>
  <files>frontend/src/lib/toast/toast-bridge.spike.test.tsx</files>
  <behavior>
    - Test 1: Render a test harness component that mounts `<Toaster toasterId="test-toaster" />` and calls `useToastController("test-toaster")` to get `{ dispatchToast, dismissToast }`, exposing them to the test body via a captured ref. Call `dispatchToast(<div>hello</div>, { toastId: "toast-1" })` inside `act()` and assert the return value is `undefined` — confirms the void-return finding empirically, not just from `.d.ts`.
    - Test 2: After Test 1's dispatch, assert the toast content ("hello") IS present in the rendered DOM (via `screen.findByText` or equivalent), then call `dismissToast("toast-1")` inside `act()`, then assert the toast content is NO LONGER present in the DOM (dismissal succeeded using the self-supplied id) — proves the explicit-toastId round-trip works.
    - Test 3: Implement and test a minimal counter-based id generator (`let counter = 0; export function nextToastId() { return \`toast-${++counter}\`; }`) directly in this spike file and assert two successive calls produce `"toast-1"` and `"toast-2"` — no collisions, no uuid dependency needed.
  </behavior>
  <action>
    Create `frontend/src/lib/toast/toast-bridge.spike.test.tsx`. Import `Toaster`, `useToastController` from `@fluentui/react-components`. Build a small test-only harness component that exposes `dispatchToast`/`dismissToast` to the test body (e.g. via a ref captured in a `useEffect`, mirroring the Phase 39 FluentThemeBridge mount-capture pattern). Use `@testing-library/react`'s `render`, `act`, `screen`, `findByText` as needed. Write the counter-based `nextToastId` helper inline in this spike file for Test 3 (TOAST-02's real bridge will implement the same logic in `toast-bridge.ts` — this spike proves the approach before that file exists). Add a file-header comment recording the confirmed conclusion: "dispatchToast returns void; explicit toastId round-trips correctly through dismissToast; counter-based ids are sufficient, no uuid needed" (or documenting any deviation found, which would require flagging to the team-lead before TOAST-02 proceeds).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/toast/toast-bridge.spike.test.tsx</automated>
  </verify>
  <done>All 3 spike assertions pass; the file's header comment records the confirmed dispatchToast/dismissToast/counter-id contract for TOAST-02 to implement verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | This plan only adds a test file exercising already-installed, already-audited Fluent toast components with static, hardcoded content. No user input, no network, no persisted data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07 | — | toast-bridge.spike.test.tsx | accept | No security-relevant surface — test-only file exercising static content strings, never shipped to a running server. |
</threat_model>

<verification>
```bash
cd frontend
npx vitest run src/lib/toast/toast-bridge.spike.test.tsx
```
Must exit 0. The spike test's pass/fail IS the TOAST-01 entry gate — do not proceed to 41-08-PLAN.md (TOAST-02) if any assertion fails; instead resolve the discrepancy against research's documented contract and re-run this spike before proceeding.
</verification>

<success_criteria>
- [ ] `toast-bridge.spike.test.tsx` exists with 3 passing assertions (void return, toastId round-trip, counter-id uniqueness)
- [ ] File header comment records the definitive dispatchToast/dismissToast contract for TOAST-02
- [ ] TOAST-01 entry gate is satisfied — 41-08-PLAN.md may proceed
</success_criteria>

<output>
After completion, create `.planning/phases/41-icon-toast-adapter-layers/41-07-SUMMARY.md`
</output>
