---
phase: 41-icon-toast-adapter-layers
plan: 12
type: execute
wave: 5
depends_on: ["41-03", "41-04", "41-05", "41-06", "41-09", "41-10", "41-11"]
files_modified:
  - frontend/e2e/icon-toast-adapter.spec.ts
autonomous: false
requirements: [ICON-03, TOAST-02]

must_haves:
  truths:
    - "An E2E test confirms the Fluent Toaster renders a role=status live region when a toast is dispatched, equivalent to sonner's prior accessibility behavior"
    - "An E2E test confirms icons render at their expected visual size on at least one migrated page from each of the 4 icon batches"
    - "A human visually spot-checks the app to confirm no icon-sizing or toast-styling regressions slipped through automated coverage"
  artifacts:
    - path: "frontend/e2e/icon-toast-adapter.spec.ts"
      provides: "E2E coverage for Phase 41's success criteria #5 (toast role=status) plus a cross-batch icon smoke check"
  key_links:
    - from: "frontend/e2e/icon-toast-adapter.spec.ts"
      to: "running app (avatar-page, an admin page, a shared component)"
      via: "Playwright page.locator('[role=status]') assertion after triggering a toast"
      pattern: "role=.status."
---

<objective>
Close out Phase 41 with the E2E verification required by ROADMAP.md's Success Criteria #5 ("E2E confirms the Fluent Toaster renders a role=status live region equivalent to the prior sonner behavior") plus a cross-batch icon smoke check, then a final human visual spot-check across all migrated surfaces.

This plan runs last, after all 4 icon batches (41-03/04/05/06) and all 3 toast batches (41-09/10/11) are committed. Note (per plan-check B1 revision): the toast batches now each `depends_on` their icon-batch sibling and run in wave 4 (after the icon batches' wave 3), so this plan's own wave is now 5 (max of all 7 dependency waves + 1) rather than 4 — its `depends_on` list is unchanged, only the wave number shifted to reflect the corrected Wave 3/4 sequencing upstream.

Purpose: provide the automated + human confirmation that Phase 41's two adapter layers work correctly in the real running app, not just in unit tests.
Output: `frontend/e2e/icon-toast-adapter.spec.ts` passing in CI, plus human sign-off on visual correctness.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-icon-toast-adapter-layers/41-RESEARCH.md
@.planning/ROADMAP.md

<interfaces>
<!-- Existing E2E pattern precedent: frontend/e2e/anonymous-avatar-qa.spec.ts uses page.route() interception
     with deterministic fixtures. Follow the same setup style (deterministic mocked API responses, not live
     backend calls) so this spec is stable in CI. -->
Playwright config: frontend/e2e/playwright.config.ts — baseURL http://localhost:5173, dual webServer
(backend uvicorn + frontend npm dev), run via `npm run test:e2e` / `playwright test --config=e2e/playwright.config.ts`.

Fluent Toaster's underlying accessibility behavior (per @fluentui/react-components docs): the Toaster region
renders with `role="status"` (or `role="alert"` for high-priority variants) automatically — no manual ARIA
wiring required in the bridge module; this test verifies that default holds true in this app's actual DOM.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write E2E spec for toast role=status + icon smoke check</name>
  <files>frontend/e2e/icon-toast-adapter.spec.ts</files>
  <action>
    Create `frontend/e2e/icon-toast-adapter.spec.ts` following the deterministic-fixture pattern from `anonymous-avatar-qa.spec.ts`. Test 1: navigate to a page that triggers a toast on a user action reachable without auth complexity (e.g. a form validation error on `/login`, or mock an API error on the avatar page via `page.route()` to trigger `toast.error`), then assert `page.locator('[role="status"]')` (or `[role="alert"]`, whichever the Fluent Toaster actually renders — confirm via a quick manual check first) becomes visible and contains the expected toast text. Test 2: navigate to one page from each of the 4 icon batches reachable in this E2E environment (e.g. `/login` for pages-batch, an admin page requiring login for admin-batch if auth fixtures exist — check `anonymous-avatar-qa.spec.ts`/other specs for an existing login fixture helper to reuse; if admin auth is too complex for this spec, cover 2-3 reachable pages and note in a comment why others are out of scope for E2E and instead covered by the batch-level unit tests) and assert at least one icon element (`svg`) is present and has non-zero rendered width/height via `boundingBox()`.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test --config=e2e/playwright.config.ts icon-toast-adapter.spec.ts</automated>
  </verify>
  <done>E2E spec passes: toast role=status/alert region confirmed visible with correct content; icon smoke check confirms non-zero rendered dimensions on reachable migrated pages.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual spot-check of icon sizing and toast behavior across the app</name>
  <files>none</files>
  <action>Human verification of icon and toast visual/behavioral correctness across the fully-migrated app.</action>
  <what-built>All icon call sites (121 files across 4 batches) migrated from lucide-react to @fluentui/react-icons via the adapter module; all toast call sites (46 files across 3 batches) migrated from sonner to Fluent Toaster via the bridge module. Automated unit + E2E coverage confirms no regression.</what-built>
  <how-to-verify>
    1. Run `cd frontend && npm run dev` and open http://localhost:5173.
    2. Visit the login page, an avatar session page, and (if credentials available) an admin page — confirm all icons render at their expected size and color (no oversized/undersized/missing icons compared to your memory of the pre-migration app).
    3. Trigger a toast (e.g. submit an invalid login, or trigger any error/success action) and confirm it appears styled consistently with the app's theme, in the expected position, and dismisses correctly.
    4. On the avatar session page, trigger the flow that shows a "processing/loading" toast (per avatar-page.tsx's loading→dismiss pattern) and confirm it disappears correctly once the action completes (not stuck, not duplicated).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/behavioral issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test-to-app boundary | E2E spec drives the real running app via Playwright; uses mocked/deterministic API responses per existing repo convention, not live backend secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-13 | — | icon-toast-adapter.spec.ts | accept | E2E test-only file, deterministic fixtures, no production secrets or live external calls. |
</threat_model>

<verification>
```bash
cd frontend
npx playwright test --config=e2e/playwright.config.ts icon-toast-adapter.spec.ts
```
Followed by the human checkpoint above.
</verification>

<success_criteria>
- [ ] E2E spec confirms toast role=status/alert live region renders correctly
- [ ] E2E spec confirms icons render with non-zero dimensions on reachable migrated pages
- [ ] Human checkpoint approved with no reported visual/behavioral regressions
- [ ] Phase 41 (ICON-01/02/03 + TOAST-01/02) fully complete
</success_criteria>

<output>
After completion, create `.planning/phases/41-icon-toast-adapter-layers/41-12-SUMMARY.md`
</output>
</content>
