---
phase: 41-icon-toast-adapter-layers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/icons/icon-sizing.spike.test.tsx
autonomous: true
requirements: [ICON-01]

must_haves:
  truths:
    - "A Fluent icon rendered with Tailwind's size-4/size-6 classes displays at the Tailwind-specified pixel size, not at Fluent's baked-in 1em width/height SVG attribute size"
    - "A Fluent icon rendered inside a Fluent-migrated Button (Phase 39 LEAF-01) still resolves size from the Tailwind class, not from Button's own icon-slot styling"
    - "A Fluent icon's rendered color follows currentColor (inherits the wrapping element's text color), matching lucide's icon color behavior"
  artifacts:
    - path: "frontend/src/components/icons/icon-sizing.spike.test.tsx"
      provides: "Automated proof that Tailwind size-* wins over Fluent's 1em width/height attribute, standalone and inside Button, plus currentColor verification"
  key_links:
    - from: "frontend/src/components/icons/icon-sizing.spike.test.tsx"
      to: "@fluentui/react-icons CheckmarkRegular/DismissRegular/SearchRegular"
      via: "direct render + getComputedStyle assertion against an injected Tailwind-equivalent <style> rule"
      pattern: "getComputedStyle"
---

<objective>
Run the ICON-01 empirical spike: prove that Tailwind's `size-*` utility classes override Fluent icon components' literal `width="1em" height="1em"` SVG attributes, and that `currentColor` fill correctly inherits text color — both standalone and nested inside the already-Fluent-migrated `Button` (Phase 39 LEAF-01), per Pitfall 4 in 41-RESEARCH.md.

This is Phase 41's entry gate for the icon track: ICON-02 (adapter module) and ICON-03 (130-call-site batch migration) must not start until this spike passes and its decision (plain re-export vs. explicit `width={undefined}` wrapper) is recorded.

Purpose: eliminate the single biggest risk in the icon migration — icons silently rendering at the wrong size — before touching any of the 121 consumer files.
Output: A permanent, automated spike test (`icon-sizing.spike.test.tsx`) whose passing result IS the go/no-go signal for ICON-02.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-icon-toast-adapter-layers/41-RESEARCH.md
@.planning/phases/39-fluent-infrastructure-leaf-components/39-CONTEXT.md

<interfaces>
<!-- Fluent icon components render <svg width="1em" height="1em" fill={primaryFill ?? "currentColor"}>.
     Confirmed via node_modules/@fluentui/react-icons/lib/utils/createFluentIcon.js (41-RESEARCH.md Sources).
     Every icon export has type `FluentIcon` (a React.FC accepting standard SVG props + className). -->

From frontend/src/components/ui/button.tsx (Phase 39 LEAF-01 — already Fluent-backed, safe to import in this test):
```typescript
export type ButtonVariant = "default" | "destructive" | "outline" | "secondary" | "ghost" | "link";
export type ButtonSize = "default" | "sm" | "lg" | "icon";
export interface ButtonProps extends Omit<React.ComponentProps<"button">, "color" | "size">, React.RefAttributes<HTMLButtonElement> {
  variant?: ButtonVariant;
  size?: ButtonSize;
  asChild?: boolean;
}
const Button: React.ForwardRefExoticComponent<ButtonProps>;
```

From @fluentui/react-icons (installed 2.0.334, verified exports):
```typescript
export const CheckmarkRegular: FluentIcon; // Check
export const DismissRegular: FluentIcon;   // X
export const SearchRegular: FluentIcon;    // Search
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Standalone icon-sizing spike (Tailwind size-* vs 1em attribute, currentColor)</name>
  <files>frontend/src/components/icons/icon-sizing.spike.test.tsx</files>
  <behavior>
    - Test 1: Render `<CheckmarkRegular data-testid="icon" />` inside a wrapper that has an injected `<style>` tag replicating Tailwind v4's generated `size-4` rule (`.size-4{width:1rem;height:1rem}`) and `.size-6{width:1.5rem;height:1.5rem}`; apply `className="size-4"` and assert `getComputedStyle(icon).width === "16px"` and `.height === "16px"` (NOT `"1em"`/browser-default-font-size-derived value) — proves the class wins over the SVG's literal `width="1em" height="1em"` attributes.
    - Test 2: Re-render the same icon with `className="size-6"` and assert computed width/height is `"24px"` — proves the override is class-driven, not a fixed coincidence.
    - Test 3: Wrap the icon in a parent with `className="text-red-500"` (inject the equivalent `<style>.text-red-500{color:rgb(239,68,68)}</style>`) and assert `getComputedStyle(icon).fill === "currentcolor"` OR (if jsdom resolves the used value) that the icon's computed `color` matches the injected rgb value — proves `fill="currentColor"` correctly inherits ancestor text color the same way lucide icons did via `className="text-*"`.
  </behavior>
  <action>
    Create `frontend/src/components/icons/icon-sizing.spike.test.tsx`. Import `CheckmarkRegular`, `DismissRegular`, `SearchRegular` directly from `@fluentui/react-icons` (no adapter exists yet — that's ICON-02's job). For each test, inject a `<style>` element into `document.head` containing the literal Tailwind-generated CSS rule text (do NOT import the real compiled `index.css` — keep the test hermetic and fast), render via `@testing-library/react`, then assert via `getComputedStyle`. Use `afterEach` to remove injected `<style>` tags to avoid cross-test leakage. Write a one-paragraph comment at the top of the file documenting the empirical conclusion (pass/fail) and which pattern ICON-02 must use as a result: "plain named re-export, no wrapper needed" if all three tests pass, or "wrap with explicit `width={undefined} height={undefined}` override" if the 1em attribute wins in any case.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/icons/icon-sizing.spike.test.tsx</automated>
  </verify>
  <done>All 3 assertions pass, proving Tailwind size-* classes control rendered icon dimensions and currentColor inheritance works identically to lucide's behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: In-situ spike inside Fluent Button (Pitfall 4) + finalize decision</name>
  <files>frontend/src/components/icons/icon-sizing.spike.test.tsx</files>
  <behavior>
    - Test 4: Render `<Button size="icon"><SearchRegular className="size-4" data-testid="icon-in-button" /></Button>` (using the real Phase-39 Fluent-backed `Button` from `@/components/ui/button`) and assert the icon's computed width/height is still `16px` — proves Button's own Griffel icon-slot styling does not silently override the consumer's Tailwind sizing class.
    - Test 5: Same Button context, assert `getComputedStyle(icon).fill` resolves to `currentcolor` (not overridden by Button's own color styling in a way that breaks the fill mechanism).
  </behavior>
  <action>
    Extend the same spike file with the two Button-nested assertions described above, importing `Button` from `@/components/ui/button`. Run the full spike file (all 5 tests). Update the top-of-file conclusion comment with the final, confirmed decision now that both standalone and in-situ contexts are proven. This decision string ("plain re-export" is the expected outcome per 41-RESEARCH.md's Summary) is what ICON-02 (41-02-PLAN.md) will implement — no code changes to `button.tsx` itself are made in this plan.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/icons/icon-sizing.spike.test.tsx && npx tsc -b</automated>
  </verify>
  <done>All 5 spike assertions pass (3 standalone + 2 in-Button); the file's header comment records the final "plain re-export" vs "wrapper needed" decision for ICON-02 to consume verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | This plan only adds a test file exercising already-installed, already-audited Fluent icon components against static, hardcoded CSS strings. No user input, no network, no persisted data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | — | icon-sizing.spike.test.tsx | accept | No security-relevant surface — decorative icon rendering, no dynamic/user-controlled input, test-only file never shipped to a running server. |
</threat_model>

<verification>
```bash
cd frontend
npx vitest run src/components/icons/icon-sizing.spike.test.tsx
npx tsc -b
```
Both must exit 0. The spike test's pass/fail IS the ICON-01 entry gate — do not proceed to 41-02-PLAN.md (ICON-02) if any assertion fails; instead flip the adapter pattern to the `width={undefined}` wrapper documented in 41-RESEARCH.md's fallback pattern and re-run this spike against that wrapper before proceeding.
</verification>

<success_criteria>
- [ ] `icon-sizing.spike.test.tsx` exists with 5 passing assertions (3 standalone, 2 in-Button)
- [ ] File header comment records the definitive adapter-pattern decision for ICON-02
- [ ] `npx tsc -b` passes with zero new type errors
- [ ] ICON-01 entry gate is satisfied — 41-02-PLAN.md may proceed
</success_criteria>

<output>
After completion, create `.planning/phases/41-icon-toast-adapter-layers/41-01-SUMMARY.md`
</output>
