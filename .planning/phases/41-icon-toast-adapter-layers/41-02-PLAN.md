---
phase: 41-icon-toast-adapter-layers
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - frontend/src/components/icons/index.ts
  - frontend/src/components/icons/index.test.ts
autonomous: true
requirements: [ICON-02]

must_haves:
  truths:
    - "A single import path (@/components/icons) exports all 84 lucide-shaped icon names the codebase currently imports from lucide-react"
    - "Each exported name is a thin named re-export of a real @fluentui/react-icons component, following the ICON-01 spike's confirmed pattern (plain re-export, no wrapper, unless the spike concluded otherwise)"
    - "The 18 research-flagged low-confidence icon names are resolved to a real Fluent icon via manual visual/semantic check, not guessed"
  artifacts:
    - path: "frontend/src/components/icons/index.ts"
      provides: "The icon adapter module — named re-exports mapping lucide names to Fluent icons"
      contains: "export {"
    - path: "frontend/src/components/icons/index.test.ts"
      provides: "Automated proof every expected lucide-shaped name is exported and is a valid React component"
  key_links:
    - from: "frontend/src/components/icons/index.ts"
      to: "@fluentui/react-icons"
      via: "named re-export"
      pattern: "export \\{ .*Regular as .* \\} from \"@fluentui/react-icons\""
---

<objective>
Create the icon adapter module (`src/components/icons/index.ts`) providing all 84 lucide-shaped icon names as thin named re-exports of `@fluentui/react-icons` components, applying the ICON-01 spike's confirmed sizing pattern. This module is the single import surface ICON-03's four batch plans will point every consumer at.

Purpose: centralize the lucide→Fluent name mapping in one reviewable file instead of scattering per-file guesses across 121 consumer files.
Output: `src/components/icons/index.ts` with 84 verified exports + a test proving every export resolves to a real component.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-icon-toast-adapter-layers/41-RESEARCH.md
@.planning/phases/41-icon-toast-adapter-layers/41-01-PLAN.md

<interfaces>
<!-- ICON-01's spike (41-01-PLAN.md) determines the exact export shape:
     - If spike passes with plain re-export: `export { CheckmarkRegular as Check } from "@fluentui/react-icons";`
     - If spike requires a sizing wrapper: each export becomes a small forwardRef wrapping the Fluent icon with
       width={undefined} height={undefined} so Tailwind classes are the only sizing source.
     Read the confirmed decision from the header comment in
     frontend/src/components/icons/icon-sizing.spike.test.tsx before writing this file. -->

Research's full 84-row lucide→Fluent mapping table (41-RESEARCH.md "Icon Mapping Table") is the base source.
66 of 84 names are confidence-flagged as resolved. The following 18 are explicitly LOW-CONFIDENCE / unresolved
and MUST be manually verified against the installed @fluentui/react-icons package (via IDE autocomplete or
`grep -r "export declare const <Name>" frontend/node_modules/@fluentui/react-icons/lib`) before finalizing:
AlertCircle, AudioLines, BarChart, ExternalLink, FileArchive, FileAudio, FlaskConical, Inbox, LogIn,
MessageSquare/MessageSquareText, Palette, Pencil, Presentation, RotateCcw, SearchX, TrendingUp, Volume2, Zap.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write and verify the 84-icon adapter module</name>
  <files>frontend/src/components/icons/index.ts, frontend/src/components/icons/index.test.ts</files>
  <behavior>
    - Test 1: Import `* as Icons` from `@/components/icons` and assert every one of the 84 expected lucide-shaped names (hardcode the full name list in the test, sourced from 41-RESEARCH.md's Grep Inventory "84 distinct icons") exists as a key on the module and is a function/object (a valid React component, not `undefined`).
    - Test 2: Render 3 representative exports (one confidently-mapped e.g. `Check`, one previously-low-confidence e.g. `Pencil`, one previously-low-confidence e.g. `Zap`) via `@testing-library/react` inside a plain `<svg>`-capable container and assert each renders an `<svg>` element without throwing — proves the resolved low-confidence mappings are real, renderable Fluent components, not typos.
  </behavior>
  <action>
    First, resolve each of the 18 low-confidence names by grepping the installed package: `grep -rl "export declare const <CandidateName>" frontend/node_modules/@fluentui/react-icons/lib` for reasonable candidate Fluent names (e.g. Pencil→EditRegular, Zap→FlashRegular, TrendingUp→ArrowTrendingRegular or DataTrendingRegular, Volume2→SpeakerRegular family, RotateCcw→ArrowCounterclockwiseRegular, SearchX→SearchOffRegular/SearchDismissRegular, LogIn→SignInRegular, AlertCircle→ErrorCircleRegular or WarningRegular, FlaskConical→BeakerRegular, Inbox→MailInboxRegular, Presentation→BoardRegular, Palette→ColorRegular, MessageSquare→ChatRegular, ExternalLink→OpenRegular, FileArchive→DocumentZipRegular, FileAudio→DocumentDataRegular or MicRegular-adjacent, BarChart→DataBarVerticalRegular, AudioLines→SoundWaveCircleRegular). Confirm each with one grep before committing to it in the file — do not guess blind. Then write `frontend/src/components/icons/index.ts` with all 84 named re-exports, ordered alphabetically by lucide name, one export per line, each with a trailing comment noting the Fluent source name (e.g. `export { EditRegular as Pencil } from "@fluentui/react-icons"; // was lucide Pencil`). Apply the ICON-01-confirmed pattern (plain re-export unless the spike required a wrapper — check the spike file's header comment first). Add a file-level comment stating this module is the required import path for all icon usage post-Phase-41, and that `components/ui/dialog.tsx`, `dropdown-menu.tsx`, `select.tsx`, `sheet.tsx` are intentionally NOT migrated here (Phase 40 territory). Then write `index.test.ts` per the behavior spec above.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/icons/index.test.ts && npx tsc -b</automated>
  </verify>
  <done>index.ts exports exactly the 84 expected names, all resolving to real @fluentui/react-icons components; index.test.ts passes; tsc passes with zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | Pure re-export module, no runtime logic, no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02 | — | components/icons/index.ts | accept | Static re-export surface, zero attack surface — icons are decorative UI elements with no data flow. |
</threat_model>

<verification>
```bash
cd frontend
npx vitest run src/components/icons/index.test.ts
npx tsc -b
```
</verification>

<success_criteria>
- [ ] `src/components/icons/index.ts` exports all 84 lucide-shaped names
- [ ] All 18 previously low-confidence names resolved via grep-confirmed Fluent equivalents (not guessed)
- [ ] `index.test.ts` passes, proving every export is a valid renderable component
- [ ] ICON-03's four batch plans can now import from `@/components/icons` with zero ambiguity
</success_criteria>

<output>
After completion, create `.planning/phases/41-icon-toast-adapter-layers/41-02-SUMMARY.md`
</output>
