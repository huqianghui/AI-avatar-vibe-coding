---
phase: 41-icon-toast-adapter-layers
plan: 09
type: execute
wave: 4
depends_on: ["41-08", "41-03"]
files_modified:
  - "frontend/src/components/admin/vl-instance-dialog.tsx"
  - "frontend/src/components/admin/assign-hcp-dialog.tsx"
  - "frontend/src/components/admin/user-personalization-dialog.tsx"
  - "frontend/src/components/admin/instructions-section.tsx"
  - "frontend/src/components/admin/persona-table.tsx"
  - "frontend/src/components/admin/service-config-card.tsx"
  - "frontend/src/components/admin/service-config-card.test.tsx"
  - "frontend/src/components/admin/__tests__/assign-hcp-dialog.test.tsx"
  - "frontend/src/pages/admin/**/*.tsx"
autonomous: true
requirements: [TOAST-02]

must_haves:
  truths:
    - "Every 'from \"sonner\"' import in components/admin/ and pages/admin/ is replaced with 'from \"@/lib/toast\"'"
    - "Every vi.mock('sonner') in this batch's test files is individually reviewed and rewritten as vi.mock('@/lib/toast') matching its own original mock surface exactly (not a bulk-sed find/replace)"
    - "assign-hcp-dialog.test.tsx's nested __tests__ directory await import('sonner') pattern is updated to await import('@/lib/toast')"
    - "All admin-scoped unit tests pass with zero behavioral regression in toast call sites"
  artifacts:
    - path: "frontend/src/components/admin/"
      provides: "Admin toast-import batch: 7 components/admin files + all pages/admin files migrated from sonner to @/lib/toast"
  key_links:
    - from: "frontend/src/components/admin/*.tsx, frontend/src/pages/admin/*.tsx"
      to: "frontend/src/lib/toast/index.ts"
      via: "named import"
      pattern: "from \"@/lib/toast\""
---

<objective>
Migrate all `sonner` imports and `vi.mock("sonner")` test mocks in `components/admin/` (7 files) and `pages/admin/` (all files importing sonner) to `@/lib/toast`, per the TOAST-02 admin batch — mirroring the ICON-03 admin batch's directory scope. This is its own commit per CLAUDE.md's one-batch-one-commit rule.

**Sequencing note (per plan-check B1):** This plan's file set (`components/admin/`, `pages/admin/`) fully overlaps with `41-03`'s (the ICON-03 admin icon batch) — both edit adjacent import lines in the same files (e.g. `instructions-section.tsx`, `persona-table.tsx`, `service-config-card.tsx`, `user-personalization-dialog.tsx`, and the entire `pages/admin/**/*.tsx` glob). `depends_on` now explicitly includes `41-03` in addition to `41-08`, and this plan's wave is 4 (after 41-03's wave-3 icon-import commit lands). Because of this dependency edge, this plan's starting working tree already contains 41-03's committed icon-import edits to every shared file before this plan's Task 1 begins — no separate manual pull/merge step is required, only awareness that the sonner-import edits in this plan land on top of (not concurrently with) 41-03's icon-import edits in the same files.

**Critical constraint (Pitfall 3 from 41-RESEARCH.md):** Do NOT bulk-`sed` the `vi.mock("sonner")` declarations. Some test files mock only `{ toast: { error, success } }` while others mock the full surface (`loading`/`dismiss`/`warning`/`info`/`promise`). Each mock must be individually opened, its exact mocked-method list preserved, and only the module path string changed from `"sonner"` to `"@/lib/toast"`.

Purpose: complete the largest toast-consumer batch, proving the bridge module works at scale in real dialog/table call sites before the remaining two smaller batches.
Output: zero `from "sonner"` imports remaining under `components/admin/` or `pages/admin/`; all vi.mock declarations updated per-file; all tests green.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-icon-toast-adapter-layers/41-RESEARCH.md
@.planning/phases/41-icon-toast-adapter-layers/41-08-PLAN.md
@.planning/phases/41-icon-toast-adapter-layers/41-03-PLAN.md

<interfaces>
From frontend/src/lib/toast/index.ts (created in 41-08):
```typescript
export const toast: {
  success(message: string, options?: { description?: string }): string;
  error(message: string, options?: { description?: string }): string;
  info(message: string, options?: { description?: string }): string;
  warning(message: string, options?: { description?: string }): string;
  loading(message: string, options?: { description?: string }): string;
  dismiss(id?: string): void;
  promise<T>(promise: Promise<T>, msgs: { loading: string; success: string; error: string }): string;
};
```
Import path change only: `from "sonner"` → `from "@/lib/toast"`. Call-site syntax is unchanged (same method names, same signatures).

`components/admin/__tests__/assign-hcp-dialog.test.tsx` is the one exception file that does `await import("sonner")` inside the test body (not a top-level `vi.mock`) — change to `await import("@/lib/toast")`.

**Sibling batch note:** 41-03 (ICON-03 admin batch, `depends_on` prerequisite of this plan) already replaced every `from "lucide-react"` import in these same files with `from "@/components/icons"` — those icon-import lines are unrelated to and must not be touched by this plan; only the `sonner` import lines are this plan's concern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate sonner imports to @/lib/toast across admin batch (components + call sites)</name>
  <files>frontend/src/components/admin/vl-instance-dialog.tsx, frontend/src/components/admin/assign-hcp-dialog.tsx, frontend/src/components/admin/user-personalization-dialog.tsx, frontend/src/components/admin/instructions-section.tsx, frontend/src/components/admin/persona-table.tsx, frontend/src/components/admin/service-config-card.tsx, frontend/src/pages/admin/*.tsx</files>
  <action>
    Run `grep -rl 'from "sonner"' frontend/src/components/admin frontend/src/pages/admin` for the authoritative non-test file list. For each production (non-test) file, replace `from "sonner"` with `from "@/lib/toast"`, keeping identical call-site usage (`toast.success(...)`, `toast.loading(...)`, etc. — no signature changes needed since 41-08's bridge preserves the sonner-shaped API). Do not touch test files in this task — those are handled individually in Task 2. Do not touch any `@/components/icons` import lines already present from 41-03.
  </action>
  <verify>
    <automated>cd frontend && ! grep -rl 'from "sonner"' src/components/admin/vl-instance-dialog.tsx src/components/admin/assign-hcp-dialog.tsx src/components/admin/user-personalization-dialog.tsx src/components/admin/instructions-section.tsx src/components/admin/persona-table.tsx src/components/admin/service-config-card.tsx && npx tsc -b</automated>
  </verify>
  <done>Zero remaining `from "sonner"` matches in the 6 named components/admin production files and all pages/admin production files; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Individually migrate each vi.mock("sonner") test file in the admin batch, run tests, commit</name>
  <files>frontend/src/components/admin/service-config-card.test.tsx, frontend/src/components/admin/__tests__/assign-hcp-dialog.test.tsx, frontend/src/pages/admin/*.test.tsx</files>
  <action>
    Run `grep -rl "vi\.mock(['\"]sonner['\"]" frontend/src/components/admin frontend/src/pages/admin` to get the exact list of test files mocking sonner in this batch. Open EACH file individually (do not script this step): read its exact `vi.mock("sonner", () => ({ ... }))` mock factory body, note precisely which methods it stubs (some only `{ toast: { error, success } }`, others the full surface), then change only the module path string to `"@/lib/toast"` while preserving the exact same mocked-method shape. For `components/admin/__tests__/assign-hcp-dialog.test.tsx`, find its `await import("sonner")` call inside the test body and change it to `await import("@/lib/toast")`. After all files are updated, run the full admin test suite and fix any mock-shape mismatches surfaced by failures (a failure here likely means the file's toast usage exceeds its mock's stubbed methods — extend the mock to cover the real usage, don't weaken the test). Stage all files changed in Task 1 + Task 2 and commit with message `feat(41-icon-toast): migrate admin+admin-pages toast imports to Fluent bridge (TOAST-02 admin batch)`. Push.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/admin src/pages/admin && cd .. && git log @{u}.. --oneline | wc -l | grep -qx 0</automated>
  </verify>
  <done>All admin-scoped unit tests pass with individually-verified vi.mock rewrites; one commit created and pushed (local HEAD not ahead of upstream).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | Import-path-only changes to notification UI; no logic, data, or trust boundary altered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-10 | — | components/admin/*.tsx, pages/admin/*.tsx | accept | Mechanical import-path swap only; existing test suite (individually reviewed mocks) is the regression guard. |
</threat_model>

<verification>
```bash
cd frontend
grep -rl 'from "sonner"' src/components/admin src/pages/admin  # must return nothing outside test-mock-only references
npx tsc -b
npx vitest run src/components/admin src/pages/admin
```
</verification>

<success_criteria>
- [ ] Zero `from "sonner"` production imports remain in components/admin/ and pages/admin/
- [ ] Every vi.mock("sonner") in this batch individually rewritten as vi.mock("@/lib/toast") with preserved mock surface
- [ ] assign-hcp-dialog.test.tsx's await import updated
- [ ] All admin-scoped tests pass, tsc clean
- [ ] One commit pushed (verified via `git log @{u}..`)
</success_criteria>

<output>
After completion, create `.planning/phases/41-icon-toast-adapter-layers/41-09-SUMMARY.md`
</output>
</content>
