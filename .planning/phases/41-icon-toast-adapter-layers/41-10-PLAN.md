---
phase: 41-icon-toast-adapter-layers
plan: 10
type: execute
wave: 4
depends_on: ["41-08", "41-04", "41-05"]
files_modified:
  - "frontend/src/components/shared/dry-run-progress.tsx"
  - "frontend/src/components/shared/dry-run-button.tsx"
  - "frontend/src/components/voice/voice-test-playground.tsx"
  - "frontend/src/components/voice/voice-session.tsx"
  - "frontend/src/components/voice/voice-test-playground.test.tsx"
  - "frontend/src/hooks/use-unified-session.ts"
  - "frontend/src/hooks/use-unified-session.test.ts"
autonomous: true
requirements: [TOAST-02]

must_haves:
  truths:
    - "Every 'from \"sonner\"' import in components/shared/, components/voice/, and hooks/use-unified-session.ts is replaced with 'from \"@/lib/toast\"'"
    - "Every vi.mock('sonner') in this batch's test files is individually reviewed and rewritten as vi.mock('@/lib/toast') matching its own original mock surface exactly"
    - "Every await import(\"sonner\") dynamic-import call in this batch's test files is individually updated to await import(\"@/lib/toast\")"
    - "All shared/voice/hooks-scoped unit tests pass with zero behavioral regression in toast call sites"
  artifacts:
    - path: "frontend/src/components/shared/dry-run-progress.tsx"
      provides: "Toast-migrated dry-run progress notifications"
    - path: "frontend/src/hooks/use-unified-session.ts"
      provides: "Toast-migrated session lifecycle notifications"
  key_links:
    - from: "frontend/src/components/shared/*.tsx, frontend/src/components/voice/*.tsx, frontend/src/hooks/use-unified-session.ts"
      to: "frontend/src/lib/toast/index.ts"
      via: "named import"
      pattern: "from \"@/lib/toast\""
---

<objective>
Migrate all `sonner` imports and `vi.mock("sonner")` test mocks in `components/shared/` (2 files), `components/voice/` (2 non-test files + 1 test file), and `hooks/use-unified-session.ts` to `@/lib/toast`, per the TOAST-02 shared+voice batch. This is its own commit per CLAUDE.md's one-batch-one-commit rule, and runs after — not in parallel with — the icon-track siblings that touch the same files (see sequencing note below).

**Sequencing note (per plan-check B1):** This plan's file set overlaps with two icon-track batches: `41-04` (`components/shared/dry-run-button.tsx`, `dry-run-progress.tsx`) and `41-05` (`components/voice/voice-session.tsx`, `voice-test-playground.tsx`). `depends_on` now explicitly includes `41-04` and `41-05` in addition to `41-08`, and this plan's wave is 4 (after those icon batches' wave-3 commits land). This plan no longer runs "in parallel" with 41-04/41-05 — it starts from a working tree that already contains their committed icon-import edits to these shared files. No separate manual pull/merge step is required; just don't touch the already-migrated `@/components/icons` import lines while editing the `sonner` import lines in the same files.

**Critical constraint (Pitfall 3):** Do NOT bulk-`sed` `vi.mock("sonner")` — each mock's exact stubbed-method surface must be preserved, only the module path changes.

**Dynamic-import gap (per plan-check W3):** `hooks/use-unified-session.test.ts` uses `await import("sonner")` (not just a top-level `vi.mock`) at 2 call sites inside test bodies. The generic `grep -l 'from "sonner"'` verify gate does NOT match this pattern — it must be found and migrated explicitly, and verified with a separate grep (see Task 2).

Purpose: complete the shared-components + voice-components + session-hook toast migration independently and reviewably.
Output: zero `from "sonner"` imports (static or dynamic) remaining in this batch's files; all tests green.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-icon-toast-adapter-layers/41-RESEARCH.md
@.planning/phases/41-icon-toast-adapter-layers/41-08-PLAN.md
@.planning/phases/41-icon-toast-adapter-layers/41-04-PLAN.md
@.planning/phases/41-icon-toast-adapter-layers/41-05-PLAN.md

<interfaces>
From frontend/src/lib/toast/index.ts (created in 41-08):
```typescript
export const toast: {
  success(message: string, options?: { description?: string }): string;
  error(message: string, options?: { description?: string }): string;
  info(message: string, options?: { description?: string }): string;
  warning(message: string, options?: { description?: string }): string;
  loading(message: string, options?: { description?: string }): string;
  dismiss(id?: string): void;
  promise<T>(promise: Promise<T>, msgs: { loading: string; success: string; error: string }): string;
};
```
Import path change only: `from "sonner"` → `from "@/lib/toast"`. Call-site syntax unchanged.

**Sibling batch note:** 41-04 and 41-05 (ICON-03 shared/voice batches, `depends_on` prerequisites of this plan) already replaced `from "lucide-react"` with `from "@/components/icons"` in `dry-run-button.tsx`, `dry-run-progress.tsx`, `voice-session.tsx`, and `voice-test-playground.tsx` — those icon-import lines are unrelated to and must not be touched by this plan.

**`await import("sonner")` exception file (confirmed via live grep, 2 call sites):** `frontend/src/hooks/use-unified-session.test.ts` does `const { toast } = await import("sonner")` inside test bodies rather than a top-level `vi.mock`. Both call sites must become `await import("@/lib/toast")`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate sonner imports to @/lib/toast across shared+voice+hooks batch</name>
  <files>frontend/src/components/shared/dry-run-progress.tsx, frontend/src/components/shared/dry-run-button.tsx, frontend/src/components/voice/voice-test-playground.tsx, frontend/src/components/voice/voice-session.tsx, frontend/src/hooks/use-unified-session.ts</files>
  <action>
    Run `grep -rl 'from "sonner"' frontend/src/components/shared frontend/src/components/voice frontend/src/hooks` for the authoritative non-test file list. For each production file, replace `from "sonner"` with `from "@/lib/toast"`, keeping identical call-site usage. Do not touch test files here — handled in Task 2. Do not touch any `@/components/icons` import lines already present from 41-04/41-05.
  </action>
  <verify>
    <automated>cd frontend && ! grep -rl 'from "sonner"' src/components/shared/dry-run-progress.tsx src/components/shared/dry-run-button.tsx src/components/voice/voice-test-playground.tsx src/components/voice/voice-session.tsx src/hooks/use-unified-session.ts && npx tsc -b</automated>
  </verify>
  <done>Zero remaining `from "sonner"` matches in the 5 named production files; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Individually migrate each vi.mock("sonner") and await import("sonner") test file in this batch, run tests, commit</name>
  <files>frontend/src/components/voice/voice-test-playground.test.tsx, frontend/src/hooks/use-unified-session.test.ts</files>
  <action>
    Run `grep -rl "vi\.mock(['\"]sonner['\"]" frontend/src/components/shared frontend/src/components/voice frontend/src/hooks` to get the exact list of test files mocking sonner via `vi.mock` in this batch (expect `voice-test-playground.test.tsx`). Open EACH file individually, note its exact mocked-method shape, then change only the module path string to `"@/lib/toast"` while preserving that exact shape.

    `hooks/use-unified-session.test.ts` also uses `await import("sonner")` (not just `vi.mock`) at 2 call sites inside test bodies — update both to `await import("@/lib/toast")`. Search explicitly with `grep -n 'import("sonner")' frontend/src/hooks/use-unified-session.test.ts` to locate both occurrences before editing; do not rely on the generic `vi.mock` grep to find them.

    Run the scoped test suite and fix any mock-shape mismatches by extending the mock to cover real usage (never weaken the test). Stage all files changed in Task 1 + Task 2 and commit with message `feat(41-icon-toast): migrate shared+voice+hooks toast imports to Fluent bridge (TOAST-02 shared/voice batch)`. Push.
  </action>
  <verify>
    <automated>cd frontend && ! grep -rn 'import([\x27"]sonner[\x27"])' src/components/shared src/components/voice src/hooks && npx vitest run src/components/shared src/components/voice src/hooks/use-unified-session.test.ts && cd .. && git log @{u}.. --oneline | wc -l | grep -qx 0</automated>
  </verify>
  <done>All shared/voice/hooks-scoped unit tests pass with individually-verified vi.mock and await-import rewrites; zero remaining `import("sonner")` matches (static or dynamic) in this batch's files; one commit created and pushed (local HEAD not ahead of upstream).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | Import-path-only changes to notification UI; no logic, data, or trust boundary altered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-11 | — | components/shared, components/voice, hooks/use-unified-session.ts | accept | Mechanical import-path swap only; existing test suite (individually reviewed mocks) is the regression guard. |
</threat_model>

<verification>
```bash
cd frontend
grep -rl 'from "sonner"' src/components/shared src/components/voice src/hooks  # must return nothing outside test-mock-only references
grep -rn 'import("sonner")' src/components/shared src/components/voice src/hooks  # must return nothing (dynamic-import check)
npx tsc -b
npx vitest run src/components/shared src/components/voice src/hooks/use-unified-session.test.ts
```
</verification>

<success_criteria>
- [ ] Zero `from "sonner"` production imports remain in this batch's files
- [ ] Zero `await import("sonner")` dynamic imports remain in this batch's files (use-unified-session.test.ts's 2 call sites migrated)
- [ ] Every vi.mock("sonner") in this batch individually rewritten with preserved mock surface
- [ ] All scoped tests pass, tsc clean
- [ ] One commit pushed (verified via `git log @{u}..`)
</success_criteria>

<output>
After completion, create `.planning/phases/41-icon-toast-adapter-layers/41-10-SUMMARY.md`
</output>
</content>
