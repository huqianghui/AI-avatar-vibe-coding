---
phase: 41-icon-toast-adapter-layers
plan: 05
type: execute
wave: 3
depends_on: ["41-02"]
files_modified:
  - "frontend/src/components/voice/*.tsx"
autonomous: true
requirements: [ICON-03]

must_haves:
  truths:
    - "Every lucide-react icon import in components/voice/ is replaced with an equivalent import from @/components/icons"
    - "No file in components/voice/ imports from lucide-react after this batch"
    - "All existing voice-component unit tests and type checks pass with zero visual/behavioral regression"
  artifacts:
    - path: "frontend/src/components/voice/"
      provides: "6-file voice-components icon batch, zero lucide-react imports remaining"
  key_links:
    - from: "frontend/src/components/voice/*.tsx"
      to: "frontend/src/components/icons/index.ts"
      via: "named import"
      pattern: "from \"@/components/icons\""
---

<objective>
Migrate all lucide-react icon imports in `components/voice/` (6 files) to the `@/components/icons` adapter. This is the ICON-03 "voice" batch — the smallest of the four directory-scoped batches, its own commit per CLAUDE.md's one-batch-one-commit rule. Runs in parallel with the admin, shared, and pages batches (disjoint file sets).

Purpose: complete the voice-components icon migration, including the `voice-test-playground.tsx`/`voice-session.tsx` files that also touch toast (handled separately by the TOAST track) but only need icon-import changes here.
Output: zero `lucide-react` imports remaining under `components/voice/`; all tests green.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-icon-toast-adapter-layers/41-RESEARCH.md
@.planning/phases/41-icon-toast-adapter-layers/41-02-PLAN.md

<interfaces>
From frontend/src/components/icons/index.ts (created in 41-02):
```typescript
export { CheckmarkRegular as Check } from "@fluentui/react-icons";
// ...84 total named re-exports, same lucide-shaped names the codebase already uses
```
Only change the import source path; named import lists stay identical. This plan touches icon imports ONLY — the same files' `sonner` toast imports are migrated separately by the TOAST-02 track (41-08/09/10-PLAN.md), not here.

EXCLUDED (Phase 40 territory): `components/ui/dialog.tsx`, `dropdown-menu.tsx`, `select.tsx`, `sheet.tsx` — do not touch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate lucide-react imports to @/components/icons across voice batch</name>
  <files>frontend/src/components/voice/*.tsx</files>
  <action>
    Run `grep -rl 'from "lucide-react"' frontend/src/components/voice` for the authoritative file list (expect ~6 files). For each file, replace `from "lucide-react"` with `from "@/components/icons"`, keeping the same named-import list, confirming each imported name exists in the adapter (41-02's index.ts) before applying — no blind batch-sed. Do not touch any `sonner`/toast import in these files.
  </action>
  <verify>
    <automated>cd frontend && ! grep -rl 'from "lucide-react"' src/components/voice && npx tsc -b</automated>
  </verify>
  <done>Zero remaining `from "lucide-react"` matches under components/voice/; tsc passes with zero new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Verify tests, record bundle-size note, commit</name>
  <files>frontend/src/components/voice/*.tsx</files>
  <action>
    Run `npx vitest run src/components/voice` and confirm zero regressions. Run `npm run build` and note `du -sh frontend/dist/assets` for the bundle-size comparison. Stage only the files changed in Task 1 and commit with message `feat(41-icon-toast): migrate voice components icons to Fluent adapter (ICON-03 voice batch)` including the bundle-size note in the commit body. Push.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/voice</automated>
  </verify>
  <done>All voice-component unit tests pass; build succeeds; one commit created and pushed with bundle-size note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | Import-path-only changes to decorative icon components; no logic, data, or trust boundary altered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-05 | — | components/voice/*.tsx | accept | Mechanical import-path swap only; existing test suite is the regression guard. |
</threat_model>

<verification>
```bash
cd frontend
grep -rl 'from "lucide-react"' src/components/voice  # must return nothing
npx tsc -b
npx vitest run src/components/voice
```
</verification>

<success_criteria>
- [ ] Zero `lucide-react` imports remain in components/voice/
- [ ] All voice-scoped tests pass, tsc clean, build succeeds
- [ ] One commit pushed with bundle-size note
</success_criteria>

<output>
After completion, create `.planning/phases/41-icon-toast-adapter-layers/41-05-SUMMARY.md`
</output>
