---
phase: 41-icon-toast-adapter-layers
plan: 06
type: execute
wave: 3
depends_on: ["41-02"]
files_modified:
  - "frontend/src/pages/*.tsx"
  - "frontend/src/pages/user/*.tsx"
  - "frontend/src/components/avatar/*.tsx"
  - "frontend/src/components/coach/*.tsx"
  - "frontend/src/components/conference/*.tsx"
  - "frontend/src/components/layouts/*.tsx"
  - "frontend/src/components/scoring/*.tsx"
  - "frontend/src/components/session/**/*.tsx"
autonomous: true
requirements: [ICON-03]

must_haves:
  truths:
    - "Every lucide-react icon import across the 'pages' catch-all batch is replaced with an equivalent import from @/components/icons"
    - "No file in the batch's 7 directories imports from lucide-react after this batch"
    - "The LucideIcon type imports in admin-layout.tsx are swapped to FluentIcon"
    - "All existing unit tests across these 7 directories and type checks pass with zero visual/behavioral regression"
  artifacts:
    - path: "frontend/src/pages/"
      provides: "37-file catch-all icon batch spanning pages/, pages/user/, and 6 component subdirectories not covered by the admin/shared/voice batches"
  key_links:
    - from: "frontend/src/pages/*.tsx and 6 component subdirectories"
      to: "frontend/src/components/icons/index.ts"
      via: "named import"
      pattern: "from \"@/components/icons\""
---

<objective>
Migrate all lucide-react icon imports in the "pages" catch-all batch — 37 files total — to the `@/components/icons` adapter. This is the ICON-03 "pages" batch, the last of the four directory-scoped batches, its own commit per CLAUDE.md's one-batch-one-commit rule.

**Scope clarification (important — do not scope this to `src/pages/` alone):** Research's "pages" batch label is a catch-all bucket covering every remaining lucide-consuming directory NOT already covered by the admin/shared/voice/ui batches. Verified exact scope (37 files, corrected per plan-check W1 — live grep shows 4 files in `components/session/`, not 3; the objective and enumeration below now include the previously-omitted `mode-switch-bar.tsx`):
- `frontend/src/pages/*.tsx` top-level (3 files: avatar-page.tsx, login.tsx, not-found.tsx)
- `frontend/src/pages/user/*.tsx` (8 files)
- `frontend/src/components/avatar/*.tsx` (3 files)
- `frontend/src/components/coach/*.tsx` (10 files)
- `frontend/src/components/conference/*.tsx` (3 files)
- `frontend/src/components/layouts/*.tsx` (2 files)
- `frontend/src/components/scoring/*.tsx` (4 files)
- `frontend/src/components/session/**/*.tsx` (4 files: `mode-switch-bar.tsx`, plus nested `session/left-panel/voice-panel.tsx` and `session/right-panel/chat-transcript.tsx`, plus one more top-level session file — trust live grep, not this count, for the authoritative list)

**Sequencing note (per plan-check B1):** Two of this batch's files (`avatar-page.tsx`, `pages/user/unified-session.tsx`) overlap with `41-11` (the TOAST-02 pages batch), which edits `sonner` import lines in those same two files. This is NOT a fully-disjoint relationship relative to 41-11 — 41-11 now explicitly `depends_on` this plan (41-06) and runs strictly after it. This plan itself has no new dependency as a result (it only edits icon-import lines, unrelated to 41-11's sonner-import lines) and its own wave/depends_on are unchanged. It remains fully parallel-safe relative to the admin (41-03), shared (41-04), and voice (41-05) icon batches.

Purpose: complete the final icon-consumer batch, closing out ICON-03 across the entire codebase (minus the 4 Phase-40-owned composite files).
Output: zero `lucide-react` imports remaining across all 7 directories above; all tests green.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-icon-toast-adapter-layers/41-RESEARCH.md
@.planning/phases/41-icon-toast-adapter-layers/41-02-PLAN.md

<interfaces>
From frontend/src/components/icons/index.ts (created in 41-02):
```typescript
export { CheckmarkRegular as Check } from "@fluentui/react-icons";
// ...84 total named re-exports, same lucide-shaped names the codebase already uses
```
Only change the import source path; named import lists stay identical. `pages/avatar-page.tsx` also has toast calls (loading/dismiss round-trip) — those are migrated separately by TOAST-02 (41-11), NOT here; this plan touches only its lucide-react icon imports.

EXCLUDED (Phase 40 territory): `components/ui/dialog.tsx`, `dropdown-menu.tsx`, `select.tsx`, `sheet.tsx` — do not touch.

**Downstream note:** `41-11` (TOAST-02 pages batch) depends on this plan and will edit `sonner` import lines in `avatar-page.tsx`/`pages/user/unified-session.tsx` after this plan's commit lands — that is expected and does not affect this plan's own scope.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate lucide-react imports to @/components/icons across the pages catch-all batch</name>
  <files>frontend/src/pages/*.tsx, frontend/src/pages/user/*.tsx, frontend/src/components/avatar/*.tsx, frontend/src/components/coach/*.tsx, frontend/src/components/conference/*.tsx, frontend/src/components/layouts/*.tsx, frontend/src/components/scoring/*.tsx, frontend/src/components/session/**/*.tsx</files>
  <action>
    Run `grep -rl 'from "lucide-react"' frontend/src/pages frontend/src/components/avatar frontend/src/components/coach frontend/src/components/conference frontend/src/components/layouts frontend/src/components/scoring frontend/src/components/session` for the authoritative file list (expect ~37 files, excluding `pages/admin/**` which belongs to the admin batch — 41-03). For each file, replace `from "lucide-react"` with `from "@/components/icons"`, keeping the same named-import list, confirming each imported name exists in the adapter (41-02's index.ts) before applying — no blind batch-sed. In `frontend/src/components/layouts/admin-layout.tsx`, change `import type { LucideIcon } from "lucide-react"` to `import type { FluentIcon } from "@fluentui/react-icons"` and update local type usages accordingly. Do not touch `pages/avatar-page.tsx`'s toast/sonner imports.
  </action>
  <verify>
    <automated>cd frontend && ! grep -rl 'from "lucide-react"' src/pages/*.tsx src/pages/user src/components/avatar src/components/coach src/components/conference src/components/layouts src/components/scoring src/components/session 2>/dev/null && npx tsc -b</automated>
  </verify>
  <done>Zero remaining `from "lucide-react"` matches across all 7 directories (excluding pages/admin/, which is 41-03's scope); tsc passes with zero new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Verify tests, record bundle-size note, commit</name>
  <files>frontend/src/pages/*.tsx, frontend/src/pages/user/*.tsx, frontend/src/components/avatar/*.tsx, frontend/src/components/coach/*.tsx, frontend/src/components/conference/*.tsx, frontend/src/components/layouts/*.tsx, frontend/src/components/scoring/*.tsx, frontend/src/components/session/**/*.tsx</files>
  <action>
    Run `npx vitest run src/pages src/components/avatar src/components/coach src/components/conference src/components/layouts src/components/scoring src/components/session` and confirm zero regressions (note: this will also run pages/admin tests since vitest scoping is directory-recursive on `src/pages` — that's fine, those were already migrated in 41-03 and should still pass). Run `npm run build` and note `du -sh frontend/dist/assets` for the bundle-size comparison — this closes out the total ICON-03 bundle-size delta across all 4 batches. Stage only the files changed in Task 1 and commit with message `feat(41-icon-toast): migrate pages+misc components icons to Fluent adapter (ICON-03 pages batch)` including the bundle-size note in the commit body. Push.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages src/components/avatar src/components/coach src/components/conference src/components/layouts src/components/scoring src/components/session && cd .. && git log @{u}.. --oneline | wc -l | grep -qx 0</automated>
  </verify>
  <done>All tests across the 7 directories pass; build succeeds; one commit created and pushed (local HEAD not ahead of upstream), with bundle-size note. ICON-03 is fully complete across all 4 batches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | Import-path-only changes to decorative icon components; no logic, data, or trust boundary altered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-06 | — | pages/*.tsx and 6 component subdirectories | accept | Mechanical import-path swap only; existing test suite is the regression guard. |
</threat_model>

<verification>
```bash
cd frontend
grep -rl 'from "lucide-react"' src/pages/*.tsx src/pages/user src/components/avatar src/components/coach src/components/conference src/components/layouts src/components/scoring src/components/session 2>/dev/null  # must return nothing
npx tsc -b
npx vitest run src/pages src/components/avatar src/components/coach src/components/conference src/components/layouts src/components/scoring src/components/session
```
</verification>

<success_criteria>
- [ ] Zero `lucide-react` imports remain across all 7 directories in this batch (excluding pages/admin/, covered by 41-03)
- [ ] admin-layout.tsx's LucideIcon type swapped to FluentIcon
- [ ] All tests pass, tsc clean, build succeeds
- [ ] One commit pushed (verified via `git log @{u}..`) with bundle-size note
- [ ] ICON-03 (all 4 batches: admin, shared, voice, pages) is complete
</success_criteria>

<output>
After completion, create `.planning/phases/41-icon-toast-adapter-layers/41-06-SUMMARY.md`
</output>
</content>
