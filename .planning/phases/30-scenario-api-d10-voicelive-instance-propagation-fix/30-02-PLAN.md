---
phase: 30
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/hcp.ts
  - frontend/src/types/scenario.ts
autonomous: true
requirements: [D-10 propagation (v1.0 audit integration gap, critical)]

must_haves:
  truths:
    - "Scenario.hcp_profile is typed as exactly what the backend returns (HcpProfileSummary), not the full HcpProfile interface, so tsc catches any consumer reading a field the scenario API never sends"
    - "The stray flat avatar_enabled field is removed from HcpProfile so no code can silently read a field that was never populated"
  artifacts:
    - path: "frontend/src/types/scenario.ts"
      provides: "HcpProfileSummary type matching backend/app/api/scenarios.py::HcpProfileBrief exactly, and Scenario.hcp_profile narrowed to it"
      contains: "HcpProfileSummary"
  key_links:
    - from: "frontend/src/types/scenario.ts::Scenario.hcp_profile"
      to: "frontend/src/types/scenario.ts::HcpProfileSummary"
      via: "type annotation"
      pattern: "hcp_profile\\?:\\s*HcpProfileSummary"
---

<objective>
Define the frontend type contract matching Plan 30-01's backend shape (`HcpProfileBrief`), and remove the stray flat `avatar_enabled?: boolean` field from `HcpProfile` (frontend/src/types/hcp.ts:28) per D-05 — that field lives only on `VoiceLiveInstanceSummary`, never on the HCP profile itself, and its presence is what let the training.tsx/scenario-group-run.tsx gating bugs (fixed in Plan 30-03) go undetected by `tsc -b`.

This plan is a pure type-contract change (no runtime logic) so it can run independently of and in parallel with Plan 30-01 (backend) — the target shape is already fully locked by 30-CONTEXT.md D-01/D-02/D-03, it does not need to wait for the backend implementation to exist.

Purpose: Interface-first ordering — Plans 30-03 (gating logic) and 30-04 (test fixtures) both depend on this type existing so their edits are checked against a concrete contract rather than the old, wrong `HcpProfile` type.
Output: `HcpProfileSummary` type in `frontend/src/types/scenario.ts`, `Scenario.hcp_profile` narrowed to it, stray `avatar_enabled` deleted from `HcpProfile` in `frontend/src/types/hcp.ts`, and a captured `tsc -b` error list (recorded in SUMMARY.md) enumerating every downstream file this narrowing breaks — so Plans 30-03/30-04's executors do not need to rediscover this list themselves.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-scenario-api-d10-voicelive-instance-propagation-fix/30-CONTEXT.md
@frontend/src/types/hcp.ts
@frontend/src/types/scenario.ts
</context>

<interfaces>
<!-- Target backend shape this type must match exactly, from Plan 30-01's HcpProfileBrief -->
```python
class HcpProfileBrief(BaseModel):
    id: str
    name: str
    specialty: str = ""
    avatar_url: str = ""
    personality_type: str = "friendly"
    voice_live_instance_id: str | None = None
    voice_live_instance: VoiceLiveInstanceSummary | None = None
```

<!-- Existing VoiceLiveInstanceSummary TS type already defined, source: frontend/src/types/hcp.ts:35-44 (read fresh before editing, do not assume line numbers) -->
```typescript
export interface VoiceLiveInstanceSummary {
  id: string;
  name: string;
  voice_live_model: string;
  enabled: boolean;
  voice_name: string;
  avatar_character: string;
  avatar_style: string;
  avatar_enabled?: boolean;
}
```
Reuse this existing interface — do not duplicate it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add HcpProfileSummary type, narrow Scenario.hcp_profile, delete stray avatar_enabled</name>
  <files>frontend/src/types/hcp.ts, frontend/src/types/scenario.ts</files>
  <read_first>
    - frontend/src/types/hcp.ts (full file — confirm exact current line for `avatar_enabled?: boolean` on `HcpProfile` and the exact `VoiceLiveInstanceSummary` export to import)
    - frontend/src/types/scenario.ts (full file — confirm exact current `hcp_profile?: HcpProfile` line and existing imports)
  </read_first>
  <action>
    In `frontend/src/types/hcp.ts`: delete the stray `avatar_enabled?: boolean;` line from the `HcpProfile` interface (per D-05 — this field was never populated by any backend endpoint for `HcpProfile`; it only exists correctly on `VoiceLiveInstanceSummary`, which `HcpProfile` already has via its own `voice_live_instance?: VoiceLiveInstanceSummary | null` field).

    In `frontend/src/types/scenario.ts`: import `VoiceLiveInstanceSummary` from `@/types/hcp` (or the correct relative path used by this file's existing imports — check the file's current import style first). Add:
    ```typescript
    export interface HcpProfileSummary {
      id: string;
      name: string;
      specialty: string;
      avatar_url: string;
      personality_type: string;
      voice_live_instance_id: string | null;
      voice_live_instance?: VoiceLiveInstanceSummary | null;
    }
    ```
    Change `Scenario.hcp_profile?: HcpProfile` to `Scenario.hcp_profile?: HcpProfileSummary`. Remove the now-unused `HcpProfile` import from this file if nothing else in it references `HcpProfile` (grep the file for other `HcpProfile` usages first — `HcpProfileSummary` is a distinct name so this is a straightforward check).
  </action>
  <acceptance_criteria>
    - `grep -c "avatar_enabled" frontend/src/types/hcp.ts` returns 0 (stray field fully removed, including its JSDoc/inline comment if any)
    - `grep -c "export interface HcpProfileSummary" frontend/src/types/scenario.ts` returns 1
    - `grep -c "hcp_profile?: HcpProfileSummary" frontend/src/types/scenario.ts` returns 1
    - `grep -c "hcp_profile?: HcpProfile[^S]" frontend/src/types/scenario.ts` returns 0 (old wide type fully replaced, not just shadowed)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "avatar_enabled" src/types/hcp.ts; grep -c "HcpProfileSummary" src/types/scenario.ts</automated>
  </verify>
  <done>HcpProfile no longer has a stray avatar_enabled field; Scenario.hcp_profile is typed as the new narrow HcpProfileSummary matching the backend's HcpProfileBrief exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Run tsc -b to catalog every downstream break, record for Plans 30-03/30-04</name>
  <files>none (discovery only — no files modified)</files>
  <read_first>
    - frontend/tsconfig.json (confirm `npx tsc -b` is the correct project-wide typecheck invocation for this repo, per CLAUDE.md's pre-commit checklist)
  </read_first>
  <action>
    Run `cd frontend && npx tsc -b 2>&1 | tee /tmp/phase30-tsc-errors.txt`. This is expected to FAIL (non-zero exit) — that is correct and expected at this point in the phase, since Plans 30-03/30-04 have not yet run. Read the full output.

    Confirm the error list is limited to exactly the D-07-named consumer set: `frontend/src/pages/user/training.tsx`, `frontend/src/pages/user/scenario-group-run.tsx`, `frontend/src/pages/user/training.test.tsx`, `frontend/src/components/coach/scenario-card.test.tsx`, `frontend/src/components/coach/scenario-panel.test.tsx`, `frontend/src/components/admin/scenario-table.test.tsx` (production display components `scenario-table.tsx`/`scenario-card.tsx`/`scenario-panel.tsx` and `unified-session.tsx`/`unified-session.test.tsx` should NOT appear in the error list — they only read fields present on both the old and new type). If any file outside this expected set appears, record it explicitly in the SUMMARY — that is new information this plan surfaces for later plans, not something to fix here (this plan's `files_modified` is type-only, per its frontmatter).
  </action>
  <acceptance_criteria>
    - `/tmp/phase30-tsc-errors.txt` exists and is non-empty
    - The file explicitly lists which of the 6 expected files appear vs. any unexpected file
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -oE "src/[a-zA-Z0-9_/.-]+\.tsx?" | sort -u</automated>
  </verify>
  <done>Complete, deduplicated list of files broken by the type narrowing is captured in SUMMARY.md, confirming or correcting the predicted D-07 consumer set before Plans 30-03/30-04 begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Pure compile-time type definitions — no runtime code, no trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | N/A | frontend/src/types/*.ts | accept | Type-only change with zero runtime behavior; no attack surface |
</threat_model>

<verification>
`cd frontend && npx tsc -b` fails with a bounded, catalogued set of errors limited to the 6 expected D-07 consumer files (or an explicitly documented deviation) — full green `tsc -b` is achieved only after Plans 30-03 and 30-04 complete.
</verification>

<success_criteria>
`HcpProfileSummary` exists and exactly mirrors the backend's `HcpProfileBrief`; `Scenario.hcp_profile` is narrowed to it; the stray `avatar_enabled` is gone from `HcpProfile`; the resulting `tsc -b` break list is fully enumerated in SUMMARY.md for downstream plans.
</success_criteria>

<output>
After completion, create `.planning/phases/30-scenario-api-d10-voicelive-instance-propagation-fix/30-02-SUMMARY.md` including the full `tsc -b` error output verbatim.
</output>
