---
phase: 30
plan: 05
type: execute
wave: 3
depends_on: [30-01, 30-03, 30-04]
files_modified:
  - frontend/e2e/training-start-session.spec.ts
autonomous: false
requirements: [D-10 propagation (v1.0 audit integration gap, critical)]

must_haves:
  truths:
    - "A scenario bound to an enabled+avatar_enabled VoiceLiveInstance offers voice and digital-human mode options on the training selection page, confirmed against the REAL running backend, not a mock"
    - "avatar_character and avatar_style resolve correctly on the unified session page for a scenario-driven session"
    - "The existing real-connection regression spec (voice-avatar-real.spec.ts) still passes after this phase's schema/type changes"
  artifacts: []
  key_links:
    - from: "frontend/e2e/training-start-session.spec.ts"
      to: "GET /api/v1/scenarios (real backend response)"
      via: "page.waitForResponse interception asserting the nested hcp_profile.voice_live_instance shape"
      pattern: "voice_live_instance"
---

<objective>
Close the final gap discovered during planning (not named in 30-CONTEXT.md's D-07 consumer list, found by grepping the E2E suite directly): `frontend/e2e/training-start-session.spec.ts` has two tests (`"scenario API returns hcp_profile with avatar_character"`, `"avatar_character in scenario matches VL Instance (not stale default)"`) that assert the OLD flat shape (`s.hcp_profile.avatar_character` at top level) as correct — exactly the same buggy-assertion pattern found and fixed in `backend/tests/test_scenario_avatar_fields.py` (Plan 30-01). These currently pass by asserting the bug and will break once Plan 30-01's fix lands, unless rewritten here.

This plan also fulfills D-09's gating-restoration E2E story and re-runs the real-connection regression spec, then executes the full multi-layer verification pass (backend pytest, frontend tsc+vitest, Playwright) required before this phase can be considered done, and closes with the human-verify checkpoint that 30-VALIDATION.md flags as manual-only (real-browser visual confirmation of mode buttons + avatar rendering, since fully automated SDK/WebRTC connection verification is out of scope for a compile/serialization fix).

Purpose: Final, evidence-based confirmation that flow F2 ("voice+avatar session with fallback") and D-10 propagation are actually restored end-to-end against the real stack, not just unit-test green.
Output: Fixed and extended `training-start-session.spec.ts`; a full green verification run; a completed human-verify checkpoint.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-scenario-api-d10-voicelive-instance-propagation-fix/30-CONTEXT.md
@.planning/phases/30-scenario-api-d10-voicelive-instance-propagation-fix/30-VALIDATION.md
@frontend/e2e/training-start-session.spec.ts
</context>

<interfaces>
<!-- Current buggy assertions to fix, source: frontend/e2e/training-start-session.spec.ts:210-261 (READ FRESH, grep exact current line numbers) -->
```typescript
test("scenario API returns hcp_profile with avatar_character", async ({ page }) => {
  ...
  const withAvatar = items.filter(
    (s: { hcp_profile?: { avatar_character?: string } }) => s.hcp_profile?.avatar_character,
  );  // WRONG -- avatar_character is no longer top-level on hcp_profile
  expect(withAvatar.length).toBeGreaterThan(0);
  expect(withAvatar[0].hcp_profile.avatar_character).toBeTruthy();  // WRONG
});

test("avatar_character in scenario matches VL Instance (not stale default)", async ({ page }) => {
  ...
  for (const s of scenarios) {
    if (s.hcp_profile?.avatar_character) {  // WRONG
      expect(validAvatarCharacters).toContain(s.hcp_profile.avatar_character);  // WRONG
      expect(s.hcp_profile.avatar_style).toBeTruthy();  // WRONG
    }
  }
  ...
});
```
Both must read `s.hcp_profile?.voice_live_instance?.avatar_character` / `.avatar_style` instead.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix stale E2E assertions and add gating-restoration coverage</name>
  <files>frontend/e2e/training-start-session.spec.ts</files>
  <read_first>
    - frontend/e2e/training-start-session.spec.ts (full file — grep fresh for both test names, confirm current line numbers, and understand the describe block's surrounding `test.use`/auth setup)
    - frontend/e2e/voice-avatar-real.spec.ts lines 1-50 (the HcpProfile interface pattern already correctly typed with nested voice_live_instance — mirror this typing style for the scenario response's hcp_profile type annotations in this file)
  </read_first>
  <behavior>
    - `"scenario API returns hcp_profile with avatar_character"`: filters scenarios by `s.hcp_profile?.voice_live_instance?.avatar_character` truthy, asserts at least one match, asserts that match's `avatar_character` is truthy via the nested path
    - `"avatar_character in scenario matches VL Instance (not stale default)"`: same nested-path read for the character/style validation loop
    - NEW test `"scenario-driven session offers voice and digital-human modes when HCP has an enabled avatar VL instance"`: navigate to `/user/training`, find (via the intercepted `/api/v1/scenarios` response) a scenario whose `hcp_profile.voice_live_instance` has `enabled: true` and `avatar_enabled: true`, select that scenario in the UI, and assert both a voice-mode control and a digital-human/avatar-mode control are visible/enabled on the page (not disabled/hidden) before starting the session — this is the automatable portion of D-09's gating-restoration story (button visibility), distinct from the manual-only real-SDK-connection verification in Task 3
  </behavior>
  <action>
    Update both existing tests' type annotations and property reads to the nested `hcp_profile?.voice_live_instance?.avatar_character`/`.avatar_style` path per the `<interfaces>` block above.

    Add the new gating-restoration test described in `<behavior>`. Use the same `page.waitForResponse`/`page.goto("/user/training")` pattern already established in this file's other tests. Locate the mode-selection controls using the same selector strategy `seven-modes.spec.ts` or `scenario-selection.spec.ts` uses for mode buttons (check those files' selector conventions first via grep — do not invent a new selector pattern if an established one exists for mode buttons on this page).
  </action>
  <acceptance_criteria>
    - `grep -c "hcp_profile?.avatar_character\|hcp_profile.avatar_character" frontend/e2e/training-start-session.spec.ts` returns 0 (no remaining flat reads)
    - `grep -c "voice_live_instance?.avatar_character\|voice_live_instance\.avatar_character" frontend/e2e/training-start-session.spec.ts` returns >= 2
    - `grep -c "digital-human\|digital_human" frontend/e2e/training-start-session.spec.ts` returns >= 1 (new gating test present)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -n "avatar_character\|avatar_style" e2e/training-start-session.spec.ts</automated>
  </verify>
  <done>Both existing E2E tests read the nested voice_live_instance path; a new automated test proves voice+digital-human mode buttons render for a scenario-driven session with an enabled avatar VL instance.</done>
</task>

<task type="auto">
  <name>Task 2: Full-stack verification pass (backend, frontend, E2E)</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/30-scenario-api-d10-voicelive-instance-propagation-fix/30-VALIDATION.md (the phase's validation contract — full suite command and sampling rate)
  </read_first>
  <action>
    Run, in order, stopping and reporting immediately if any step fails:
    1. `cd backend && ruff check . && ruff format --check .`
    2. `cd backend && .venv/bin/pytest -v` (full backend suite — confirms Plan 30-01's change broke nothing else beyond what Plan 30-01 itself already verified narrowly)
    3. `cd frontend && npx tsc -b` (must be zero errors project-wide — confirms Plans 30-02/30-03/30-04 together fully closed every break Plan 30-02's Task 2 catalogued)
    4. `cd frontend && npx vitest run --reporter=dot` (full unit suite)
    5. `cd frontend && npx playwright test training-start-session.spec.ts --config=e2e/playwright.config.ts` (this plan's fixed/extended spec, against the real dev backend+frontend per this repo's Playwright webServer config)
    6. `cd frontend && npx playwright test voice-avatar-real.spec.ts --config=e2e/playwright.config.ts` (D-09's real-connection regression re-run; if this spec's tests self-skip due to missing live Azure credentials in this environment, record that explicitly as SKIPPED — not a failure — per the spec file's own `test.skip()` guards, but do not silently treat an actual assertion failure as a skip)
  </action>
  <acceptance_criteria>
    - Backend: `ruff check .` and `ruff format --check .` both exit 0
    - Backend: full `pytest -v` exits 0
    - Frontend: `npx tsc -b` exits 0 with zero output
    - Frontend: full `npx vitest run` exits 0
    - E2E: `training-start-session.spec.ts` run exits 0
    - E2E: `voice-avatar-real.spec.ts` run exits 0 or reports only `test.skip()`-guarded skips, zero actual failures
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ruff check . && ruff format --check . && .venv/bin/pytest -q && cd ../frontend && npx tsc -b && npx vitest run --reporter=dot</automated>
  </verify>
  <done>Every layer of the project's test/lint/typecheck stack is green, and both targeted E2E specs pass (or explicitly, non-silently skip only where already gated on live credentials).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual confirmation of restored voice/digital-human gating and avatar propagation</name>
  <files>none</files>
  <action>
Scenario API now returns nested `hcp_profile.voice_live_instance` data end-to-end; training.tsx and scenario-group-run.tsx gating logic reads the correct nested path; a new automated E2E test confirms voice/digital-human mode buttons render for a scenario-driven session. All automated verification (backend tests, frontend typecheck/tests, targeted E2E) is green per Task 2. This checkpoint confirms it visually in a real browser, since full SDK/WebRTC connection behavior is out of scope for a compile/serialization fix.

Steps for human verifier:
1. Start both dev servers: `cd backend && uvicorn app.main:app --reload --port 8000` and `cd frontend && npm run dev`.
2. Log in and confirm (via Admin > HCP Profiles or Admin > Scenarios) that at least one scenario is bound to an HCP whose Voice Live Instance has "Enabled" and "Avatar Enabled" both on. If none exists, configure one via Admin > Voice & Avatar.
3. Navigate to `/user/training`, select that scenario. Confirm BOTH a voice mode option AND a digital-human/avatar mode option are visible and selectable (not just text mode).
4. Start a digital-human mode session for that scenario. Confirm the avatar character/style shown matches the one configured on that HCP's Voice Live Instance (not the old stale default "lori"/"casual").
5. Repeat steps 3-4 via a Scenario Group run (if one exists bound to the same HCP) to confirm scenario-group-run.tsx's independent gating fix also works.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && cd ../backend && ruff check .</automated>
  </verify>
  <done>Human has confirmed: voice and digital-human mode options both appear for a scenario-driven session bound to an enabled+avatar_enabled VL instance, avatar character/style match the VL Instance configuration (not stale defaults), and the same holds for a Scenario Group run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Playwright test runner -> real dev backend | E2E tests hit the actual running backend with seeded admin/user credentials from `.auth` storage state — no new boundary introduced by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07 | N/A | training-start-session.spec.ts | accept | Test-only file, same auth/credential handling already established by the existing spec (`.auth` storage state files, never printed) |
</threat_model>

<verification>
Backend pytest, ruff, frontend tsc, vitest, and the two targeted Playwright specs all exit 0. Human-verify checkpoint approved.
</verification>

<success_criteria>
The phase goal is met end-to-end and evidenced: scenario-driven voice/digital-human training modes are offered again for HCPs bound to enabled Voice Live Instances, avatar character/style resolve correctly, and every automated test layer plus one human visual confirmation attests to it.
</success_criteria>

<output>
After completion, create `.planning/phases/30-scenario-api-d10-voicelive-instance-propagation-fix/30-05-SUMMARY.md`.
</output>
