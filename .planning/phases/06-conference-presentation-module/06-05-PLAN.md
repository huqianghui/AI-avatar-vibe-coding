---
phase: 06-conference-presentation-module
plan: 05
type: execute
wave: 3
depends_on: ["06-03", "06-04"]
files_modified:
  - frontend/src/router.tsx
  - frontend/src/components/shared/user-nav.tsx
  - backend/scripts/seed_data.py
  - backend/app/config.py
  - frontend/src/pages/admin/scenario-editor.tsx
  - frontend/src/components/admin/audience-config.tsx
autonomous: false
requirements: [CONF-01, CONF-02, CONF-03, CONF-04, COACH-04, COACH-05, COACH-06, COACH-07]

must_haves:
  truths:
    - "Conference session route /user/conference registered in router"
    - "User navigation includes Conference link"
    - "Seed data includes a conference-type scenario with 3 audience HCPs"
    - "Admin scenario editor shows audience config panel when mode is conference"
    - "Full conference flow works: create session -> present -> receive questions -> respond -> end -> score"
    - "feature_conference_enabled toggle controls conference availability"
  artifacts:
    - path: "frontend/src/router.tsx"
      provides: "Conference route registration"
      contains: "conference-session"
    - path: "backend/scripts/seed_data.py"
      provides: "Conference seed data"
      contains: "conference"
    - path: "frontend/src/components/admin/audience-config.tsx"
      provides: "Admin audience HCP assignment UI"
      contains: "AudienceConfig"
  key_links:
    - from: "frontend/src/router.tsx"
      to: "frontend/src/pages/user/conference-session.tsx"
      via: "lazy route import"
      pattern: "conference-session"
    - from: "frontend/src/components/shared/user-nav.tsx"
      to: "frontend/src/router.tsx"
      via: "navigation link"
      pattern: "conference"
---

<objective>
Integration wiring: route registration, navigation, seed data, admin audience config, feature toggle, and full-flow verification checkpoint.

Purpose: Connects all conference backend and frontend pieces into a working end-to-end flow.
Output: Complete conference module accessible from navigation with seed data for immediate testing.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-conference-presentation-module/06-CONTEXT.md
@.planning/phases/06-conference-presentation-module/06-03-SUMMARY.md
@.planning/phases/06-conference-presentation-module/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route registration, navigation, seed data, admin audience config</name>
  <files>
    frontend/src/router.tsx,
    frontend/src/components/shared/user-nav.tsx,
    backend/scripts/seed_data.py,
    backend/app/config.py,
    frontend/src/pages/admin/scenario-editor.tsx,
    frontend/src/components/admin/audience-config.tsx
  </files>
  <read_first>
    frontend/src/router.tsx,
    frontend/src/components/shared/user-nav.tsx,
    backend/scripts/seed_data.py,
    backend/app/config.py,
    frontend/src/pages/admin/scenarios.tsx
  </read_first>
  <action>
1. **Router**: Add lazy route for conference session page in `frontend/src/router.tsx`:
   - `const ConferenceSession = lazy(() => import("@/pages/user/conference-session"));`
   - Add route `{ path: "conference", element: <ConferenceSession /> }` under user routes (full-screen, outside UserLayout, same pattern as training-session)
   - Wrap in Suspense with LoadingState fallback

2. **User navigation**: In `frontend/src/components/shared/user-nav.tsx` (or equivalent nav component):
   - Add "Conference" navigation link with Presentation icon (lucide-react `Presentation`) pointing to `/user/conference`
   - Conditionally show only when `feature_conference_enabled` is true (from config context)
   - i18n key: use `nav.conference` or add to nav namespace: "Conference" / "会议演讲"

3. **Seed data**: Extend `backend/scripts/seed_data.py`:
   - Add a conference-type scenario: name="Product Launch Conference", mode="conference", description, key_messages (3-4 topics), difficulty="intermediate"
   - Add 3 ConferenceAudienceHcp records linking the conference scenario to 3 existing HCP profiles with sort_order 0,1,2 and distinct voice_ids (zh-CN-XiaoxiaoNeural, zh-CN-YunxiNeural, zh-CN-XiaoyiNeural)
   - Import ConferenceAudienceHcp model

4. **Config**: Verify `feature_conference_enabled: bool = False` exists in `backend/app/config.py` Settings class (it was added in Phase 1). If missing, add it.

5. **Admin audience config component**: Create `frontend/src/components/admin/audience-config.tsx`:
   - Props: `scenarioId: string, audienceHcps: AudienceHcp[], onUpdate: (hcps: AudienceHcpCreate[]) => void`
   - List of assigned HCPs with name, specialty, role selector (audience/moderator), voice_id selector (only if feature_voice_enabled), remove button
   - "Add HCP to Audience" button opens select dropdown of available HCP profiles (from useHcpProfiles hook)
   - Minimum 2 HCPs validation warning (text from admin i18n)
   - Uses `useTranslation("admin")`

6. **Scenario editor integration**: Modify existing scenario editor page to show AudienceConfig component when scenario mode is "conference". Conditionally render below existing form fields.

7. Add nav i18n keys if not present: `nav.conference` = "Conference" (en-US), "会议演讲" (zh-CN)
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b --noEmit 2>&1 | head -30 && grep -q "conference" src/router.tsx && cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend && python3 -c "from scripts.seed_data import *; print('seed OK')" 2>&1 | head -5 && ruff check scripts/seed_data.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "conference-session\|ConferenceSession" frontend/src/router.tsx`
    - `grep -q "conference" frontend/src/components/shared/user-nav.tsx` (or equivalent nav file)
    - `grep -q "ConferenceAudienceHcp" backend/scripts/seed_data.py`
    - `grep -q 'mode.*conference' backend/scripts/seed_data.py`
    - `grep -q "feature_conference_enabled" backend/app/config.py`
    - `grep -q "AudienceConfig" frontend/src/components/admin/audience-config.tsx`
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Conference route registered, navigation link added, seed data with conference scenario and 3 audience HCPs, admin audience config component integrated into scenario editor</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full conference flow verification</name>
  <what-built>Complete conference presentation module: backend models + service + API with SSE streaming, frontend components + page with audience panel + question queue + transcription + topic guide, route registration, navigation, seed data, admin audience config</what-built>
  <how-to-verify>
    1. Start backend: `cd backend && uvicorn app.main:app --reload --port 8000`
    2. Run seed data: `cd backend && python3 scripts/seed_data.py`
    3. Start frontend: `cd frontend && npm run dev`
    4. Log in as admin, navigate to Scenarios, verify conference scenario exists with 3 audience HCPs
    5. Edit a scenario, switch mode to "conference", verify audience config panel appears with HCP assignment UI
    6. Log in as user, verify "Conference" link appears in navigation (set feature_conference_enabled=true in .env)
    7. Navigate to conference page, select conference scenario, start session
    8. Verify full-screen layout: header with timer + sub-state badge, topic guide (left), conference stage (center), transcription panel (right), audience panel (bottom)
    9. Type a presentation message, verify it appears in transcription panel with "MR" label
    10. Verify HCP questions appear in question queue after presenting
    11. Click "Respond" on a question, verify response flow
    12. Click "End Presentation", verify confirmation dialog, verify session completes
    13. Verify both panels collapse/expand correctly
    14. Check responsive behavior at tablet/mobile widths
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Conference route accessible at /user/conference
- Navigation link visible when feature_conference_enabled is true
- Seed data creates conference scenario with 3 audience HCPs
- Admin can configure audience HCPs for conference scenarios
- Full presentation flow works end-to-end
- TypeScript and Python pass all checks
</verification>

<success_criteria>
Conference presentation module fully integrated and demonstrable. MR can start a conference session, present to virtual HCP audience, receive and respond to questions, view live transcription, and complete with scoring.
</success_criteria>

<output>
After completion, create `.planning/phases/06-conference-presentation-module/06-05-SUMMARY.md`
</output>
