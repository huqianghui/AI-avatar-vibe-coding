---
phase: 06-conference-presentation-module
plan: 06
type: execute
wave: 4
depends_on: ["06-03", "06-04", "06-05"]
files_modified:
  - backend/tests/test_turn_manager.py
  - backend/tests/test_conference_service.py
  - backend/tests/test_conference_api.py
  - backend/tests/test_conference_schemas.py
  - backend/tests/test_stt_tts_adapters.py
  - backend/tests/test_conference_models.py
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-04, COACH-04, COACH-05, COACH-07]

must_haves:
  truths:
    - "pytest --cov=app --cov-fail-under=95 passes for all Phase 6 backend modules"
    - "TurnManager unit tests cover add/get/activate/answer/cleanup/get_active_speaker"
    - "Conference API integration tests cover session CRUD, SSE streaming, sub-state transition, audience management"
    - "Conference schema validation tests cover valid and invalid inputs"
    - "STT/TTS adapter tests verify conditional import handling and is_available logic"
  artifacts:
    - path: "backend/tests/test_turn_manager.py"
      provides: "TurnManager unit tests"
      contains: "test_add_question"
    - path: "backend/tests/test_conference_api.py"
      provides: "Conference API integration tests"
      contains: "test_create_conference_session"
    - path: "backend/tests/test_conference_service.py"
      provides: "Conference service unit tests"
      contains: "test_generate_hcp_questions"
    - path: "backend/tests/test_stt_tts_adapters.py"
      provides: "Azure STT/TTS adapter tests"
      contains: "test_azure_stt_unavailable"
  key_links:
    - from: "backend/tests/test_conference_api.py"
      to: "backend/app/api/conference.py"
      via: "httpx AsyncClient testing endpoints"
      pattern: "client\\.post.*conference"
    - from: "backend/tests/test_turn_manager.py"
      to: "backend/app/services/turn_manager.py"
      via: "direct unit test imports"
      pattern: "from app.services.turn_manager import"
---

<objective>
Comprehensive backend tests for all Phase 6 conference modules to achieve >=95% code coverage.

Purpose: Phase 6 success criterion #5 requires >=95% test coverage for all new code. This plan covers TurnManager unit tests, conference_service tests, API integration tests, STT/TTS adapter tests, schema validation tests, and model tests.
Output: 6 test files achieving >=95% coverage on all Phase 6 backend modules.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-conference-presentation-module/06-CONTEXT.md
@.planning/phases/06-conference-presentation-module/06-03-SUMMARY.md
@.planning/phases/06-conference-presentation-module/06-05-SUMMARY.md

<interfaces>
From backend/tests/conftest.py (test patterns to follow):
```python
@pytest.fixture(autouse=True)
async def setup_db():
    async with test_engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
    yield
    async with test_engine.begin() as conn:
        await conn.run_sync(Base.metadata.drop_all)

@pytest.fixture
async def client():
    app.dependency_overrides[get_db] = override_get_db
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
        yield ac
    app.dependency_overrides.clear()

@pytest.fixture
async def db_session():
    async with TestSessionLocal() as session:
        yield session
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TurnManager unit tests, conference schema tests, and model tests</name>
  <files>
    backend/tests/test_turn_manager.py,
    backend/tests/test_conference_schemas.py,
    backend/tests/test_conference_models.py
  </files>
  <read_first>
    backend/app/services/turn_manager.py,
    backend/app/schemas/conference.py,
    backend/app/models/conference.py,
    backend/app/models/session.py,
    backend/tests/test_models_phase2.py,
    backend/tests/test_prompt_builder.py
  </read_first>
  <action>
1. Create `backend/tests/test_turn_manager.py` with comprehensive TurnManager tests:
   - `test_add_question_sorts_by_relevance`: add 3 questions with different relevance_scores, verify get_queue returns sorted descending
   - `test_get_queue_returns_only_waiting`: add questions, activate one, verify get_queue excludes active
   - `test_activate_question_sets_status`: activate by hcp_profile_id, verify status="active"
   - `test_activate_question_returns_none_for_unknown`: call with non-existent hcp_id
   - `test_mark_answered_sets_status`: mark active question answered, verify status="answered"
   - `test_cleanup_session_removes_queue`: cleanup session, verify get_queue returns empty
   - `test_get_active_speaker_returns_active_question`: activate question, verify get_active_speaker returns it
   - `test_get_active_speaker_returns_none_when_no_active`: verify returns None for session with no active question
   - `test_multiple_sessions_isolated`: verify questions for session A do not appear in session B queue
   - `test_add_question_to_empty_session`: verify first question can be added
   - All tests use `from app.services.turn_manager import TurnManager, QueuedQuestion` (fresh instance per test, not singleton)

2. Create `backend/tests/test_conference_schemas.py`:
   - `test_conference_session_create_valid`: valid ConferenceSessionCreate
   - `test_audience_hcp_create_defaults`: verify default role_in_conference="audience", voice_id="", sort_order=0
   - `test_conference_sub_state_update_valid`: "presenting" and "qa" both pass
   - `test_conference_sub_state_update_invalid`: other values raise ValidationError
   - `test_conference_message_send_defaults`: verify action default, optional target_hcp_id
   - `test_queued_question_response_fields`: verify all fields present
   - `test_audience_hcp_response_from_attributes`: verify ConfigDict(from_attributes=True) works

3. Create `backend/tests/test_conference_models.py`:
   - `test_conference_audience_hcp_create`: create ConferenceAudienceHcp, verify all columns stored
   - `test_coaching_session_conference_type`: create session with session_type="conference", verify sub_state, presentation_topic, audience_config
   - `test_session_message_speaker_fields`: create message with speaker_id and speaker_name, verify
   - `test_coaching_session_defaults_f2f`: verify session_type defaults to "f2f" (backward compatibility)
   - Uses db_session fixture from conftest.py, creates required parent records (User, Scenario, HcpProfile)
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend && python3 -m pytest tests/test_turn_manager.py tests/test_conference_schemas.py tests/test_conference_models.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>TurnManager has 10+ unit tests covering all methods and edge cases. Conference schemas validated for correct and invalid input. Conference models tested for new columns and backward compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Conference service tests, API integration tests, and STT/TTS adapter tests</name>
  <files>
    backend/tests/test_conference_service.py,
    backend/tests/test_conference_api.py,
    backend/tests/test_stt_tts_adapters.py
  </files>
  <read_first>
    backend/app/services/conference_service.py,
    backend/app/api/conference.py,
    backend/app/services/agents/stt/azure.py,
    backend/app/services/agents/tts/azure.py,
    backend/app/services/agents/stt/base.py,
    backend/app/services/agents/tts/base.py,
    backend/tests/test_full_stack.py,
    backend/tests/test_material_api_unit.py,
    backend/tests/conftest.py
  </read_first>
  <action>
1. Create `backend/tests/test_conference_service.py`:
   - Helper fixture: creates User, Scenario (mode="conference"), 3 HcpProfile records, 3 ConferenceAudienceHcp records
   - `test_create_conference_session_success`: verify session created with session_type="conference", sub_state="presenting", audience_config populated
   - `test_create_conference_session_non_conference_scenario`: scenario.mode="f2f", expect 409
   - `test_create_conference_session_insufficient_audience`: only 1 HCP, expect 409
   - `test_transition_sub_state`: verify sub_state changes to "qa"
   - `test_end_conference_session`: verify status="completed", turn_manager cleaned up
   - `test_generate_hcp_questions` (mock LLM adapter): patch ServiceRegistry.get("llm") to return mock, verify questions added to turn_manager
   - `test_handle_respond` (mock LLM adapter): patch LLM, verify MR message saved, HCP response saved with speaker_id
   - Uses db_session fixture, unittest.mock.patch for LLM adapter

2. Create `backend/tests/test_conference_api.py`:
   - Helper: auth fixture (login as test user, get JWT token), seed conference scenario with audience
   - `test_create_conference_session_endpoint`: POST /api/v1/conference/sessions, verify 201
   - `test_get_conference_session_endpoint`: GET /api/v1/conference/sessions/{id}, verify 200
   - `test_update_sub_state_endpoint`: PATCH /api/v1/conference/sessions/{id}/sub-state, verify 200
   - `test_end_conference_session_endpoint`: POST /api/v1/conference/sessions/{id}/end, verify 200
   - `test_get_audience_hcps_endpoint`: GET /api/v1/conference/scenarios/{id}/audience, verify 200
   - `test_set_audience_hcps_endpoint`: PUT /api/v1/conference/scenarios/{id}/audience, verify 200
   - `test_create_session_unauthenticated`: no token, verify 401
   - `test_stream_endpoint_exists`: POST /api/v1/conference/sessions/{id}/stream, verify route exists (may return error without full setup, but route resolves)
   - Uses client fixture from conftest.py

3. Create `backend/tests/test_stt_tts_adapters.py`:
   - `test_azure_stt_adapter_unavailable_without_credentials`: AzureSTTAdapter("",""), is_available() returns False
   - `test_azure_stt_adapter_available_with_credentials`: AzureSTTAdapter("key","region"), is_available() returns True
   - `test_azure_tts_adapter_unavailable_without_credentials`: same pattern
   - `test_azure_tts_adapter_available_with_credentials`: same pattern
   - `test_azure_tts_list_voices`: verify returns list of dicts with voice_id, display_name, gender, language keys
   - `test_azure_stt_transcribe_without_sdk`: mock ImportError for azure.cognitiveservices.speech, verify RuntimeError raised
   - `test_azure_tts_synthesize_without_sdk`: same pattern
   - Uses asyncio.run or pytest-asyncio for async tests

4. Run full coverage check:
   ```bash
   pytest --cov=app.services.turn_manager --cov=app.services.conference_service --cov=app.api.conference --cov=app.schemas.conference --cov=app.models.conference --cov=app.services.agents.stt.azure --cov=app.services.agents.tts.azure --cov-fail-under=95 -v
   ```
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend && python3 -m pytest tests/test_conference_service.py tests/test_conference_api.py tests/test_stt_tts_adapters.py -v --tb=short 2>&1 | tail -30 && python3 -m pytest tests/test_turn_manager.py tests/test_conference_schemas.py tests/test_conference_models.py tests/test_conference_service.py tests/test_conference_api.py tests/test_stt_tts_adapters.py --cov=app.services.turn_manager --cov=app.schemas.conference --cov=app.models.conference --cov-report=term-missing 2>&1 | tail -20</automated>
  </verify>
  <done>Conference service, API, and adapter tests all pass. Combined coverage across all Phase 6 modules is >=95%. pytest --cov-fail-under=95 succeeds.</done>
</task>

</tasks>

<verification>
- All 6 test files pass: `pytest tests/test_turn_manager.py tests/test_conference_schemas.py tests/test_conference_models.py tests/test_conference_service.py tests/test_conference_api.py tests/test_stt_tts_adapters.py -v`
- Coverage >=95% on all Phase 6 modules: `pytest --cov=app --cov-fail-under=95`
- Existing tests still pass (no regressions): `pytest -v`
- Ruff passes on all test files
</verification>

<success_criteria>
Phase 6 success criterion #5 (>=95% test coverage) is satisfied. All conference backend modules have comprehensive unit and integration tests covering happy paths, error cases, edge cases, and backward compatibility.
</success_criteria>

<output>
After completion, create `.planning/phases/06-conference-presentation-module/06-06-SUMMARY.md`
</output>
