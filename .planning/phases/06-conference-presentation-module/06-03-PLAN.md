---
phase: 06-conference-presentation-module
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - backend/app/services/conference_service.py
  - backend/app/services/prompt_builder.py
  - backend/app/api/conference.py
  - backend/app/services/agents/stt/azure.py
  - backend/app/services/agents/tts/azure.py
  - backend/app/main.py
autonomous: true
requirements: [CONF-01, CONF-02, CONF-04, COACH-04, COACH-05, COACH-07]

must_haves:
  truths:
    - "Conference session can be created with multi-HCP audience loaded from ConferenceAudienceHcp"
    - "MR can send presentation text and receive HCP questions via SSE with speaker attribution"
    - "MR can respond to a specific HCP question via click-to-respond pattern"
    - "Conference sessions are scored using existing scoring_service with conference-adapted prompts"
    - "Azure STT and TTS adapters exist and register with ServiceRegistry"
    - "SSE heartbeat events sent every 15 seconds during active conference sessions"
  artifacts:
    - path: "backend/app/services/conference_service.py"
      provides: "Conference session orchestration and multi-HCP turn management"
      contains: "create_conference_session"
    - path: "backend/app/api/conference.py"
      provides: "Conference REST + SSE endpoints"
      contains: "router = APIRouter"
    - path: "backend/app/services/agents/stt/azure.py"
      provides: "Azure Speech STT adapter"
      contains: "class AzureSTTAdapter"
    - path: "backend/app/services/agents/tts/azure.py"
      provides: "Azure Speech TTS adapter"
      contains: "class AzureTTSAdapter"
  key_links:
    - from: "backend/app/api/conference.py"
      to: "backend/app/services/conference_service.py"
      via: "router delegates to service"
      pattern: "conference_service\\."
    - from: "backend/app/services/conference_service.py"
      to: "backend/app/services/turn_manager.py"
      via: "uses turn_manager singleton"
      pattern: "from app.services.turn_manager import turn_manager"
    - from: "backend/app/main.py"
      to: "backend/app/api/conference.py"
      via: "router registration"
      pattern: "include_router.*conference"
---

<objective>
Backend services and API for conference module: conference_service.py for session orchestration, conference API router with SSE streaming, conference prompt engineering, and Azure STT/TTS adapters.

Purpose: Implements the full backend conference flow -- session creation, multi-HCP question generation, turn management, SSE streaming, scoring, and voice adapter integration.
Output: Working conference API endpoints, conference service, voice adapters registered with ServiceRegistry.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-conference-presentation-module/06-CONTEXT.md
@.planning/phases/06-conference-presentation-module/06-RESEARCH.md
@.planning/phases/06-conference-presentation-module/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conference service, prompt builder extension, and API router with SSE</name>
  <files>
    backend/app/services/conference_service.py,
    backend/app/services/prompt_builder.py,
    backend/app/api/conference.py,
    backend/app/main.py
  </files>
  <read_first>
    backend/app/services/session_service.py,
    backend/app/services/scoring_service.py,
    backend/app/services/prompt_builder.py,
    backend/app/services/turn_manager.py,
    backend/app/api/sessions.py,
    backend/app/models/conference.py,
    backend/app/models/session.py,
    backend/app/schemas/conference.py,
    backend/app/config.py,
    backend/app/main.py,
    backend/app/dependencies.py
  </read_first>
  <action>
1. Create `backend/app/services/conference_service.py`:
   - `async def create_conference_session(db: AsyncSession, scenario_id: str, user_id: str) -> CoachingSession`:
     - Verify scenario.mode == "conference" (raise 409 NOT_CONFERENCE_SCENARIO if not)
     - Load ConferenceAudienceHcp records with selectinload(hcp_profile) for the scenario
     - Require >= 2 audience HCPs (raise 409 INSUFFICIENT_AUDIENCE if not)
     - Build audience_config JSON from HCP profiles (hcp_profile_id, name, specialty, personality_type, role, voice_id)
     - Build key_messages_status from scenario.key_messages
     - Create CoachingSession with session_type="conference", sub_state="presenting"
     - Return session after db.flush()
   - `async def generate_hcp_questions(db: AsyncSession, session: CoachingSession, mr_text: str) -> list`:
     - Parse audience_config JSON
     - For each HCP (sequential, NOT parallel -- per RESEARCH.md Pitfall 4):
       - Build conference audience prompt via prompt_builder.build_conference_audience_prompt()
       - Call LLM adapter via ServiceRegistry.get("llm") to generate a question
       - Assign relevance_score based on keyword matching (mock) or LLM output
       - Add to turn_manager queue if question generated
     - Return list of newly queued questions
   - `async def handle_respond(db: AsyncSession, session: CoachingSession, hcp_id: str, mr_response: str) -> AsyncIterator[dict]`:
     - Activate question in turn_manager
     - Save MR response as SessionMessage with speaker_id=None, role="user"
     - Build follow-up prompt for target HCP
     - Stream HCP follow-up via LLM adapter as SSE events with speaker attribution
     - Save HCP response as SessionMessage with speaker_id=hcp_id, speaker_name=hcp_name
     - Mark question answered in turn_manager
   - `async def transition_sub_state(db: AsyncSession, session_id: str, new_state: str) -> None`:
     - Update session.sub_state, flush
   - `async def end_conference_session(db: AsyncSession, session_id: str) -> CoachingSession`:
     - Set status="completed", cleanup turn_manager
     - Trigger scoring via existing scoring_service.score_session()

2. Extend `backend/app/services/prompt_builder.py` with:
   - `def build_conference_audience_prompt(hcp_profile, scenario, presentation_topic, conversation_history, other_hcp_questions) -> str`:
     - System prompt instructs HCP to act as conference audience member
     - Includes HCP personality, specialty, and knowledge from profile
     - Includes conversation history and other HCPs' prior questions to avoid duplication
     - Asks for a relevant question about the MR's presentation (or empty string if no question)
   - `def build_conference_scoring_prompt(scenario, messages, audience_config) -> str`:
     - Adapts existing scoring prompt for conference: "key_message" evaluates presentation completeness, "objection_handling" evaluates Q&A handling, "communication" evaluates presentation delivery

3. Create `backend/app/api/conference.py` router:
   - `router = APIRouter(prefix="/conference", tags=["conference"])`
   - `POST /sessions` -- create conference session (requires auth)
   - `GET /sessions/{session_id}` -- get conference session details
   - `POST /sessions/{session_id}/stream` -- SSE endpoint for conference interaction
     - Accepts ConferenceMessageSend body (action: "present" or "respond", message, target_hcp_id)
     - Returns EventSourceResponse with conference events
     - Includes heartbeat every 15 seconds via asyncio background task
     - Events: text, speaker_text, queue_update, turn_change, sub_state, transcription, hint, key_messages, heartbeat, done, error
   - `PATCH /sessions/{session_id}/sub-state` -- transition presenting/qa
   - `POST /sessions/{session_id}/end` -- end session and trigger scoring
   - `GET /scenarios/{scenario_id}/audience` -- get audience HCPs for scenario
   - `PUT /scenarios/{scenario_id}/audience` -- set audience HCPs (admin only, accepts list of AudienceHcpCreate)
   - Static routes before parameterized routes per CLAUDE.md Gotcha #3

4. Register conference router in `backend/app/main.py`:
   - `from app.api.conference import router as conference_router`
   - `app.include_router(conference_router, prefix=settings.api_prefix)`
   - Place after existing router registrations
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend && python3 -c "from app.api.conference import router; print(len(router.routes), 'routes')" && python3 -c "from app.services.conference_service import create_conference_session; print('service OK')" && ruff check app/services/conference_service.py app/api/conference.py app/services/prompt_builder.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "async def create_conference_session" backend/app/services/conference_service.py`
    - `grep -q "async def generate_hcp_questions" backend/app/services/conference_service.py`
    - `grep -q "async def handle_respond" backend/app/services/conference_service.py`
    - `grep -q "build_conference_audience_prompt" backend/app/services/prompt_builder.py`
    - `grep -q "build_conference_scoring_prompt" backend/app/services/prompt_builder.py`
    - `grep -q 'router = APIRouter' backend/app/api/conference.py`
    - `grep -q "heartbeat" backend/app/api/conference.py`
    - `grep -q "conference_router" backend/app/main.py`
  </acceptance_criteria>
  <done>Conference service orchestrates multi-HCP sessions, API router exposes all conference endpoints with SSE streaming and heartbeat, prompt builder extended for conference audience and scoring contexts</done>
</task>

<task type="auto">
  <name>Task 2: Azure STT/TTS adapters with ServiceRegistry registration</name>
  <files>
    backend/app/services/agents/stt/azure.py,
    backend/app/services/agents/tts/azure.py,
    backend/app/main.py
  </files>
  <read_first>
    backend/app/services/agents/stt/base.py,
    backend/app/services/agents/stt/mock.py,
    backend/app/services/agents/tts/base.py,
    backend/app/services/agents/tts/mock.py,
    backend/app/services/agents/avatar/base.py,
    backend/app/services/agents/registry.py,
    backend/app/config.py,
    backend/app/main.py
  </read_first>
  <action>
1. Create `backend/app/services/agents/stt/azure.py`:
   - `class AzureSTTAdapter(BaseSTTAdapter)`:
     - `name = "azure"`
     - `__init__(self, key: str, region: str)` stores credentials
     - `async def transcribe(self, audio_data: bytes, language: str = "zh-CN") -> str`:
       - Import `azure.cognitiveservices.speech as speechsdk` inside method (conditional import -- SDK may not be installed)
       - Wrap in try/except ImportError to raise RuntimeError("azure-cognitiveservices-speech not installed")
       - Use PushAudioInputStream, SpeechRecognizer
       - Use `await asyncio.to_thread(recognizer.recognize_once)` to avoid blocking event loop (per RESEARCH.md Pitfall 2)
       - Return recognized text or empty string for NoMatch
     - `async def is_available(self) -> bool`: return bool(self._key and self._region)

2. Create `backend/app/services/agents/tts/azure.py`:
   - `class AzureTTSAdapter(BaseTTSAdapter)`:
     - `name = "azure"`
     - `__init__(self, key: str, region: str)` stores credentials
     - `async def synthesize(self, text: str, voice_id: str = "zh-CN-XiaoxiaoNeural", language: str = "zh-CN") -> bytes`:
       - Conditional import of speechsdk
       - Use SpeechSynthesizer with audio output to stream (not speaker)
       - Use SSML for voice selection: `<speak><voice name="{voice_id}">{text}</voice></speak>`
       - Wrap blocking call with `asyncio.to_thread()`
       - Return audio bytes
     - `async def list_voices(self, language: str = "zh-CN") -> list[dict]`:
       - Return curated list of 6 zh-CN voices: XiaoxiaoNeural, YunxiNeural, XiaoyiNeural, YunjianNeural, XiaochenNeural, YunzeNeural
       - Each entry: `{"voice_id": "...", "display_name": "...", "gender": "...", "language": "zh-CN"}`
     - `async def is_available(self) -> bool`: return bool(self._key and self._region)

3. Register Azure adapters in `backend/app/main.py` lifespan:
   - After existing mock adapter registrations
   - Only register if `settings.azure_speech_key` and `settings.azure_speech_region` are configured (non-empty)
   - Register with: `registry.register("stt", AzureSTTAdapter(key, region))`
   - Register with: `registry.register("tts", AzureTTSAdapter(key, region))`
   - Add `azure_speech_key: str = ""` and `azure_speech_region: str = ""` to Settings in config.py if not already present

4. For COACH-07 (Avatar): The existing `BaseAvatarAdapter` and `MockAvatarAdapter` from Phase 1 already handle the avatar interface. The avatar toggle in the conference UI will use the existing `feature_avatar_enabled` toggle. A real Azure Avatar adapter is a thin wrapper -- add a stub `backend/app/services/agents/avatar/azure.py` with `AzureAvatarAdapter` that has `name = "azure"`, `is_available()` returning False, and a TODO comment for real implementation. This satisfies the "configurable premium option" requirement.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend && python3 -c "from app.services.agents.stt.azure import AzureSTTAdapter; print('STT OK')" && python3 -c "from app.services.agents.tts.azure import AzureTTSAdapter; a = AzureTTSAdapter('',''); import asyncio; print(asyncio.run(a.is_available()))" && ruff check app/services/agents/stt/azure.py app/services/agents/tts/azure.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class AzureSTTAdapter" backend/app/services/agents/stt/azure.py`
    - `grep -q "asyncio.to_thread" backend/app/services/agents/stt/azure.py`
    - `grep -q "class AzureTTSAdapter" backend/app/services/agents/tts/azure.py`
    - `grep -q "asyncio.to_thread" backend/app/services/agents/tts/azure.py`
    - `grep -q "XiaoxiaoNeural" backend/app/services/agents/tts/azure.py`
    - `grep -q "azure_speech_key" backend/app/config.py`
    - `grep -q "AzureSTTAdapter\|AzureTTSAdapter" backend/app/main.py`
  </acceptance_criteria>
  <done>Azure STT and TTS adapters implemented with conditional SDK import, asyncio.to_thread wrapping, and ServiceRegistry registration. Avatar stub created for COACH-07. Voice adapters work for both F2F and conference modes.</done>
</task>

</tasks>

<verification>
- Conference API router has 6+ routes registered
- SSE streaming endpoint returns EventSourceResponse
- Heartbeat mechanism present in SSE generator
- Azure adapters use conditional imports and asyncio.to_thread
- Conference router registered in main.py
- Ruff passes on all new files
- Existing tests still pass (no breaking changes to F2F flow)
</verification>

<success_criteria>
Full conference backend operational: session creation with multi-HCP audience, SSE streaming with speaker attribution, turn management with question queue, conference scoring, and pluggable voice adapters.
</success_criteria>

<output>
After completion, create `.planning/phases/06-conference-presentation-module/06-03-SUMMARY.md`
</output>
