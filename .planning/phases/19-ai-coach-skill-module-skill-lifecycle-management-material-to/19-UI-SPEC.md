---
phase: 19
slug: ai-coach-skill-module
status: draft
shadcn_initialized: true
preset: custom (Figma Make Design System for SaaS + medical brand)
created: 2026-04-11
---

# Phase 19 — UI Design Contract

> Visual and interaction contract for the AI Avatar Skill Module frontend. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | Custom Radix UI wrapper (shadcn-style) |
| Preset | Figma Make Design System for SaaS (medical brand override) |
| Component library | Radix UI primitives (Dialog, Select, Tabs, ScrollArea, etc.) |
| Icon library | lucide-react ^0.460.0 |
| Font | Inter + Noto Sans SC (sans), JetBrains Mono (mono) |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, badge internal |
| sm | 8px | Compact element spacing, card internal gaps |
| md | 16px | Default element spacing, form field gaps |
| lg | 24px | Section padding, card body padding |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Page-level top/bottom spacing |
| 3xl | 64px | Full-page empty state centering |

Exceptions: File tree indentation uses 20px per level (5 x 4px) for clear hierarchy readability.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (text-sm) | 400 | 1.5 |
| Label | 14px (text-sm) | 600 | 1.5 |
| Heading | 20px (text-xl) | 600 | 1.5 |
| Display | 24px (text-2xl) | 600 | 1.5 |

Notes:
- Two font weights only: 400 (regular) for body text, 600 (semibold) for labels, headings, and display titles
- Page titles use Display (24px/600) per existing admin page pattern
- Section headings within editors use Heading (20px/600)
- All card content and table cells use Body (14px/400); form labels and interactive labels use Label (14px/600)
- Code/file names in file tree use `font-mono` at 13px for differentiation

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | var(--background) #FFFFFF | Page background, surfaces, empty areas |
| Secondary (30%) | var(--card) #FFFFFF + var(--muted) #F9FAFB | Cards, sidebar panels, file tree background, muted sections |
| Accent (10%) | var(--primary) #1E40AF | Primary CTAs, active tab indicator, selected file tree item, Skill status "published" badge |
| Destructive | var(--destructive) #EF4444 | Delete Skill, archive action confirmation, "failed" status badge |

Accent reserved for:
- "Create Skill" primary button
- "Publish Skill" button
- Published status badge (bg-primary/10 text-primary)
- Active file tree node highlight (bg-primary/5 border-l-2 border-primary)
- Active tab underline in Skill editor
- Radar chart primary fill for L2 quality scores

Additional semantic colors for this phase:
| Color | Token | Usage |
|-------|-------|-------|
| Success green | var(--strength) #22C55E | L1 PASS badge, published status dot |
| Warning orange | var(--weakness) #F97316 | L2 score 50-69 warning state, "review" status badge |
| Purple | var(--improvement) #A855F7 | "processing" conversion status indicator |
| Muted gray | var(--muted-foreground) #6B7280 | "draft" status badge, disabled states, file metadata |

---

## Component Inventory (Phase 19 Specific)

### New Pages

| Page | Route | Layout | Pattern Reference |
|------|-------|--------|-------------------|
| Skill Hub | `/admin/skills` | Admin layout (sidebar + main) | scenarios.tsx card grid + table hybrid |
| Skill Editor | `/admin/skills/:id/edit` | Admin layout, full-width content | hcp-profile-editor.tsx tabbed editor |
| Skill Create (via Material Upload) | `/admin/skills/new` | Admin layout, wizard flow | training-materials.tsx upload + dialog |

### New Shared/Domain Components

| Component | Purpose | Base UI |
|-----------|---------|---------|
| SkillCard | Grid card for Skill Hub — name, product, status, quality score, tags | Card + Badge |
| SkillStatusBadge | Five-state lifecycle badge (draft/review/published/archived/failed) | Badge variant + dot |
| ConversionProgress | Async conversion status indicator (pending/processing/completed/failed) | Progress + Badge |
| FileTreeView | VSCode-style collapsible file tree for Skill package structure | Custom (div + lucide icons) |
| FileTreeNode | Individual tree node (folder/file) with icon, name, click handler | Button variant |
| QualityRadarChart | L2 six-dimension radar chart for Skill quality scores | recharts RadarChart (existing) |
| QualityScoreCard | Per-dimension score with label, score bar, verdict | Card + DimensionBar |
| SopEditor | Dual-mode SOP editor (manual Markdown edit + AI feedback input) | Textarea + Dialog |
| SkillMaterialUploader | Drag-and-drop zone for reference materials | react-dropzone (existing pattern) |
| PublishGateDialog | Modal showing L1/L2 gate results before publish decision | Dialog + quality summary |

### Reused Existing Components

| Component | From | Usage in Phase 19 |
|-----------|------|-------------------|
| EmptyState | shared/empty-state.tsx | Skill Hub with no skills, file tree with no resources |
| Badge | ui/badge.tsx | Status badges, file type badges, tag chips |
| Dialog | ui/dialog.tsx | Delete confirmation, publish gate, AI feedback, ZIP import |
| Tabs | ui/tabs.tsx | Skill editor sections (Content / Resources / Quality / Settings) |
| ScrollArea | ui/scroll-area.tsx | File tree scroll, SOP content preview scroll |
| Select | ui/select.tsx | Skill status filter, product filter, Scenario skill picker |
| Progress | ui/progress.tsx | Conversion progress bar, L2 score bars |
| Button | ui/button.tsx | All CTAs with existing variants (default, outline, destructive, ghost) |
| Input | ui/input.tsx | Search, form fields |
| Textarea | ui/textarea.tsx | SOP Markdown editing, AI feedback input |
| Separator | ui/separator.tsx | Section dividers in editor |
| Skeleton | ui/skeleton.tsx | Loading states for cards, file tree |
| Switch | ui/switch.tsx | Toggle settings (e.g., auto-publish on L1+L2 pass) |

---

## Layout Specifications

### Skill Hub Page (`/admin/skills`)

**Focal point:** The card grid is the primary focal point. On page load the user's eye should land on the top-left SkillCard first, then scan right and down across the grid. The "+ Create Skill" primary button in the page header serves as the secondary focal point, drawing attention to the main action when no cards exist or the user is ready to add a new skill.

```
+--------------------------------------------------+
| [Page Title: Skill Hub]         [+ Create Skill] |
+--------------------------------------------------+
| [Search____]  [Status: All v]  [Product: All v]  |
+--------------------------------------------------+
| [SkillCard] [SkillCard] [SkillCard]              |
| [SkillCard] [SkillCard] [SkillCard]              |
| ...grid-cols-1 md:grid-cols-2 lg:grid-cols-3     |
+--------------------------------------------------+
```

- Page header: Display title (24px/600) + primary "Create Skill" button aligned right
- Filter row: gap-md (16px), search input (flex-1), status Select, product Select
- Card grid: gap-lg (24px), responsive 1/2/3 columns
- Each SkillCard: h-auto, min-h-[180px], padding-lg (24px)

### Skill Editor Page (`/admin/skills/:id/edit`)

```
+-----------------------------------------------------------+
| [< Back] Skill: {name}    [Save Draft] [Publish Skill]    |
+-----------------------------------------------------------+
| [Content] [Resources] [Quality] [Settings]  <- Tabs       |
+-----------------------------------------------------------+
| Tab: Content                                               |
| +----------------------------+---------------------------+ |
| | SOP Editor (left 60%)     | Preview (right 40%)       | |
| | [Markdown textarea]        | [Rendered Markdown]       | |
| | [AI Feedback input]        |                           | |
| | [Regenerate button]        |                           | |
| +----------------------------+---------------------------+ |
+-----------------------------------------------------------+
| Tab: Resources                                             |
| +----------------------------+---------------------------+ |
| | File Tree (left 30%)      | File Preview (right 70%)  | |
| | SKILL.md                   | [Markdown / Info / Code]  | |
| | references/                |                           | |
| |   training-doc.pdf         |                           | |
| |   slides.pptx              |                           | |
| | scripts/                   |                           | |
| | assets/                    |                           | |
| +----------------------------+---------------------------+ |
+-----------------------------------------------------------+
```

- Editor uses full admin content width (no extra sidebar)
- Content tab: 2-column grid (3fr/2fr split), min-height 500px
- Resources tab: 2-column grid (3fr/7fr split), file tree fixed width ~280px
- Quality tab: L1 result banner + L2 radar chart + dimension cards (2-col grid)
- Settings tab: Form fields for metadata (product, tags, compatibility, etc.)

### Skill Card (within grid)

```
+----------------------------------------+
| [StatusBadge: Draft]     [Quality: 72] |
| Skill Name (font-semibold, text-base)  |
| Product: Zanubrutinib  (text-sm muted) |
| [Tag] [Tag] [Tag]                      |
|              [Edit Skill] [More v]     |
+----------------------------------------+
```

- Card uses `rounded-lg border bg-card` (existing card pattern)
- Status badge top-left, quality score top-right
- Name truncated with `line-clamp-1`
- Tags as small outline badges, max 3 visible + "+N"
- Action buttons bottom-right: "Edit Skill" ghost button, "More" dropdown (archive, export, delete)

---

## Interaction Contracts

### Skill Creation Flow

1. Admin clicks "+ Create Skill" on Skill Hub
2. Dialog appears: "Create from materials" OR "Import ZIP package"
3a. **From materials:** Navigate to `/admin/skills/new` with upload zone active
   - Admin drags files (PDF, DOCX, PPTX, TXT, MD)
   - Upload completes -> conversion starts (status: "processing")
   - Poll every 3 seconds until completed/failed
   - On complete: navigate to editor with Content tab active, SOP pre-filled
3b. **Import ZIP:** File picker opens, admin selects .zip file
   - Client-side extraction with jszip, preview structure in dialog
   - Confirm import -> POST to backend -> navigate to editor

### SOP Editing (Dual Mode)

- **Manual edit:** Direct Markdown textarea editing, live preview in right panel
- **AI feedback:** Textarea below SOP editor, "Apply Changes" button
  - Admin types modification instructions (e.g., "Add more objection handling for pricing concerns")
  - Button triggers async AI regeneration
  - On complete: SOP content updates in editor, diff highlighted temporarily (1.5s bg-primary/5 fade)

### Quality Gate Flow

1. Admin clicks "Request Quality Review" button (visible when status=draft/review)
2. L1 structure check runs immediately (< 1s, rule-based)
   - Result shown as banner: green "L1 PASS" or red "L1 FAIL: {reason}"
3. L2 AI assessment runs async (polling, ~10-30s)
   - Progress indicator during assessment
   - On complete: radar chart renders, dimension cards populate
4. When admin clicks "Publish Skill":
   - If L1 FAIL: blocked, toast error "Please fix structure issues before publishing"
   - If L2 < 50: blocked, dialog shows low scores + "Cannot publish -- quality score too low"
   - If L2 50-69: warning dialog with dimension breakdown + "Publish Anyway" / "Return to Edit"
   - If L2 >= 70: direct publish, toast success

### Lifecycle Transitions

| Current Status | Available Actions | Target Status |
|----------------|-------------------|---------------|
| draft | Edit Skill, Request Review, Delete | review / (stays draft) |
| review | Edit Skill (returns to draft), Publish Skill, Delete | draft / published |
| published | Edit Skill (creates new version as draft), Archive | draft (new version) / archived |
| archived | Restore | draft |
| failed | Retry Conversion, Delete | processing -> draft |

### File Tree Interaction

- Click folder: expand/collapse (chevron rotates)
- Click file: loads content in right preview panel
- SKILL.md: renders Markdown
- references/*: shows file info card (name, size, type) + download button
- scripts/*: renders code with font-mono, syntax highlighting via `<pre>` styling
- assets/*: shows file info + download button
- Upload button at tree top: opens file picker, adds to selected resource_type folder

### Scenario Skill Association

- In Scenario editor (existing page), add a "Skill" Select dropdown
- Shows only `published` status skills
- Selecting a skill shows skill name + quality score badge inline
- If associated skill is archived: show warning icon + tooltip "This skill is archived"

---

## Copywriting Contract

| Element | Copy (en-US) | Copy (zh-CN) |
|---------|------|------|
| Primary CTA | Create Skill | 创建技能 |
| Publish CTA | Publish Skill | 发布技能 |
| Edit CTA | Edit Skill | 编辑技能 |
| Empty state heading | No skills yet | 暂无技能 |
| Empty state body | Upload training materials to create your first AI coaching skill, or import a skill package. | 上传培训材料以创建您的第一个 AI 教练技能，或导入技能包。 |
| Error state (conversion failed) | Conversion failed. Check your materials and try again. | 转换失败，请检查材料后重试。 |
| Error state (L2 assessment failed) | Quality assessment could not complete. Please try again later. | 质量评估未能完成，请稍后重试。 |
| Destructive: Delete Skill | Delete "{name}"? This will permanently remove the skill and all associated resources. This action cannot be undone. | 删除"{name}"？这将永久移除该技能及所有关联资源。此操作不可撤销。 |
| Destructive: Archive Skill | Archive "{name}"? The skill will no longer be available for new training sessions. Existing associations will show a warning. | 归档"{name}"？该技能将不再可用于新的训练会话。现有关联将显示警告。 |
| Publish gate (50-69 warning) | Quality score is {score}/100. Some dimensions need improvement. Publish anyway? | 质量评分为 {score}/100。部分维度需要改进。仍然发布？ |
| Publish gate (< 50 block) | Quality score is {score}/100. The skill cannot be published until the score is at least 50. | 质量评分为 {score}/100。技能质量评分需达到 50 分方可发布。 |
| L1 PASS | Structure check passed | 结构检查通过 |
| L1 FAIL | Structure check failed: {reason} | 结构检查未通过：{reason} |
| Conversion processing | Converting materials to skill... | 正在将材料转换为技能... |
| Conversion complete | Skill generated successfully. Please review the content. | 技能已成功生成，请审阅内容。 |
| Retry button | Retry Conversion | 重试转换 |
| AI feedback placeholder | Describe changes you want to make to the SOP... | 描述您希望对 SOP 进行的修改... |
| AI feedback CTA | Apply Changes | 应用修改 |

---

## State Specifications

### Loading States

| Context | Implementation |
|---------|---------------|
| Skill Hub cards loading | 6 Skeleton cards in grid (matching card dimensions) |
| Skill editor loading | Skeleton for tabs + content area |
| File tree loading | 4 Skeleton lines with indentation |
| Conversion in progress | Progress component (indeterminate) + "Converting..." text |
| L2 assessment running | Skeleton radar chart + "Evaluating quality..." text |

### Error States

| Context | Implementation |
|---------|---------------|
| Skill Hub fetch failed | EmptyState with error icon + "Failed to load skills" + Retry button |
| Conversion failed | Banner with destructive background + error message + "Retry" button |
| L2 assessment failed | Alert banner in Quality tab + "Try Again" button |
| File upload failed | Toast error via sonner + file stays in upload zone |
| ZIP import invalid | Dialog error message: "Invalid skill package format" |

### Empty States

| Context | Implementation |
|---------|---------------|
| Skill Hub (no skills) | EmptyState: BookOpen icon, "No skills yet", CTA "Create Skill" |
| Resources tab (no files) | EmptyState: FolderOpen icon, "No resources", CTA "Upload Files" |
| Quality tab (not evaluated) | Muted card: "Quality has not been evaluated. Request a review to see results." |
| File preview (no selection) | Centered muted text: "Select a file from the tree to preview" |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official (Radix wrappers) | Dialog, Select, Tabs, ScrollArea, Progress, Badge, Button, Input, Textarea, Separator, Skeleton, Switch, Card, DropdownMenu | not required -- all already installed |
| No third-party registries | n/a | n/a |

---

## Accessibility Requirements

| Element | Requirement |
|---------|-------------|
| File tree | aria-expanded on folders, aria-selected on active file, keyboard nav (Arrow Up/Down/Left/Right) |
| Status badges | aria-label with full status text (not just color) |
| Conversion progress | aria-live="polite" region for status updates |
| Quality radar chart | aria-label describing overall score, detailed scores in adjacent text |
| Delete/Archive dialogs | Focus trap, ESC to dismiss, confirmation requires explicit click |
| Drag-and-drop upload | Keyboard accessible via button fallback "Browse files" |
| SOP editor | Proper label association, placeholder text for AI feedback |

---

## Responsive Behavior

| Breakpoint | Adaptation |
|------------|------------|
| Desktop (>= 1024px) | Full 3-col card grid, 2-panel editor layouts |
| Tablet (768-1023px) | 2-col card grid, editor panels stack vertically |
| Mobile (< 768px) | 1-col card grid, file tree collapses to drawer, editor full-width single panel |

---

## Animation & Transitions

| Element | Animation | Duration |
|---------|-----------|----------|
| Page mount | page-fade-in (existing) | 150ms |
| Card hover | scale(1.01) + shadow elevation | 150ms ease |
| File tree expand/collapse | height transition | 200ms ease |
| Tab switch | opacity crossfade | 100ms |
| Conversion status update | badge pulse animation | 300ms |
| SOP content update (AI) | bg-primary/5 highlight fade | 1500ms ease-out |
| Toast notifications | sonner default slide-in | sonner default |

---

## i18n Namespace

New namespace: `skill.json` (both en-US and zh-CN)

Key groups:
- `skill.hub.*` -- Skill Hub page labels
- `skill.editor.*` -- Skill Editor labels
- `skill.status.*` -- Status labels (draft, review, published, archived, failed)
- `skill.conversion.*` -- Conversion flow messages
- `skill.quality.*` -- Quality gate labels and dimension names
- `skill.actions.*` -- Button and action labels
- `skill.fileTree.*` -- File tree labels
- `skill.errors.*` -- Error messages

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
