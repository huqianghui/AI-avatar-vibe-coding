---
phase: 19-ai-coach-skill-module
plan: 04
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - frontend/src/types/skill.ts
  - frontend/src/api/skills.ts
  - frontend/src/hooks/use-skills.ts
  - frontend/src/locales/en-US/skill.json
  - frontend/src/locales/zh-CN/skill.json
  - frontend/src/pages/admin/skill-hub.tsx
  - frontend/src/components/shared/skill-card.tsx
  - frontend/src/components/shared/skill-status-badge.tsx
  - frontend/src/router/index.tsx
  - frontend/src/components/layouts/admin-layout.tsx
  - frontend/package.json
autonomous: true
requirements:
  - D-14
  - D-16

must_haves:
  truths:
    - "Skill Hub page at /admin/skills renders a card grid of skills"
    - "SkillCard shows name, product, status badge, quality score, tags"
    - "SkillStatusBadge renders 5 states: draft/review/published/archived/failed"
    - "Skill Hub supports search, status filter, product filter with debounced search"
    - "Skill Hub is admin-only -- sidebar link and route guard"
    - "Query-key factory pattern prevents cache invalidation bugs"
  artifacts:
    - path: "frontend/src/types/skill.ts"
      provides: "TypeScript interfaces for Skill domain"
      contains: "interface Skill"
    - path: "frontend/src/api/skills.ts"
      provides: "Axios API client for /api/v1/skills"
      contains: "export function getSkills"
    - path: "frontend/src/hooks/use-skills.ts"
      provides: "TanStack Query hooks with query-key factory"
      contains: "export function useSkills"
    - path: "frontend/src/pages/admin/skill-hub.tsx"
      provides: "Skill Hub admin page with card grid"
      contains: "export default function SkillHubPage"
  key_links:
    - from: "frontend/src/pages/admin/skill-hub.tsx"
      to: "frontend/src/hooks/use-skills.ts"
      via: "useSkills hook"
      pattern: "useSkills"
    - from: "frontend/src/router/index.tsx"
      to: "frontend/src/pages/admin/skill-hub.tsx"
      via: "lazy import route"
      pattern: "skill-hub"
---

<objective>
Build the Skill Hub frontend: TypeScript types, API client, TanStack Query hooks with query-key factory pattern, i18n namespaces, SkillCard and SkillStatusBadge components, Skill Hub page with card grid + filters, router registration, and admin sidebar entry.

Addresses review concerns: query-key factory to prevent cache invalidation bugs across 15+ hooks, admin-only route guards, pagination contract coordination with backend.

Purpose: Admin needs a central place to view, search, and manage all Skills.
Output: Working Skill Hub page at /admin/skills with card grid, filtering, navigation, and correct cache management.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-CONTEXT.md
@.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-UI-SPEC.md
@.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-01-SUMMARY.md

<interfaces>
From frontend/src/types/scenario.ts (type pattern):
```typescript
export interface Scenario {
  id: string;
  name: string;
  status: "draft" | "active";
  created_at: string;
  updated_at: string;
}
```

From frontend/src/hooks/use-materials.ts (hook pattern):
```typescript
export function useMaterials(params?: { page?: number; page_size?: number }) {
  return useQuery({ queryKey: ["materials", params], queryFn: () => getMaterials(params) });
}
```

From frontend/src/api/materials.ts (API client pattern):
```typescript
import apiClient from "./client";
export async function getMaterials(params?) {
  const { data } = await apiClient.get("/materials", { params });
  return data;
}
```

From frontend/src/pages/admin/scenarios.tsx (card grid pattern):
// grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6

From frontend/src/components/layouts/admin-layout.tsx (sidebar):
```typescript
const sidebarGroups: SidebarGroup[] = [
  { labelKey: "content", items: [
    { path: "/admin/scenarios", labelKey: "scenarios", icon: BookOpen },
    { path: "/admin/materials", labelKey: "materials", icon: FileText },
  ]},
];
```

From frontend/src/router/index.tsx (route registration):
```typescript
const ScenariosPage = lazy(() => import("@/pages/admin/scenarios"));
{ path: "scenarios", element: <SuspensePage><ScenariosPage /></SuspensePage> },
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript types, API client, TanStack Query hooks with query-key factory, i18n, jszip</name>
  <read_first>
    - frontend/src/types/scenario.ts (type pattern with snake_case)
    - frontend/src/api/materials.ts (API client pattern)
    - frontend/src/hooks/use-materials.ts (TanStack Query hook pattern)
    - frontend/src/locales/en-US/common.json (i18n pattern)
    - frontend/src/locales/zh-CN/common.json (i18n pattern)
    - frontend/package.json (dependencies)
  </read_first>
  <files>
    frontend/src/types/skill.ts
    frontend/src/api/skills.ts
    frontend/src/hooks/use-skills.ts
    frontend/src/locales/en-US/skill.json
    frontend/src/locales/zh-CN/skill.json
    frontend/package.json
  </files>
  <action>
1. Install jszip: `cd frontend && npm install jszip @types/jszip`

2. Create `frontend/src/types/skill.ts` with all types matching backend schemas exactly (snake_case per project convention):
   - `SkillStatus = "draft" | "review" | "published" | "archived" | "failed"`
   - `ConversionStatus = "pending" | "processing" | "completed" | "failed"`
   - `ResourceType = "reference" | "script" | "asset"`
   - `QualityVerdict = "PASS" | "NEEDS_REVIEW" | "FAIL"`
   - `SkillResource` interface: id, skill_id, version_id (string | null), resource_type, filename, content_type, file_size, extraction_status (string | null), created_at, updated_at
   - `SkillVersion` interface: id, skill_id, version_number, content, metadata_json, change_notes, is_published, created_by, created_at
   - `SkillListItem` interface: id, name, description, product, status (SkillStatus), tags, quality_score (number | null), quality_verdict (QualityVerdict | null), structure_check_passed (boolean | null), conversion_status (ConversionStatus | null), current_version, created_by, created_at, updated_at
   - `Skill extends SkillListItem`: therapeutic_area, compatibility, metadata_json, content, structure_check_details, quality_details, conversion_error, resources (SkillResource[]), versions (SkillVersion[])
   - `SkillCreate`: name (string), description?, product?, therapeutic_area?, compatibility?, tags?, content?, metadata_json?
   - `SkillUpdate extends Partial<SkillCreate>`: status? (SkillStatus)
   - `StructureCheckResult`: passed, score, issues (array with severity/dimension/message/suggestion), content_hash
   - `QualityDimension`: name, score, verdict, strengths (string[]), improvements (string[]), critical_issues (string[]), rationale (string)
   - `QualityEvaluation`: overall_score, overall_verdict, dimensions (QualityDimension[]), summary, top_improvements (string[]), content_hash, evaluated_at
   - `SkillEvaluationSummary`: structure_check (passed, details), quality (score, verdict, details, is_stale)
   - `PaginatedSkills`: items (SkillListItem[]), total, page, page_size, total_pages

3. Create `frontend/src/api/skills.ts` with all API functions matching backend endpoints:
   - getSkills(params?), getPublishedSkills(params?), getSkill(id), createSkill(payload), updateSkill(id, payload), deleteSkill(id), publishSkill(id), archiveSkill(id), restoreSkill(id), createNewVersion(id)
   - startConversion(id), retryConversion(id), getConversionStatus(id), uploadAndConvert(id, files)
   - checkStructure(id), evaluateQuality(id), getEvaluation(id)
   - regenerateSop(id, feedback) -- calls POST /{id}/regenerate-sop
   - uploadResources(id, files, resourceType), deleteResource(skillId, resourceId), downloadResource(skillId, resourceId)
   - exportSkillZip(id) -- returns blob
   - importSkillZip(file) -- POST /import with FormData

4. Create `frontend/src/hooks/use-skills.ts` with query-key factory pattern (addresses review: 15+ hooks need disciplined cache invalidation):

   **Query-key factory (module-level constant):**
   ```typescript
   export const skillKeys = {
     all: ["skills"] as const,
     lists: () => [...skillKeys.all, "list"] as const,
     list: (params: Record<string, unknown>) => [...skillKeys.lists(), params] as const,
     published: (params?: Record<string, unknown>) => [...skillKeys.all, "published", params] as const,
     details: () => [...skillKeys.all, "detail"] as const,
     detail: (id: string) => [...skillKeys.details(), id] as const,
     conversion: (id: string) => [...skillKeys.detail(id), "conversion"] as const,
     evaluation: (id: string) => [...skillKeys.detail(id), "evaluation"] as const,
   };
   ```

   **Mutation invalidation matrix:**
   - Create/Delete/Archive/Restore/Publish: invalidate skillKeys.all (all lists + details)
   - Update: invalidate skillKeys.detail(id) + skillKeys.lists()
   - Conversion: invalidate skillKeys.detail(id) + skillKeys.conversion(id)
   - Quality check: invalidate skillKeys.detail(id) + skillKeys.evaluation(id)

   **Hooks (all use skillKeys factory):**
   - useSkills(params?) -- queryKey: skillKeys.list(params)
   - usePublishedSkills(params?) -- queryKey: skillKeys.published(params)
   - useSkill(id) -- queryKey: skillKeys.detail(id), enabled: !!id
   - useCreateSkill(), useUpdateSkill(), useDeleteSkill(), usePublishSkill(), useArchiveSkill(), useRestoreSkill() -- mutations with proper invalidation via skillKeys
   - useConversionStatus(id, enabled) -- queryKey: skillKeys.conversion(id), refetchInterval: 3000 when enabled
   - useStartConversion(), useRetryConversion() -- mutations
   - useCheckStructure(), useEvaluateQuality() -- mutations
   - useSkillEvaluation(id) -- queryKey: skillKeys.evaluation(id)
   - useUploadResources() -- mutation
   - useRegenerateSop() -- mutation that invalidates skillKeys.detail(id)

5. Create i18n files `frontend/src/locales/en-US/skill.json` and `frontend/src/locales/zh-CN/skill.json` with full key groups:
   - hub.* (title, createSkill, search, filters, empty states)
   - editor.* (title, backToHub, saveDraft, publishSkill, tab names)
   - status.* (draft, review, published, archived, failed)
   - conversion.* (processing, completed, failed, retry)
   - quality.* (structureCheck, qualityAssessment, dimension names in both languages, evaluating, notEvaluated)
   - actions.* (edit, archive, restore, delete, export, importZip, fromMaterials, applyChanges)
   - fileTree.* (upload, noResources, selectFile, download)
   - errors.* (loadFailed, conversionFailed, deleteFailed)
   - confirm.* (delete, archive, publishWarning, publishBlock, staleEvaluation)

   zh-CN translations per UI-SPEC copywriting contract.

6. Register `skill` namespace in i18n config file (check frontend/src/locales/i18n.ts or equivalent).
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/types/skill.ts contains `export interface Skill extends SkillListItem`
    - frontend/src/types/skill.ts contains `export type SkillStatus =`
    - frontend/src/types/skill.ts contains `content_hash: string` (in StructureCheckResult)
    - frontend/src/api/skills.ts contains `export async function getSkills(`
    - frontend/src/api/skills.ts contains `export async function regenerateSop(`
    - frontend/src/api/skills.ts contains `export async function importSkillZip(`
    - frontend/src/hooks/use-skills.ts contains `export const skillKeys =`
    - frontend/src/hooks/use-skills.ts contains `export function useSkills(`
    - frontend/src/hooks/use-skills.ts contains `export function useRegenerateSop(`
    - frontend/src/hooks/use-skills.ts contains `export function useConversionStatus(`
    - frontend/src/locales/en-US/skill.json contains `"hub"`
    - frontend/src/locales/zh-CN/skill.json contains `"hub"`
    - frontend/package.json contains `jszip`
  </acceptance_criteria>
  <done>TypeScript types matching backend schemas. API client for all endpoints including regenerateSop (moved from Plan 07). Query-key factory pattern for disciplined cache invalidation (addresses review: 15+ hooks). i18n en-US/zh-CN. jszip installed.</done>
</task>

<task type="auto">
  <name>Task 2: SkillCard, SkillStatusBadge, Skill Hub page, routing, sidebar</name>
  <read_first>
    - frontend/src/pages/admin/scenarios.tsx (card grid layout pattern)
    - frontend/src/components/shared/empty-state.tsx (empty state pattern)
    - frontend/src/components/ui/badge.tsx (Badge component)
    - frontend/src/components/ui/card.tsx (Card component)
    - frontend/src/components/ui/select.tsx (Select for filters)
    - frontend/src/components/ui/input.tsx (Input for search)
    - frontend/src/components/layouts/admin-layout.tsx (sidebar navigation groups)
    - frontend/src/router/index.tsx (route registration pattern)
    - frontend/src/types/skill.ts (just created)
    - frontend/src/hooks/use-skills.ts (just created)
    - .planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-UI-SPEC.md (layout specs)
  </read_first>
  <files>
    frontend/src/components/shared/skill-status-badge.tsx
    frontend/src/components/shared/skill-card.tsx
    frontend/src/pages/admin/skill-hub.tsx
    frontend/src/pages/admin/skill-editor.tsx
    frontend/src/router/index.tsx
    frontend/src/components/layouts/admin-layout.tsx
  </files>
  <action>
1. Create `frontend/src/components/shared/skill-status-badge.tsx`:
   - Props: `{ status: SkillStatus }`
   - Badge with colored dot per UI-SPEC:
     - draft: outline variant, muted-foreground dot
     - review: outline variant, weakness/orange dot
     - published: bg-primary/10 text-primary, strength/green dot
     - archived: outline variant, muted-foreground dot
     - failed: destructive variant, destructive/red dot
   - aria-label with full status text

2. Create `frontend/src/components/shared/skill-card.tsx`:
   - Props: `{ skill: SkillListItem; onEdit, onArchive, onDelete, onExport callbacks }`
   - Per UI-SPEC card layout: rounded-lg border bg-card hover:shadow-md transition duration-150
   - Top: SkillStatusBadge left, quality score right
   - Name: font-semibold text-base line-clamp-1
   - Product: text-sm text-muted-foreground
   - Tags: outline badges, max 3 + "+N"
   - Bottom: "Edit Skill" ghost button, "More" DropdownMenu (Archive, Export, Delete)

3. Create `frontend/src/pages/admin/skill-hub.tsx`:
   - Use useSkills(params) with state for page, status, product, search
   - Debounced search (300ms) via useState + useEffect
   - Filter row: Input search (flex-1), Select status, Select product
   - Card grid: grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6
   - Loading: 6 Skeleton cards
   - Error: EmptyState with retry
   - Empty: EmptyState with BookOpen icon, "No skills yet", CTA
   - "Create Skill" dialog: "Create from Materials" and "Import ZIP Package" options
   - Pagination at bottom
   - Confirmation dialogs for archive/delete actions

4. Create minimal placeholder `frontend/src/pages/admin/skill-editor.tsx`:
   ```typescript
   export default function SkillEditorPage() { return <div>Skill Editor (loading...)</div>; }
   ```
   (Full implementation in Plans 05a/05b)

5. Update router with lazy imports:
   - `/admin/skills` -> SkillHubPage
   - `/admin/skills/new` -> SkillEditorPage
   - `/admin/skills/:id/edit` -> SkillEditorPage

6. Update admin-layout sidebar: add Skill Hub entry with Lightbulb icon (from lucide-react) in "content" group after scenarios. Add i18n key "skillHub" to nav namespace.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc --noEmit --pretty 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/shared/skill-status-badge.tsx contains `SkillStatusBadge`
    - frontend/src/components/shared/skill-status-badge.tsx contains `aria-label`
    - frontend/src/components/shared/skill-card.tsx contains `SkillCard`
    - frontend/src/pages/admin/skill-hub.tsx contains `export default function SkillHubPage`
    - frontend/src/pages/admin/skill-hub.tsx contains `useSkills`
    - frontend/src/pages/admin/skill-hub.tsx contains `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
    - frontend/src/router/index.tsx contains `skills` route path
    - frontend/src/components/layouts/admin-layout.tsx contains `/admin/skills`
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>SkillCard with 5-state badge, quality score, tags, action menu. Skill Hub with card grid, debounced search, filters, empty/loading/error states. Admin sidebar entry. Router routes registered. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> API | All skill operations go through authenticated API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-16 | Elevation | Admin routes | mitigate | Routes wrapped in AdminRoute guard component. Sidebar visible only to admin role. API enforces admin role server-side. |
| T-19-17 | Information Disclosure | Skill data in client | accept | Admin-only pages. No PII exposure in skill data. |
</threat_model>

<verification>
```bash
cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend
npx tsc --noEmit
npm run build
```
</verification>

<success_criteria>
- Skill Hub page at /admin/skills renders card grid (D-14)
- SkillCard shows name, product, status badge, quality score, tags
- SkillStatusBadge renders all 5 lifecycle states (D-18)
- Search (debounced), status filter, product filter functional
- Admin-only (D-16) -- sidebar link and route guard
- Query-key factory prevents cache bugs (addresses review)
- jszip installed
- TypeScript compiles, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-04-SUMMARY.md`
</output>
