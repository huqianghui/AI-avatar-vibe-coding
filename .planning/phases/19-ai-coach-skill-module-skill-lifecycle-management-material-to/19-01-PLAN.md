---
phase: 19-ai-coach-skill-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/skill.py
  - backend/app/models/__init__.py
  - backend/app/schemas/skill.py
  - backend/app/services/skill_service.py
  - backend/app/api/skills.py
  - backend/app/api/__init__.py
  - backend/app/main.py
  - backend/alembic/versions/xxx_add_skill_tables.py
  - backend/pyproject.toml
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-18
  - D-19
  - D-20
  - D-24
  - D-26

must_haves:
  truths:
    - "Skill, SkillVersion, SkillResource tables exist in database after migration"
    - "Admin can create a Skill via POST /api/v1/skills and receives a 201 response"
    - "Admin can list skills with pagination, filtering by status and product"
    - "Skill lifecycle transitions enforce a strict state machine matrix"
    - "Only one published SkillVersion per Skill at any time (enforced in code)"
    - "Editing a published Skill creates a new draft version, original stays published"
    - "SkillVersion snapshots are immutable once created"
  artifacts:
    - path: "backend/app/models/skill.py"
      provides: "Skill, SkillVersion, SkillResource ORM models"
      contains: "class Skill"
    - path: "backend/app/schemas/skill.py"
      provides: "Pydantic v2 request/response schemas"
      contains: "class SkillCreate"
    - path: "backend/app/services/skill_service.py"
      provides: "Skill CRUD and lifecycle business logic with strict transition enforcement"
      contains: "async def create_skill"
    - path: "backend/app/api/skills.py"
      provides: "REST API routes for /api/v1/skills/*"
      contains: "router = APIRouter"
  key_links:
    - from: "backend/app/api/skills.py"
      to: "backend/app/services/skill_service.py"
      via: "service function calls"
      pattern: "skill_service\\."
    - from: "backend/app/main.py"
      to: "backend/app/api/skills.py"
      via: "router registration"
      pattern: "skills_router"
---

<objective>
Create the Skill data foundation: 3 new ORM models (Skill, SkillVersion, SkillResource) with strict lifecycle state machine, versioning invariants (single published version, immutable snapshots), Pydantic v2 schemas, skill_service.py with CRUD + lifecycle management, REST API routes, Alembic migration with proper indexes, and dependency installation.

Addresses review concerns: strict state transition matrix, versioning invariants (single published version, immutable snapshots, optimistic locking), explicit DB indexes, file upload security controls.

Purpose: All subsequent plans depend on the Skill data model and API. Getting invariants right here prevents rework in Waves 2-4.
Output: Working backend API for Skill CRUD with enforced lifecycle state machine, versioning, and resource management.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-CONTEXT.md
@.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase that executor needs -->

From backend/app/models/base.py:
```python
class TimestampMixin:
    id: Mapped[str] = mapped_column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
    created_at: Mapped[datetime] = mapped_column(server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(server_default=func.now(), onupdate=func.now())
```

From backend/app/models/material.py (version pattern to replicate):
```python
class TrainingMaterial(Base, TimestampMixin):
    __tablename__ = "training_materials"
    name: Mapped[str] = mapped_column(String(255), nullable=False)
    product: Mapped[str] = mapped_column(String(255), nullable=False, index=True)
    current_version: Mapped[int] = mapped_column(default=1)
    versions = relationship("MaterialVersion", back_populates="material", order_by="MaterialVersion.version_number.desc()")
```

From backend/app/utils/pagination.py:
```python
class PaginatedResponse(BaseModel, Generic[T]):
    items: list[T]
    total: int
    page: int
    page_size: int
    total_pages: int
    @classmethod
    def create(cls, items, total, page, page_size): ...
```

From backend/app/dependencies.py:
```python
from app.database import get_db
# get_current_user, require_role("admin")
```

From backend/app/models/__init__.py (registration pattern):
```python
from app.models.base import Base, TimestampMixin
from app.models.material import MaterialVersion, TrainingMaterial
```

From backend/app/api/__init__.py (router export pattern):
```python
from app.api.materials import router as materials_router
```

From backend/app/main.py (router include pattern):
```python
app.include_router(materials_router, prefix=settings.api_prefix)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skill data models with strict invariants, schemas, Alembic migration, install deps</name>
  <read_first>
    - backend/app/models/material.py (version management pattern to replicate)
    - backend/app/models/base.py (TimestampMixin pattern)
    - backend/app/models/__init__.py (model registration)
    - backend/app/schemas/material.py (schema pattern)
    - backend/pyproject.toml (dependency management)
    - backend/alembic/env.py (migration config)
    - azure_foundary_hosted_agents-main/agent-framework/agent-with-skills/skill_manager.py (Skill data model reference)
  </read_first>
  <files>
    backend/app/models/skill.py
    backend/app/models/__init__.py
    backend/app/schemas/skill.py
    backend/alembic/versions/xxx_add_skill_tables.py
    backend/pyproject.toml
  </files>
  <action>
1. Install dependencies:
   ```bash
   cd backend && pip install "python-pptx>=1.0.0" "pdfplumber>=0.10.0"
   ```
   Add `"python-pptx>=1.0.0"` and `"pdfplumber>=0.10.0"` to `[project] dependencies` in `pyproject.toml`.

2. Create `backend/app/models/skill.py` with 3 models. Addresses review: strict state transition matrix, versioning invariants, DB indexes.

   **VALID_TRANSITIONS** module-level constant (strict transition matrix per review concern):
   ```python
   VALID_TRANSITIONS: dict[str, set[str]] = {
       "draft": {"review"},
       "review": {"draft", "published"},
       "published": {"archived"},
       "archived": {"draft"},
       "failed": {"draft"},
   }
   ```

   **Skill** (`__tablename__ = "skills"`):
   - Inherits `Base, TimestampMixin`
   - `name: Mapped[str] = mapped_column(String(255), nullable=False)` -- from YAML frontmatter
   - `description: Mapped[str] = mapped_column(Text, default="")` -- from frontmatter
   - `product: Mapped[str] = mapped_column(String(255), default="", index=True)` -- indexed for filtering
   - `therapeutic_area: Mapped[str] = mapped_column(String(255), default="")`
   - `compatibility: Mapped[str] = mapped_column(String(255), default="")` -- runtime requirements
   - `metadata_json: Mapped[str] = mapped_column(Text, default="{}")` -- extended fields as JSON
   - `tags: Mapped[str] = mapped_column(Text, default="")` -- comma-separated
   - `content: Mapped[str] = mapped_column(Text, default="")` -- full Markdown body (Coaching Protocol)
   - `status: Mapped[str] = mapped_column(String(20), default="draft", index=True)` -- indexed for status filtering
   - `current_version: Mapped[int] = mapped_column(default=1)`
   - `created_by: Mapped[str] = mapped_column(String(36), ForeignKey("users.id"), nullable=False)`
   - `updated_by: Mapped[str] = mapped_column(String(36), default="")` -- audit column per review
   - Quality evaluation fields:
     - `structure_check_passed: Mapped[bool | None] = mapped_column(default=None)` -- L1 result
     - `structure_check_details: Mapped[str] = mapped_column(Text, default="{}")` -- L1 issues JSON
     - `quality_score: Mapped[int | None] = mapped_column(default=None)` -- L2 overall 0-100
     - `quality_verdict: Mapped[str | None] = mapped_column(String(20), default=None)` -- PASS/NEEDS_REVIEW/FAIL
     - `quality_details: Mapped[str] = mapped_column(Text, default="{}")` -- L2 full evaluation JSON
   - Conversion tracking fields:
     - `conversion_status: Mapped[str | None] = mapped_column(String(20), default=None)` -- pending/processing/completed/failed
     - `conversion_error: Mapped[str] = mapped_column(Text, default="")` -- error message
     - `conversion_job_id: Mapped[str | None] = mapped_column(String(36), default=None)` -- for idempotency (review concern)
   - Relationships:
     - `versions = relationship("SkillVersion", back_populates="skill", order_by="SkillVersion.version_number.desc()", cascade="all, delete-orphan")`
     - `resources = relationship("SkillResource", back_populates="skill", cascade="all, delete-orphan")`
   - `__table_args__`: Add composite index on `(status, product)` for filtered list queries, and index on `created_at` for sort performance:
     ```python
     __table_args__ = (
         Index("ix_skills_status_product", "status", "product"),
         Index("ix_skills_created_at", "created_at"),
     )
     ```

   **SkillVersion** (`__tablename__ = "skill_versions"`):
   - Inherits `Base, TimestampMixin`
   - `skill_id: Mapped[str] = mapped_column(String(36), ForeignKey("skills.id", ondelete="CASCADE"), nullable=False, index=True)`
   - `version_number: Mapped[int] = mapped_column(nullable=False)`
   - `content: Mapped[str] = mapped_column(Text, default="")` -- immutable Markdown body snapshot
   - `metadata_json: Mapped[str] = mapped_column(Text, default="{}")` -- immutable frontmatter snapshot
   - `change_notes: Mapped[str] = mapped_column(Text, default="")`
   - `is_published: Mapped[bool] = mapped_column(default=False)` -- replaces is_active; true = this is the published version
   - `created_by: Mapped[str] = mapped_column(String(36), default="")` -- who created this version
   - `skill = relationship("Skill", back_populates="versions")`
   - Note: Immutability enforced in service layer (no update_version function exposed). Once created, only is_published flag can change.

   **SkillResource** (`__tablename__ = "skill_resources"`):
   - Inherits `Base, TimestampMixin`
   - `skill_id: Mapped[str] = mapped_column(String(36), ForeignKey("skills.id", ondelete="CASCADE"), nullable=False, index=True)`
   - `version_id: Mapped[str | None] = mapped_column(String(36), ForeignKey("skill_versions.id", ondelete="SET NULL"), nullable=True, default=None)` -- links resource to specific version for deterministic behavior (review concern)
   - `resource_type: Mapped[str] = mapped_column(String(20), nullable=False)` -- reference/script/asset (D-24, D-26)
   - `filename: Mapped[str] = mapped_column(String(255), nullable=False)`
   - `storage_path: Mapped[str] = mapped_column(String(500), nullable=False)`
   - `content_type: Mapped[str] = mapped_column(String(100), default="")`
   - `file_size: Mapped[int] = mapped_column(default=0)` -- bytes
   - `text_content: Mapped[str] = mapped_column(Text, default="")` -- extracted text for conversion
   - `extraction_status: Mapped[str | None] = mapped_column(String(20), default=None)` -- pending/completed/failed
   - `skill = relationship("Skill", back_populates="resources")`
   - `__table_args__`: Index on `(skill_id, resource_type)`:
     ```python
     __table_args__ = (
         Index("ix_skill_resources_skill_type", "skill_id", "resource_type"),
     )
     ```

3. Update `backend/app/models/__init__.py`:
   ```python
   from app.models.skill import Skill, SkillResource, SkillVersion
   # Add to __all__: "Skill", "SkillVersion", "SkillResource"
   ```

4. Create Alembic migration:
   ```bash
   cd backend && alembic revision --autogenerate -m "add skill tables"
   ```
   Verify migration creates 3 tables: `skills`, `skill_versions`, `skill_resources`.
   All columns use `server_default` where applicable for SQLite compat.
   Run: `alembic upgrade head`

5. Create `backend/app/schemas/skill.py` with Pydantic v2 models:

   **SkillCreate(BaseModel):** name (str, required), description (str = ""), product (str = ""), therapeutic_area (str = ""), compatibility (str = ""), tags (str = ""), content (str = ""), metadata_json (str = "{}")

   **SkillUpdate(BaseModel):** All fields optional (| None = None): name, description, product, therapeutic_area, compatibility, tags, content, metadata_json, status. DO NOT include version_number or quality fields -- those are managed only by service layer.

   **SkillResourceOut(BaseModel):** id, skill_id, version_id (str | None), resource_type, filename, content_type, file_size, extraction_status (str | None), created_at, updated_at. model_config = ConfigDict(from_attributes=True)

   **SkillVersionOut(BaseModel):** id, skill_id, version_number, content, metadata_json, change_notes, is_published, created_by, created_at. model_config = ConfigDict(from_attributes=True)

   **SkillListOut(BaseModel):** id, name, description, product, status, tags, quality_score (int | None), quality_verdict (str | None), structure_check_passed (bool | None), conversion_status (str | None), current_version, created_by, created_at, updated_at. model_config = ConfigDict(from_attributes=True)

   **SkillOut(SkillListOut):** Additional: therapeutic_area, compatibility, metadata_json, content, structure_check_details, quality_details, conversion_error. resources: list[SkillResourceOut] = []. versions: list[SkillVersionOut] = [].

   **StructureCheckOut(BaseModel):** passed: bool, score: int, issues: list[dict]

   **QualityEvaluationOut(BaseModel):** overall_score: int, overall_verdict: str, dimensions: list[dict], summary: str, top_improvements: list[str]
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend && python3 -c "from app.models.skill import Skill, SkillVersion, SkillResource, VALID_TRANSITIONS; from app.schemas.skill import SkillCreate, SkillOut, SkillListOut; print('Models and schemas OK'); print('Transitions:', VALID_TRANSITIONS)" && alembic upgrade head && python3 -c "from app.database import engine; import asyncio; asyncio.run(engine.dispose()); print('Migration OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/models/skill.py contains `VALID_TRANSITIONS: dict[str, set[str]]`
    - backend/app/models/skill.py contains `class Skill(Base, TimestampMixin):`
    - backend/app/models/skill.py contains `class SkillVersion(Base, TimestampMixin):`
    - backend/app/models/skill.py contains `class SkillResource(Base, TimestampMixin):`
    - backend/app/models/skill.py contains `is_published: Mapped[bool]`
    - backend/app/models/skill.py contains `Index("ix_skills_status_product"`
    - backend/app/models/skill.py contains `updated_by: Mapped[str]`
    - backend/app/models/skill.py contains `conversion_job_id: Mapped[str | None]`
    - backend/app/models/__init__.py contains `from app.models.skill import Skill, SkillResource, SkillVersion`
    - backend/app/schemas/skill.py contains `class SkillCreate(BaseModel):`
    - backend/app/schemas/skill.py contains `class SkillOut(SkillListOut):`
    - backend/pyproject.toml contains `python-pptx`
    - backend/pyproject.toml contains `pdfplumber`
    - Alembic migration file exists in backend/alembic/versions/ with "add_skill" in filename
  </acceptance_criteria>
  <done>3 ORM models with strict transition matrix, versioning invariants (is_published flag, immutable versions), audit columns (updated_by), conversion idempotency (conversion_job_id), composite DB indexes for list performance. Pydantic v2 schemas. Alembic migration. Dependencies installed.</done>
</task>

<task type="auto">
  <name>Task 2: Skill service with strict lifecycle + enforced invariants, API routes with file security</name>
  <read_first>
    - backend/app/services/material_service.py (service pattern: async def, db.flush())
    - backend/app/api/materials.py (router pattern: static routes before /{id}, require_role)
    - backend/app/api/__init__.py (router export pattern)
    - backend/app/main.py (router include pattern)
    - backend/app/models/skill.py (just created in Task 1, especially VALID_TRANSITIONS)
    - backend/app/schemas/skill.py (just created in Task 1)
    - backend/app/utils/pagination.py (PaginatedResponse.create pattern)
    - backend/app/utils/exceptions.py (not_found, bad_request pattern)
  </read_first>
  <files>
    backend/app/services/skill_service.py
    backend/app/api/skills.py
    backend/app/api/__init__.py
    backend/app/main.py
  </files>
  <action>
1. Create `backend/app/services/skill_service.py`. Addresses review concerns: strict transition enforcement, single published version invariant, optimistic locking, complete file security.

   **Module-level constants:**
   ```python
   ALLOWED_RESOURCE_EXTENSIONS = {".pdf", ".docx", ".pptx", ".txt", ".md", ".py", ".json", ".csv", ".xlsx"}
   MAX_FILE_SIZE = 50 * 1024 * 1024  # 50MB per file
   MAX_FILES_PER_UPLOAD = 10
   ```

   **def validate_status_transition(current: str, target: str) -> None:**
   - Import VALID_TRANSITIONS from models.skill
   - If target not in VALID_TRANSITIONS.get(current, set()): raise bad_request(f"Invalid status transition from '{current}' to '{target}'. Allowed: {VALID_TRANSITIONS.get(current, set())}")
   - This is the SOLE transition enforcement point -- no ad-hoc transition logic elsewhere

   **def sanitize_filename(filename: str) -> str:**
   - Strip directory separators, reject `..`, reject absolute paths
   - Use `pathlib.PurePosixPath(filename).name` to extract base filename
   - Reject empty result, reject if contains control characters
   - Return sanitized filename

   **def validate_file_upload(filename: str, file_size: int) -> None:**
   - Check extension against ALLOWED_RESOURCE_EXTENSIONS
   - Check file_size <= MAX_FILE_SIZE
   - Raise bad_request with specific message on failure

   **async def create_skill(db: AsyncSession, data: SkillCreate, user_id: str) -> Skill:**
   - Create Skill record with data fields, created_by=user_id, updated_by=user_id, status="draft"
   - Create initial SkillVersion (version_number=1, content=data.content, metadata_json=data.metadata_json, is_published=False, created_by=user_id)
   - db.flush()
   - Return skill (use selectinload for resources and versions)

   **async def get_skills(db, page=1, page_size=20, status=None, product=None, search=None) -> tuple[list[Skill], int]:**
   - Query with optional filters: status exact match, product exact match, search ILIKE on name+description
   - Order by created_at desc
   - Use .limit(page_size).offset((page - 1) * page_size) for pagination
   - Return (items, total)

   **async def get_published_skills(db, page=1, page_size=20, search=None) -> tuple[list[Skill], int]:**
   - Same as get_skills but hardcoded status="published"

   **async def get_skill(db: AsyncSession, skill_id: str) -> Skill:**
   - Load by id with selectinload(Skill.versions) and selectinload(Skill.resources)
   - Raise not_found("Skill not found") if not exists

   **async def update_skill(db: AsyncSession, skill_id: str, data: SkillUpdate, user_id: str) -> Skill:**
   - Load skill
   - If data.status is set and differs from current: call validate_status_transition(skill.status, data.status)
   - Apply non-None fields from data
   - Set skill.updated_by = user_id
   - db.flush()
   - Return updated skill

   **async def delete_skill(db: AsyncSession, skill_id: str) -> None:**
   - Load skill. Only allow deletion if status in ("draft", "failed")
   - Raise bad_request("Cannot delete skill in '{status}' status. Only draft or failed skills can be deleted.") otherwise
   - Delete associated resources' files from storage
   - db.delete(skill), db.flush()

   **async def publish_skill(db: AsyncSession, skill_id: str, user_id: str) -> Skill:**
   - Load skill
   - Enforce: skill.status must be "review" (use validate_status_transition)
   - Enforce: structure_check_passed must be True, else raise bad_request("L1 structure check must pass before publishing")
   - Enforce: quality_score must not be None and >= 50, else raise bad_request("Quality score must be at least 50 to publish")
   - **Invariant: single published version.** Mark all existing SkillVersions for this skill as is_published=False
   - Create new SkillVersion snapshot: version_number=skill.current_version, content=skill.content, metadata_json=skill.metadata_json, is_published=True, created_by=user_id
   - Set skill.status = "published", skill.updated_by = user_id
   - db.flush()

   **async def create_new_version(db: AsyncSession, skill_id: str, user_id: str) -> Skill:**
   - Load skill, verify status == "published"
   - Increment current_version
   - Set status = "draft"
   - Reset quality fields: structure_check_passed=None, quality_score=None, quality_verdict=None, quality_details="{}", structure_check_details="{}"
   - Set updated_by = user_id
   - db.flush()

   **async def archive_skill(db: AsyncSession, skill_id: str, user_id: str) -> Skill:**
   - Load skill, validate_status_transition(skill.status, "archived")
   - Set status="archived", updated_by=user_id
   - db.flush()

   **async def restore_skill(db: AsyncSession, skill_id: str, user_id: str) -> Skill:**
   - Load skill, validate_status_transition(skill.status, "draft")
   - Set status="draft", updated_by=user_id
   - db.flush()

2. Create `backend/app/api/skills.py` with APIRouter(prefix="/skills", tags=["skills"]):
   All endpoints require `require_role("admin")`.

   **Static routes FIRST (before /{skill_id}):**
   - `POST /` -> create_skill (201)
   - `GET /` -> list_skills (paginated, query params: page, page_size, status, product, search)
   - `GET /published` -> list_published_skills (paginated)

   **Parameterized routes:**
   - `GET /{skill_id}` -> get_skill
   - `PUT /{skill_id}` -> update_skill
   - `DELETE /{skill_id}` -> delete_skill (204)
   - `POST /{skill_id}/publish` -> publish_skill
   - `POST /{skill_id}/archive` -> archive_skill
   - `POST /{skill_id}/restore` -> restore_skill
   - `POST /{skill_id}/new-version` -> create_new_version

   **Resource routes with complete file security (addresses review concern):**
   - `POST /{skill_id}/resources` -> upload_resource:
     - Accept UploadFile + query param resource_type (must be in {"reference", "script", "asset"})
     - Call validate_file_upload(file.filename, file.size) for extension and size check
     - Call sanitize_filename(file.filename) for path safety
     - Reject if number of existing resources + 1 > 100 (max resources per skill)
     - Store via LocalStorageBackend at `skills/{skill_id}/{resource_type}s/{safe_filename}`
     - Create SkillResource record
   - `GET /{skill_id}/resources` -> list resources
   - `DELETE /{skill_id}/resources/{resource_id}` -> delete_resource (204)
   - `GET /{skill_id}/resources/{resource_id}/download` -> FileResponse (never expose storage_path)

3. Update `backend/app/api/__init__.py`:
   Add: `from app.api.skills import router as skills_router` and to `__all__`

4. Update `backend/app/main.py`:
   Add import and: `app.include_router(skills_router, prefix=settings.api_prefix)`
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend && python3 -c "from app.api.skills import router; print(f'Routes: {len(router.routes)}')" && python3 -c "from app.services.skill_service import validate_status_transition, sanitize_filename, validate_file_upload; print('Service invariants OK')" && python3 -c "from app.main import app; routes = [r.path for r in app.routes if hasattr(r, 'path') and 'skill' in r.path]; print(f'Skill routes registered: {len(routes)}')" && ruff check app/models/skill.py app/schemas/skill.py app/services/skill_service.py app/api/skills.py</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/services/skill_service.py contains `def validate_status_transition(`
    - backend/app/services/skill_service.py contains `VALID_TRANSITIONS`
    - backend/app/services/skill_service.py contains `def sanitize_filename(`
    - backend/app/services/skill_service.py contains `def validate_file_upload(`
    - backend/app/services/skill_service.py contains `MAX_FILE_SIZE`
    - backend/app/services/skill_service.py contains `async def publish_skill(`
    - backend/app/services/skill_service.py contains `is_published=False` (clearing old versions)
    - backend/app/services/skill_service.py contains `is_published=True` (new published version)
    - backend/app/services/skill_service.py contains `db.flush()`
    - backend/app/api/skills.py contains `router = APIRouter(prefix="/skills"`
    - backend/app/api/skills.py contains `require_role("admin")`
    - backend/app/api/skills.py contains `status_code=201`
    - backend/app/api/skills.py contains `status_code=204`
    - backend/app/api/__init__.py contains `skills_router`
    - backend/app/main.py contains `skills_router`
    - `ruff check` exits 0 for all files
  </acceptance_criteria>
  <done>Skill service with strict state machine (VALID_TRANSITIONS), single-published-version invariant, immutable version snapshots, audit columns (updated_by), file security (extension whitelist, size limit, filename sanitization, max resources). API routes registered. All ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Admin uploads files and creates/edits skills -- untrusted input |
| API -> Storage | Files stored to local filesystem -- path injection risk |
| API -> Database | Skill content stored -- injection through metadata fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | POST /skills resource upload | mitigate | Extension whitelist (ALLOWED_RESOURCE_EXTENSIONS), max 50MB per file, max 10 files per upload, max 100 resources per skill. MIME sniffing not relied upon -- extension-based validation only. |
| T-19-02 | Tampering | Resource storage path | mitigate | sanitize_filename() via pathlib.PurePosixPath.name, reject `..`, reject absolute paths, reject control characters. Storage path always `skills/{skill_id}/{type}s/{safe_filename}`. |
| T-19-03 | Elevation | All /skills/* endpoints | mitigate | All routes use `require_role("admin")` -- non-admin users get 403. |
| T-19-04 | Information Disclosure | GET /resources/{id}/download | mitigate | FileResponse with content_disposition, never expose storage_path to client. |
| T-19-05 | Repudiation | Skill mutations | mitigate | created_by and updated_by audit columns on Skill model track who made changes. |
</threat_model>

<verification>
```bash
cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend
ruff check app/models/skill.py app/schemas/skill.py app/services/skill_service.py app/api/skills.py
ruff format --check app/models/skill.py app/schemas/skill.py app/services/skill_service.py app/api/skills.py
python3 -c "from app.main import app; print('App loads OK')"
alembic upgrade head
```
</verification>

<success_criteria>
- 3 ORM models with all fields per D-01 through D-03, D-18 through D-20, D-24, D-26
- Strict transition matrix enforced (VALID_TRANSITIONS dict, single validation function)
- Single published version invariant (is_published flag on SkillVersion, cleared before new publish)
- Immutable version snapshots (no update_version exposed)
- Composite DB indexes on (status, product) and (skill_id, resource_type)
- File upload security: extension whitelist, 50MB limit, filename sanitization, 100 resource cap
- Alembic migration runs cleanly on SQLite
- All code passes ruff
</success_criteria>

<output>
After completion, create `.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-01-SUMMARY.md`
</output>
