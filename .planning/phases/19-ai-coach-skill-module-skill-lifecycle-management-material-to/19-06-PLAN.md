---
phase: 19-ai-coach-skill-module
plan: 06
type: execute
wave: 3
depends_on: ["19-03", "19-05"]
files_modified:
  - frontend/src/components/shared/quality-radar-chart.tsx
  - frontend/src/components/shared/quality-score-card.tsx
  - frontend/src/components/shared/publish-gate-dialog.tsx
  - frontend/src/pages/admin/skill-editor.tsx
autonomous: true
requirements:
  - D-11
  - D-12

must_haves:
  truths:
    - "L2 quality scores display as radar chart with 6 dimensions"
    - "Each dimension has expandable score card with strengths, improvements, rationale"
    - "Quality tab shows L1 result banner + L2 radar chart + dimension cards"
    - "Publish gate dialog enforces L1 PASS + L2 >= 50, warns on stale evaluation"
    - "Settings tab has metadata form (name, product, tags, etc.)"
    - "Skill editor has all 4 tabs functional"
  artifacts:
    - path: "frontend/src/components/shared/quality-radar-chart.tsx"
      provides: "L2 six-dimension radar chart"
      contains: "QualityRadarChart"
    - path: "frontend/src/components/shared/publish-gate-dialog.tsx"
      provides: "Quality gate dialog with staleness check"
      contains: "PublishGateDialog"
  key_links:
    - from: "frontend/src/pages/admin/skill-editor.tsx"
      to: "frontend/src/components/shared/quality-radar-chart.tsx"
      via: "Quality tab rendering"
      pattern: "QualityRadarChart"
    - from: "frontend/src/components/shared/quality-radar-chart.tsx"
      to: "recharts"
      via: "RadarChart component"
      pattern: "RadarChart"
---

<objective>
Complete the Skill Editor with Quality tab (radar chart + dimension cards + L1 banner), Settings tab (metadata form), and PublishGateDialog with staleness detection. This is the second half of the editor split from original Plan 05.

Addresses review: staleness warning in publish gate (content changed since evaluation), dimension rationale display for transparency.

Purpose: Quality visualization and publish flow complete the editor experience. Admin needs to see evaluation results and safely publish.
Output: All 4 editor tabs functional with quality gate enforcement.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-CONTEXT.md
@.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-UI-SPEC.md
@.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-05-SUMMARY.md

<interfaces>
From frontend/src/types/skill.ts (Plan 04):
```typescript
export interface QualityDimension {
  name: string; score: number; verdict: string;
  strengths: string[]; improvements: string[]; critical_issues: string[];
  rationale: string;
}
export interface QualityEvaluation {
  overall_score: number; overall_verdict: QualityVerdict;
  dimensions: QualityDimension[]; summary: string;
  top_improvements: string[]; content_hash: string; evaluated_at: string;
}
export interface SkillEvaluationSummary {
  structure_check: { passed: boolean | null; details: StructureCheckResult | null };
  quality: { score: number | null; verdict: QualityVerdict | null; details: QualityEvaluation | null; is_stale: boolean };
}
```

From frontend/src/hooks/use-skills.ts (Plan 04):
```typescript
export function useCheckStructure(): UseMutationResult;
export function useEvaluateQuality(): UseMutationResult;
export function useSkillEvaluation(id): UseQueryResult<SkillEvaluationSummary>;
export function usePublishSkill(): UseMutationResult;
export function useUpdateSkill(): UseMutationResult;
```

From existing recharts usage:
```typescript
import { RadarChart, PolarGrid, PolarAngleAxis, PolarRadiusAxis, Radar, ResponsiveContainer } from "recharts";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QualityRadarChart, QualityScoreCard, PublishGateDialog components</name>
  <read_first>
    - frontend/src/pages/admin/scoring-feedback.tsx (existing recharts RadarChart usage pattern)
    - frontend/src/components/ui/dialog.tsx (Dialog component)
    - frontend/src/components/ui/progress.tsx (Progress bar for score visualization)
    - frontend/src/components/ui/card.tsx (Card component)
    - frontend/src/types/skill.ts (QualityDimension, SkillEvaluationSummary types)
    - frontend/src/hooks/use-skills.ts (useSkillEvaluation, usePublishSkill)
    - .planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-UI-SPEC.md (quality visualization specs)
  </read_first>
  <files>
    frontend/src/components/shared/quality-radar-chart.tsx
    frontend/src/components/shared/quality-score-card.tsx
    frontend/src/components/shared/publish-gate-dialog.tsx
  </files>
  <action>
1. Create `frontend/src/components/shared/quality-radar-chart.tsx`:
   - Props: `{ dimensions: QualityDimension[]; overallScore?: number; overallVerdict?: string }`
   - Import recharts: RadarChart, PolarGrid, PolarAngleAxis, PolarRadiusAxis, Radar, ResponsiveContainer
   - Transform dimensions to chart data array: `{ dimension: t("skill:quality.dimensions." + dim.name), score: dim.score, fullMark: 100 }`
   - Chart layout:
     - ResponsiveContainer width="100%" height={300}
     - RadarChart with PolarGrid
     - PolarAngleAxis dataKey="dimension" tick with text-xs
     - PolarRadiusAxis domain={[0, 100]} tick={false}
     - Radar: fill="var(--primary)" fillOpacity={0.3} stroke="var(--primary)" strokeWidth={2}
   - Below chart: overall score large number display (text-4xl font-semibold) + verdict text badge
     - PASS: text-strength, NEEDS_REVIEW: text-weakness, FAIL: text-destructive
   - aria-label on chart container: "Quality assessment: overall score {score} out of 100"

2. Create `frontend/src/components/shared/quality-score-card.tsx`:
   - Props: `{ dimension: QualityDimension; isExpanded?: boolean; onToggle?: () => void }`
   - Card layout:
     - Header row: translated dimension name (font-semibold) + score number badge
     - Progress bar: width = score%, color by score (green >= 70, orange 50-69, red < 50)
       - Use cn() with conditional: `bg-strength` / `bg-weakness` / `bg-destructive`
     - Expandable section (collapsible via state or props):
       - Rationale text (italic, text-sm text-muted-foreground) -- addresses review: per-dimension rationale
       - Strengths: Check icon (green) + list items
       - Improvements: AlertTriangle icon (orange) + list items
       - Critical Issues: XCircle icon (red) + list items (only if non-empty)
     - Click header to toggle expand/collapse

3. Create `frontend/src/components/shared/publish-gate-dialog.tsx`:
   - Props: `{ open: boolean; onOpenChange: (b: boolean) => void; skill: Skill; evaluation: SkillEvaluationSummary | null; onPublish: () => void; onCancel: () => void }`
   - Dialog content per UI-SPEC Quality Gate Flow:

   Addresses review: staleness warning before publish.

   **Staleness check first:**
   - If evaluation?.quality?.is_stale === true:
     - Show warning banner: "Quality evaluation is outdated -- content has changed since last assessment. Please re-run quality review before publishing."
     - Only show "Close" button (no publish option)
     - Return early from rest of gate logic

   **L1 check:**
   - If L1 not run (structure_check.passed === null): "Structure check has not been run. Run it first."
   - If L1 FAIL: red banner "Structure check failed: {issues}", only "Close" button

   **L2 check:**
   - If L2 not run (quality.score === null): "Quality assessment has not been run."
   - If L2 < 50: blocked -- dialog shows low scores + per i18n confirm.publishBlock, only "Close"
   - If L2 50-69: warning -- per i18n confirm.publishWarning, show low-scoring dimensions list, "Publish Anyway" + "Return to Edit" buttons
   - If L2 >= 70: success -- "Publish Skill" + "Cancel" buttons

   - "Publish Skill"/"Publish Anyway" calls onPublish
   - All button labels from i18n
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/shared/quality-radar-chart.tsx contains `QualityRadarChart` export
    - frontend/src/components/shared/quality-radar-chart.tsx contains `RadarChart` from recharts
    - frontend/src/components/shared/quality-radar-chart.tsx contains `aria-label`
    - frontend/src/components/shared/quality-score-card.tsx contains `QualityScoreCard` export
    - frontend/src/components/shared/quality-score-card.tsx contains `rationale` (dimension rationale display)
    - frontend/src/components/shared/publish-gate-dialog.tsx contains `PublishGateDialog` export
    - frontend/src/components/shared/publish-gate-dialog.tsx contains `is_stale` (staleness check)
    - frontend/src/components/shared/publish-gate-dialog.tsx contains `50` (threshold reference)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>QualityRadarChart with 6-dimension recharts visualization (D-11). QualityScoreCard with rationale display per dimension (addresses review transparency). PublishGateDialog with staleness detection + L1/L2 gate logic (D-12, addresses review publish race condition).</done>
</task>

<task type="auto">
  <name>Task 2: Complete Skill Editor with Quality tab, Settings tab, and Publish flow</name>
  <read_first>
    - frontend/src/pages/admin/skill-editor.tsx (current state from Plan 05 -- Content + Resources tabs)
    - frontend/src/components/shared/quality-radar-chart.tsx (just created)
    - frontend/src/components/shared/quality-score-card.tsx (just created)
    - frontend/src/components/shared/publish-gate-dialog.tsx (just created)
    - frontend/src/hooks/use-skills.ts (useCheckStructure, useEvaluateQuality, useSkillEvaluation, usePublishSkill, useUpdateSkill)
    - frontend/src/types/skill.ts (Skill, SkillEvaluationSummary)
    - .planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-UI-SPEC.md (quality tab and settings tab specs)
  </read_first>
  <files>
    frontend/src/pages/admin/skill-editor.tsx
  </files>
  <action>
1. Update `frontend/src/pages/admin/skill-editor.tsx` to replace Quality and Settings tab placeholders with full implementations:

   **Add hooks:** useCheckStructure(), useEvaluateQuality(), useSkillEvaluation(id), usePublishSkill()

   **Quality Tab (replace placeholder):**
   - Load evaluation with useSkillEvaluation(id)
   - If not evaluated: muted card with text from i18n "skill:quality.notEvaluated" + "Request Quality Review" button
   - "Request Quality Review" button handler:
     1. Run checkStructure.mutateAsync(id) -- immediate result
     2. Run evaluateQuality.mutateAsync(id) -- starts async
     3. Show toast "Quality review started"
     4. Refetch evaluation data (will show results when L2 completes)
   - L1 section: banner at top
     - Pass: green bg with Check icon + "Structure check passed"
     - Fail: red bg with XCircle icon + "Structure check failed" + issue list
   - L2 section (only if quality data available):
     - QualityRadarChart with dimensions
     - QualityScoreCards in 2-column grid (grid-cols-1 md:grid-cols-2 gap-4)
     - If evaluation is stale (is_stale === true): warning banner "Evaluation may be outdated. Content has changed since last assessment."
   - Dimension card expand/collapse: use local state array tracking which dimensions are expanded

   **Settings Tab (replace placeholder):**
   - Form using react-hook-form + zod for validation:
     - Name: Input, required, min 2 chars
     - Description: Textarea
     - Product: Input
     - Therapeutic Area: Input
     - Tags: Input (comma-separated)
     - Compatibility: Input (e.g. "Requires python3")
   - "Save" button calls useUpdateSkill with form values
   - Pre-populate form with skill data on load using useEffect + reset()

   **Wire Publish flow:**
   - "Publish Skill" button in page header: opens PublishGateDialog
   - Pass current evaluation data to dialog
   - On publish confirmation: call usePublishSkill().mutateAsync(id)
   - On success: toast success, navigate to /admin/skills
   - On error: toast error with message

   **Also wire "Save Draft" button:**
   - Collect current content from SopEditor local state + Settings form values
   - Call useUpdateSkill with combined data
   - Toast success on save
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc --noEmit --pretty 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/admin/skill-editor.tsx contains `QualityRadarChart`
    - frontend/src/pages/admin/skill-editor.tsx contains `QualityScoreCard`
    - frontend/src/pages/admin/skill-editor.tsx contains `PublishGateDialog`
    - frontend/src/pages/admin/skill-editor.tsx contains `useCheckStructure`
    - frontend/src/pages/admin/skill-editor.tsx contains `useEvaluateQuality`
    - frontend/src/pages/admin/skill-editor.tsx contains `usePublishSkill`
    - frontend/src/pages/admin/skill-editor.tsx contains `react-hook-form` or `useForm` (Settings tab)
    - frontend/src/pages/admin/skill-editor.tsx contains all 4 tab labels: `Content`, `Resources`, `Quality`, `Settings`
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Quality tab with L1 banner + L2 radar chart + dimension cards with rationale. Settings tab with react-hook-form metadata editing. Publish flow with PublishGateDialog including staleness detection. All 4 tabs functional. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evaluation data -> UI | AI-generated scores displayed to admin for publishing decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-20 | Elevation | Publish gate bypass via stale evaluation | mitigate | PublishGateDialog checks is_stale flag from backend. If stale, publish button is hidden. Backend also enforces staleness check in publish_skill (double protection). |
| T-19-21 | Tampering | Settings form XSS | accept | Form values sent as JSON to API, stored as DB fields. No HTML rendering of settings values. Tags are rendered as Badge components with text content only. |
</threat_model>

<verification>
```bash
cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend
npx tsc --noEmit
npm run build
```
</verification>

<success_criteria>
- Quality tab: L1 banner + L2 six-dimension radar chart (D-11) + dimension cards with rationale
- Staleness warning when evaluation is outdated (addresses review)
- Settings tab: metadata form with validation
- Publish gate: L1 PASS + L2 >= 50 + not stale (D-12)
- All 4 editor tabs fully functional
- TypeScript compiles, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-06-SUMMARY.md`
</output>
