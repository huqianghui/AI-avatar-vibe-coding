---
phase: 19-ai-coach-skill-module
plan: 05
type: execute
wave: 3
depends_on: ["19-02", "19-04"]
files_modified:
  - frontend/src/components/shared/sop-editor.tsx
  - frontend/src/components/shared/conversion-progress.tsx
  - frontend/src/components/shared/skill-material-uploader.tsx
  - frontend/src/components/shared/file-tree-view.tsx
  - frontend/src/pages/admin/skill-editor.tsx
autonomous: true
requirements:
  - D-09
  - D-17

must_haves:
  truths:
    - "Admin can edit SOP content in dual mode: manual Markdown editing and AI feedback-driven modification"
    - "AI feedback triggers regenerate-sop endpoint (available from Plan 02) and updates content"
    - "File tree shows VSCode-style collapsible directory structure of Skill package"
    - "Conversion progress indicator shows async status with polling"
    - "Skill editor page has working Content and Resources tabs"
  artifacts:
    - path: "frontend/src/components/shared/sop-editor.tsx"
      provides: "Dual-mode SOP editor (manual + AI feedback)"
      contains: "SopEditor"
    - path: "frontend/src/components/shared/file-tree-view.tsx"
      provides: "VSCode-style file tree component"
      contains: "FileTreeView"
    - path: "frontend/src/pages/admin/skill-editor.tsx"
      provides: "Skill editor page with Content and Resources tabs (Quality + Settings in Plan 06)"
      contains: "export default function SkillEditorPage"
  key_links:
    - from: "frontend/src/pages/admin/skill-editor.tsx"
      to: "frontend/src/hooks/use-skills.ts"
      via: "useSkill, useUpdateSkill, useRegenerateSop"
      pattern: "useSkill|useRegenerateSop"
    - from: "frontend/src/components/shared/sop-editor.tsx"
      to: "frontend/src/hooks/use-skills.ts"
      via: "AI regeneration callback"
      pattern: "regenerateSop"
---

<objective>
Build the Skill Editor MVP: SopEditor with dual-mode editing (manual + AI feedback), FileTreeView with VSCode-style navigation, ConversionProgress indicator, SkillMaterialUploader, and the editor page shell with Content and Resources tabs wired to backend APIs.

Addresses review: Split from original overloaded Plan 05 into MVP editor (this plan: Content + Resources) and advanced features (Plan 06: Quality + Settings + Publish flow). AI regeneration wired to endpoint from Plan 02 (dependency mismatch fixed).

Purpose: The Content and Resources tabs are the core editor experience. Splitting reduces scope concentration risk.
Output: Working editor with Content tab (SOP editing + AI feedback) and Resources tab (file tree + preview).
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-CONTEXT.md
@.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-UI-SPEC.md
@.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-01-SUMMARY.md
@.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-04-SUMMARY.md

<interfaces>
From frontend/src/types/skill.ts (Plan 04):
```typescript
export interface Skill extends SkillListItem {
  content: string;
  resources: SkillResource[];
  versions: SkillVersion[];
  conversion_error: string;
}
export interface SkillResource {
  id: string; skill_id: string; resource_type: ResourceType;
  filename: string; content_type: string; file_size: number;
}
export type ConversionStatus = "pending" | "processing" | "completed" | "failed";
```

From frontend/src/hooks/use-skills.ts (Plan 04):
```typescript
export const skillKeys = { all, lists, list, detail, conversion, evaluation };
export function useSkill(id): UseQueryResult<Skill>;
export function useUpdateSkill(): UseMutationResult;
export function useRegenerateSop(): UseMutationResult;
export function useConversionStatus(id, enabled): UseQueryResult;
export function useUploadResources(): UseMutationResult;
export function useStartConversion(): UseMutationResult;
```

From frontend/src/api/skills.ts (Plan 04):
```typescript
export async function regenerateSop(id: string, feedback: string);
export async function uploadAndConvert(id: string, files: File[]);
export async function downloadResource(skillId: string, resourceId: string);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SopEditor, ConversionProgress, SkillMaterialUploader components</name>
  <read_first>
    - frontend/src/pages/admin/hcp-profile-editor.tsx (tabbed editor pattern)
    - frontend/src/pages/admin/training-materials.tsx (material upload with react-dropzone)
    - frontend/src/components/ui/textarea.tsx (Textarea component)
    - frontend/src/components/ui/progress.tsx (Progress component)
    - frontend/src/hooks/use-skills.ts (useRegenerateSop, useConversionStatus hooks)
    - .planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-UI-SPEC.md (interaction contracts for SOP editing and conversion)
  </read_first>
  <files>
    frontend/src/components/shared/sop-editor.tsx
    frontend/src/components/shared/conversion-progress.tsx
    frontend/src/components/shared/skill-material-uploader.tsx
  </files>
  <action>
1. Create `frontend/src/components/shared/sop-editor.tsx`:
   - Props: `{ content: string; onChange: (content: string) => void; onAiRegenerate: (feedback: string) => Promise<void>; isRegenerating?: boolean; readOnly?: boolean }`
   - Layout per UI-SPEC Content tab (2-column grid, 3fr/2fr):
     - Left column (60%):
       - Textarea for Markdown editing: font-mono, min-h-[500px], placeholder text
       - Below textarea -- AI feedback section (D-09 dual-mode):
         - Textarea: placeholder from i18n "skill:actions.aiFeedbackPlaceholder" = "Describe changes you want to make to the SOP..."
         - "Apply Changes" Button (primary variant, disabled when feedback empty or isRegenerating)
         - When clicked: call onAiRegenerate(feedbackText), clear feedback on success
         - Loading: Button shows spinner, textarea disabled during regeneration
     - Right column (40%):
       - Rendered Markdown preview using react-markdown + rehype-raw (already installed)
       - Wrap in ScrollArea for overflow handling
       - Updates in real-time as user types in left column
       - XSS mitigation: react-markdown sanitizes by default (addresses review: Markdown XSS)
   - Highlight animation on AI update: parent sets a `highlighted` state, SopEditor applies `bg-primary/5 transition-colors duration-[1500ms]` class that fades
   - useTranslation(["skill"]) for all labels

2. Create `frontend/src/components/shared/conversion-progress.tsx`:
   - Props: `{ status: ConversionStatus | null; error?: string; onRetry?: () => void }`
   - Status-based rendering:
     - null/undefined: render nothing (return null)
     - "pending": Progress indeterminate + purple text "Converting..."
     - "processing": Progress indeterminate + text from i18n "skill:conversion.processing"
     - "completed": green Badge "Completed" + success message from i18n
     - "failed": destructive Banner with error message + "Retry Conversion" Button (D-08)
   - aria-live="polite" on status region for accessibility

3. Create `frontend/src/components/shared/skill-material-uploader.tsx`:
   - Props: `{ onUpload: (files: File[]) => void; isUploading?: boolean; acceptedTypes?: string[] }`
   - Use react-dropzone (same pattern as training-materials.tsx):
     - Accept: .pdf, .docx, .pptx, .txt, .md (D-05)
     - Max file size: 50MB, max files: 10
     - Dashed border drop zone with Upload icon from lucide-react
     - File list below zone showing selected files (name, size, type icon)
     - "Upload and Convert" primary button (disabled when no files or isUploading)
     - Remove individual file from selection before upload
   - useTranslation(["skill"]) for labels
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/shared/sop-editor.tsx contains `SopEditor` export
    - frontend/src/components/shared/sop-editor.tsx contains `onAiRegenerate` in props
    - frontend/src/components/shared/sop-editor.tsx imports react-markdown or uses ReactMarkdown
    - frontend/src/components/shared/conversion-progress.tsx contains `ConversionProgress` export
    - frontend/src/components/shared/conversion-progress.tsx contains `aria-live`
    - frontend/src/components/shared/skill-material-uploader.tsx contains `react-dropzone` import
    - frontend/src/components/shared/skill-material-uploader.tsx contains `50` (MB limit reference)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SopEditor with dual mode (manual Markdown + AI feedback per D-09), wired to regenerateSop API. ConversionProgress with all status states and retry (D-08). SkillMaterialUploader with dropzone and file validation. All type-safe.</done>
</task>

<task type="auto">
  <name>Task 2: FileTreeView and Skill Editor page with Content + Resources tabs</name>
  <read_first>
    - frontend/src/pages/admin/hcp-profile-editor.tsx (tabbed editor with useParams)
    - frontend/src/components/shared/sop-editor.tsx (just created)
    - frontend/src/components/shared/conversion-progress.tsx (just created)
    - frontend/src/components/shared/skill-material-uploader.tsx (just created)
    - frontend/src/hooks/use-skills.ts (all hooks including skillKeys)
    - frontend/src/types/skill.ts (Skill, SkillResource types)
    - frontend/src/components/ui/tabs.tsx (Tabs component)
    - frontend/src/components/ui/scroll-area.tsx (ScrollArea)
    - .planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-UI-SPEC.md (editor layout and file tree specs)
  </read_first>
  <files>
    frontend/src/components/shared/file-tree-view.tsx
    frontend/src/pages/admin/skill-editor.tsx
  </files>
  <action>
1. Create `frontend/src/components/shared/file-tree-view.tsx`:
   - Props: `{ resources: SkillResource[]; onSelectFile: (resource: SkillResource | "SKILL.md") => void; selectedId?: string; onUpload?: (files: File[], type: ResourceType) => void }`
   - Build tree from resources:
     - Virtual root "SKILL.md" node (always present)
     - Group by resource_type: "references/", "scripts/", "assets/"
     - Only show directories that have files
   - TreeNode sub-component:
     - Folder: ChevronRight icon (rotates on expand via CSS transform), FolderOpen/Folder icon, name, click toggles expand
     - File: type-specific icon from lucide-react (FileText for .md/.txt/.pdf, Presentation for .pptx, FileCode for .py, File default), filename, file_size formatted (KB/MB)
     - Selected: bg-primary/5 border-l-2 border-primary per UI-SPEC
     - Indent: 20px per level per UI-SPEC spacing exception
   - Keyboard navigation: ArrowUp/Down to move selection, ArrowRight to expand folder, ArrowLeft to collapse, Enter to select file
   - aria-expanded on folder nodes, aria-selected on active file
   - Upload button at top (only if onUpload provided)

2. Replace placeholder `frontend/src/pages/admin/skill-editor.tsx` with full editor:
   - Default export: `SkillEditorPage`
   - Use useParams() for `id` (undefined = new skill)
   - Hooks: useSkill(id), useUpdateSkill(), useCreateSkill(), useRegenerateSop(), useConversionStatus(id, isConverting), useUploadResources(), useStartConversion()
   - Page header per UI-SPEC:
     - Back link: ArrowLeft + "Back to Skill Hub" -> navigate /admin/skills
     - Title: "Skill: {name}" or "Create New Skill"
     - "Save Draft" outline Button, "Publish Skill" primary Button (Publish disabled until Plan 06 adds PublishGateDialog)
   - Tabs: Content | Resources | Quality | Settings
     - Quality and Settings tab content: placeholder text "Coming soon" (implemented in Plan 06)

   **Content Tab:**
   - If new skill (no id, no content): show SkillMaterialUploader
     - On upload: createSkill first (if new), then uploadAndConvert, poll conversion status
     - Also show "or create empty skill" link that creates skill with empty content
   - If converting (conversion_status pending/processing): show ConversionProgress with polling
     - useConversionStatus(id, true) with refetchInterval: 3000
     - On completed: refetch skill data to load generated content
   - If content exists: show SopEditor
     - onChange: local state, save on "Save Draft" button click
     - onAiRegenerate: call useRegenerateSop().mutateAsync({id, feedback}), refetch skill
     - Highlight animation: set highlightedRef on successful regeneration, clear after 1500ms

   **Resources Tab:**
   - 2-column layout (30%/70% per UI-SPEC)
   - Left: FileTreeView with skill.resources
   - Right: File preview panel based on selection:
     - "SKILL.md" selected: render skill.content as Markdown via react-markdown
     - Reference file: file info card (filename, size, type, upload date) + "Download" button
     - Script file: render text_content or filename in monospace `<pre>` block
     - Asset file: file info card + "Download" button
     - No selection: centered muted text from i18n "skill:fileTree.selectFile"
   - Download uses downloadResource API function (opens blob URL)
   - Upload button in FileTreeView triggers useUploadResources mutation

   - useTranslation(["skill", "common"]) for all text
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc --noEmit --pretty 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/shared/file-tree-view.tsx contains `FileTreeView` export
    - frontend/src/components/shared/file-tree-view.tsx contains `aria-expanded`
    - frontend/src/components/shared/file-tree-view.tsx contains `20` (px indent reference)
    - frontend/src/pages/admin/skill-editor.tsx contains `export default function SkillEditorPage`
    - frontend/src/pages/admin/skill-editor.tsx contains `SopEditor`
    - frontend/src/pages/admin/skill-editor.tsx contains `FileTreeView`
    - frontend/src/pages/admin/skill-editor.tsx contains `ConversionProgress`
    - frontend/src/pages/admin/skill-editor.tsx contains `useRegenerateSop` or `regenerateSop`
    - frontend/src/pages/admin/skill-editor.tsx contains `Content` and `Resources` tab labels
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>FileTreeView with VSCode-style tree, keyboard nav, accessibility (D-17). Skill Editor with Content tab (upload/convert/SOP edit/AI feedback D-09) and Resources tab (file tree + preview). Quality and Settings tabs as placeholders for Plan 06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> Markdown preview | Content from textarea rendered in preview panel |
| AI feedback -> API | User feedback text sent to regenerate-sop endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-18 | Tampering | SOP Markdown rendering | mitigate | react-markdown sanitizes HTML by default. rehype-raw used but react-markdown's sanitization is active. No dangerouslySetInnerHTML. |
| T-19-19 | Information Disclosure | File download | mitigate | Download URLs go through authenticated API (/api/v1/skills/{id}/resources/{rid}/download), require admin role server-side. |
</threat_model>

<verification>
```bash
cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend
npx tsc --noEmit
npm run build
```
</verification>

<success_criteria>
- SopEditor: manual edit + AI feedback (D-09), wired to regenerate-sop API (dependency fixed)
- FileTreeView: VSCode-style with keyboard nav (D-17), accessibility
- ConversionProgress: status indicator with retry (D-08)
- Editor Content tab: upload -> convert -> edit flow
- Editor Resources tab: file tree + preview
- TypeScript compiles, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-05-SUMMARY.md`
</output>
