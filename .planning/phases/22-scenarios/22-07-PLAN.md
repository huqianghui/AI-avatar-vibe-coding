---
phase: 22-scenarios
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/scenario.py
  - backend/app/schemas/scenario.py
  - backend/alembic/versions/s22b_scenario_tags_migration.py
  - backend/alembic/versions/s22c_skill_id_not_null.py
autonomous: true
gap_closure: true
requirements: []
must_haves:
  truths:
    - "Scenario model has tags JSON column and no product/therapeutic_area columns"
    - "skill_id is Mapped[str] with nullable=False and ondelete=RESTRICT"
    - "status field comment includes 'archived' as valid value"
    - "ScenarioCreate requires tags list and skill_id (not nullable)"
    - "ScenarioUpdate does NOT contain status field"
    - "Migration chain is sequential: q20a -> s22b -> s22c"
  artifacts:
    - path: "backend/app/models/scenario.py"
      provides: "Updated Scenario ORM model with tags, NOT NULL skill_id, no product/therapeutic_area"
      contains: "tags"
    - path: "backend/app/schemas/scenario.py"
      provides: "Updated Pydantic schemas with tags, required skill_id, no status in Update"
      contains: "tags: list[str]"
    - path: "backend/alembic/versions/s22b_scenario_tags_migration.py"
      provides: "Tags migration with correct down_revision pointing to q20a00000001"
    - path: "backend/alembic/versions/s22c_skill_id_not_null.py"
      provides: "skill_id NOT NULL migration with down_revision pointing to s22b00000001"
  key_links:
    - from: "backend/app/models/scenario.py"
      to: "backend/alembic/versions/s22b_scenario_tags_migration.py"
      via: "model columns match migration schema"
      pattern: "tags.*Text"
---

<objective>
Fix backend Scenario model, schemas, and Alembic migration chain to implement D-03 (tags system), D-04 (archived status support in model), and D-05 (skill_id NOT NULL).

Purpose: The model and schemas are the foundation for all subsequent service/API/frontend work. Migration chain must be sequential and valid before any other changes.
Output: Updated model, schemas, and fixed migration files with correct chain ordering.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-scenarios/22-CONTEXT.md
@.planning/phases/22-scenarios/22-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Scenario model and schemas</name>
  <files>backend/app/models/scenario.py, backend/app/schemas/scenario.py</files>
  <read_first>
    - backend/app/models/scenario.py
    - backend/app/schemas/scenario.py
    - backend/app/models/base.py
  </read_first>
  <action>
Update backend/app/models/scenario.py:
1. REMOVE these two lines (product and therapeutic_area mapped columns):
   - `product: Mapped[str] = mapped_column(String(255), nullable=False)`
   - `therapeutic_area: Mapped[str] = mapped_column(String(255), default="")`
2. ADD tags column AFTER description:
   `tags: Mapped[str] = mapped_column(Text, default="[]")  # JSON array of tag strings`
3. Change status comment to: `# draft / active / archived`
4. Change skill_id from nullable to NOT NULL with RESTRICT:
   ```python
   skill_id: Mapped[str] = mapped_column(
       String(36), ForeignKey("skills.id", ondelete="RESTRICT"), nullable=False
   )
   ```
   (Remove `| None` from type hint, remove `default=None`)
5. Keep skill_version_id as nullable (version pinning is optional initially)

Update backend/app/schemas/scenario.py:
1. In ScenarioCreate:
   - REMOVE `product: str` field
   - REMOVE `therapeutic_area: str = ""` field
   - ADD `tags: list[str] = []` field
   - Change `skill_id: str | None = None` to `skill_id: str` (required, no default)
   - REMOVE `status: str = "draft"` field (status set via transition only, default in model)
2. In ScenarioUpdate:
   - REMOVE `product: str | None = None` field
   - REMOVE `therapeutic_area: str | None = None` field
   - ADD `tags: list[str] | None = None` field
   - REMOVE `status: str | None = None` field (status changes via transition endpoint only)
   - Keep `skill_id: str | None = None` (optional for partial update, but must be non-empty if provided)
3. In ScenarioResponse:
   - REMOVE `product: str` field
   - REMOVE `therapeutic_area: str` field
   - ADD `tags: str` field (raw JSON string from DB, will be parsed in API layer)
   - Change `skill_id: str | None = None` to `skill_id: str`
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend && grep -c "product" app/models/scenario.py | grep "^0$" && grep "tags" app/models/scenario.py && grep "nullable=False" app/models/scenario.py | grep "skill" && grep -c "status" app/schemas/scenario.py | head -1 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/models/scenario.py does NOT contain "product" or "therapeutic_area" as mapped_column
    - backend/app/models/scenario.py contains `tags: Mapped[str] = mapped_column(Text, default="[]")`
    - backend/app/models/scenario.py contains `skill_id: Mapped[str] = mapped_column(` with `nullable=False` and `ondelete="RESTRICT"`
    - backend/app/models/scenario.py status comment contains "archived"
    - backend/app/schemas/scenario.py ScenarioCreate has `tags: list[str] = []` and `skill_id: str` (no None, no default)
    - backend/app/schemas/scenario.py ScenarioUpdate has `tags: list[str] | None = None` and does NOT have `status` field
    - backend/app/schemas/scenario.py ScenarioResponse has `tags: str` and `skill_id: str` (not Optional)
  </acceptance_criteria>
  <done>Scenario model reflects tags system, required skill_id with RESTRICT, and archived-capable status. Schemas match model with no status in Update.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Alembic migration chain (s22b and s22c)</name>
  <files>backend/alembic/versions/s22b_scenario_tags_migration.py, backend/alembic/versions/s22c_skill_id_not_null.py</files>
  <read_first>
    - backend/alembic/versions/s22b_scenario_tags_migration.py
    - backend/alembic/versions/s22c_skill_id_not_null.py
    - backend/alembic/versions/q20a_add_dry_run_tables.py
  </read_first>
  <action>
Fix the migration chain so it is sequential: q20a00000001 -> s22b00000001 -> s22c_skill_id_not_null

1. In backend/alembic/versions/s22b_scenario_tags_migration.py:
   - Change `down_revision: str = "ed6e59a95958"` to `down_revision: str = "q20a00000001"`
   - Verify the upgrade() logic:
     - Step 1: Add tags column (Text, server_default="[]", nullable=False)
     - Step 2: Migrate data from product/therapeutic_area to tags JSON array (format: `["product:X", "area:Y"]`)
     - Step 3: Drop product and therapeutic_area columns using batch_alter_table
   - Verify the downgrade() logic reverses correctly (add product/therapeutic_area back, migrate data from tags, drop tags)

2. In backend/alembic/versions/s22c_skill_id_not_null.py:
   - Change `down_revision = "q20a_add_dry_run_tables"` to `down_revision = "s22b00000001"`
   - Verify the upgrade() logic:
     - Pre-check: COUNT scenarios WHERE skill_id IS NULL, raise if > 0
     - batch_alter_table: alter_column skill_id nullable=False
     - batch_alter_table: drop existing FK constraint, re-create with ondelete="RESTRICT"
   - Verify the downgrade() reverses (nullable=True, ondelete="SET NULL")

The chain must be: q20a00000001 -> s22b00000001 -> s22c_skill_id_not_null (linear, no branches).
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend && grep "down_revision" alembic/versions/s22b_scenario_tags_migration.py | grep "q20a00000001" && grep "down_revision" alembic/versions/s22c_skill_id_not_null.py | grep "s22b00000001" && echo "CHAIN VALID"</automated>
  </verify>
  <acceptance_criteria>
    - s22b_scenario_tags_migration.py has `down_revision: str = "q20a00000001"`
    - s22c_skill_id_not_null.py has `down_revision = "s22b00000001"`
    - s22b upgrade() adds tags column, migrates data from product/therapeutic_area, drops old columns
    - s22c upgrade() pre-checks NULL skill_ids, then alters to nullable=False with RESTRICT
    - Both files use batch_alter_table for SQLite compatibility
  </acceptance_criteria>
  <done>Migration chain is linear q20a -> s22b -> s22c with no conflicts. Both migrations use batch operations for SQLite compatibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migrations | Schema changes directly modify database structure |
| Model layer | ORM constraints enforce data integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07-01 | Tampering | Alembic migration data migration step | mitigate | Pre-check NULL skill_ids before NOT NULL constraint; fail migration with helpful message if data is invalid |
| T-22-07-02 | Denial of Service | Migration on large dataset | accept | Dataset is small (admin-created scenarios); batch operations handle SQLite limits |
| T-22-07-03 | Information Disclosure | Tags field storing former product data | accept | Tags are visible to admin users who already had access to product field |
</threat_model>

<verification>
- `grep "tags" backend/app/models/scenario.py` returns the tags column definition
- `grep -c "product" backend/app/models/scenario.py` returns 0
- `grep "down_revision" backend/alembic/versions/s22b*.py` shows q20a00000001
- `grep "down_revision" backend/alembic/versions/s22c*.py` shows s22b00000001
- `grep "RESTRICT" backend/app/models/scenario.py` shows ondelete constraint
</verification>

<success_criteria>
Backend model and schemas correctly reflect the tags system (D-03), archived status (D-04), and required skill_id (D-05). Migration chain is valid and sequential.
</success_criteria>

<output>
After completion, create `.planning/phases/22-scenarios/22-07-SUMMARY.md`
</output>
