---
phase: 01.1-ui-figma-alignment
plan: 04
type: execute
wave: 3
depends_on: ["01.1-01", "01.1-03"]
files_modified:
  - frontend/src/pages/user/dashboard.tsx
  - frontend/src/pages/user/training.tsx
  - frontend/src/router/index.tsx
autonomous: true
requirements:
  - UI-04
  - UI-05

must_haves:
  truths:
    - "User dashboard displays 4 stat cards, 5 recent training sessions, 2 action cards, and 1 recommended scenario per Figma"
    - "Scenario selection page displays 6 HCP cards in a 3-column grid with tab navigation and filter dropdowns"
    - "Both pages use i18n for all user-facing text"
    - "Route /user/training renders the scenario selection page"
    - "Both pages render with mock data (no backend API calls)"
  artifacts:
    - path: "frontend/src/pages/user/dashboard.tsx"
      provides: "Full Figma-matching dashboard with StatCards, SessionItems, ActionCards"
      contains: "StatCard"
    - path: "frontend/src/pages/user/training.tsx"
      provides: "Scenario selection page with tabs, filters, HCP card grid"
      contains: "HCPProfileCard"
    - path: "frontend/src/router/index.tsx"
      provides: "Route registration for /user/training"
      contains: "training"
  key_links:
    - from: "frontend/src/pages/user/dashboard.tsx"
      to: "frontend/src/components/shared/stat-card.tsx"
      via: "import and render"
      pattern: "import.*StatCard.*from.*@/components/shared"
    - from: "frontend/src/pages/user/training.tsx"
      to: "frontend/src/components/shared/hcp-profile-card.tsx"
      via: "import and render in grid"
      pattern: "import.*HCPProfileCard.*from.*@/components/shared"
    - from: "frontend/src/router/index.tsx"
      to: "frontend/src/pages/user/training.tsx"
      via: "route definition"
      pattern: "path:.*training.*element:.*ScenarioSelection"
---

<objective>
Build the User Dashboard page (replace EmptyState with full Figma layout) and Scenario Selection page (new page with HCP card grid), then register the training route.

Purpose: These two pages are the primary user-facing screens. Dashboard is the landing page after login; Scenario Selection is the entry point to training.
Output: Full dashboard matching Figma, scenario selection page with 6 HCP cards, route wired.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.1-ui-figma-figma-make-ui-sceenshot-figma-make/01.1-RESEARCH.md
@.planning/phases/01.1-ui-figma-figma-make-ui-sceenshot-figma-make/01.1-UI-SPEC.md
@figma-make/Medical Representative Dashboard/Medical Representative Dashboard_Page.png
@figma-make/Scenario Selection Page Design/Scenario Selection Page.png

<interfaces>
From frontend/src/components/shared/index.ts (available after Plan 03):
```typescript
export { StatCard } from "./stat-card";
export { SessionItem } from "./session-item";
export { ActionCard } from "./action-card";
export { RecommendedScenario } from "./recommended-scenario";
export { MiniRadarChart, MiniTrendChart } from "./mini-charts";
export { HCPProfileCard } from "./hcp-profile-card";
```

From frontend/src/components/ui/index.ts:
```typescript
export { Tabs, TabsList, TabsTrigger, TabsContent } from "./tabs";
export { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "./select";
export { Input } from "./input";
export { Badge } from "./badge";
```

From frontend/src/router/index.tsx (current):
```typescript
{ path: "/user", element: <UserLayout />, children: [
  { path: "dashboard", element: <UserDashboard /> },
  // Phase 2+: training, history, reports
]}
```

From frontend/src/stores/auth-store.ts:
```typescript
export function useAuthStore(): { user: User | null; ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite user dashboard page with full Figma layout and mock data</name>
  <files>frontend/src/pages/user/dashboard.tsx</files>
  <read_first>
    frontend/src/pages/user/dashboard.tsx
    frontend/src/components/shared/index.ts
    frontend/src/components/ui/index.ts
    frontend/src/stores/auth-store.ts
    frontend/public/locales/en-US/dashboard.json
    .planning/phases/01.1-ui-figma-figma-make-ui-sceenshot-figma-make/01.1-UI-SPEC.md
  </read_first>
  <action>
    Completely rewrite `frontend/src/pages/user/dashboard.tsx` to match the Medical Representative Dashboard Figma screenshot.

    **Imports:**
    ```typescript
    import { useNavigate } from "react-router-dom";
    import { useTranslation } from "react-i18next";
    import { CheckCircle, Target, Calendar, TrendingUp, Users, Mic } from "lucide-react";
    import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui";
    import { StatCard, SessionItem, ActionCard, RecommendedScenario, MiniRadarChart, MiniTrendChart } from "@/components/shared";
    import { useAuthStore } from "@/stores/auth-store";
    ```

    **Mock data** (at top of file with `// TODO: Replace with TanStack Query hook in Phase 2` comment):
    ```typescript
    const mockStats = [
      { label: "sessionsCompleted", value: 24, icon: CheckCircle, trend: { value: "+3 this week", direction: "up" as const } },
      { label: "averageScore", value: 78, icon: Target },
      { label: "thisWeek", value: 5, icon: Calendar, progress: { current: 5, total: 7 } },
      { label: "improvement", value: "+12%", icon: TrendingUp },
    ];

    const mockSessions = [
      { id: "1", hcpName: "Dr. Sarah Mitchell", specialty: "Cardiology", mode: "F2F" as const, score: 85, timeAgo: "2 hours ago" },
      { id: "2", hcpName: "Dr. James Wong", specialty: "Oncology", mode: "Conference" as const, score: 72, timeAgo: "5 hours ago" },
      { id: "3", hcpName: "Dr. Michael Chen", specialty: "Neurology", mode: "F2F" as const, score: 92, timeAgo: "1 day ago" },
      { id: "4", hcpName: "Dr. Emily Roberts", specialty: "Endocrinology", mode: "Conference" as const, score: 55, timeAgo: "2 days ago" },
      { id: "5", hcpName: "Dr. Robert Thompson", specialty: "Rheumatology", mode: "F2F" as const, score: 88, timeAgo: "3 days ago" },
    ];
    ```

    **Layout structure** (per UI-SPEC Dashboard Layout):
    1. Welcome header: `text-3xl font-semibold` with user name from useAuthStore, subtitle from `dashboard:overview`
    2. Row 1: 4-column grid of StatCards (`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-6`). Each stat card uses `t("dashboard:" + stat.label)` for i18n label. Stats 1 and 4 get MiniTrendChart. Stat 2 gets MiniRadarChart.
    3. Row 2: 2-section grid (`grid grid-cols-1 lg:grid-cols-5 gap-6`):
       - Left (lg:col-span-3): Card with "Recent Training Sessions" heading + "View All" link. List of 5 SessionItem components. Each clickable (no-op in this phase).
       - Right (lg:col-span-2): Two ActionCards (F2F blue gradient with Users icon, Conference purple gradient with Mic icon), each with "Start" button navigating to `/user/training`. Below: RecommendedScenario card (Dr. Amanda Hayes, Intermediate).

    **Card styling:** `bg-card rounded-lg shadow-sm border border-border`

    **All text via i18n:** Use `useTranslation("dashboard")` for page text, `useTranslation("common")` for shared text like "Start Training".
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/user/dashboard.tsx contains "StatCard" (imported and rendered)
    - frontend/src/pages/user/dashboard.tsx contains "SessionItem" (imported and rendered)
    - frontend/src/pages/user/dashboard.tsx contains "ActionCard" (imported and rendered)
    - frontend/src/pages/user/dashboard.tsx contains "RecommendedScenario" (imported and rendered)
    - frontend/src/pages/user/dashboard.tsx contains "useTranslation" (i18n)
    - frontend/src/pages/user/dashboard.tsx contains "TODO: Replace with TanStack Query" (mock data marker)
    - frontend/src/pages/user/dashboard.tsx contains "useAuthStore" (user name for welcome)
    - frontend/src/pages/user/dashboard.tsx contains "grid-cols-4" or "lg:grid-cols-4" (4-column stat grid)
    - frontend/src/pages/user/dashboard.tsx contains "Dr. Sarah Mitchell" (mock data)
    - frontend/src/pages/user/dashboard.tsx does NOT contain "EmptyState" (replaced)
  </acceptance_criteria>
  <done>Dashboard shows 4 stat cards, 5 recent sessions, 2 action cards, 1 recommended scenario with mock data and i18n, matching Figma layout</done>
</task>

<task type="auto">
  <name>Task 2: Create scenario selection page and register training route</name>
  <files>
    frontend/src/pages/user/training.tsx
    frontend/src/router/index.tsx
  </files>
  <read_first>
    frontend/src/router/index.tsx
    frontend/src/components/shared/index.ts
    frontend/src/components/ui/index.ts
    frontend/public/locales/en-US/training.json
    .planning/phases/01.1-ui-figma-figma-make-ui-sceenshot-figma-make/01.1-UI-SPEC.md
  </read_first>
  <action>
    **1. Create `frontend/src/pages/user/training.tsx`:**

    Imports:
    ```typescript
    import { useState } from "react";
    import { useNavigate } from "react-router-dom";
    import { useTranslation } from "react-i18next";
    import { Search } from "lucide-react";
    import { Input, Tabs, TabsList, TabsTrigger, TabsContent, Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui";
    import { HCPProfileCard } from "@/components/shared";
    ```

    Mock data at top (with `// TODO: Replace with TanStack Query hook in Phase 2`):
    ```typescript
    const mockHCPs = [
      { id: "1", name: "Dr. Wang Wei", nameZh: "王伟", specialty: "Oncologist", hospital: "Beijing Cancer Hospital", personality: ["Skeptical", "Detail-oriented"], difficulty: "Hard" as const, product: "PD-1 Inhibitor", avatar: undefined },
      { id: "2", name: "Dr. Li Na", nameZh: "李娜", specialty: "Cardiologist", hospital: "Shanghai Cardiovascular Center", personality: ["Friendly"], difficulty: "Easy" as const, product: "ACE Inhibitor", avatar: undefined },
      { id: "3", name: "Dr. Zhang Ming", nameZh: "张明", specialty: "Neurologist", hospital: "Guangzhou General Hospital", personality: ["Busy"], difficulty: "Medium" as const, product: "Migraine Treatment", avatar: undefined },
      { id: "4", name: "Dr. Chen Hui", nameZh: "陈慧", specialty: "Pulmonologist", hospital: "Shenzhen Respiratory Institute", personality: ["Detail-oriented"], difficulty: "Medium" as const, product: "COPD Inhaler", avatar: undefined },
      { id: "5", name: "Dr. Liu Yang", nameZh: "刘洋", specialty: "Endocrinologist", hospital: "Hangzhou Diabetes Center", personality: ["Skeptical"], difficulty: "Hard" as const, product: "GLP-1 Agonist", avatar: undefined },
      { id: "6", name: "Dr. Zhao Lin", nameZh: "赵琳", specialty: "Hematologist", hospital: "Nanjing Blood Disease Hospital", personality: ["Friendly"], difficulty: "Easy" as const, product: "Anticoagulant Therapy", avatar: undefined },
    ];
    ```

    Component structure per Figma Scenario Selection screenshot and UI-SPEC:
    1. Page title: `<h1 className="text-3xl font-semibold mb-4">{t("selectScenario")}</h1>`
    2. Tabs: `<Tabs defaultValue="f2f">` with TabsTrigger "f2f" = `t("f2fTraining")` and "conference" = `t("conferenceTraining")`
    3. Filter row inside TabsContent: `flex flex-wrap gap-4 mb-8`
       - Select for Products (`t("allProducts")`), Difficulties (`t("allDifficulties")`), Specialties (`t("allSpecialties")`)
       - Search Input with Search icon placeholder `t("search")`
       - Wire filter state with useState for searchTerm, selectedProduct, selectedDifficulty, selectedSpecialty
       - Apply client-side filtering on mockHCPs
    4. Card grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
    5. Each card: `<HCPProfileCard {...hcp} onStartTraining={() => navigate("/user/training/session")} />`

    Export as default: `export default function ScenarioSelection()`

    **2. Update `frontend/src/router/index.tsx`:**

    Add import at top:
    ```typescript
    import ScenarioSelection from "@/pages/user/training";
    ```

    Add route inside the UserLayout children array, after the dashboard route:
    ```typescript
    { path: "training", element: <ScenarioSelection /> },
    ```

    Keep the existing comment `// Phase 2+: training, history, reports` but move it after the training route. The result should be:
    ```typescript
    children: [
      { path: "dashboard", element: <UserDashboard /> },
      { path: "training", element: <ScenarioSelection /> },
      // Phase 2+: history, reports
    ],
    ```
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/user/training.tsx contains "export default function ScenarioSelection"
    - frontend/src/pages/user/training.tsx contains "HCPProfileCard" (imported and rendered)
    - frontend/src/pages/user/training.tsx contains "useTranslation" (i18n)
    - frontend/src/pages/user/training.tsx contains "Dr. Wang Wei" (mock data)
    - frontend/src/pages/user/training.tsx contains "Tabs" (tab navigation)
    - frontend/src/pages/user/training.tsx contains "Select" (filter dropdowns)
    - frontend/src/pages/user/training.tsx contains "lg:grid-cols-3" (3-column grid)
    - frontend/src/pages/user/training.tsx contains "TODO: Replace with TanStack Query" (mock data marker)
    - frontend/src/router/index.tsx contains "ScenarioSelection"
    - frontend/src/router/index.tsx contains 'path: "training"'
  </acceptance_criteria>
  <done>Scenario selection page created with 6 HCP cards, tab navigation, filter dropdowns, search, and client-side filtering. Route /user/training registered.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc -b --noEmit` passes
2. `cd frontend && npm run build` completes
3. Dashboard page renders 4 stat cards, 5 sessions, 2 action cards, 1 recommended scenario
4. Scenario selection page renders 6 HCP cards in 3-column grid
5. Route /user/training is registered and accessible
6. All text uses i18n keys
</verification>

<success_criteria>
- Dashboard replaces EmptyState with full Figma-matching layout
- Scenario Selection is a new page with tabs, filters, 3-column HCP card grid
- Route /user/training is wired and navigable from dashboard action cards
- All mock data has TODO comments for Phase 2 replacement
- TypeScript builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ui-figma-figma-make-ui-sceenshot-figma-make/01.1-04-SUMMARY.md`
</output>
