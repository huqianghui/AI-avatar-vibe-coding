---
phase: 01.1-ui-figma-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/login.tsx
  - frontend/src/components/layouts/auth-layout.tsx
autonomous: true
requirements:
  - UI-02

must_haves:
  truths:
    - "Login page displays a blue gradient square with lightbulb SVG icon instead of plain 'AI' text"
    - "Language switcher is positioned in the top-right corner of the login page"
    - "Copyright text is at the bottom center of the page, outside the card"
    - "Login card has shadow-xl and rounded-2xl styling"
    - "Background gradient includes via-white (3-stop gradient)"
  artifacts:
    - path: "frontend/src/pages/login.tsx"
      provides: "Figma-aligned login page with SVG logo"
      contains: "svg"
    - path: "frontend/src/components/layouts/auth-layout.tsx"
      provides: "Auth layout with top-right language switcher and bottom copyright"
      contains: "top-6 right-6"
  key_links:
    - from: "frontend/src/components/layouts/auth-layout.tsx"
      to: "frontend/src/components/shared/language-switcher.tsx"
      via: "import and render in top-right position"
      pattern: "LanguageSwitcher"
---

<objective>
Polish the login page and auth layout to match the Figma screenshot: update logo to blue gradient square with lightbulb SVG, move language switcher to top-right, move copyright to bottom center outside card, add shadow-xl + rounded-2xl to card, fix background gradient to 3-stop.

Purpose: Visual alignment of the existing login page with Figma design. Small delta from current state (structure is correct, just visual tweaks).
Output: Updated login.tsx and auth-layout.tsx matching the Login_Page.png screenshot.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.1-ui-figma-figma-make-ui-sceenshot-figma-make/01.1-UI-SPEC.md
@figma-make/Design Login and Layout Shell/Login_Page.png

<interfaces>
From frontend/src/pages/login.tsx (current state):
```typescript
// Logo: plain "AI" text in primary square div
<div className="flex size-12 items-center justify-center rounded-xl bg-primary text-primary-foreground text-lg font-bold">AI</div>
// Card: no shadow-xl, no rounded-2xl
<Card className="w-full max-w-[480px]">
// Copyright: inside card at bottom of form
<p className="text-center text-xs text-muted-foreground">{t("copyright")}</p>
```

From frontend/src/components/layouts/auth-layout.tsx (current state):
```typescript
// Gradient: only 2-stop (from-blue-50 to-white)
<div className="relative flex min-h-screen items-center justify-center bg-gradient-to-br from-blue-50 to-white p-4">
// Language switcher: bottom-right
<div className="fixed bottom-4 right-4"><LanguageSwitcher /></div>
// No copyright element in layout
```

From frontend/src/hooks/use-auth.ts:
```typescript
export function useLogin(): UseMutationResult<LoginResponse, Error, LoginCredentials>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update auth-layout.tsx -- gradient, language switcher position, copyright footer</name>
  <files>frontend/src/components/layouts/auth-layout.tsx</files>
  <read_first>
    frontend/src/components/layouts/auth-layout.tsx
    frontend/src/components/shared/language-switcher.tsx
    .planning/phases/01.1-ui-figma-figma-make-ui-sceenshot-figma-make/01.1-UI-SPEC.md
  </read_first>
  <action>
    Rewrite `frontend/src/components/layouts/auth-layout.tsx` to:

    1. Change the outer div background from `bg-gradient-to-br from-blue-50 to-white` to `bg-gradient-to-br from-blue-50 via-white to-blue-50` (3-stop gradient per Figma)

    2. Move LanguageSwitcher from `fixed bottom-4 right-4` to `fixed top-6 right-6`. Wrap it in a container with `bg-white shadow-sm rounded-lg` styling to match the Figma screenshot (white pill with flag emoji).

    3. Add copyright footer at bottom center: `<p className="fixed bottom-6 left-1/2 -translate-x-1/2 text-xs text-muted-foreground">` with content `{'\u00A9'} 2026 AI Avatar Platform`

    4. Keep the `<Outlet />` centered as-is.

    Complete file structure:
    ```tsx
    import { Outlet } from "react-router-dom";
    import { LanguageSwitcher } from "@/components/shared/language-switcher";

    export function AuthLayout() {
      return (
        <div className="relative flex min-h-screen items-center justify-center bg-gradient-to-br from-blue-50 via-white to-blue-50 p-4">
          <Outlet />
          <div className="fixed top-6 right-6">
            <LanguageSwitcher />
          </div>
          <p className="fixed bottom-6 left-1/2 -translate-x-1/2 text-xs text-muted-foreground">
            {'\u00A9'} 2026 AI Avatar Platform
          </p>
        </div>
      );
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/layouts/auth-layout.tsx contains "from-blue-50 via-white to-blue-50"
    - frontend/src/components/layouts/auth-layout.tsx contains "fixed top-6 right-6"
    - frontend/src/components/layouts/auth-layout.tsx contains "fixed bottom-6 left-1/2"
    - frontend/src/components/layouts/auth-layout.tsx does NOT contain "bottom-4 right-4"
  </acceptance_criteria>
  <done>Auth layout has 3-stop gradient, language switcher in top-right, copyright at bottom center</done>
</task>

<task type="auto">
  <name>Task 2: Update login.tsx -- SVG logo icon, card shadow, remove inline copyright</name>
  <files>frontend/src/pages/login.tsx</files>
  <read_first>
    frontend/src/pages/login.tsx
    frontend/src/components/layouts/auth-layout.tsx
    .planning/phases/01.1-ui-figma-figma-make-ui-sceenshot-figma-make/01.1-UI-SPEC.md
  </read_first>
  <action>
    Update `frontend/src/pages/login.tsx` with these changes:

    1. Replace the logo div (currently `<div className="flex size-12 ...">AI</div>`) with a 64x64 blue gradient rounded square containing a lightbulb/gear SVG icon matching the Figma screenshot. The icon is a settings-gear-like shape with radiating lines around a central circle. Use an inline SVG:
       ```tsx
       <div className="flex size-16 items-center justify-center rounded-2xl bg-primary text-primary-foreground">
         <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
           <circle cx="12" cy="12" r="3" />
           <path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42" />
         </svg>
       </div>
       ```

    2. Update the Card className from `w-full max-w-[480px]` to `w-full max-w-[480px] rounded-2xl shadow-xl` per UI-SPEC layout contract.

    3. Update CardTitle from `text-2xl` to `text-3xl font-semibold` to match the "AI Avatar" title size in Figma (30px semibold per typography spec).

    4. Remove the copyright `<p>` at the bottom of the form (it is now in auth-layout.tsx).

    5. Add aria-label to the password visibility toggle button:
       ```tsx
       <Button
         type="button"
         variant="ghost"
         size="icon"
         className="absolute right-0 top-0 h-full px-3 hover:bg-transparent"
         onClick={() => setShowPassword(!showPassword)}
         tabIndex={-1}
         aria-label={showPassword ? t("ariaHidePassword") : t("ariaShowPassword")}
       >
       ```

    6. Keep ALL existing auth integration (useLogin hook, form submit handler, error display, remember me checkbox, i18n) exactly as-is. Only visual changes.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/login.tsx contains "svg" (SVG logo icon)
    - frontend/src/pages/login.tsx contains "size-16" (64px logo container)
    - frontend/src/pages/login.tsx contains "rounded-2xl shadow-xl" (card styling)
    - frontend/src/pages/login.tsx contains "text-3xl font-semibold" (title size)
    - frontend/src/pages/login.tsx contains "ariaHidePassword" (aria label for password toggle)
    - frontend/src/pages/login.tsx does NOT contain the copyright paragraph (moved to layout)
    - frontend/src/pages/login.tsx contains "useLogin" (auth integration preserved)
  </acceptance_criteria>
  <done>Login page matches Figma: SVG logo, shadow-xl rounded card, large title, aria labels on password toggle, copyright removed (in layout)</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc -b --noEmit` passes
2. `cd frontend && npm run build` completes successfully
3. Login page renders with blue gradient SVG icon instead of "AI" text
4. Language switcher is in top-right corner
5. Copyright is at bottom center of page
6. Card has visible shadow and rounded corners
</verification>

<success_criteria>
- Login page visually matches Login_Page.png: SVG logo, shadow card, 3-stop gradient background
- Language switcher moved to top-right corner
- Copyright moved to bottom center outside card
- All existing auth functionality preserved (login mutation, error handling, remember me)
- TypeScript builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ui-figma-figma-make-ui-sceenshot-figma-make/01.1-02-SUMMARY.md`
</output>
