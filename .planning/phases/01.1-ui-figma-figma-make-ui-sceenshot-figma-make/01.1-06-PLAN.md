---
phase: 01.1-ui-figma-alignment
plan: 06
type: execute
wave: 4
depends_on: ["01.1-02", "01.1-04", "01.1-05"]
files_modified: []
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05

must_haves:
  truths:
    - "All 5 screens visually match their Figma screenshots"
    - "All text switches correctly between en-US and zh-CN"
    - "Navigation between pages works: login -> dashboard -> training -> session"
    - "TypeScript and build pass with zero errors"
  artifacts: []
  key_links: []
---

<objective>
Final verification checkpoint: run build validation, then have the user visually compare all 5 screens against Figma screenshots and test navigation flows.

Purpose: Ensure all pages match Figma designs before marking phase complete.
Output: Verified UI alignment or list of issues to fix.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.1-ui-figma-figma-make-ui-sceenshot-figma-make/01.1-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full build and type check validation</name>
  <files></files>
  <read_first>
    frontend/package.json
  </read_first>
  <action>
    Run the full validation suite to confirm all code compiles:
    1. `cd frontend && npx tsc -b --noEmit` -- must pass with zero errors
    2. `cd frontend && npm run build` -- must complete successfully
    3. If any errors, fix them before proceeding to the checkpoint

    Also verify file existence:
    - All 4 new UI components exist in frontend/src/components/ui/
    - All 11 new shared components exist in frontend/src/components/shared/
    - All 3 coach components exist in frontend/src/components/coach/
    - All 4 locale files exist in frontend/public/locales/
    - All 3 new pages exist in frontend/src/pages/user/
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b --noEmit && npm run build && echo "BUILD PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b --noEmit` exits with code 0
    - `npm run build` exits with code 0 and produces dist/ directory
    - frontend/src/components/ui/scroll-area.tsx exists
    - frontend/src/components/ui/tabs.tsx exists
    - frontend/src/components/ui/progress.tsx exists
    - frontend/src/components/ui/textarea.tsx exists
    - frontend/src/components/shared/stat-card.tsx exists
    - frontend/src/components/shared/hcp-profile-card.tsx exists
    - frontend/src/components/shared/chat-bubble.tsx exists
    - frontend/src/components/coach/left-panel.tsx exists
    - frontend/src/components/coach/center-panel.tsx exists
    - frontend/src/components/coach/right-panel.tsx exists
    - frontend/src/pages/user/training.tsx exists
    - frontend/src/pages/user/training-session.tsx exists
  </acceptance_criteria>
  <done>Full TypeScript and Vite build pass with zero errors, all expected files exist</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all 5 screens against Figma screenshots</name>
  <files></files>
  <action>
    Present the user with instructions to visually compare each screen against the Figma screenshots. Start the dev server with `cd frontend && npm run dev`.
  </action>
  <verify>User confirms visual alignment by typing "approved"</verify>
  <done>User has verified all 5 screens match Figma designs and navigation flow works</done>
  <what-built>
    Complete UI alignment with Figma designs across 5 screens:
    1. Login page (SVG logo, shadow card, gradient background, top-right language switcher)
    2. User Dashboard (4 stat cards, 5 recent sessions, 2 action cards, recommended scenario)
    3. Scenario Selection (tab navigation, 3 filter dropdowns, 6 HCP cards in 3-column grid)
    4. F2F Training Session (3-panel full-screen layout, collapsible side panels, chat, timer)
    5. Component library (11 new shared components, 4 new UI base components)
  </what-built>
  <how-to-verify>
    Start the dev server:
    ```
    cd frontend && npm run dev
    ```

    **Screen 1 - Login (http://localhost:5173/login):**
    - Compare against figma-make/Design Login and Layout Shell/Login_Page.png
    - Check: blue gradient SVG logo (not "AI" text), shadow card, 3-stop gradient background
    - Check: language switcher in top-right corner, copyright at bottom center
    - Switch language to zh-CN: all labels should change to Chinese

    **Screen 2 - Dashboard (login first, then http://localhost:5173/user/dashboard):**
    - Compare against figma-make/Medical Representative Dashboard/Medical Representative Dashboard_Page.png
    - Check: 4 stat cards in top row (24, 78, 5, +12%)
    - Check: Recent Training Sessions list with 5 doctors
    - Check: Start Training cards (blue F2F, purple Conference)
    - Check: Recommended Scenario card at bottom-right

    **Screen 3 - Scenario Selection (http://localhost:5173/user/training):**
    - Compare against figma-make/Scenario Selection Page Design/Scenario Selection Page.png
    - Check: Tabs (F2F Training / Conference Training)
    - Check: 3 filter dropdowns + search
    - Check: 6 HCP cards in 3-column grid
    - Check: Difficulty badges (Hard=red, Easy=green, Medium=orange)
    - Check: "Start Training" buttons on each card

    **Screen 4 - F2F Training Session (click Start Training on any HCP card):**
    - Compare against figma-make/F2F HCP Training Page Design/F2F HCP Training Page.png
    - Check: Full-screen layout (no top nav bar from UserLayout)
    - Check: Left panel with scenario info, collapsible
    - Check: Center panel with avatar area and chat
    - Check: Right panel with AI hints, collapsible
    - Check: Timer incrementing
    - Type a message and press Enter: should appear and trigger HCP response
    - Click "End Session": should show confirmation dialog

    **Navigation flow:**
    - Login -> Dashboard -> click "Start Training" -> Scenario Selection -> click card "Start Training" -> Training Session -> "End Session" -> Dashboard
  </how-to-verify>
  <resume-signal>Type "approved" if all screens match Figma, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript and Vite build pass
2. All 5 screens visually match Figma screenshots
3. i18n works for both en-US and zh-CN
4. Navigation flow works end-to-end
5. No console errors in browser
</verification>

<success_criteria>
- All 5 Figma screens implemented and visually verified
- Build passes with zero errors
- User approves visual alignment
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ui-figma-figma-make-ui-sceenshot-figma-make/01.1-06-SUMMARY.md`
</output>
