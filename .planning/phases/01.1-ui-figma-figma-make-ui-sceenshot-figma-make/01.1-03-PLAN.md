---
phase: 01.1-ui-figma-alignment
plan: 03
type: execute
wave: 2
depends_on: ["01.1-01"]
files_modified:
  - frontend/src/components/shared/stat-card.tsx
  - frontend/src/components/shared/session-item.tsx
  - frontend/src/components/shared/action-card.tsx
  - frontend/src/components/shared/recommended-scenario.tsx
  - frontend/src/components/shared/mini-charts.tsx
  - frontend/src/components/shared/hcp-profile-card.tsx
  - frontend/src/components/shared/chat-bubble.tsx
  - frontend/src/components/shared/chat-input.tsx
  - frontend/src/components/shared/dimension-bar.tsx
  - frontend/src/components/shared/status-badge.tsx
  - frontend/src/components/shared/score-card.tsx
  - frontend/src/components/shared/index.ts
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "11 new shared domain components exist and are importable from @/components/shared"
    - "All components use design tokens (text-foreground, bg-primary, etc.) not raw Tailwind colors"
    - "All components use cn() for className merging"
    - "All components have typed props interfaces"
    - "npx tsc -b passes with zero errors"
  artifacts:
    - path: "frontend/src/components/shared/stat-card.tsx"
      provides: "Dashboard stat card with icon, value, trend, progress"
      exports: ["StatCard"]
    - path: "frontend/src/components/shared/session-item.tsx"
      provides: "Training session row with HCP info and score"
      exports: ["SessionItem"]
    - path: "frontend/src/components/shared/action-card.tsx"
      provides: "Gradient CTA card for F2F/Conference training"
      exports: ["ActionCard"]
    - path: "frontend/src/components/shared/hcp-profile-card.tsx"
      provides: "HCP scenario card with avatar, specialty, difficulty"
      exports: ["HCPProfileCard"]
    - path: "frontend/src/components/shared/chat-bubble.tsx"
      provides: "Left/right aligned chat message bubble"
      exports: ["ChatBubble"]
    - path: "frontend/src/components/shared/chat-input.tsx"
      provides: "Chat input with send button and voice toggle"
      exports: ["ChatInput"]
    - path: "frontend/src/components/shared/index.ts"
      provides: "Barrel export for all shared components"
      contains: "export.*StatCard"
  key_links:
    - from: "frontend/src/components/shared/stat-card.tsx"
      to: "frontend/src/components/ui/progress.tsx"
      via: "import Progress for optional progress bar"
      pattern: "import.*Progress.*from.*@/components/ui"
    - from: "frontend/src/components/shared/chat-input.tsx"
      to: "frontend/src/components/ui/textarea.tsx"
      via: "import Textarea for message input"
      pattern: "import.*Textarea.*from.*@/components/ui"
---

<objective>
Create all 11 shared domain components defined in the UI-SPEC component inventory. These are the building blocks used by Dashboard, Scenario Selection, and F2F Training pages.

Purpose: Reusable component library that bridges design system and page-level implementations. All components follow project conventions (design tokens, cn(), i18n, typed props, barrel exports).
Output: 11 new component files in frontend/src/components/shared/ + barrel export index.ts.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.1-ui-figma-figma-make-ui-sceenshot-figma-make/01.1-RESEARCH.md
@.planning/phases/01.1-ui-figma-figma-make-ui-sceenshot-figma-make/01.1-UI-SPEC.md
@figma-make/Medical Representative Dashboard/Medical Representative Dashboard_Page.png
@figma-make/Design System for SaaS/Design_System_Page.png
@figma-make/Scenario Selection Page Design/Scenario Selection Page.png
@figma-make/F2F HCP Training Page Design/F2F HCP Training Page.png

<interfaces>
From frontend/src/components/ui/index.ts (available after Plan 01):
```typescript
export { Card, CardContent } from "./card";
export { Badge, badgeVariants } from "./badge";
export { Avatar, AvatarImage, AvatarFallback } from "./avatar";
export { Button, buttonVariants } from "./button";
export { Checkbox } from "./checkbox";
export { Progress } from "./progress";
export { Textarea } from "./textarea";
export { Tooltip, TooltipTrigger, TooltipContent, TooltipProvider } from "./tooltip";
```

From frontend/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)); }
```

Design token color mapping (from UI-SPEC):
- text-gray-900 -> text-foreground
- text-gray-600 -> text-muted-foreground
- bg-gray-50 -> bg-muted
- bg-white -> bg-card
- border-gray-200 -> border-border
- bg-blue-600 -> bg-primary
- text-blue-600 -> text-primary
- bg-green-600 -> text-strength
- bg-red-600 -> text-destructive
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard domain components (StatCard, SessionItem, ActionCard, RecommendedScenario, MiniCharts, ScoreCard)</name>
  <files>
    frontend/src/components/shared/stat-card.tsx
    frontend/src/components/shared/session-item.tsx
    frontend/src/components/shared/action-card.tsx
    frontend/src/components/shared/recommended-scenario.tsx
    frontend/src/components/shared/mini-charts.tsx
    frontend/src/components/shared/score-card.tsx
  </files>
  <read_first>
    frontend/src/components/ui/index.ts
    frontend/src/components/shared/empty-state.tsx
    frontend/src/lib/utils.ts
    frontend/src/styles/index.css
    .planning/phases/01.1-ui-figma-figma-make-ui-sceenshot-figma-make/01.1-UI-SPEC.md
  </read_first>
  <action>
    Create 6 component files following the UI-SPEC component inventory. Use design tokens, cn(), typed interfaces, no raw Tailwind colors. Reference the Figma dashboard screenshot for visual accuracy.

    **1. `frontend/src/components/shared/stat-card.tsx`**
    Props interface:
    ```typescript
    import type { LucideIcon } from "lucide-react";
    interface StatCardProps {
      label: string;
      value: string | number;
      icon?: LucideIcon;
      trend?: { value: string; direction: "up" | "down" };
      chart?: React.ReactNode;
      progress?: { current: number; total: number };
    }
    ```
    Structure: Card with CardContent p-6. Top row: icon in `size-10 rounded-lg bg-primary/10` left, chart slot right. Value: `text-3xl font-semibold text-foreground`. Label: `text-sm text-muted-foreground`. Trend: `text-sm font-medium` with `text-strength` for up, `text-destructive` for down. If progress, render Progress component below with `{current} of {total} goal` text. Import `Card, CardContent` from `@/components/ui`, `Progress` from `@/components/ui`, `cn` from `@/lib/utils`.

    **2. `frontend/src/components/shared/session-item.tsx`**
    Props interface:
    ```typescript
    interface SessionItemProps {
      hcpName: string;
      specialty: string;
      mode: "F2F" | "Conference";
      score: number;
      timeAgo: string;
      avatar?: string;
      onClick?: () => void;
    }
    ```
    Structure: Horizontal flex row with `hover:bg-muted/50 rounded-lg p-3 cursor-pointer transition-colors`. Left: Avatar with AvatarFallback (first letters of hcpName). Name bold, specialty muted text. Mode Badge: F2F = `bg-blue-100 text-blue-700`, Conference = `bg-purple-100 text-purple-700`. Right side: score (color coded: >=80 text-strength, >=60 text-foreground, <60 text-destructive), timeAgo text, ChevronRight icon. Import `Avatar, AvatarFallback, Badge` from `@/components/ui`, `ChevronRight` from `lucide-react`.

    **3. `frontend/src/components/shared/action-card.tsx`**
    Props interface:
    ```typescript
    import type { LucideIcon } from "lucide-react";
    interface ActionCardProps {
      title: string;
      description: string;
      icon: LucideIcon;
      gradient: "blue" | "purple";
      onStart: () => void;
    }
    ```
    Structure: Rounded-xl card with gradient background. Blue: `from-blue-500 to-blue-600`. Purple: `from-purple-500 to-purple-600`. White text. Icon in semi-transparent circle. Title in `text-lg font-semibold text-white`. Description in `text-sm text-white/80`. "Start" button in white bg with gradient text color. Import `Button` from `@/components/ui`.

    **4. `frontend/src/components/shared/recommended-scenario.tsx`**
    Props interface:
    ```typescript
    interface RecommendedScenarioProps {
      hcpName: string;
      difficulty: string;
      onStart?: () => void;
    }
    ```
    Structure: Flex row with orange circle avatar placeholder left, text center (hcpName bold, "Practice with {hcpName}" muted, difficulty Badge), "Start Training" button right. Difficulty badge colors: Easy=green, Medium=orange, Hard=red per UI-SPEC mapping. Import `Badge, Button` from `@/components/ui`.

    **5. `frontend/src/components/shared/mini-charts.tsx`**
    Exports: `MiniRadarChart` and `MiniTrendChart` (no props, pure SVG).
    MiniRadarChart: 48x48 SVG with 5-point radar polygon in primary color with 20% opacity fill and primary stroke. Static decorative element.
    MiniTrendChart: 48x48 SVG with polyline showing upward trend in strength color. Static decorative element.
    Both are purely decorative visual indicators matching the Figma dashboard stat cards.

    **6. `frontend/src/components/shared/score-card.tsx`**
    Props interface:
    ```typescript
    interface ScoreCardProps {
      score: number;
      label: string;
      trend?: { value: string; direction: "up" | "down" };
      chart?: React.ReactNode;
    }
    ```
    Structure: Card with colored left gradient accent. Score in `text-4xl font-semibold text-foreground`. Label in `text-sm text-muted-foreground`. Trend indicator. Mini bar chart SVG on right side (5 blue bars of varying height). Per Design System screenshot. Import `Card, CardContent` from `@/components/ui`.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/shared/stat-card.tsx contains "export function StatCard"
    - frontend/src/components/shared/stat-card.tsx contains "interface StatCardProps"
    - frontend/src/components/shared/stat-card.tsx contains "text-foreground" (design token, not text-gray-900)
    - frontend/src/components/shared/session-item.tsx contains "export function SessionItem"
    - frontend/src/components/shared/session-item.tsx contains "interface SessionItemProps"
    - frontend/src/components/shared/session-item.tsx contains "bg-blue-100 text-blue-700" (F2F mode badge)
    - frontend/src/components/shared/action-card.tsx contains "export function ActionCard"
    - frontend/src/components/shared/action-card.tsx contains "from-blue-500 to-blue-600" (blue gradient)
    - frontend/src/components/shared/recommended-scenario.tsx contains "export function RecommendedScenario"
    - frontend/src/components/shared/mini-charts.tsx contains "export function MiniRadarChart"
    - frontend/src/components/shared/mini-charts.tsx contains "export function MiniTrendChart"
    - frontend/src/components/shared/score-card.tsx contains "export function ScoreCard"
    - No file contains "text-gray-900" or "text-gray-600" (must use design tokens)
  </acceptance_criteria>
  <done>6 dashboard/design-system domain components created with typed interfaces, design tokens, project conventions</done>
</task>

<task type="auto">
  <name>Task 2: Create training domain components (HCPProfileCard, ChatBubble, ChatInput, DimensionBar, StatusBadge) and barrel export</name>
  <files>
    frontend/src/components/shared/hcp-profile-card.tsx
    frontend/src/components/shared/chat-bubble.tsx
    frontend/src/components/shared/chat-input.tsx
    frontend/src/components/shared/dimension-bar.tsx
    frontend/src/components/shared/status-badge.tsx
    frontend/src/components/shared/index.ts
  </files>
  <read_first>
    frontend/src/components/ui/index.ts
    frontend/src/components/shared/empty-state.tsx
    frontend/src/lib/utils.ts
    .planning/phases/01.1-ui-figma-figma-make-ui-sceenshot-figma-make/01.1-UI-SPEC.md
  </read_first>
  <action>
    Create 5 component files + barrel export. Use design tokens, cn(), typed interfaces.

    **1. `frontend/src/components/shared/hcp-profile-card.tsx`**
    Props interface:
    ```typescript
    interface HCPProfileCardProps {
      name: string;
      nameZh?: string;
      specialty: string;
      hospital: string;
      personality: string[];
      difficulty: "Easy" | "Medium" | "Hard";
      product: string;
      avatar?: string;
      onStartTraining: () => void;
    }
    ```
    Structure per Scenario Selection screenshot: Card with border rounded-lg p-6 text-center. Top: circular Avatar (96px, size-24). Doctor name: `text-base font-semibold`. Chinese name in parens if provided. Specialty as Badge with primary colors. Hospital in `text-sm text-muted-foreground`. Personality tags row: each tag as small rounded Badge with semantic colors. Product: `text-sm text-muted-foreground`. Difficulty badge per UI-SPEC mapping: Easy=`bg-green-100 text-green-700`, Medium=`bg-orange-100 text-orange-700`, Hard=`bg-red-100 text-red-700`. Full-width "Start Training" Button primary at bottom. Card hover: `hover:shadow-md transition-shadow`. Import `Card, CardContent, Avatar, AvatarFallback, AvatarImage, Badge, Button` from `@/components/ui`.

    **2. `frontend/src/components/shared/chat-bubble.tsx`**
    Props interface:
    ```typescript
    interface ChatBubbleProps {
      sender: "hcp" | "mr";
      text: string;
      timestamp: Date;
    }
    ```
    Structure per F2F Training screenshot: Flex row with `justify-start` for hcp (left-aligned), `justify-end` for mr (right-aligned). HCP bubble: `bg-primary text-primary-foreground rounded-2xl rounded-tl-sm px-4 py-2 max-w-[75%]`. MR bubble: `bg-muted text-foreground rounded-2xl rounded-tr-sm px-4 py-2 max-w-[75%]`. Timestamp below bubble: `text-xs text-muted-foreground mt-1`. Format timestamp as HH:MM. Import `cn` from `@/lib/utils`.

    **3. `frontend/src/components/shared/chat-input.tsx`**
    Props interface:
    ```typescript
    interface ChatInputProps {
      onSend: (text: string) => void;
      inputMode: "text" | "audio";
      onMicClick: () => void;
      recordingState: "idle" | "recording" | "processing";
      disabled?: boolean;
    }
    ```
    Structure per F2F Training screenshot: Flex row with Textarea (auto-grow, Enter sends, Shift+Enter newline), Mic button (Mic/MicOff icon, colored by state: idle=primary, recording=destructive with animate-pulse, processing=weakness), Send button (Send icon, disabled when empty or audio mode). Below input: Input Mode pill selector ("Text" | "Audio" with active state bg-primary text-white). All icon buttons 44px touch target (h-11 w-11). aria-labels from `training:ariaSendMessage`, `training:ariaStartRecording`, `training:ariaStopRecording` via useTranslation. Import `Textarea, Button, Tooltip, TooltipTrigger, TooltipContent, TooltipProvider` from `@/components/ui`, `Mic, MicOff, Send` from `lucide-react`, `useTranslation` from `react-i18next`.

    **4. `frontend/src/components/shared/dimension-bar.tsx`**
    Props interface:
    ```typescript
    interface DimensionBarProps {
      label: string;
      value: number;
      maxValue?: number;
    }
    ```
    Structure: Flex column. Label row: `text-sm font-medium text-foreground` left, `{value}%` right. Bar: `h-2 w-full rounded-full bg-primary/20` with inner `bg-primary rounded-full` at `width: {percentage}%`. maxValue defaults to 100. Import `cn` from `@/lib/utils`.

    **5. `frontend/src/components/shared/status-badge.tsx`**
    Props interface:
    ```typescript
    interface StatusBadgeProps {
      status: "delivered" | "in-progress" | "pending";
      label: string;
    }
    ```
    Structure: Inline flex with dot indicator + label text. Status colors: delivered=`bg-strength` (green dot), in-progress=`bg-weakness` (orange dot), pending=`bg-muted-foreground` (gray dot). Dot: `size-2 rounded-full`. Label: `text-sm`. Import `cn` from `@/lib/utils`.

    **6. Create `frontend/src/components/shared/index.ts`** barrel export:
    ```typescript
    export { EmptyState } from "./empty-state";
    export { LanguageSwitcher } from "./language-switcher";
    export { LoadingState } from "./loading-state";
    export { StatCard } from "./stat-card";
    export { SessionItem } from "./session-item";
    export { ActionCard } from "./action-card";
    export { RecommendedScenario } from "./recommended-scenario";
    export { MiniRadarChart, MiniTrendChart } from "./mini-charts";
    export { ScoreCard } from "./score-card";
    export { HCPProfileCard } from "./hcp-profile-card";
    export { ChatBubble } from "./chat-bubble";
    export { ChatInput } from "./chat-input";
    export { DimensionBar } from "./dimension-bar";
    export { StatusBadge } from "./status-badge";
    ```
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/shared/hcp-profile-card.tsx contains "export function HCPProfileCard"
    - frontend/src/components/shared/hcp-profile-card.tsx contains "interface HCPProfileCardProps"
    - frontend/src/components/shared/hcp-profile-card.tsx contains "bg-green-100 text-green-700" (Easy badge)
    - frontend/src/components/shared/chat-bubble.tsx contains "export function ChatBubble"
    - frontend/src/components/shared/chat-bubble.tsx contains "sender: \"hcp\" | \"mr\""
    - frontend/src/components/shared/chat-input.tsx contains "export function ChatInput"
    - frontend/src/components/shared/chat-input.tsx contains "useTranslation" (i18n for aria labels)
    - frontend/src/components/shared/chat-input.tsx contains "ariaSendMessage"
    - frontend/src/components/shared/dimension-bar.tsx contains "export function DimensionBar"
    - frontend/src/components/shared/status-badge.tsx contains "export function StatusBadge"
    - frontend/src/components/shared/index.ts contains "export { StatCard }"
    - frontend/src/components/shared/index.ts contains "export { ChatBubble }"
    - frontend/src/components/shared/index.ts contains "export { HCPProfileCard }"
    - No file contains "text-gray-900" or "text-gray-600" (must use design tokens)
  </acceptance_criteria>
  <done>5 training domain components created, barrel export index.ts covers all 14 shared components (3 existing + 11 new), TypeScript compiles</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc -b --noEmit` passes
2. `cd frontend && npm run build` completes
3. All 11 new shared components exist in frontend/src/components/shared/
4. Barrel export frontend/src/components/shared/index.ts exports all 14 shared components
5. No raw Tailwind color classes (text-gray-900, bg-gray-50) -- all mapped to design tokens
6. All components have explicit TypeScript interfaces for props
</verification>

<success_criteria>
- 11 new shared domain components created per UI-SPEC inventory
- All use design token colors, cn() utility, typed props
- Barrel export provides single import path for all shared components
- ChatInput uses i18n for aria labels
- TypeScript strict mode builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ui-figma-figma-make-ui-sceenshot-figma-make/01.1-03-SUMMARY.md`
</output>
