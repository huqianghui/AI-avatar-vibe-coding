---
phase: 36-avatar-persona-selection-post-login-landing
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - frontend/src/api/avatar-personas.ts
  - frontend/src/hooks/use-avatar-personas.ts
  - frontend/public/locales/en-US/admin.json
  - frontend/public/locales/zh-CN/admin.json
  - frontend/public/locales/es-ES/admin.json
  - frontend/public/locales/es-MX/admin.json
  - frontend/public/locales/es-US/admin.json
  - frontend/public/locales/en-US/nav.json
  - frontend/public/locales/zh-CN/nav.json
  - frontend/public/locales/es-ES/nav.json
  - frontend/public/locales/es-MX/nav.json
  - frontend/public/locales/es-US/nav.json
  - frontend/src/components/admin/persona-dialog.tsx
  - frontend/src/components/admin/persona-table.tsx
  - frontend/src/pages/admin/avatar-personas.tsx
  - frontend/src/router/index.tsx
  - frontend/src/components/layouts/admin-layout.tsx
  - frontend/src/hooks/use-avatar-personas.test.ts
  - frontend/e2e/admin-avatar-personas.spec.ts
autonomous: true
requirements: [PERSONA-01, PERSONA-02]
must_haves:
  truths:
    - "An admin navigating the sidebar reaches /admin/avatar-personas and sees the existing persona catalog in a table"
    - "An admin can open a create dialog, fill identity/character+style/voice-per-language/greeting/prompt fields, and save a new persona"
    - "An admin can edit an existing persona, toggle enabled, and set a different persona as default from the table"
    - "An admin attempting to delete or disable the current default sees a blocking error and the row's Delete action is disabled with a tooltip"
    - "The existing admin sidebar 'Voice Live' link remains visible and unaffected (voice-live-proxy.spec.ts:489 still passes)"
    - "All new UI copy renders correctly in all 5 locales with no missing-key fallback text"
  artifacts:
    - path: "frontend/src/api/avatar-personas.ts"
      provides: "Typed API client for persona CRUD"
      exports: ["avatarPersonasApi"]
    - path: "frontend/src/hooks/use-avatar-personas.ts"
      provides: "TanStack Query hooks"
      exports: ["useAvatarPersonas", "useCreateAvatarPersona", "useUpdateAvatarPersona", "useDeleteAvatarPersona", "useSetDefaultAvatarPersona"]
    - path: "frontend/src/components/admin/persona-dialog.tsx"
      provides: "Create/edit dialog per UI-SPEC section 2"
      contains: "PersonaDialog"
    - path: "frontend/src/components/admin/persona-table.tsx"
      provides: "Persona catalog table per UI-SPEC section 2"
      contains: "PersonaTable"
    - path: "frontend/src/pages/admin/avatar-personas.tsx"
      provides: "Admin page composing table + dialog"
      contains: "export default function"
    - path: "frontend/e2e/admin-avatar-personas.spec.ts"
      provides: "Playwright E2E covering the admin CRUD user story"
      contains: "test("
  key_links:
    - from: "frontend/src/router/index.tsx"
      to: "frontend/src/pages/admin/avatar-personas.tsx"
      via: "lazy route registration under AdminRoute > AdminLayout"
      pattern: "path: \"avatar-personas\""
    - from: "frontend/src/components/layouts/admin-layout.tsx"
      to: "/admin/avatar-personas"
      via: "sidebarGroups nav entry"
      pattern: "path: \"/admin/avatar-personas\""
    - from: "frontend/src/pages/admin/avatar-personas.tsx"
      to: "frontend/src/hooks/use-avatar-personas.ts"
      via: "hook calls"
      pattern: "useAvatarPersonas|useCreateAvatarPersona|useUpdateAvatarPersona"
    - from: "frontend/src/hooks/use-avatar-personas.ts"
      to: "frontend/src/api/avatar-personas.ts"
      via: "queryFn/mutationFn"
      pattern: "avatarPersonasApi\\."
---

<objective>
Implement the admin-facing UI for PERSONA-01/02: a dedicated `/admin/avatar-personas` page (Table + Dialog, per 36-UI-SPEC.md section "Component Inventory & Placement #1/#2") backed by the 36-01 backend API, with a sidebar nav entry, typed API client, TanStack Query hooks, and full 5-locale i18n coverage.

Purpose: Give admins a working CRUD surface for the persona catalog created in 36-01, matching the locked UI-SPEC contract exactly (D-01..D-03).
Output: Admin persona management page, reusable dialog/table components, i18n keys in `admin.json`/`nav.json` for all 5 locales, Vitest hook tests, Playwright E2E.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-avatar-persona-selection-post-login-landing/36-CONTEXT.md
@.planning/phases/36-avatar-persona-selection-post-login-landing/36-UI-SPEC.md
@.planning/phases/36-avatar-persona-selection-post-login-landing/36-01-SUMMARY.md
</context>

<interfaces>
<!-- Verified this session by reading the actual files. Mirror exactly, no exploration needed. -->

**Backend contract (from 36-01, verify exact field names against `36-01-SUMMARY.md` before wiring):**
```
GET    /api/v1/admin/avatar-personas          -> AvatarPersonaOut[]
POST   /api/v1/admin/avatar-personas          -> 201 AvatarPersonaOut
GET    /api/v1/admin/avatar-personas/{id}     -> AvatarPersonaOut
PUT    /api/v1/admin/avatar-personas/{id}     -> AvatarPersonaOut  (body may include new_default_persona_id when disabling the current default)
DELETE /api/v1/admin/avatar-personas/{id}     -> 204              (body may include new_default_persona_id when deleting the current default; 409 if omitted and target is default)
POST   /api/v1/admin/avatar-personas/{id}/set-default -> AvatarPersonaOut
GET    /api/v1/personas                        -> AvatarPersonaOut[] (enabled-only, public)
```
`AvatarPersonaOut` shape: `{ id, name, character, style, voice_map: Record<string,string>, greeting, prompt_fragment, enabled, is_default, created_at, updated_at }`.

**API client module pattern (`frontend/src/api/user-preferences.ts` — verified exact file):**
```typescript
import apiClient from "./client";

export interface UserPreference { id: string; ... }

export const userPreferencesApi = {
  getSummary: async (userId: string): Promise<PersonalizationSummary> => {
    const { data } = await apiClient.get<PersonalizationSummary>(`/users/${userId}/personalization`);
    return data;
  },
  create: async (userId: string, data: UserPreferenceCreate): Promise<UserPreference> => {
    const { data: result } = await apiClient.post<UserPreference>(`/users/${userId}/preferences`, data);
    return result;
  },
};
```
`apiClient` is a pre-configured Axios instance (default export from `./client`) already targeting `/api/v1` with JWT auto-attach — do not construct a new client.

**TanStack Query hooks pattern (`frontend/src/hooks/use-voice-live-instances.ts` — verified exact file):**
```typescript
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
const QUERY_KEY = "voice-live-instances";

export function useVoiceLiveInstances(params?: {...}) {
  return useQuery({ queryKey: [QUERY_KEY, params], queryFn: () => fetchVoiceLiveInstances(params) });
}
export function useCreateVoiceLiveInstance() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (data) => createVoiceLiveInstance(data),
    onSuccess: () => { queryClient.invalidateQueries({ queryKey: [QUERY_KEY] }); },
  });
}
```

**Router registration (`frontend/src/router/index.tsx` — verified exact lines this session, inside the `AdminRoute > AdminLayout` children array alongside `voice-live`):**
```tsx
const VoiceLiveManagementPage = lazy(() => import("@/pages/admin/voice-live-management"));
// ...
{ path: "voice-live", element: <SuspensePage><VoiceLiveManagementPage /></SuspensePage> },
```
Add `const AvatarPersonasPage = lazy(() => import("@/pages/admin/avatar-personas"));` near the other `admin/*` lazy imports, and `{ path: "avatar-personas", element: <SuspensePage><AvatarPersonasPage /></SuspensePage> },` inside the same children array (resulting full path `/admin/avatar-personas`).

**Sidebar nav (`frontend/src/components/layouts/admin-layout.tsx` — verified exact array this session):**
```tsx
const sidebarGroups: SidebarGroup[] = [
  {
    labelKey: "configuration",
    items: [
      { path: "/admin/azure-config", labelKey: "azureServices", icon: Cloud },
      { path: "/admin/voice-live", labelKey: "voiceLive", icon: Radio },
      { path: "/admin/meta-skills", labelKey: "metaSkills", icon: Wand2 },
      ...
```
Per UI-SPEC section 1: insert `{ path: "/admin/avatar-personas", labelKey: "avatarPersonas", icon: Smile }` immediately after the `voiceLive` entry, before `metaSkills`, in the `configuration` group. Import `Smile` from `lucide-react` at the top of the file alongside the other icon imports. `labelKey` resolves via `useTranslation("nav")` (verified: `const { t } = useTranslation("nav")` at line 100) — add the `avatarPersonas` key to `nav.json`, NOT `admin.json`.

**i18n file locations (verified via filesystem listing this session):** `frontend/public/locales/{locale}/admin.json` and `frontend/public/locales/{locale}/nav.json`, one file per locale in `["en-US", "zh-CN", "es-ES", "es-MX", "es-US"]`. Existing `admin.json` top-level namespacing style (verified, e.g. `"personalization": { "title": "...", "emptyTitle": "...", ... }`) — add a new top-level `"personas"` object following this exact nesting convention.

**Character/style grid precedent (`frontend/src/components/admin/vl-instance-dialog.tsx`, `frontend/src/data/avatar-characters.ts` — verified structure this session):**
```typescript
export interface AvatarCharacterMeta {
  id: string;
  // ...
  styles: readonly string[];   // empty array for photo avatars
  isPhotoAvatar: boolean;
}
export const AVATAR_CHARACTERS: readonly AvatarCharacterMeta[] = [ /* Lisa, Harry, Meg, Max, Lori, ... */ ];
```
Clone `vl-instance-dialog.tsx`'s `filteredAvatarItems` `useMemo` (builds one grid item per `{characterId, style}` pair, `h-14 w-14` thumbnail circles, `ring-2 ring-primary` on selection, initials-gradient fallback via `getAvatarInitials`/`gradientClasses`) directly into `persona-dialog.tsx` per UI-SPEC section 2 item 2 — do not build a new cascading `<Select>`.

**Voice-per-language precedent:** UI-SPEC section 2 item 3 — one `Select` row per locale in `["zh-CN", "en-US", "es-ES", "es-MX", "es-US"]`, options from the existing `VOICE_NAME_OPTIONS` constant (locate it via the Phase 34 `settings.tsx` "Voice per Language" Card — grep for `VOICE_NAME_OPTIONS` if not immediately visible in `frontend/src/lib/voice-constants.ts`), with a leading "(use default)" empty option (persona-level voice is optional per D-07's fallback chain).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API client, TanStack Query hooks, and i18n keys</name>
  <files>frontend/src/api/avatar-personas.ts, frontend/src/hooks/use-avatar-personas.ts, frontend/src/hooks/use-avatar-personas.test.ts, frontend/public/locales/en-US/admin.json, frontend/public/locales/zh-CN/admin.json, frontend/public/locales/es-ES/admin.json, frontend/public/locales/es-MX/admin.json, frontend/public/locales/es-US/admin.json, frontend/public/locales/en-US/nav.json, frontend/public/locales/zh-CN/nav.json, frontend/public/locales/es-ES/nav.json, frontend/public/locales/es-MX/nav.json, frontend/public/locales/es-US/nav.json</files>
  <behavior>
    - Test: `useAvatarPersonas()` calls `avatarPersonasApi.list` and exposes the returned array via `data`.
    - Test: `useCreateAvatarPersona()` mutation calls `avatarPersonasApi.create` and invalidates the list query key on success.
    - Test: `useUpdateAvatarPersona()` mutation calls `avatarPersonasApi.update` with `{ id, data }` and invalidates on success.
    - Test: `useDeleteAvatarPersona()` mutation calls `avatarPersonasApi.remove` and invalidates on success.
    - Test: `useSetDefaultAvatarPersona()` mutation calls `avatarPersonasApi.setDefault` and invalidates on success.
    (Mock `avatarPersonasApi` module per the existing `use-voice-live-instances.test.ts`-style mocking convention if one exists — check for a sibling test file to mirror; otherwise use `vi.mock("@/api/avatar-personas")`.)
  </behavior>
  <action>
    Create `frontend/src/api/avatar-personas.ts` exporting `AvatarPersona`, `AvatarPersonaCreate`, `AvatarPersonaUpdate` interfaces matching 36-01's `AvatarPersonaOut`/`AvatarPersonaCreate`/`AvatarPersonaUpdate` schemas exactly (read `36-01-SUMMARY.md` field list before typing), and `avatarPersonasApi = { list, get, create, update, remove, setDefault }` following the `userPreferencesApi` pattern (all `admin/avatar-personas` paths). Create `frontend/src/hooks/use-avatar-personas.ts` with `useAvatarPersonas()`, `useCreateAvatarPersona()`, `useUpdateAvatarPersona()`, `useDeleteAvatarPersona()`, `useSetDefaultAvatarPersona()` following the `use-voice-live-instances.ts` `QUERY_KEY` + `invalidateQueries` pattern exactly. Add a `"personas"` object to `admin.json` in all 5 locale files with genuinely translated (not machine-filler) strings for: `create` ("Create Persona"), `save` ("Save Persona"), `emptyTitle`, `emptyBody`, `defaultGuardError: { title, body }`, `saveError: { title, body }`, `deletePersona: { title, body, confirm, cancel }`, `toggleEnabled` ("Enabled"), `toggleDefault` ("Set as default"), `toggleDefaultDisabledTooltip` ("Enable this persona first"), `deleteDisabledTooltip` ("Set another default first"), `editAriaLabel` ("Edit persona"), `deleteAriaLabel` ("Delete persona"), `characterSectionTitle` ("Character & Style"), `voiceSectionTitle` ("Voice per Language"), `greetingLabel`, `greetingHelper`, `promptFragmentLabel`, `promptFragmentHelper`, `useDefaultVoiceOption` ("(use default)") — copy exactly from 36-UI-SPEC.md's Copywriting Contract table where strings are given verbatim; translate the rest faithfully for zh-CN/es-ES/es-MX/es-US (es-MX/es-US may reuse es-ES text where regionally neutral, per existing repo convention — check an existing key across the 3 es-* files for the reuse pattern before deciding). Add `"avatarPersonas": "Avatar Personas"` (translated per locale) to `nav.json` in all 5 locales.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/use-avatar-personas.test.ts && npx vitest run src/i18n/locale-parity.test.ts</automated>
  </verify>
  <done>All hook tests pass; locale-parity test passes with the new `personas` (admin.json) and `avatarPersonas` (nav.json) keys present and identical in shape across all 5 locales; `untranslated-whitelist.ts` length is unchanged (still <=15).</done>
</task>

<task type="auto">
  <name>Task 2: PersonaDialog, PersonaTable, admin page, router + sidebar wiring</name>
  <files>frontend/src/components/admin/persona-dialog.tsx, frontend/src/components/admin/persona-table.tsx, frontend/src/pages/admin/avatar-personas.tsx, frontend/src/router/index.tsx, frontend/src/components/layouts/admin-layout.tsx</files>
  <action>
    Build `persona-dialog.tsx` per 36-UI-SPEC.md section "Component Inventory & Placement #2" exactly: `Dialog` sized `sm:max-w-2xl max-h-[90vh] overflow-y-auto`, sections in order — Identity (`name` Input, `enabled` Switch labeled "Enabled" with `text-sm font-semibold` per the Typography exception, `is_default` Switch labeled "Set as default", disabled+tooltip when `enabled` is off), Character & Style (cloned `filteredAvatarItems` grid from `vl-instance-dialog.tsx`, `AVATAR_CHARACTERS` data source, `h-14 w-14` thumbnails, `ring-2 ring-primary` selection), Voice per Language (one `Select` per locale from `VOICE_NAME_OPTIONS` with a leading "(use default)" option), Greeting (`Textarea` 2 rows), Persona Prompt Fragment (`Textarea` 3 rows). Footer: `Cancel` (outline) + `Save Persona` (primary, disabled while `name` empty or mutation `isPending`). Wire it to `useCreateAvatarPersona`/`useUpdateAvatarPersona` from Task 1. Build `persona-table.tsx` per section #2's table spec: columns thumbnail (32px)/Name/Character-Style/Default badge (primary-accent, only on the default row)/Enabled Switch (inline toggle calling `useUpdateAvatarPersona`)/Actions (Edit pencil + Delete trash icon buttons, each with a permanent `aria-label` — "Edit persona"/"Delete persona" — plus a `Tooltip` showing the same text; Delete additionally `disabled` + tooltip text swapped to "Set another default first" when the row `is_default`). Delete confirmation is a plain `Dialog` (not `AlertDialog` — none exists in this repo) per UI-SPEC, with the exact copy from the Copywriting Contract table ("Delete persona '{name}'? This can't be undone...."). On any 409 from the default-guard (create/update/delete), show a toast using the `personas.defaultGuardError` i18n key. Build `frontend/src/pages/admin/avatar-personas.tsx` composing `PersonaTable` + `PersonaDialog` + a page header ("Avatar Personas" title, "Create Persona" primary button top-right) following the existing `voice-live-management.tsx` page-composition pattern (Card-less flat layout is fine per UI-SPEC's `xl` page padding note — match `AdminLayout`'s existing `main` padding, no override needed). Register the route: add `const AvatarPersonasPage = lazy(() => import("@/pages/admin/avatar-personas"));` near the other admin lazy imports in `router/index.tsx`, and `{ path: "avatar-personas", element: <SuspensePage><AvatarPersonasPage /></SuspensePage> },` inside the `AdminRoute > AdminLayout` children array (after `voice-live/:id/edit`, matching UI-SPEC's nav ordering intent). Add the sidebar entry to `admin-layout.tsx`'s `configuration` group per the Interfaces block: `{ path: "/admin/avatar-personas", labelKey: "avatarPersonas", icon: Smile }` immediately after `voiceLive`, importing `Smile` from `lucide-react`. Do NOT alter the existing `voiceLive` entry or its surrounding `<nav>` structure — the new entry must be a sibling `<NavLink>` inside the same existing `<nav>` element.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b</automated>
  </verify>
  <done>`/admin/avatar-personas` route resolves to the new page; sidebar shows "Avatar Personas" immediately after "Voice Live"; `npx tsc -b` passes with zero errors; no existing route/nav entry was removed or reordered besides the single insertion.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright E2E + full frontend regression gate</name>
  <files>frontend/e2e/admin-avatar-personas.spec.ts</files>
  <action>
    Write `frontend/e2e/admin-avatar-personas.spec.ts` covering the primary admin CRUD user story end-to-end against a running dev server (mirror an existing admin E2E spec's auth-setup/login helper, e.g. the pattern used by `voice-live-proxy.spec.ts` or `routing.spec.ts` for admin login state): (1) admin logs in and navigates to `/admin/avatar-personas` via the sidebar "Avatar Personas" link, (2) table shows the seeded default persona with the Default badge, (3) admin clicks "Create Persona", fills name + selects a character/style + sets a voice + greeting + prompt fragment, saves, and the new row appears in the table, (4) admin sets the new persona as default and confirms the badge moves, (5) admin attempts to delete the (now non-default) previously-seeded persona and it succeeds, OR attempts to delete the current default and sees the disabled-button tooltip / blocked action. Also run `frontend/e2e/voice-live-proxy.spec.ts` to confirm line 489's "Voice Live" link assertion still passes (regression guard for the sidebar edit in Task 2). Finish with the full frontend gate per CLAUDE.md: `npx tsc -b`, `npm run build`, `npm run test` (full Vitest suite), and the two Playwright specs.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run build && npm run test && npx playwright test e2e/admin-avatar-personas.spec.ts e2e/voice-live-proxy.spec.ts --config=e2e/playwright.config.ts</automated>
  </verify>
  <done>New E2E spec passes covering create/edit/set-default/delete-guard; voice-live-proxy.spec.ts's "Voice Live" link assertion still passes unchanged; full tsc/build/vitest gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| admin browser -> `/api/v1/admin/avatar-personas` | authenticated admin input crosses here (form fields become API request bodies) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05 | Tampering | `persona-dialog.tsx` character/style fields | mitigate | Character/style values are selected from the static `AVATAR_CHARACTERS` grid only (no free-text input for these fields), matching D-03's allowlist-only constraint; backend independently re-validates per 36-01's threat register |
| T-36-06 | Information Disclosure | greeting/prompt-fragment `Textarea` fields | accept | Admin-only write surface (already gated by existing `require_role("admin")` on every backing endpoint); sanitization of injected content into chat happens at PERSONA-04 (36-03+), out of this plan's scope |

</threat_model>

<verification>
Run `cd frontend && npx tsc -b && npm run build && npm run test && npx playwright test e2e/admin-avatar-personas.spec.ts e2e/voice-live-proxy.spec.ts --config=e2e/playwright.config.ts` and confirm all green. Manually confirm `frontend/src/i18n/locale-parity.test.ts` still passes with the new keys present in all 5 locales.
</verification>

<success_criteria>
- `/admin/avatar-personas` is reachable via sidebar, renders the persona catalog table
- Admin can create/edit/delete/set-default a persona through the dialog and table, backed by 36-01's API
- Default-guard 409 surfaces as a toast; delete/disable actions on the default row are visibly disabled with a tooltip
- `voice-live-proxy.spec.ts:489`'s "Voice Live" link assertion is unaffected
- All new copy present and distinct (not fallback English) in zh-CN/en-US/es-ES/es-MX/es-US; `untranslated-whitelist.ts` cap unchanged
- `tsc -b`, `npm run build`, `npm run test`, and both Playwright specs pass
</success_criteria>

<output>
After completion, create `.planning/phases/36-avatar-persona-selection-post-login-landing/36-02-SUMMARY.md`
</output>
