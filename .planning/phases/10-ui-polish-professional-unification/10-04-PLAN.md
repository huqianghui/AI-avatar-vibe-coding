---
phase: 10-ui-polish-professional-unification
plan: 04
type: execute
wave: 3
depends_on: [10-02, 10-03]
files_modified:
  - frontend/src/pages/user/dashboard.tsx
  - frontend/src/pages/user/training.tsx
  - frontend/src/pages/user/session-history.tsx
  - frontend/src/pages/user/scoring-feedback.tsx
  - frontend/src/pages/user/reports.tsx
  - frontend/src/pages/user/training-session.tsx
  - frontend/src/pages/user/conference-session.tsx
  - frontend/src/pages/user/voice-session.tsx
  - frontend/src/pages/login.tsx
  - frontend/src/components/layouts/auth-layout.tsx
autonomous: true
requirements: [UI-02, UI-03, UI-04, UI-05]

must_haves:
  truths:
    - "User dashboard matches Figma 02-user-dashboard: stat cards row, session list, skill radar, recommended scenarios"
    - "Scenario selection matches Figma 03-scenario-selection: card grid, filters, difficulty badges"
    - "F2F training session matches Figma 04-f2f-coaching: 3-panel layout, mobile vertical stacking"
    - "Scoring feedback matches Figma 06-scoring-feedback: dimension breakdown, strengths/weaknesses, quotes"
    - "Session history matches Figma 07-session-history: table with filters, status badges"
    - "Login page matches Figma 01-login-and-layout A1: centered card, gradient background, dark mode support"
    - "All user pages use design token colors, consistent spacing (24px gaps between cards), proper typography"
    - "Training session multi-panel layout stacks vertically on mobile per D-24"
  artifacts:
    - path: "frontend/src/pages/user/dashboard.tsx"
      provides: "Polished user dashboard page"
      contains: "bg-card"
    - path: "frontend/src/pages/user/training.tsx"
      provides: "Polished scenario selection page"
      contains: "gap-6"
    - path: "frontend/src/pages/user/training-session.tsx"
      provides: "Polished F2F training with mobile stacking"
      contains: "flex-col lg:flex-row"
    - path: "frontend/src/pages/login.tsx"
      provides: "Polished login page with dark mode support"
      contains: "bg-background"
  key_links:
    - from: "frontend/src/pages/user/dashboard.tsx"
      to: "frontend/src/components/shared/stat-card.tsx"
      via: "StatCard usage with design tokens"
      pattern: "StatCard"
    - from: "frontend/src/pages/user/training.tsx"
      to: "frontend/src/components/shared/hcp-profile-card.tsx"
      via: "HCPProfileCard in scenario grid"
      pattern: "HCPProfileCard"
---

<objective>
Audit and polish all user-facing pages (8 pages + login + auth-layout) against their Figma prompt specs. Fix spacing, typography, dark mode, responsive layout, and visual consistency.

Purpose: Per D-17, all pages get equal polish — demo could visit any page. User pages are the primary demo path. Per D-01/D-02, each page must pixel-close match its Figma prompt spec. Per D-22, desktop/tablet/mobile all get equal attention.
Output: 8 polished user pages + login page + auth-layout, all matching Figma specs with dark mode and responsive support.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-polish-professional-unification/10-CONTEXT.md
@.planning/phases/10-ui-polish-professional-unification/10-UI-SPEC.md
@.planning/phases/10-ui-polish-professional-unification/10-01-SUMMARY.md
@.planning/phases/10-ui-polish-professional-unification/10-02-SUMMARY.md
@.planning/phases/10-ui-polish-professional-unification/10-03-SUMMARY.md

Figma prompt references (read during execution):
@docs/figma-prompts/01-login-and-layout.md
@docs/figma-prompts/02-user-dashboard.md
@docs/figma-prompts/03-scenario-selection.md
@docs/figma-prompts/04-f2f-coaching.md
@docs/figma-prompts/05-conference-mode.md
@docs/figma-prompts/06-scoring-feedback.md
@docs/figma-prompts/07-session-history.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and polish login, dashboard, scenario selection, session history, scoring feedback, reports pages</name>
  <files>frontend/src/pages/login.tsx, frontend/src/components/layouts/auth-layout.tsx, frontend/src/pages/user/dashboard.tsx, frontend/src/pages/user/training.tsx, frontend/src/pages/user/session-history.tsx, frontend/src/pages/user/scoring-feedback.tsx, frontend/src/pages/user/reports.tsx</files>
  <read_first>
    - docs/figma-prompts/01-login-and-layout.md (login spec A1)
    - docs/figma-prompts/02-user-dashboard.md (dashboard layout spec)
    - docs/figma-prompts/03-scenario-selection.md (scenario cards spec)
    - docs/figma-prompts/06-scoring-feedback.md (scoring report spec)
    - docs/figma-prompts/07-session-history.md (session list spec)
    - frontend/src/pages/login.tsx (current login page)
    - frontend/src/components/layouts/auth-layout.tsx (current auth layout)
    - frontend/src/pages/user/dashboard.tsx (current user dashboard)
    - frontend/src/pages/user/training.tsx (current scenario selection)
    - frontend/src/pages/user/session-history.tsx (current session history)
    - frontend/src/pages/user/scoring-feedback.tsx (current scoring feedback)
    - frontend/src/pages/user/reports.tsx (current user reports)
    - .planning/phases/10-ui-polish-professional-unification/10-UI-SPEC.md (spacing, typography, responsive breakpoints)
  </read_first>
  <action>
For each page, read its corresponding Figma prompt doc, then audit and fix. Apply these universal corrections across ALL pages:

**Universal page rules:**
- Page title: `<h1 className="text-2xl font-medium text-foreground">` (per UI-SPEC typography)
- Section gaps: `space-y-6` or `gap-6` (24px per D-06)
- Card gaps: `gap-6` (24px between cards per Figma "consistent 24px gaps")
- Card styling: `bg-card rounded-lg border border-border shadow-sm p-4` (16px padding per UI-SPEC)
- No hardcoded colors — all use design tokens
- Dark mode: `bg-background` for page containers, `bg-card` for cards, `text-foreground` for text
- Responsive: mobile single-column, tablet 2-col, desktop 3-4 col grids per UI-SPEC breakpoints
- Loading states: use `<LoadingState variant="card|table|spinner" />` from shared components
- Empty states: use `<EmptyState />` from shared components

**Per-page fixes:**

**login.tsx** (per Figma 01-login-and-layout A1):
- Card width: `max-w-[480px] w-full` (480px per spec)
- Card: `bg-card rounded-lg shadow-lg p-8` with subtle shadow
- Remove any hardcoded blue backgrounds — use `bg-primary` or gradient `from-primary/5 to-background`
- Dark mode: card bg uses `bg-card`, inputs use `bg-input-background`
- "Sign In" button: full-width `w-full` primary variant
- Add `transition-colors duration-150` to all interactive elements

**auth-layout.tsx** (per Figma 01-login-and-layout A1):
- Background: subtle gradient that uses the accent theme. Use `bg-gradient-to-br from-primary/5 via-background to-primary/3` for light mode, which automatically adapts to any accent color.
- Replace any hardcoded gradient colors with token-based classes
- Dark mode support on background
- Ensure language switcher position is top-right per existing pattern
- Copyright uses `text-xs text-muted-foreground`

**dashboard.tsx** (per Figma 02-user-dashboard):
- Top row: 4 stat cards in `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6` (per responsive breakpoints)
- Middle section: 60/40 split on desktop (`grid grid-cols-1 lg:grid-cols-5 gap-6` with session list in `lg:col-span-3` and action cards in `lg:col-span-2`)
- Stat cards use `bg-card` container, `text-2xl font-medium` for the number
- Session list items use consistent `gap-4` spacing between items
- Recommended scenarios section at bottom
- Ensure skill radar chart has proper sizing and respects theme colors

**training.tsx** (per Figma 03-scenario-selection):
- Tab navigation for scenario types (F2F, Conference, Voice)
- Card grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6`
- Scenario cards: `bg-card rounded-lg border border-border shadow-sm hover:shadow-md hover:border-primary/30 transition-all duration-150`
- Difficulty badges using Badge component variants (easy=default, medium=secondary, hard=destructive)
- Filter bar above the grid with proper spacing `mb-6`

**session-history.tsx** (per Figma 07-session-history):
- Table layout with proper headers: `text-sm font-medium text-muted-foreground` for headers, `text-sm text-foreground` for data
- Status badges using StatusBadge component
- Score display with color coding (>=80 green/strength, 60-79 primary, <60 orange/weakness)
- Date formatting consistent across all instances
- Pagination controls at bottom
- Filter row above table with scenario type, date range selectors
- Responsive: on mobile, switch to card list instead of table

**scoring-feedback.tsx** (per Figma 06-scoring-feedback):
- Overall score: large `text-4xl font-medium` with color-coded primary/strength/weakness
- Dimension breakdown: vertical list of `DimensionBar` components with `gap-4`
- Strengths section: `bg-strength/5 rounded-lg p-4` with bullet points
- Weaknesses section: `bg-weakness/5 rounded-lg p-4` with bullet points
- Improvement suggestions: `bg-improvement/5 rounded-lg p-4` with bullet points
- Conversation quotes in styled blockquote: `border-l-2 border-primary pl-4 text-sm italic text-muted-foreground`
- Action buttons: "Try Again" (primary), "Back to History" (ghost)

**reports.tsx:**
- Chart containers use `bg-card rounded-lg border border-border p-4`
- Date range filter at top
- Performance trend chart respects theme colors (uses chart-1 through chart-5)
- Radar chart for skill overview
- Ensure all Recharts components use CSS variable colors from design tokens
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/login.tsx contains `max-w-` (card width constraint)
    - frontend/src/pages/login.tsx does NOT contain hardcoded hex color values in className or style props
    - frontend/src/components/layouts/auth-layout.tsx contains `bg-gradient` or `from-primary`
    - frontend/src/pages/user/dashboard.tsx contains `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` or similar responsive grid
    - frontend/src/pages/user/dashboard.tsx contains `gap-6`
    - frontend/src/pages/user/training.tsx contains `grid` and `gap-6`
    - frontend/src/pages/user/session-history.tsx contains `text-muted-foreground` (table headers)
    - frontend/src/pages/user/scoring-feedback.tsx contains `DimensionBar` or `dimension`
    - frontend/src/pages/user/scoring-feedback.tsx contains `bg-strength` or `text-strength`
    - `npx tsc -b` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Login, auth-layout, dashboard, training, session history, scoring feedback, and reports pages all audited against Figma specs. Consistent spacing (24px gaps), typography, dark mode, responsive grids, and design token colors. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Polish training session pages (F2F, conference, voice) — mobile stacking + dark mode</name>
  <files>frontend/src/pages/user/training-session.tsx, frontend/src/pages/user/conference-session.tsx, frontend/src/pages/user/voice-session.tsx</files>
  <read_first>
    - docs/figma-prompts/04-f2f-coaching.md (F2F training layout spec)
    - docs/figma-prompts/05-conference-mode.md (conference layout spec)
    - frontend/src/pages/user/training-session.tsx (current F2F training page)
    - frontend/src/pages/user/conference-session.tsx (current conference page)
    - frontend/src/pages/user/voice-session.tsx (current voice session page)
    - frontend/src/components/coach/left-panel.tsx (HCP info panel)
    - frontend/src/components/coach/center-panel.tsx (chat area)
    - frontend/src/components/coach/right-panel.tsx (hints panel)
    - .planning/phases/10-ui-polish-professional-unification/10-UI-SPEC.md (responsive breakpoints, mobile stacking D-24)
  </read_first>
  <action>
**Training session pages** are full-screen immersive experiences (no UserLayout wrapper). They need special attention for:

1. **Dark mode support** (per D-03): Replace any `bg-white` with `bg-background`, any `text-black` with `text-foreground`, any hardcoded colors with design tokens.

2. **Mobile vertical stacking** (per D-24):
   Current layout is horizontal 3-panel (left: HCP info, center: chat, right: hints). On mobile, stack vertically:
   - Use `flex flex-col lg:flex-row` on the panel container
   - Mobile order: HCP info panel (top, compact) → chat area (middle, fills remaining height) → coaching hints panel (bottom, collapsible)
   - HCP info on mobile: compact single-row layout with `flex items-center gap-3` showing avatar + name + specialty
   - Hints panel on mobile: wrap in a collapsible details/summary or accordion pattern. Hidden by default, toggle with a "Show Hints" button at the bottom
   - Chat area on mobile: `flex-1 min-h-0` to fill remaining space with scroll

3. **Panel proportions on desktop** (per Figma 04-f2f-coaching):
   - Left panel (HCP info): `w-64 lg:w-72` (fixed width)
   - Center panel (chat): `flex-1` (fills remaining)
   - Right panel (hints): `w-72 lg:w-80` (fixed width)
   - Panel separator: `border-r border-border` between panels

4. **Header bar** for session pages:
   - Sticky top bar with `bg-background border-b` containing: back button (ghost), session title, timer, end session button (destructive variant)
   - Timer uses `text-sm font-mono text-muted-foreground`

**Per-page specifics:**

**training-session.tsx** (F2F):
- 3-panel layout as described above
- Chat area: messages scroll container with `flex-1 overflow-y-auto`
- Input area fixed at bottom of center panel
- Key message checklist in right panel uses `text-sm` with checkmark icons

**conference-session.tsx**:
- Similar 3-panel but left panel shows audience HCP list instead of single HCP
- Each audience HCP shows avatar + name + status (speaking/waiting)
- Question queue visible in right panel
- Same responsive stacking on mobile

**voice-session.tsx**:
- Avatar/video display area in center instead of text chat
- Transcript display below avatar area
- Voice controls (mic, speaker) in bottom bar
- Same responsive stacking on mobile: avatar on top, transcript below
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/user/training-session.tsx contains `flex-col lg:flex-row` or `flex-col md:flex-row` (mobile stacking)
    - frontend/src/pages/user/training-session.tsx contains `bg-background` (dark mode)
    - frontend/src/pages/user/training-session.tsx does NOT contain `bg-white` as a standalone background class (should be bg-background)
    - frontend/src/pages/user/conference-session.tsx contains `flex-col` (mobile stacking)
    - frontend/src/pages/user/conference-session.tsx contains `bg-background`
    - frontend/src/pages/user/voice-session.tsx contains `bg-background`
    - `npx tsc -b` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 3 training session pages polished: dark mode support, mobile vertical stacking (D-24), proper panel proportions, consistent design tokens, responsive breakpoints. Build passes.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc -b` passes
- `cd frontend && npm run build` passes
- All 10 user-facing pages use design token colors (no hardcoded hex)
- Dashboard shows responsive 4-col stat grid
- Training session pages stack vertically on mobile
- Login page has proper card width and gradient background
- Scoring feedback shows color-coded dimension bars
</verification>

<success_criteria>
All user pages and login audited against Figma prompts with consistent spacing (24px gaps), typography, dark mode, responsive layouts, and design token colors. Training session pages have proper mobile stacking per D-24.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-polish-professional-unification/10-04-SUMMARY.md`
</output>
