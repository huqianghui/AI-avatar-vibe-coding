---
phase: 10-ui-polish-professional-unification
plan: 05
type: execute
wave: 3
depends_on: [10-02, 10-03]
files_modified:
  - frontend/src/pages/admin/dashboard.tsx
  - frontend/src/pages/admin/users.tsx
  - frontend/src/pages/admin/hcp-profiles.tsx
  - frontend/src/pages/admin/scenarios.tsx
  - frontend/src/pages/admin/scoring-rubrics.tsx
  - frontend/src/pages/admin/training-materials.tsx
  - frontend/src/pages/admin/reports.tsx
  - frontend/src/pages/admin/azure-config.tsx
  - frontend/src/pages/admin/settings.tsx
autonomous: true
requirements: [UI-06]

must_haves:
  truths:
    - "Admin dashboard matches Figma 08-admin-dashboard: org stats, user activity, chart containers"
    - "Admin user management matches Figma 08-admin-dashboard: user table with filters"
    - "HCP profiles matches Figma 09-admin-hcp-scenarios: profile cards or table with CRUD actions"
    - "Scenarios matches Figma 09-admin-hcp-scenarios: scenario list with difficulty and weights"
    - "Azure config matches Figma 10-admin-azure-settings: service config cards with status indicators"
    - "Materials matches Figma 11-admin-materials-reports: file list with upload area"
    - "Reports matches Figma 11-admin-materials-reports: chart dashboard with filters"
    - "All admin pages use consistent spacing, typography, dark mode, and design tokens"
  artifacts:
    - path: "frontend/src/pages/admin/dashboard.tsx"
      provides: "Polished admin dashboard"
      contains: "bg-card"
    - path: "frontend/src/pages/admin/azure-config.tsx"
      provides: "Polished Azure config page"
      contains: "bg-card"
    - path: "frontend/src/pages/admin/hcp-profiles.tsx"
      provides: "Polished HCP profiles management"
      contains: "gap-6"
  key_links:
    - from: "frontend/src/pages/admin/*.tsx"
      to: "frontend/src/components/shared/"
      via: "Shared components (StatCard, StatusBadge, EmptyState, LoadingState)"
      pattern: "StatCard|StatusBadge|EmptyState|LoadingState"
---

<objective>
Audit and polish all 9 admin pages against their Figma prompt specs. Fix spacing, typography, dark mode, responsive layout, and visual consistency for professional demo presentation.

Purpose: Per D-17, all pages get equal polish — demo could visit any page. Admin pages are visited during the demo flow for configuration and analytics. Per D-01/D-02, each page must match its Figma prompt spec. The admin experience must feel as polished as the user experience.
Output: 9 polished admin pages matching Figma specs with dark mode and responsive support.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-polish-professional-unification/10-CONTEXT.md
@.planning/phases/10-ui-polish-professional-unification/10-UI-SPEC.md
@.planning/phases/10-ui-polish-professional-unification/10-01-SUMMARY.md
@.planning/phases/10-ui-polish-professional-unification/10-02-SUMMARY.md
@.planning/phases/10-ui-polish-professional-unification/10-03-SUMMARY.md

Figma prompt references (read during execution):
@docs/figma-prompts/08-admin-dashboard-users.md
@docs/figma-prompts/09-admin-hcp-scenarios.md
@docs/figma-prompts/10-admin-azure-settings.md
@docs/figma-prompts/11-admin-materials-reports.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and polish admin dashboard, users, HCP profiles, scenarios, scoring rubrics pages</name>
  <files>frontend/src/pages/admin/dashboard.tsx, frontend/src/pages/admin/users.tsx, frontend/src/pages/admin/hcp-profiles.tsx, frontend/src/pages/admin/scenarios.tsx, frontend/src/pages/admin/scoring-rubrics.tsx</files>
  <read_first>
    - docs/figma-prompts/08-admin-dashboard-users.md (admin dashboard + user management spec)
    - docs/figma-prompts/09-admin-hcp-scenarios.md (HCP profiles + scenarios spec)
    - frontend/src/pages/admin/dashboard.tsx (current admin dashboard)
    - frontend/src/pages/admin/users.tsx (current user management)
    - frontend/src/pages/admin/hcp-profiles.tsx (current HCP profiles)
    - frontend/src/pages/admin/scenarios.tsx (current scenarios)
    - frontend/src/pages/admin/scoring-rubrics.tsx (current scoring rubrics)
    - .planning/phases/10-ui-polish-professional-unification/10-UI-SPEC.md (spacing, typography, responsive breakpoints)
  </read_first>
  <action>
Apply the same universal page rules from Plan 04 (spacing, typography, dark mode, responsive grids, design tokens) plus these per-page specifics:

**admin/dashboard.tsx** (per Figma 08-admin-dashboard-users):
- Top row: org-level stat cards in `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6`
- Stats: Total Users, Active Sessions, Avg Score, Training Completion
- Chart section: 2-column grid `grid grid-cols-1 lg:grid-cols-2 gap-6`
  - Left: Bar chart (BU comparison) in `bg-card rounded-lg border p-4`
  - Right: Skill gap heatmap in `bg-card rounded-lg border p-4`
- Recent activity section at bottom
- Chart colors: use `var(--chart-1)` through `var(--chart-5)` via CSS variables
- All chart containers: `bg-card rounded-lg border border-border shadow-sm p-4`
- Page title: `<h1 className="text-2xl font-medium text-foreground mb-6">` with i18n

**admin/users.tsx** (per Figma 08-admin-dashboard-users):
- Search bar + role filter at top in a row: `flex flex-col sm:flex-row gap-4 mb-6`
- User table with columns: Name, Email, Role, Business Unit, Last Active, Status
- Table header: `text-sm font-medium text-muted-foreground`
- Table rows: `text-sm text-foreground` with `hover:bg-muted/50 transition-colors duration-150`
- Status uses StatusBadge component (active=green, inactive=gray)
- Role badge: admin=primary, user=secondary
- Pagination at bottom: `flex items-center justify-between mt-4`
- Responsive: on mobile, use card layout instead of table
- Empty state when no users match filter

**admin/hcp-profiles.tsx** (per Figma 09-admin-hcp-scenarios):
- "Create Profile" button at top right: primary variant
- Profile cards OR table view (keep whichever exists, polish it):
  - Cards: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6` using `HCPProfileCard`
  - Table: proper headers, avatar column, specialty, personality tags
- Each card/row: edit + delete actions with `transition-colors duration-150`
- Delete confirmation uses Dialog component with destructive button text from i18n
- Loading state: skeleton cards/table rows
- Empty state when no profiles

**admin/scenarios.tsx** (per Figma 09-admin-hcp-scenarios):
- "Create Scenario" button at top right
- Scenario list/cards with: name, product, difficulty badge, assigned HCP, scoring weights
- Difficulty badge: Easy=`<Badge variant="outline">`, Medium=`<Badge variant="secondary">`, Hard=`<Badge variant="destructive">`
- Grid: `grid grid-cols-1 lg:grid-cols-2 gap-6`
- Edit form in Dialog or full page (keep existing pattern, polish styling)
- Scoring weights: Slider components with percentage labels
- Loading and empty states

**admin/scoring-rubrics.tsx:**
- Rubric list with type indicator (F2F, Conference)
- Dimension list with weight percentages
- Edit form with slider controls for weights
- Sum validation display (must total 100%)
- `bg-card rounded-lg border shadow-sm p-4` for each rubric card
- Loading and empty states
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/admin/dashboard.tsx contains `gap-6` (card spacing)
    - frontend/src/pages/admin/dashboard.tsx contains `bg-card` (chart containers)
    - frontend/src/pages/admin/dashboard.tsx contains `grid-cols-` (responsive grid)
    - frontend/src/pages/admin/users.tsx contains `text-muted-foreground` (table headers)
    - frontend/src/pages/admin/users.tsx contains `transition` (hover states)
    - frontend/src/pages/admin/hcp-profiles.tsx contains `gap-6`
    - frontend/src/pages/admin/scenarios.tsx contains `Badge` or `badge`
    - frontend/src/pages/admin/scoring-rubrics.tsx contains `bg-card`
    - `npx tsc -b` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Admin dashboard, users, HCP profiles, scenarios, and scoring rubrics pages polished against Figma specs with consistent spacing, typography, dark mode, responsive grids, and design tokens. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and polish admin materials, reports, azure config, settings pages</name>
  <files>frontend/src/pages/admin/training-materials.tsx, frontend/src/pages/admin/reports.tsx, frontend/src/pages/admin/azure-config.tsx, frontend/src/pages/admin/settings.tsx</files>
  <read_first>
    - docs/figma-prompts/10-admin-azure-settings.md (Azure config card layout spec)
    - docs/figma-prompts/11-admin-materials-reports.md (materials + reports spec)
    - frontend/src/pages/admin/training-materials.tsx (current materials page)
    - frontend/src/pages/admin/reports.tsx (current admin reports)
    - frontend/src/pages/admin/azure-config.tsx (current Azure config page)
    - frontend/src/pages/admin/settings.tsx (current settings page)
    - .planning/phases/10-ui-polish-professional-unification/10-UI-SPEC.md (spacing, typography, responsive breakpoints)
  </read_first>
  <action>
Apply universal page rules plus these per-page specifics:

**admin/training-materials.tsx** (per Figma 11-admin-materials-reports):
- Upload area: drag-and-drop zone with `border-2 border-dashed border-border rounded-lg p-8` on desktop, `p-4` on mobile
  - Hover state: `hover:border-primary/50 hover:bg-primary/5 transition-all duration-150`
  - Icon: Upload icon `size-6` centered with `text-muted-foreground`
  - Text: "Drop files here or click to upload" — `text-sm text-muted-foreground`
- Material list: table with columns: Name, Product, Type (PDF/DOCX/XLSX icon), Version, Size, Uploaded date
- Each row: download + delete actions
- File type icons: use lucide-react `FileText` for PDF, `FileSpreadsheet` for XLSX, `FileText` for DOCX — each `size-4`
- Version badge: `<Badge variant="outline">v{n}</Badge>`
- Grid of product folders at top, then file list below
- Loading: skeleton table rows
- Empty: EmptyState with upload CTA

**admin/reports.tsx** (per Figma 11-admin-materials-reports):
- Date range filter + BU filter at top in row: `flex flex-col sm:flex-row gap-4 mb-6`
- Chart grid: `grid grid-cols-1 lg:grid-cols-2 gap-6`
- Chart containers: `bg-card rounded-lg border border-border shadow-sm p-4`
- Charts:
  - Training completion by BU (bar chart)
  - Score distribution (histogram)
  - Top performers table
  - Skill gap heatmap
- Export buttons: PDF (primary outline) and Excel (primary outline) at top right
- Chart colors: CSS variables `var(--chart-1)` through `var(--chart-5)`
- Loading: skeleton chart placeholders

**admin/azure-config.tsx** (per Figma 10-admin-azure-settings):
- Service config cards in `grid grid-cols-1 lg:grid-cols-2 gap-6`
- Each card: `bg-card rounded-lg border border-border shadow-sm`
  - Header: service icon (Cloud for OpenAI, Mic for Speech, User for Avatar, Waves for Voice Live) + service name + status dot (green=connected, red=disconnected, gray=not configured)
  - Status dot: `w-2.5 h-2.5 rounded-full` with `bg-strength` (connected), `bg-destructive` (error), `bg-muted-foreground` (not configured)
  - Body: Form fields (endpoint, API key masked, model, region)
  - Footer: "Test Connection" button (outline variant), "Save" button (primary)
- AI Foundry master config card at top (full width, `lg:col-span-2`) — highlighted with `border-primary/30`
- Test connection result: success toast (sonner) or inline error message `text-sm text-destructive`
- Loading: skeleton form fields in card layout

**admin/settings.tsx:**
- Settings grouped in cards: General, Features, Notifications
- Each card: `bg-card rounded-lg border border-border shadow-sm p-6`
- Feature toggles: Switch components with labels
- Form layout: `space-y-4` within each card
- Save button at bottom of each section or single save at bottom
- Dark mode support on all form elements
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/admin/training-materials.tsx contains `border-dashed` (upload area)
    - frontend/src/pages/admin/training-materials.tsx contains `bg-card`
    - frontend/src/pages/admin/reports.tsx contains `gap-6`
    - frontend/src/pages/admin/reports.tsx contains `bg-card`
    - frontend/src/pages/admin/azure-config.tsx contains `bg-card`
    - frontend/src/pages/admin/azure-config.tsx contains `rounded-full` (status dot)
    - frontend/src/pages/admin/settings.tsx contains `bg-card`
    - frontend/src/pages/admin/settings.tsx contains `Switch` or `switch`
    - `npx tsc -b` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Admin materials, reports, Azure config, and settings pages polished against Figma specs with consistent spacing, typography, dark mode, and design tokens. Build passes.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc -b` passes
- `cd frontend && npm run build` passes
- All 9 admin pages use consistent spacing (24px gaps), card styling, and design tokens
- Azure config page shows service status indicators
- Materials page has styled drag-and-drop upload area
- Reports page has properly contained charts
</verification>

<success_criteria>
All 9 admin pages audited against Figma prompts with consistent spacing, typography, dark mode, responsive layouts, and design token colors. No hardcoded colors remain. Professional appearance suitable for customer demo.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-polish-professional-unification/10-05-SUMMARY.md`
</output>
