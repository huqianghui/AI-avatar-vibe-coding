---
phase: 10-ui-polish-professional-unification
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - frontend/src/components/ui/badge.tsx
  - frontend/src/components/shared/stat-card.tsx
  - frontend/src/components/shared/session-item.tsx
  - frontend/src/components/shared/action-card.tsx
  - frontend/src/components/shared/hcp-profile-card.tsx
  - frontend/src/components/shared/chat-bubble.tsx
  - frontend/src/components/shared/score-card.tsx
  - frontend/src/components/shared/dimension-bar.tsx
  - frontend/src/components/shared/empty-state.tsx
  - frontend/src/components/shared/loading-state.tsx
  - frontend/src/components/shared/status-badge.tsx
  - frontend/src/components/shared/language-switcher.tsx
  - frontend/src/components/ui/sonner.tsx
  - frontend/src/pages/not-found.tsx
autonomous: true
requirements: [UI-01, UI-03, UI-05]

must_haves:
  truths:
    - "All shared components use design token Tailwind classes — no hardcoded hex colors"
    - "Badge component has 'success' variant for score badges >80"
    - "All shared components have consistent icon sizing (16/20/24px per role)"
    - "Sonner toast wrapper passes through theme-aware styling"
    - "EmptyState and LoadingState components used consistently across the app"
    - "Not-found page styled professionally with BeiGene branding"
    - "All components use transition-colors duration-150 on interactive elements per D-07"
  artifacts:
    - path: "frontend/src/components/ui/badge.tsx"
      provides: "Badge with success variant"
      contains: "success"
    - path: "frontend/src/components/shared/empty-state.tsx"
      provides: "Standardized empty state with i18n"
      contains: "text-muted-foreground"
    - path: "frontend/src/pages/not-found.tsx"
      provides: "Polished 404 page"
      contains: "bg-background"
  key_links:
    - from: "frontend/src/components/shared/*.tsx"
      to: "frontend/src/styles/index.css"
      via: "CSS custom property-backed Tailwind classes (bg-primary, text-foreground, etc.)"
      pattern: "bg-primary|text-foreground|bg-card|text-muted-foreground"
---

<objective>
Audit and polish all shared components (14 domain components + UI base components) for design token consistency, icon sizing standardization, transition animations, and dark mode compatibility. Add Badge success variant, polish EmptyState/LoadingState, and style the 404 page.

Purpose: Per D-05, all shared components have ONE canonical global style. This plan ensures every component responds correctly to accent themes and dark mode, uses consistent typography/spacing/icons per the UI-SPEC. These components are reused across all 16+ pages, so polishing here has maximum impact.
Output: Polished, theme-responsive shared components with no hardcoded colors.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-polish-professional-unification/10-CONTEXT.md
@.planning/phases/10-ui-polish-professional-unification/10-UI-SPEC.md
@.planning/phases/10-ui-polish-professional-unification/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish shared domain components — design tokens, icons, transitions, dark mode</name>
  <files>frontend/src/components/shared/stat-card.tsx, frontend/src/components/shared/session-item.tsx, frontend/src/components/shared/action-card.tsx, frontend/src/components/shared/hcp-profile-card.tsx, frontend/src/components/shared/chat-bubble.tsx, frontend/src/components/shared/score-card.tsx, frontend/src/components/shared/dimension-bar.tsx, frontend/src/components/shared/empty-state.tsx, frontend/src/components/shared/loading-state.tsx, frontend/src/components/shared/status-badge.tsx, frontend/src/components/shared/language-switcher.tsx</files>
  <read_first>
    - frontend/src/components/shared/stat-card.tsx
    - frontend/src/components/shared/session-item.tsx
    - frontend/src/components/shared/action-card.tsx
    - frontend/src/components/shared/hcp-profile-card.tsx
    - frontend/src/components/shared/chat-bubble.tsx
    - frontend/src/components/shared/score-card.tsx
    - frontend/src/components/shared/dimension-bar.tsx
    - frontend/src/components/shared/empty-state.tsx
    - frontend/src/components/shared/loading-state.tsx
    - frontend/src/components/shared/status-badge.tsx
    - frontend/src/components/shared/language-switcher.tsx
    - docs/figma-prompts/00-design-system.md (component specs)
    - .planning/phases/10-ui-polish-professional-unification/10-UI-SPEC.md (component variant audit table, typography, spacing)
  </read_first>
  <action>
Audit each shared component file. For each component, apply these corrections where needed:

**Universal rules to apply to ALL components:**

1. **No hardcoded hex colors.** Search for any `#` followed by hex digits or any `style={{ ... color }}` inline. Replace with Tailwind design token classes:
   - `#1E40AF` → `bg-primary` or `text-primary`
   - `#FFFFFF` → `bg-background` or `text-primary-foreground`
   - `#475569` → `text-secondary` or `text-muted-foreground`
   - `#22C55E` → `text-strength`
   - `#F97316` → `text-weakness`
   - `#A855F7` → `text-improvement`
   - Any other raw color → find the closest design token

2. **Icon sizing standardization** (per Claude's discretion in CONTEXT.md):
   - Inline icons (inside text/labels): `size-4` (16px)
   - Nav/button icons: `size-5` (20px)
   - Page header/display icons: `size-6` (24px)
   - All icons: `strokeWidth={2}` (if not default)

3. **Hover/focus transitions** (per D-07): Ensure all interactive elements (buttons, links, clickable cards) have `transition-colors duration-150` class.

4. **Typography consistency** (per D-04, UI-SPEC):
   - Captions/timestamps: `text-xs text-muted-foreground` (12px)
   - Body text: `text-sm text-foreground` (14px, 400 weight)
   - Labels/card titles: `text-sm font-medium` (14px, 500 weight)
   - Section headings: `text-xl font-medium` (20px)
   - Display numbers: `text-2xl font-medium` (24px)

5. **Spacing consistency** (per D-06, UI-SPEC):
   - Card content padding: `p-4` (16px)
   - Card-to-card gaps: `gap-6` (24px)
   - Icon-to-text gaps: `gap-1` (4px) for inline, `gap-2` (8px) for nav items, `gap-3` (12px) for list items

**Per-component specific fixes:**

**stat-card.tsx:** Ensure trend arrow colors use `text-strength` (up/positive) and `text-destructive` (down/negative) instead of any hardcoded green/red. Display number should use `text-2xl font-medium`. Card container should use `bg-card` and `rounded-lg shadow-sm border border-border`.

**session-item.tsx:** Ensure score display uses design token colors. Timestamp uses `text-xs text-muted-foreground`. Add `transition-colors duration-150` to the clickable row/card.

**action-card.tsx:** Ensure hover state uses `hover:border-primary/50 hover:shadow-md transition-all duration-150`. Icon should be `size-5`.

**hcp-profile-card.tsx:** Difficulty badge should use the appropriate Badge variant. Personality tags use `bg-muted text-muted-foreground` not hardcoded colors. Avatar circle uses `bg-primary/10 text-primary`.

**chat-bubble.tsx:** HCP bubble (left) uses `bg-primary text-primary-foreground`, user bubble (right) uses `bg-muted text-foreground`. System messages use `text-center text-muted-foreground text-xs`. Ensure speaker name + color in conference mode also use tokens.

**score-card.tsx:** Score number uses `text-2xl font-medium`. Color coding: >=80 uses `text-strength`, 60-79 uses `text-primary`, <60 uses `text-weakness`.

**dimension-bar.tsx:** Progress bar fill uses `bg-primary`. Label uses `text-sm font-medium`. Percentage uses `text-sm text-muted-foreground`.

**empty-state.tsx:** Ensure it uses `text-muted-foreground` for the message, `bg-muted/50` for the container background, and the action button (if present) uses primary variant. Center the content vertically and horizontally.

**loading-state.tsx:** Ensure skeleton elements use `bg-muted animate-pulse` (existing pattern). Dark mode: `bg-muted` auto-adjusts.

**status-badge.tsx:** Ensure all variants use design tokens: active=`bg-strength/10 text-strength`, draft=`bg-muted text-muted-foreground`, error=`bg-destructive/10 text-destructive`, pending=`bg-chart-3/10 text-chart-3`.

**language-switcher.tsx:** Ensure button uses `transition-colors duration-150`. If it has any hardcoded colors, replace with tokens.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "style={{.*backgroundColor.*#" frontend/src/components/shared/ returns no results (no inline hex colors)
    - frontend/src/components/shared/stat-card.tsx contains `text-2xl` (display number)
    - frontend/src/components/shared/stat-card.tsx contains `bg-card`
    - frontend/src/components/shared/chat-bubble.tsx contains `bg-primary`
    - frontend/src/components/shared/empty-state.tsx contains `text-muted-foreground`
    - frontend/src/components/shared/action-card.tsx contains `transition`
    - frontend/src/components/shared/dimension-bar.tsx contains `bg-primary`
    - `npx tsc -b` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 11 shared domain components polished: no hardcoded colors, consistent icon sizing, hover transitions, dark mode compatible, typography matches UI-SPEC. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Badge success variant + Sonner theming + Not-found page polish</name>
  <files>frontend/src/components/ui/badge.tsx, frontend/src/components/ui/sonner.tsx, frontend/src/pages/not-found.tsx</files>
  <read_first>
    - frontend/src/components/ui/badge.tsx (current badge variants)
    - frontend/src/components/ui/sonner.tsx (current Sonner wrapper)
    - frontend/src/pages/not-found.tsx (current 404 page)
    - .planning/phases/10-ui-polish-professional-unification/10-UI-SPEC.md (component variant audit, toast standards)
  </read_first>
  <action>
**1. Add `success` variant to `frontend/src/components/ui/badge.tsx`** (per UI-SPEC component variant audit):

In the `badgeVariants` CVA definition, add a new `success` variant alongside existing `default`, `secondary`, `destructive`, `outline`:

```typescript
success: "border-transparent bg-strength/10 text-strength",
```

This uses the `--strength` (#22C55E) design token for score badges >=80.

**2. Update `frontend/src/components/ui/sonner.tsx`** (per D-21):

The Sonner Toaster component should be styled to use design token CSS variables for consistent theming. Ensure the component passes `className` prop that includes appropriate dark-mode-aware styles. Add default `toastOptions` in the component if not already set by App.tsx:

```typescript
<Sonner
  className="toaster group"
  toastOptions={{
    classNames: {
      toast: "group toast group-[.toaster]:bg-background group-[.toaster]:text-foreground group-[.toaster]:border-border group-[.toaster]:shadow-lg",
      description: "group-[.toast]:text-muted-foreground",
      actionButton: "group-[.toast]:bg-primary group-[.toast]:text-primary-foreground",
      cancelButton: "group-[.toast]:bg-muted group-[.toast]:text-muted-foreground",
    },
  }}
  {...props}
/>
```

This ensures toasts automatically adapt to dark mode and accent themes.

**3. Polish `frontend/src/pages/not-found.tsx`** (per Claude's discretion — error boundary page styling):

Update the 404 page to be professional and branded:
- Full-screen centered content with `min-h-screen bg-background flex items-center justify-center`
- Large "404" text: `text-8xl font-bold text-primary/20` (subtle primary color)
- i18n heading: use `t("error.notFound")` from common namespace → "Page not found. Return to dashboard."
- Split into heading `text-xl font-medium text-foreground` + description `text-sm text-muted-foreground mt-2`
- "Return to Dashboard" Button with `<Link to="/user/dashboard">` using primary variant
- Add the same lightbulb SVG icon used in layouts (32px, `text-primary`) above the 404 text for branding
- Import `useTranslation` from `react-i18next`, `Link` from `react-router-dom`, `Button` from `@/components/ui`
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ui/badge.tsx contains `success`
    - frontend/src/components/ui/badge.tsx contains `bg-strength`
    - frontend/src/components/ui/sonner.tsx contains `bg-background` or `group-[.toaster]:bg-background`
    - frontend/src/components/ui/sonner.tsx contains `text-foreground` or `group-[.toaster]:text-foreground`
    - frontend/src/pages/not-found.tsx contains `bg-background`
    - frontend/src/pages/not-found.tsx contains `404`
    - frontend/src/pages/not-found.tsx contains `text-primary`
    - frontend/src/pages/not-found.tsx contains `Link` or `useNavigate`
    - `npx tsc -b` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Badge has success variant using design token. Sonner toasts themed with CSS custom properties for dark mode + accent colors. Not-found page professionally styled with branding. Build passes.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc -b` passes
- `cd frontend && npm run build` passes
- No hardcoded hex colors remain in shared components (grep confirms)
- Badge component exports `success` variant
- Sonner toasts use design token CSS classes
- 404 page renders with professional styling and branding
</verification>

<success_criteria>
All shared components (14 domain + UI base) are polished with consistent design tokens, icon sizing, transitions, and dark mode support. Badge has success variant. Sonner themed. 404 page branded. No hardcoded colors in any shared component.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-polish-professional-unification/10-03-SUMMARY.md`
</output>
