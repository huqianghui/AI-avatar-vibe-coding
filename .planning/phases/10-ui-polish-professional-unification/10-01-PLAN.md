---
phase: 10-ui-polish-professional-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/styles/index.css
  - frontend/src/stores/theme-store.ts
  - frontend/index.html
  - frontend/src/components/shared/splash-screen.tsx
  - frontend/src/App.tsx
  - frontend/public/locales/en-US/common.json
  - frontend/public/locales/zh-CN/common.json
autonomous: true
requirements: [UI-01, UI-07]

must_haves:
  truths:
    - "5 accent color themes (blue, teal, purple, rose, amber) each override --primary, --sidebar-primary, --chart-1, --ring in both light and dark modes"
    - "Theme store persists accent + mode to localStorage and applies classes to document.documentElement"
    - "No flash of wrong theme on page load — synchronous script in index.html reads localStorage before React mounts"
    - "Splash screen shows on app startup with AI Avatar branding, auto-dismisses after loading"
    - "Page transition fadeIn keyframe animation defined in CSS"
  artifacts:
    - path: "frontend/src/styles/index.css"
      provides: "5 accent theme CSS class blocks + page-fade-in keyframe"
      contains: ".theme-teal"
    - path: "frontend/src/stores/theme-store.ts"
      provides: "Theme state management with useSyncExternalStore"
      exports: ["useThemeStore", "setThemeMode", "setAccentColor"]
    - path: "frontend/index.html"
      provides: "Synchronous theme flash prevention script"
      contains: "ai-coach-theme"
    - path: "frontend/src/components/shared/splash-screen.tsx"
      provides: "Branded splash screen component"
      exports: ["SplashScreen"]
    - path: "frontend/src/App.tsx"
      provides: "Splash screen integration + Sonner theme integration"
      contains: "SplashScreen"
  key_links:
    - from: "frontend/src/stores/theme-store.ts"
      to: "document.documentElement"
      via: "classList.add/toggle for .dark and .theme-{accent}"
      pattern: "classList"
    - from: "frontend/index.html"
      to: "localStorage"
      via: "synchronous script reading ai-coach-theme and ai-coach-accent"
      pattern: "ai-coach-theme"
---

<objective>
Build the accent color theme system foundation: CSS variable overrides for 5 themes, theme store with localStorage persistence, flash prevention in index.html, splash screen component, page transition keyframes, and Sonner toast theme integration.

Purpose: This is the foundation for all other Phase 10 plans. Every visual element depends on the theme system being in place. The splash screen provides a polished first impression.
Output: Theme CSS classes, theme store, splash screen component, flash prevention script, page-fade-in CSS animation.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-polish-professional-unification/10-CONTEXT.md
@.planning/phases/10-ui-polish-professional-unification/10-RESEARCH.md
@.planning/phases/10-ui-polish-professional-unification/10-UI-SPEC.md

<interfaces>
From frontend/src/stores/auth-store.ts (pattern to follow for theme store):
```typescript
import { useSyncExternalStore } from "react";
// Module-level state, listeners Set, subscribe/getSnapshot/emitChange pattern
// Exported hook: useAuthStore() — returns state + actions
// Exported functions: setAuth(), clearAuth()
```

From frontend/src/styles/index.css (CSS variable system — theme classes will be added here):
```css
:root {
  --primary: #1E40AF;
  --primary-foreground: #FFFFFF;
  --sidebar-primary: #1E40AF;
  --sidebar-primary-foreground: #FFFFFF;
  --chart-1: #1E40AF;
  --ring: oklch(0.708 0 0);
}
.dark {
  --primary: #3B82F6;
  --primary-foreground: oklch(0.205 0 0);
  --sidebar-primary: #3B82F6;
  --chart-1: #3B82F6;
  --ring: oklch(0.439 0 0);
}
```

From frontend/src/App.tsx (where splash screen + toast theming will be wired):
```typescript
import { Toaster } from "@/components/ui/sonner";
export default function App() { ... <Toaster position="top-right" /> ... }
```

From frontend/index.html (where flash prevention script goes):
```html
<body>
  <div id="root"></div>
  <script type="module" src="/src/main.tsx"></script>
</body>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Accent color theme CSS + theme store + index.html flash prevention</name>
  <files>frontend/src/styles/index.css, frontend/src/stores/theme-store.ts, frontend/index.html, frontend/public/locales/en-US/common.json, frontend/public/locales/zh-CN/common.json</files>
  <read_first>
    - frontend/src/styles/index.css (current CSS variables and @theme inline block)
    - frontend/src/stores/auth-store.ts (useSyncExternalStore pattern to replicate)
    - frontend/index.html (where to add flash prevention script)
    - frontend/public/locales/en-US/common.json (existing i18n keys)
    - frontend/public/locales/zh-CN/common.json (existing i18n keys)
    - .planning/phases/10-ui-polish-professional-unification/10-UI-SPEC.md (accent color values)
    - .planning/phases/10-ui-polish-professional-unification/10-RESEARCH.md (theme store code examples)
  </read_first>
  <action>
**1. Add accent theme CSS classes to `frontend/src/styles/index.css`** (per D-08, D-10):

After the existing `.dark { ... }` block and before `@theme inline`, add 4 accent theme class blocks (blue is default — no class needed). Each theme overrides `--primary`, `--primary-foreground`, `--sidebar-primary`, `--sidebar-primary-foreground`, `--chart-1`, and `--ring`:

```css
/* Accent theme: Teal */
.theme-teal { --primary: #0D9488; --primary-foreground: #FFFFFF; --sidebar-primary: #0D9488; --sidebar-primary-foreground: #FFFFFF; --chart-1: #0D9488; --ring: #0D9488; }
.dark.theme-teal, .dark .theme-teal { --primary: #2DD4BF; --primary-foreground: #042F2E; --sidebar-primary: #2DD4BF; --sidebar-primary-foreground: #042F2E; --chart-1: #2DD4BF; --ring: #2DD4BF; }

/* Accent theme: Purple */
.theme-purple { --primary: #7C3AED; --primary-foreground: #FFFFFF; --sidebar-primary: #7C3AED; --sidebar-primary-foreground: #FFFFFF; --chart-1: #7C3AED; --ring: #7C3AED; }
.dark.theme-purple, .dark .theme-purple { --primary: #A78BFA; --primary-foreground: #1E1B4B; --sidebar-primary: #A78BFA; --sidebar-primary-foreground: #1E1B4B; --chart-1: #A78BFA; --ring: #A78BFA; }

/* Accent theme: Rose */
.theme-rose { --primary: #BE185D; --primary-foreground: #FFFFFF; --sidebar-primary: #BE185D; --sidebar-primary-foreground: #FFFFFF; --chart-1: #BE185D; --ring: #BE185D; }
.dark.theme-rose, .dark .theme-rose { --primary: #F472B6; --primary-foreground: #500724; --sidebar-primary: #F472B6; --sidebar-primary-foreground: #500724; --chart-1: #F472B6; --ring: #F472B6; }

/* Accent theme: Amber */
.theme-amber { --primary: #B45309; --primary-foreground: #FFFFFF; --sidebar-primary: #B45309; --sidebar-primary-foreground: #FFFFFF; --chart-1: #B45309; --ring: #B45309; }
.dark.theme-amber, .dark .theme-amber { --primary: #FBBF24; --primary-foreground: #451A03; --sidebar-primary: #FBBF24; --sidebar-primary-foreground: #451A03; --chart-1: #FBBF24; --ring: #FBBF24; }
```

Also add page transition keyframe (per D-14) and hover transition utility in the base layer:

```css
@keyframes page-fade-in {
  from { opacity: 0; transform: translateY(4px); }
  to { opacity: 1; transform: translateY(0); }
}
.page-transition {
  animation: page-fade-in 150ms ease;
}
```

**2. Create `frontend/src/stores/theme-store.ts`** (per D-08, D-09):

Follow the exact `useSyncExternalStore` pattern from `auth-store.ts`. Module-level state with listeners Set.

Types:
```typescript
type ThemeMode = "light" | "dark";
type AccentColor = "blue" | "teal" | "purple" | "rose" | "amber";
interface ThemeState { mode: ThemeMode; accent: AccentColor; }
```

Constants:
- `THEME_STORAGE_KEY = "ai-coach-theme"`
- `ACCENT_STORAGE_KEY = "ai-coach-accent"`
- `ACCENT_COLORS` array of `{ name: AccentColor, color: string, label: string }` with values: blue/#1E40AF/"BeiGene Blue", teal/#0D9488/"Teal", purple/#7C3AED/"Purple", rose/#BE185D/"Rose", amber/#B45309/"Amber"

`applyTheme(mode, accent)` function:
- Toggle `.dark` class on `document.documentElement` based on mode
- Remove all `theme-*` classes from documentElement using regex `/theme-\w+/g`
- If accent is not "blue", add `theme-{accent}` class

Initialization: read from localStorage, call applyTheme on module load.

Export `setThemeMode(mode: ThemeMode)`, `setAccentColor(accent: AccentColor)`, and `useThemeStore()` hook returning `{ mode, accent, setThemeMode, setAccentColor }`.

**3. Add flash prevention script to `frontend/index.html`**:

Insert a synchronous `<script>` tag BEFORE the `<div id="root">`:

```html
<script>
  (function() {
    var mode = localStorage.getItem('ai-coach-theme') || 'light';
    var accent = localStorage.getItem('ai-coach-accent') || 'blue';
    if (mode === 'dark') document.documentElement.classList.add('dark');
    if (accent !== 'blue') document.documentElement.classList.add('theme-' + accent);
  })();
</script>
```

**4. Add i18n keys to both locale files** for theme picker (per D-09, UI-07):

Add to both `en-US/common.json` and `zh-CN/common.json`:
- `"theme": "Theme"` / `"theme": "主题"`
- `"lightMode": "Light"` / `"lightMode": "浅色"`
- `"darkMode": "Dark"` / `"darkMode": "深色"`
- `"accentBlue": "BeiGene Blue"` / `"accentBlue": "百济蓝"`
- `"accentTeal": "Teal"` / `"accentTeal": "青色"`
- `"accentPurple": "Purple"` / `"accentPurple": "紫色"`
- `"accentRose": "Rose"` / `"accentRose": "玫瑰色"`
- `"accentAmber": "Amber"` / `"accentAmber": "琥珀色"`

Also add splash screen i18n:
- `"appName": "AI Avatar"` / `"appName": "AI Avatar"`
- `"poweredBy": "BeiGene"` / `"poweredBy": "百济神州"`

Also add empty state / error state keys from UI-SPEC copywriting contract:
- `"emptyState.sessions.title": "No Sessions Yet"` / `"emptyState.sessions.title": "暂无训练记录"`
- `"emptyState.sessions.body": "Start your first training session to track progress."` / `"emptyState.sessions.body": "开始您的第一次训练以跟踪进度。"`
- `"emptyState.generic.title": "No Data Available"` / `"emptyState.generic.title": "暂无数据"`
- `"emptyState.generic.body": "Check back later or adjust your filters."` / `"emptyState.generic.body": "请稍后查看或调整筛选条件。"`
- `"error.network": "Connection Error. Check your network and try again."` / `"error.network": "连接错误。请检查网络后重试。"`
- `"error.server": "Something went wrong. Please try again later."` / `"error.server": "出现问题，请稍后重试。"`
- `"error.notFound": "Page not found. Return to dashboard."` / `"error.notFound": "页面未找到。返回仪表盘。"`
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/styles/index.css contains `.theme-teal` with `--primary: #0D9488`
    - frontend/src/styles/index.css contains `.theme-purple` with `--primary: #7C3AED`
    - frontend/src/styles/index.css contains `.theme-rose` with `--primary: #BE185D`
    - frontend/src/styles/index.css contains `.theme-amber` with `--primary: #B45309`
    - frontend/src/styles/index.css contains `.dark.theme-teal` with `--primary: #2DD4BF`
    - frontend/src/styles/index.css contains `@keyframes page-fade-in`
    - frontend/src/styles/index.css contains `.page-transition`
    - frontend/src/stores/theme-store.ts contains `export function useThemeStore`
    - frontend/src/stores/theme-store.ts contains `useSyncExternalStore`
    - frontend/src/stores/theme-store.ts contains `ai-coach-theme`
    - frontend/src/stores/theme-store.ts contains `ai-coach-accent`
    - frontend/src/stores/theme-store.ts contains `type ThemeMode = "light" | "dark"`
    - frontend/src/stores/theme-store.ts contains `type AccentColor = "blue" | "teal" | "purple" | "rose" | "amber"`
    - frontend/index.html contains `ai-coach-theme`
    - frontend/index.html contains `ai-coach-accent`
    - frontend/public/locales/en-US/common.json contains `"theme"`
    - frontend/public/locales/en-US/common.json contains `"lightMode"`
    - frontend/public/locales/en-US/common.json contains `"darkMode"`
    - frontend/public/locales/zh-CN/common.json contains `"主题"`
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>5 accent theme CSS classes with light+dark variants defined in index.css. Theme store exports useThemeStore hook with mode/accent state and setters. index.html has synchronous flash prevention script. Both locale files have theme picker and empty/error state i18n keys. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Splash screen component + App.tsx integration + Sonner theme wiring</name>
  <files>frontend/src/components/shared/splash-screen.tsx, frontend/src/App.tsx, frontend/src/components/shared/index.ts</files>
  <read_first>
    - frontend/src/App.tsx (current app setup with Toaster)
    - frontend/src/components/shared/index.ts (barrel export file)
    - frontend/src/stores/theme-store.ts (just created — need useThemeStore for Sonner)
    - frontend/src/components/ui/sonner.tsx (current Sonner Toaster wrapper)
    - .planning/phases/10-ui-polish-professional-unification/10-UI-SPEC.md (splash screen spec)
  </read_first>
  <action>
**1. Create `frontend/src/components/shared/splash-screen.tsx`** (per D-20):

Create a splash screen component that:
- Centers content vertically and horizontally with `fixed inset-0 z-50`
- Background: `bg-primary` (so it uses the accent theme color)
- Content: Lightbulb SVG icon (same one from user-layout.tsx header, enlarged to 64px), "AI Avatar" text (text-2xl font-medium text-primary-foreground), "BeiGene" subtitle (text-sm text-primary-foreground/70)
- Uses `useTranslation("common")` for `t("appName")` and `t("poweredBy")`
- Animation: CSS-only. Fade in 300ms, then after auto-dismiss, fade out 300ms.
- State management: `const [visible, setVisible] = useState(true)` and `const [fadeOut, setFadeOut] = useState(false)`. After 1.5s, start fade out. After 1.8s, setVisible(false).
- When `!visible`, return null (removed from DOM).
- Uses `useEffect` with `setTimeout` for auto-dismiss timing.
- Export as named export `SplashScreen`.

CSS classes for animation: Use inline style `animation: splash-fade-in 300ms ease` and `animation: splash-fade-out 300ms ease forwards` for the fade-out phase. Define these keyframes in index.css if not already:
```css
@keyframes splash-fade-in {
  from { opacity: 0; }
  to { opacity: 1; }
}
@keyframes splash-fade-out {
  from { opacity: 1; }
  to { opacity: 0; }
}
```

**2. Update `frontend/src/App.tsx`**:

- Import `SplashScreen` from `@/components/shared/splash-screen`
- Import `useThemeStore` from `@/stores/theme-store`
- Render `<SplashScreen />` as the first child inside the return (before QueryClientProvider, so it shows immediately)
- Update `<Toaster>` to use dynamic theme: read `mode` from `useThemeStore()` and pass `theme={mode === "dark" ? "dark" : "light"}` to `<Toaster>`. Also add `toastOptions` to use CSS custom property variables for toast styling:
```typescript
<Toaster
  position="top-right"
  theme={mode === "dark" ? "dark" : "light"}
  toastOptions={{
    style: {
      "--normal-bg": "var(--popover)",
      "--normal-text": "var(--popover-foreground)",
      "--normal-border": "var(--border)",
    } as React.CSSProperties,
  }}
/>
```
Note: Move `<Toaster>` INSIDE the QueryClientProvider but keep it last. SplashScreen goes outside the QueryClientProvider since it has no data deps.

**3. Update `frontend/src/components/shared/index.ts`** barrel export:

Add `export { SplashScreen } from "./splash-screen";` to the barrel file.

**4. Add splash screen keyframes to `frontend/src/styles/index.css`** if not already present from Task 1:

```css
@keyframes splash-fade-in {
  from { opacity: 0; }
  to { opacity: 1; }
}
@keyframes splash-fade-out {
  from { opacity: 1; }
  to { opacity: 0; }
}
```
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/shared/splash-screen.tsx contains `export function SplashScreen`
    - frontend/src/components/shared/splash-screen.tsx contains `AI Avatar` or `t("appName")`
    - frontend/src/components/shared/splash-screen.tsx contains `BeiGene` or `t("poweredBy")`
    - frontend/src/components/shared/splash-screen.tsx contains `useState`
    - frontend/src/components/shared/splash-screen.tsx contains `setTimeout`
    - frontend/src/App.tsx contains `SplashScreen`
    - frontend/src/App.tsx contains `useThemeStore`
    - frontend/src/App.tsx contains `theme={` (Sonner dynamic theme)
    - frontend/src/components/shared/index.ts contains `SplashScreen`
    - frontend/src/styles/index.css contains `splash-fade-in`
    - `npx tsc -b` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Splash screen shows on app startup with BeiGene/AI Avatar branding and auto-dismisses. Sonner toasts use dynamic light/dark theme from theme store. Barrel export updated. Build passes.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc -b` passes (no type errors)
- `cd frontend && npm run build` passes (production build succeeds)
- `frontend/src/styles/index.css` contains 4 accent theme class blocks (teal, purple, rose, amber) each with light and dark variants
- `frontend/src/stores/theme-store.ts` exports useThemeStore, setThemeMode, setAccentColor
- `frontend/index.html` has synchronous script preventing flash of wrong theme
- `frontend/src/components/shared/splash-screen.tsx` exports SplashScreen component
- `frontend/src/App.tsx` renders SplashScreen and passes dynamic theme to Toaster
</verification>

<success_criteria>
Theme system is fully functional: 5 accent color themes defined in CSS, theme store persists to localStorage, no theme flash on page load, splash screen renders on startup, page transition CSS animation available, Sonner toasts themed dynamically.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-polish-professional-unification/10-01-SUMMARY.md`
</output>
