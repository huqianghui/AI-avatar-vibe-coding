---
phase: 38-voice-mode-config-foundry-portal
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - frontend/src/lib/voice-constants.ts
  - frontend/src/components/admin/avatar-character-gallery.tsx
  - frontend/src/components/admin/avatar-character-gallery.test.tsx
  - frontend/src/components/admin/agent-config-left-panel.tsx
  - frontend/src/components/admin/agent-config-left-panel.test.tsx
  - frontend/src/pages/admin/hcp-profile-editor.tsx
  - frontend/src/pages/admin/hcp-profile-editor.test.tsx
  - frontend/src/__tests__/hcp-editor-tabs.test.tsx
  - frontend/src/types/hcp.ts
  - frontend/public/locales/en-US/admin.json
  - frontend/public/locales/zh-CN/admin.json
  - frontend/public/locales/es-ES/admin.json
  - frontend/public/locales/es-MX/admin.json
  - frontend/public/locales/es-US/admin.json
  - frontend/e2e/hcp-editor-voice-tab.spec.ts
autonomous: true
requirements: [VMODE-01]

must_haves:
  truths:
    - "HCP profile editor's 语音和数字人 tab no longer shows a 'Voice Live Instance' card, selector, badge row, or 'Manage in Voice Live' link"
    - "In its place, the tab shows: a Model deployment dropdown (realtime Voice Live models, not the chat-completion AgentFoundationModelSelect catalog), a Language dropdown, a Speech output Voice dropdown, an Avatar enabled toggle, and an avatar character gallery with preview thumbnails"
    - "Saving an HCP profile persists these 6 fields via the existing PUT/POST HCP profile endpoints, with no client-side requirement to select a VoiceLiveInstance"
    - "An admin can create a brand-new HCP profile end-to-end (no VoiceLiveInstance involved at any point) and it saves successfully"
  artifacts:
    - path: "frontend/src/components/admin/avatar-character-gallery.tsx"
      provides: "Shared, filterable (all/photo/video) avatar character+style picker with thumbnail-fallback-to-initials, reusable by both HCP and persona editors"
      exports: ["AvatarCharacterGallery"]
    - path: "frontend/src/components/admin/agent-config-left-panel.tsx"
      provides: "Voice Live Instance card removed; new direct voice-mode config card added"
    - path: "frontend/src/pages/admin/hcp-profile-editor.tsx"
      provides: "hcpSchema/HcpFormValues extended with voice_live_model, voice_name, recognition_language, avatar_character, avatar_style, avatar_enabled; voice_live_instance_id validation relaxed"
    - path: "frontend/e2e/hcp-editor-voice-tab.spec.ts"
      provides: "E2E coverage proving an admin can configure and save the direct voice-mode fields without any VoiceLiveInstance"
  key_links:
    - from: "frontend/src/components/admin/agent-config-left-panel.tsx"
      to: "frontend/src/components/admin/avatar-character-gallery.tsx"
      via: "<AvatarCharacterGallery character={...} style={...} onSelect={...} />"
      pattern: "AvatarCharacterGallery"
    - from: "frontend/src/pages/admin/hcp-profile-editor.tsx::handleSubmit"
      to: "backend HcpProfileCreate/Update (Plan 38-01)"
      via: "createMutation.mutate(data) / updateMutation.mutate({id, data})"
      pattern: "voice_live_model|avatar_character|avatar_style|avatar_enabled|recognition_language"
---

<objective>
Remove the "Voice Live Instance" card from the HCP profile editor's 语音和数字人 tab and replace it with a Foundry-portal-style direct voice-mode config: Model deployment dropdown, Language dropdown, Speech output Voice dropdown, Avatar toggle + character gallery -- wired to the new inline `HcpProfile` fields from Plan 38-01 (VMODE-01, frontend half).

Purpose: This is the actual user-visible swap the whole phase exists for (ROADMAP success criterion 1). It also produces the shared `AvatarCharacterGallery` component that Plan 38-03 (VMODE-02, persona editor alignment) will reuse, so the same gallery UX/markup isn't duplicated across the two admin pages.
Output: A working "Voice & Avatar" config card in `agent-config-left-panel.tsx` bound to real, persisted `HcpProfile` fields; zero references to `voice_live_instance_id` remaining in the HCP editor's UI or its required-field validation; a Playwright spec proving the end-to-end flow.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-voice-mode-config-foundry-portal/38-CONTEXT.md
@.planning/phases/38-voice-mode-config-foundry-portal/38-01-SUMMARY.md

<interfaces>
<!-- Extracted directly from the repo. This is the exact card being removed,
the exact reusable pieces available, and the exact zod/type shapes to extend --
executor should not need to re-explore the codebase for any of this. -->

**Card being removed** -- `frontend/src/components/admin/agent-config-left-panel.tsx`, the entire first `<Card>` block (currently lines ~125-223, comment `{/* 1. VL Instance Summary (D-11) */}`): renders `selectedInstance.name` + badges (`voice_live_model`, `voice_name`, `avatar_character · avatar_style`), a `<Select>` of `useVoiceLiveInstances()` results, an unassign `X` button + confirm `<Dialog>`, and a "Manage in Voice Live" link. Its hooks (`useVoiceLiveInstances`, `useAssignVoiceLiveInstance`, `useUnassignVoiceLiveInstance`) and the `handleInstanceChange`/`handleConfirmRemove`/`showRemoveDialog` state become entirely unused once this card is deleted -- remove them too (don't leave dead imports/state, `noUnusedLocals` in `tsconfig.json` will fail the build otherwise).

**Existing "Model Deployment" card in the SAME file** (lines ~225-239) uses `AgentFoundationModelSelect` bound to local-only `foundationModel` state -- this is a *different, decorative* selector over the chat-completion-model catalog (`agent_foundation_models.py`), NOT the realtime Voice Live model catalog. Leave this card exactly as-is (it is out of this phase's scope -- PEDIT-03 territory, deferred). The NEW "Model deployment" dropdown this plan adds is a different control entirely, using `VoiceLiveModelSelect` (below), bound to the real persisted `voice_live_model` form field.

**Reusable, ALREADY-EXISTING component** -- `frontend/src/components/admin/voice-live-model-select.tsx`:
```typescript
interface VoiceLiveModelSelectProps { value: string; onValueChange: (value: string) => void; disabled?: boolean; }
export function VoiceLiveModelSelect(props: VoiceLiveModelSelectProps): JSX.Element
```
Renders a grouped (pro/basic/lite tier) `<Select>` over `VOICE_LIVE_MODEL_OPTIONS` (12 models incl. `gpt-4o`, `gpt-realtime`, etc.) -- this IS the realtime-voice-compatible catalog (matches backend `VOICE_LIVE_MODELS` used to validate `hcp_profile.voice_live_model` server-side). Use this directly for the new Model deployment dropdown; do not build a new selector.

**Reusable, ALREADY-EXISTING catalog** -- `frontend/src/lib/voice-constants.ts`:
```typescript
export const CDN_BASE = "...";
export const VOICE_NAME_OPTIONS: readonly { value: string; labelKey: string }[]; // 8 entries, unfiltered by locale (out of scope: PEDIT-06 locale filtering stays deferred)
```

**Locale constants to EXTRACT from `frontend/src/pages/admin/persona-editor.tsx` into `frontend/src/lib/voice-constants.ts`** (currently private to that file, lines 37-55 -- move them so both HCP and persona editors import one shared copy; persona-editor.tsx keeps working via a re-export/import swap, no behavior change there in this plan -- Plan 38-03 handles persona-editor.tsx's own refactor):
```typescript
export const SUPPORTED_VOICE_LOCALES = ["zh-CN", "en-US", "es-ES", "es-MX", "es-US"] as const;
export const LOCALE_FLAGS: Record<string, string> = { "zh-CN": "\u{1F1E8}\u{1F1F3}", "en-US": "\u{1F1FA}\u{1F1F8}", "es-ES": "\u{1F1EA}\u{1F1F8}", "es-MX": "\u{1F1F2}\u{1F1FD}", "es-US": "\u{1F1FA}\u{1F1F8}" };
export const LOCALE_LABEL_KEY: Record<string, string> = { "zh-CN": "zhCN", "en-US": "enUS", "es-ES": "esES", "es-MX": "esMX", "es-US": "esUS" };
```
The HCP Language dropdown is a single-value field (`recognition_language`), not a per-locale map like personas' `voiceMap`/`greetingMap` -- so it needs one extra option beyond these 5: `"auto"`, labeled via the EXISTING key `admin:hcp.autoDetect` ("Auto Detect").

**Gallery markup to GENERALIZE into a new shared component** -- `frontend/src/pages/admin/persona-editor.tsx` lines ~401-478 (Card #2, "Character & Avatar"): a `filteredAvatarItems` grid (4 cols, All/Photo/Video filter buttons) of buttons rendering `item.thumbnailUrl` with `onError` fallback to a gradient-circle `getAvatarInitials(item.displayName)`, built from `AVATAR_CHARACTERS` (`@/data/avatar-characters`) expanded per-style into `AvatarGridItem` entries. Extract this into a new self-contained component:
```typescript
// frontend/src/components/admin/avatar-character-gallery.tsx
interface AvatarCharacterGalleryProps {
  character: string;
  style: string;
  onSelect: (characterId: string, style: string) => void;
  className?: string;
}
export function AvatarCharacterGallery(props: AvatarCharacterGalleryProps): JSX.Element
```
Owns its own `avatarFilter` state (all/photo/video), its own `filteredAvatarItems` derivation from `AVATAR_CHARACTERS`, and its own failed-thumbnail tracking (a `useRef<Set<string>>` -- do not require the parent to pass a ref). This makes it a drop-in replacement for the inline block in both `agent-config-left-panel.tsx` (this plan) and, later, `persona-editor.tsx` (Plan 38-03).

**Zod schema to extend** -- `frontend/src/pages/admin/hcp-profile-editor.tsx` lines 58-88 (`hcpSchema`/`HcpFormValues`):
```typescript
export const hcpSchema = z.object({
  // ...existing fields...
  voice_live_instance_id: z.string().nullable().refine((val) => Boolean(val && val.length > 0), {
    message: "hcp.vlInstanceValidationError",
  }), // CHANGE: drop the .refine() entirely -- z.string().nullable() only, no longer required
  agent_instructions_override: z.string().default(""),
});
```
Add: `voice_live_model: z.string().default("gpt-4o")`, `voice_name: z.string().default("en-US-AvaNeural")`, `recognition_language: z.string().default("auto")`, `avatar_character: z.string().default("lisa")`, `avatar_style: z.string().default("casual")`, `avatar_enabled: z.boolean().default(true)`.

**Backend contract these fields must match exactly** (Plan 38-01, already implemented before this plan runs): `HcpProfileCreate`/`HcpProfileUpdate` in `backend/app/schemas/hcp_profile.py` accept the same 6 field names with the same defaults; `HcpProfile` TS type in `frontend/src/types/hcp.ts` needs the same 6 fields added (required, matching `HcpProfileResponse`) plus added to `HcpProfileCreate`/`HcpProfileUpdate` TS interfaces (all optional).

**Existing i18n keys to REUSE (no new keys needed for these controls)** -- all already present, with parity across all 5 locales (`en-US`, `zh-CN`, `es-ES`, `es-MX`, `es-US`), verified in `frontend/public/locales/*/admin.json` `hcp` section: `hcp.modelDeployment` ("Model Deployment"), `hcp.recognitionLanguage` ("Recognition Language" -- use as the Language dropdown's label), `hcp.autoDetect` ("Auto Detect" -- the leading language-select option), `hcp.voiceName` ("Voice Name" -- use as the Speech-output Voice dropdown's label), `hcp.avatarCharacter`/`hcp.avatarStyle` (not directly rendered as labels since the gallery replaces free-text selects, but keep them for backward-compat -- do not delete). Locale-option labels for the Language dropdown reuse `common:lang.{zhCN,enUS,esES,esMX,esUS}` (already exist, already loaded via the `common` namespace on this page). Avatar-toggle label reuses `admin:voiceLive.playgroundSection.enableAvatar` ("Enable avatar"). Gallery filter buttons reuse `admin:voiceLive.vlDialogFilter{All,Photo,Video}` (already used by the pre-extraction persona-editor gallery, per Task 1).

**One genuinely NEW string this plan needs** -- a title for the new card, distinct from the existing `hcp.modelDeployment`/`hcp.tabVoiceAvatar` copy so the card doesn't read as a duplicate of the tab name: key `admin:hcp.voiceAvatarConfigTitle`, defined in Task 3 below (all 5 locales) and consumed by Task 2's new `<CardTitle>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared locale constants + build AvatarCharacterGallery component</name>
  <files>frontend/src/lib/voice-constants.ts, frontend/src/components/admin/avatar-character-gallery.tsx, frontend/src/components/admin/avatar-character-gallery.test.tsx</files>
  <behavior>
    - Test: renders one grid item per style-variant of every video avatar plus one item per photo avatar in `AVATAR_CHARACTERS` (matches today's `persona-editor.tsx` gallery item count).
    - Test: clicking an item calls `onSelect(characterId, style)` with the clicked item's `characterId`/`style` (empty string style for photo avatars).
    - Test: the currently-selected item (matching `character`+`style` props) renders with the "selected" ring/border class.
    - Test: clicking the "photo" filter button hides video-avatar items and shows only photo avatars (and vice-versa for "video"; "all" shows both).
    - Test: an `<img>` `onError` swaps that specific item to the initials/gradient fallback without affecting other items.
  </behavior>
  <action>
    In `frontend/src/lib/voice-constants.ts`, add the three exported constants (`SUPPORTED_VOICE_LOCALES`, `LOCALE_FLAGS`, `LOCALE_LABEL_KEY`) shown in the `<interfaces>` block, copied verbatim from `persona-editor.tsx`'s current private `PERSONA_VOICE_LOCALES`/`FLAGS`/`LOCALE_LABEL_KEY`. Do not modify `persona-editor.tsx` in this task (Plan 38-03 switches it to import from here) -- this task only ADDS the exports to `voice-constants.ts`.

    Create `frontend/src/components/admin/avatar-character-gallery.tsx` implementing `AvatarCharacterGallery` per the `<interfaces>` contract above: internal `avatarFilter` state (`"all" | "photo" | "video"`, default `"all"`) with the same 3 filter buttons persona-editor.tsx currently renders (reuse i18n keys `admin:voiceLive.vlDialogFilterAll/Photo/Video` -- already exist, confirmed in use today), an internal `filteredAvatarItems` memo built from `AVATAR_CHARACTERS` (mirror persona-editor.tsx's current expansion logic: video avatars expand to one `AvatarGridItem` per style, photo avatars to a single item with `isPhotoAvatar: true`), and an internal `useRef<Set<string>>` for failed-thumbnail tracking with an `onError` handler per `<img>`. Grid: `grid grid-cols-4 gap-2 max-h-52 overflow-y-auto pr-1` (match persona-editor's current classes for visual consistency). Selection highlight: `isSelected = character === item.characterId && (item.isPhotoAvatar || style === item.style)`.

    Write `avatar-character-gallery.test.tsx` covering the 5 behaviors listed above using React Testing Library (mirror the existing test setup/mocking style in `agent-config-left-panel.test.tsx` for i18n/provider wrapping).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/admin/avatar-character-gallery.test.tsx</automated>
  </verify>
  <done>AvatarCharacterGallery renders, filters, selects, and falls back to initials correctly in isolation; voice-constants.ts exports the 3 new shared locale constants with no change to any existing consumer's behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the Voice Live Instance card with direct voice-mode config</name>
  <files>frontend/src/components/admin/agent-config-left-panel.tsx, frontend/src/components/admin/agent-config-left-panel.test.tsx, frontend/src/pages/admin/hcp-profile-editor.tsx, frontend/src/pages/admin/hcp-profile-editor.test.tsx, frontend/src/types/hcp.ts</files>
  <behavior>
    - Test: `agent-config-left-panel.tsx` no longer renders any text matching `admin:hcp.vlInstanceLabel`/`vlInstanceEmptyTitle`/`vlInstanceRequiredBadge` and no longer calls `useVoiceLiveInstances`.
    - Test: it renders a `VoiceLiveModelSelect` bound to `form.watch("voice_live_model")`, a Language `<Select>` over `SUPPORTED_VOICE_LOCALES` + `"auto"`, a Speech-output Voice `<Select>` over `VOICE_NAME_OPTIONS`, an `avatar_enabled` `<Switch>`, and an `<AvatarCharacterGallery>` bound to `form.watch("avatar_character")`/`form.watch("avatar_style")`.
    - Test: selecting a gallery item calls `form.setValue("avatar_character", ...)` and `form.setValue("avatar_style", ...)` with `shouldDirty: true`.
    - Test: `hcpSchema` no longer rejects a `null`/empty `voice_live_instance_id` (the `.refine()` is gone) and DOES validate the 6 new fields with their stated defaults when omitted.
    - Test: `handleSubmit`'s built payload includes `voice_live_model`/`voice_name`/`recognition_language`/`avatar_character`/`avatar_style`/`avatar_enabled` (spread via `...values`, no extra plumbing needed since they're now part of `HcpFormValues`).
    - Test: update `hcp-profile-editor.test.tsx`'s existing `"hcpSchema accepts a non-empty voice_live_instance_id"` / `"hcpSchema rejects an empty voice_live_instance_id"` / `"hcpSchema rejects a null voice_live_instance_id"` cases -- the latter two now assert SUCCESS (empty string and `null` both parse), not a Zod issue. Remove or rewrite the test asserting the `handleInvalidSubmit` -> `admin:hcp.vlInstanceSaveBlockedToast` toast (that code path no longer exists).
  </behavior>
  <action>
    In `agent-config-left-panel.tsx`: delete the entire "VL Instance Summary" `<Card>` block and its now-dead imports/hooks/state (`useVoiceLiveInstances`, `useAssignVoiceLiveInstance`, `useUnassignVoiceLiveInstance`, `handleInstanceChange`, `handleConfirmRemove`, `showRemoveDialog`, the confirm `<Dialog>`, the `ExternalLink`/ "Manage in Voice Live" button). In its place, add a new `<Card>` titled `t("admin:hcp.voiceAvatarConfigTitle")` (new key, defined in Task 3 -- reference it now, the JSON value lands later in this same plan) containing, top to bottom: (1) `<VoiceLiveModelSelect value={form.watch("voice_live_model")} onValueChange={(v) => form.setValue("voice_live_model", v, { shouldDirty: true })} disabled={isNew} />` under a `t("admin:hcp.modelDeployment")` label, (2) a Language `<Select>` iterating `SUPPORTED_VOICE_LOCALES` (using `LOCALE_FLAGS`/`LOCALE_LABEL_KEY` + `common:lang.*` for per-option labels, matching persona-editor's existing pattern) plus a leading `"auto"` item labeled `admin:hcp.autoDetect`, under a `t("admin:hcp.recognitionLanguage")` label, bound to `form.watch("recognition_language")`/`form.setValue`, (3) a Speech-output Voice `<Select>` over `VOICE_NAME_OPTIONS` under a `t("admin:hcp.voiceName")` label, bound to `form.watch("voice_name")`/`form.setValue` (reuse the existing `admin:hcp.{labelKey}` translation pattern already used elsewhere for these options), (4) an `avatar_enabled` `<Switch>` labeled `t("admin:voiceLive.playgroundSection.enableAvatar")`, bound to `form.watch("avatar_enabled")`/`form.setValue`, (5) `<AvatarCharacterGallery character={form.watch("avatar_character")} style={form.watch("avatar_style")} onSelect={(c, s) => { form.setValue("avatar_character", c, { shouldDirty: true }); form.setValue("avatar_style", s, { shouldDirty: true }); }} />`. Keep the pre-existing "Model Deployment" (AgentFoundationModelSelect, decorative) card, `InstructionsSection`, and "Knowledge & Tools" card exactly as they are today, just renumber the leading comments (`{/* 1. ... */}` etc.) to stay accurate.

    In `hcp-profile-editor.tsx`: change `voice_live_instance_id` in `hcpSchema` to `z.string().nullable().default(null)` (drop the `.refine()` requiring non-empty). Add the 6 new zod fields with the stated defaults. Add matching entries to the `useForm` `defaultValues` object and to the `form.reset({...})` call inside the profile-load `useEffect` (reading from `profile.voice_live_model ?? "gpt-4o"` etc., mirroring how every other field there is populated). Remove the now-dead `handleInvalidSubmit`'s `if (errors.voice_live_instance_id) toast.error(t("admin:hcp.vlInstanceSaveBlockedToast"))` block (the field can no longer produce this validation error).

    In `types/hcp.ts`: add the 6 fields (required, matching backend `HcpProfileResponse` types) to `HcpProfile`; add them (all optional) to `HcpProfileCreate`; change `HcpProfileCreate.voice_live_instance_id` from required `string` to optional `string | null`.

    In `agent-config-left-panel.test.tsx`: delete the `useVoiceLiveInstances`/`useAssignVoiceLiveInstance`/`useUnassignVoiceLiveInstance` mocks and the `MOCK_INSTANCE` fixture; delete assertions on `admin:hcp.vlInstanceEmptyTitle`/`vlInstanceRequiredBadge`/`vlInstanceEmptyBody`/`vlInstanceValidationError`. Add RTL assertions matching the 5 new behaviors listed above (render `VoiceLiveModelSelect`, Language `<Select>`, Voice `<Select>`, `avatar_enabled` `<Switch>`, `<AvatarCharacterGallery>`; clicking a gallery item calls the mocked `form.setValue` with `avatar_character`/`avatar_style` and `{ shouldDirty: true }`).

    In `hcp-profile-editor.test.tsx`: rewrite the three `hcpSchema`/`voice_live_instance_id` tests per the `<behavior>` block above (empty string and `null` now both parse successfully instead of raising a Zod issue). Remove the test asserting the `vlInstanceSaveBlockedToast` toast path.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/admin/hcp-profile-editor.test.tsx src/components/admin/agent-config-left-panel.test.tsx && npx tsc -b</automated>
  </verify>
  <done>`agent-config-left-panel.tsx` renders the new "Voice & Avatar" direct-config card (Model deployment, Language, Speech-output Voice, Avatar toggle, AvatarCharacterGallery) with zero references to `voice_live_instance_id`/`useVoiceLiveInstances`/`vlInstance*` UI copy; `hcpSchema` no longer requires an instance; `types/hcp.ts` carries the 6 new fields; both rewritten test files and `tsc -b` pass.</done>
</task>

<task type="auto">
  <name>Task 3: New card title string, tab-structure regression guard, and E2E coverage</name>
  <files>frontend/src/__tests__/hcp-editor-tabs.test.tsx, frontend/public/locales/en-US/admin.json, frontend/public/locales/zh-CN/admin.json, frontend/public/locales/es-ES/admin.json, frontend/public/locales/es-MX/admin.json, frontend/public/locales/es-US/admin.json, frontend/e2e/hcp-editor-voice-tab.spec.ts</files>
  <action>
    Add one new key to the `hcp` section of all 5 `admin.json` locale files, placed near the existing `tabVoiceAvatar`/`voiceAvatarCol` keys (~line 77): `"voiceAvatarConfigTitle"`, with values:
    - en-US: `"Voice & Avatar Configuration"`
    - zh-CN: `"语音和数字人配置"`
    - es-ES: `"Configuración de Voz y Avatar"`
    - es-MX: `"Configuración de Voz y Avatar"`
    - es-US: `"Configuración de Voz y Avatar"`
    Do NOT remove the now-unused `vlInstance*` keys (`vlInstanceLabel`, `vlInstanceNone`, `vlInstanceRequired`, `vlInstanceEmptyTitle`, `vlInstanceEmptyBody`, `vlInstanceValidationError`, `vlInstanceSaveBlockedToast`, `vlInstanceRequiredBadge`) -- leaving unused i18n strings in place is the existing codebase norm (see the still-present `knowledgePlaceholder`/`toolsPlaceholder` "coming soon" keys) and avoids a five-file deletion diff with no functional benefit.

    In `frontend/src/__tests__/hcp-editor-tabs.test.tsx`: add `"voiceAvatarConfigTitle"` to the existing `phase15Keys` parity array (the `"en-US and zh-CN admin.json have matching Phase 15 keys"` test) so the new key's cross-locale presence is guarded going forward. Add one new test to the `"HCP editor tab structure"` describe block, mirroring the existing source-text-analysis style (read `agent-config-left-panel.tsx` via `fs.readFileSync`, not full render): assert the source does NOT contain `"useVoiceLiveInstances"` and does NOT contain `"vlInstanceEmptyTitle"`, as a regression guard against the VL Instance card silently returning.

    Create `frontend/e2e/hcp-editor-voice-tab.spec.ts`, following the existing patterns in `frontend/e2e/admin-avatar-personas.spec.ts` (admin login/session setup, `page.goto`, `page.waitForResponse` around save). Cover: (1) navigate to an existing HCP profile's edit page, open the "语音和数字人"/Voice & Avatar tab, assert no text matching `/voice live instance/i` is present and the new card (`VoiceLiveModelSelect`, Language select, Voice select, avatar toggle, gallery) is visible; (2) select a different model deployment, language, voice, and gallery character/style, toggle avatar on, save, and wait for the PUT response to resolve 200; (3) reload the page and assert the newly-selected values are still shown (persistence). Use `data-testid`/`getByRole` selectors consistent with the rest of the HCP editor's existing E2E specs (grep `frontend/e2e/hcp-*.spec.ts` if present for the login/navigation boilerplate to reuse verbatim rather than re-deriving it).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/hcp-editor-tabs.test.tsx && npx playwright test e2e/hcp-editor-voice-tab.spec.ts</automated>
  </verify>
  <done>All 5 locale files carry `hcp.voiceAvatarConfigTitle` with parity; the tab-structure regression guard fails if the VL Instance card's markers reappear; `hcp-editor-voice-tab.spec.ts` proves an admin can configure and persist the direct voice-mode fields end-to-end with no VoiceLiveInstance involved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser (this plan's new form controls) -> `PUT/POST /api/v1/hcp-profiles` | Same authenticated admin boundary as Plan 38-01; this plan only changes which UI widgets produce the request body |
| Gallery/select UI -> `voice_live_model`/`avatar_character`/`avatar_style` form fields | Client-side constraint (dropdown/gallery instead of free text) is defense-in-depth, not the enforcement boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-38-04 | Tampering | Client bypassing the new dropdowns/gallery to POST arbitrary `voice_live_model`/`avatar_character`/`avatar_style` values directly | accept | Authoritative validation already lives server-side (`T-38-01`/`T-38-02` from Plan 38-01's `resolve_voice_config`/`_load_connection_config` fallback logic); this plan's UI is a convenience layer, not a new trust boundary |
| T-38-05 | Information Disclosure | Removing the "Manage in Voice Live" link/instance name from the UI | accept | Purely a UI simplification; no new data exposure, and the underlying `voice_live_instance_id` FK (unused for session config as of Plan 38-01) is not surfaced anywhere in this plan's new card |
</threat_model>

<verification>
1. `cd frontend && npx vitest run` -- full suite passes, including the new/updated `avatar-character-gallery.test.tsx`, `agent-config-left-panel.test.tsx`, `hcp-profile-editor.test.tsx`, `hcp-editor-tabs.test.tsx`.
2. `cd frontend && npx tsc -b` -- clean, no unused-locals/params from the deleted VL-instance hooks/state.
3. `cd frontend && npx playwright test e2e/hcp-editor-voice-tab.spec.ts` -- passes.
4. Manual spot-check: open an HCP profile's "语音和数字人" tab in the running app; confirm no "Voice Live Instance" text/badges/link remain, and the new Model deployment / Language / Voice / Avatar toggle / gallery controls are all present and bound to real, persisted values after a save + reload.
</verification>

<success_criteria>
- "Voice Live Instance" card, its hooks, and its dead state are fully removed from `agent-config-left-panel.tsx`.
- A new "Voice & Avatar Configuration" card exists, wired to the 6 inline `HcpProfile` fields from Plan 38-01.
- `AvatarCharacterGallery` is a standalone, tested, reusable component consumed by the HCP editor (and, in Plan 38-03, the persona editor).
- `hcpSchema` no longer requires `voice_live_instance_id`.
- E2E spec proves the full configure -> save -> reload -> persisted flow with no VoiceLiveInstance involved.
- Full frontend test suite + `tsc -b` + the new E2E spec all pass.
</success_criteria>

<output>
After completion, create `.planning/phases/38-voice-mode-config-foundry-portal/38-02-SUMMARY.md`
</output>
