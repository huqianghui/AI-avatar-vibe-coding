---
phase: 38-voice-mode-config-foundry-portal
plan: 03
type: execute
wave: 3
depends_on: ["38-02"]
files_modified:
  - frontend/src/pages/admin/persona-editor.tsx
  - frontend/src/pages/admin/persona-editor.test.tsx
  - frontend/public/locales/en-US/admin.json
  - frontend/public/locales/zh-CN/admin.json
  - frontend/public/locales/es-ES/admin.json
  - frontend/public/locales/es-MX/admin.json
  - frontend/public/locales/es-US/admin.json
  - frontend/e2e/admin-avatar-personas.spec.ts
autonomous: true
requirements: [VMODE-02]

must_haves:
  truths:
    - "Persona editor's Character & Avatar card renders using the same shared AvatarCharacterGallery component the HCP editor uses (Plan 38-02), not a duplicated inline grid implementation"
    - "Persona editor's Speech card presents as portal-style 'Speech output' (Language dropdown + Voice dropdown), preserving today's per-locale voiceMap semantics and the '(use default)' fallback option exactly"
    - "No backend changes are made; persona create/update/session wiring (Phases 36/37) continues to work unmodified"
    - "Selecting a gallery character/style and saving persists across a reload, matching pre-refactor behavior"
  artifacts:
    - path: "frontend/src/pages/admin/persona-editor.tsx"
      provides: "Character & Avatar card delegates to <AvatarCharacterGallery>; no more local avatarFilter/filteredAvatarItems/failedThumbnailsRef duplication"
    - path: "frontend/src/pages/admin/persona-editor.test.tsx"
      provides: "Updated avatar-grid tests exercising the shared component through the page, unchanged assertions for speech/language/voice behavior"
  key_links:
    - from: "frontend/src/pages/admin/persona-editor.tsx"
      to: "frontend/src/components/admin/avatar-character-gallery.tsx"
      via: "<AvatarCharacterGallery character={form.character} style={form.style} onSelect={...} />"
      pattern: "AvatarCharacterGallery"
    - from: "frontend/src/pages/admin/persona-editor.tsx"
      to: "frontend/src/lib/voice-constants.ts"
      via: "import { SUPPORTED_VOICE_LOCALES, LOCALE_FLAGS, LOCALE_LABEL_KEY } from \"@/lib/voice-constants\""
      pattern: "from \"@/lib/voice-constants\""
---

<objective>
Align the persona editor's (`persona-editor.tsx`) Character & Avatar and Speech cards with the same Foundry-portal-style presentation established in Plan 38-02 -- swap the duplicated inline avatar-gallery block for the shared `AvatarCharacterGallery` component, dedupe the private locale constants against the shared `voice-constants.ts` copy, and retitle the Speech card's copy to "Speech output" (VMODE-02).

Purpose: Plan 38-02 built `AvatarCharacterGallery` explicitly so both admin pages share one gallery implementation instead of two near-identical copies drifting apart over time. The persona editor's Speech card already has the right Language + Voice dropdown shape (built in the Phase 37 rebuild); this plan is presentation alignment only -- no new backend field, no language filtering (PEDIT-06 stays deferred), no instance selector (personas never had one).
Output: `persona-editor.tsx` with its Character & Avatar card delegating to `<AvatarCharacterGallery>`, its private locale constants replaced by shared imports, and its Speech card's title copy updated to "Speech output" phrasing; updated unit + E2E coverage proving the swap is behavior-preserving.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-voice-mode-config-foundry-portal/38-CONTEXT.md
@.planning/phases/38-voice-mode-config-foundry-portal/38-02-SUMMARY.md

<interfaces>
<!-- Extracted directly from the repo (persona-editor.tsx as it exists before
this plan). This is the exact block being replaced and the exact shared
pieces Plan 38-02 already produced -- no further exploration needed. -->

**Shared component to consume (built in Plan 38-02)** -- `frontend/src/components/admin/avatar-character-gallery.tsx`:
```typescript
interface AvatarCharacterGalleryProps {
  character: string;
  style: string;
  onSelect: (characterId: string, style: string) => void;
  className?: string;
}
export function AvatarCharacterGallery(props: AvatarCharacterGalleryProps): JSX.Element
```
Renders the identical filter-buttons + 4-col grid + thumbnail/initials-fallback markup that persona-editor.tsx currently builds inline -- it was extracted directly FROM this file's Card #2, so swapping in the component should be a drop-in replacement with no visual/DOM change.

**Shared locale constants to consume (added in Plan 38-02)** -- `frontend/src/lib/voice-constants.ts`:
```typescript
export const SUPPORTED_VOICE_LOCALES = ["zh-CN", "en-US", "es-ES", "es-MX", "es-US"] as const;
export const LOCALE_FLAGS: Record<string, string>;
export const LOCALE_LABEL_KEY: Record<string, string>;
```
Byte-identical in shape/values to this file's current private `PERSONA_VOICE_LOCALES`/`FLAGS`/`LOCALE_LABEL_KEY` (lines 37-55) -- this plan deletes the private copies and imports the shared ones instead.

**Block being replaced** -- `frontend/src/pages/admin/persona-editor.tsx`, Card #2 "Character & Avatar" (lines ~401-478, comment `{/* 2. Character & Avatar -- replaces the HCP page's "VL Instance Summary" card ... */}`): filter-button row (lines 410-425) + `filteredAvatarItems.map` grid (lines 427-476) with per-item thumbnail/`onError`-fallback/initials rendering. Delete this markup and replace with a single `<AvatarCharacterGallery character={form.character} style={form.style} onSelect={(c, s) => { updateField("character", c); updateField("style", s); }} className="..." />` call (match the Card's existing `<CardContent className="space-y-3">` wrapper).

Also delete, once the inline grid is gone and nothing else references them: the `avatarFilter`/`setAvatarFilter` state (line 115), the `filteredAvatarItems` memo (lines 192-222), the `failedThumbnailsRef` ref + `handleThumbnailError` callback (lines 224-230ish), the `AvatarGridItem` type (lines 62-70), and the now-unused imports `AVATAR_CHARACTERS`, `getAvatarInitials` (from `@/data/avatar-characters`) and `cn` (from `@/lib/utils`) -- confirm via grep that `cn(` has no other call sites in this file before deleting the import (all four current `cn(` call sites are inside the block being deleted).

**Speech card, NOT functionally changed** -- Card #3 "Speech" (lines ~482-567): Language `<Select>` over `PERSONA_VOICE_LOCALES` (switch its iterable to the imported `SUPPORTED_VOICE_LOCALES`), Voice `<Select>` over `VOICE_NAME_OPTIONS` with a leading `t("personas.useDefaultVoiceOption")` "(use default)" item, Greeting `<Textarea>`, and a configured-locales `<Badge>` row. Keep all of this behavior byte-for-byte (per-locale `voiceMap`/`greetingMap` semantics, the "(use default)" sentinel, no language-availability filtering) -- the ONLY change is the `<CardTitle>` text this plan retitles (see below); the `personas.editor.speechSectionTitle` i18n KEY itself does not change (only its translated VALUE), so no code paths that reference the key need touching.

**Private constants being deleted (replaced by shared imports)** -- lines 37-55:
```typescript
const PERSONA_VOICE_LOCALES = ["zh-CN", "en-US", "es-ES", "es-MX", "es-US"] as const;
const FLAGS: Record<string, string> = { /* ...5 entries... */ };
const LOCALE_LABEL_KEY: Record<string, string> = { /* ...5 entries... */ };
```
Every existing reference to `PERSONA_VOICE_LOCALES`/`FLAGS`/`LOCALE_LABEL_KEY` elsewhere in the file (the Language `<Select>`, the configured-locales `<Badge>` loop, `DEFAULT_LOCALE`'s type annotation) keeps working unchanged once these three names resolve to the imported ones instead of the local `const`s -- this is a pure import swap, do not rename call sites.

**Test mocking pattern already in place** -- `frontend/src/pages/admin/persona-editor.test.tsx` mocks `@/data/avatar-characters` directly (lines 44-69, 2 fixture characters incl. one photo avatar). Because `AvatarCharacterGallery` imports from the SAME `@/data/avatar-characters` module path, this existing `vi.mock` continues to apply transparently to the gallery once it's rendered as a child component -- no new mock needed for avatar data.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap in AvatarCharacterGallery, dedupe locale constants, retitle Speech card</name>
  <files>frontend/src/pages/admin/persona-editor.tsx, frontend/src/pages/admin/persona-editor.test.tsx, frontend/public/locales/en-US/admin.json, frontend/public/locales/zh-CN/admin.json, frontend/public/locales/es-ES/admin.json, frontend/public/locales/es-MX/admin.json, frontend/public/locales/es-US/admin.json</files>
  <behavior>
    - Test (existing, must still pass unmodified): `"renders avatar grid with filter buttons"`, `"renders avatar thumbnails from mock data"`, `"filters avatars by photo filter"`, `"filters avatars by video filter"`, `"selecting an avatar updates the preview character/style"` -- all currently assert against the inline grid's rendered DOM; they must pass identically against `<AvatarCharacterGallery>`'s rendered DOM since the markup is a direct extraction (same filter button i18n keys, same button/img/initials structure).
    - Test (existing, must still pass unmodified): `"renders speech section with language, voice, and greeting fields"`, `"does not show configured-languages chips when no locale is configured"`.
    - Test (new): `"renders the shared AvatarCharacterGallery component"` -- assert the gallery's grid container is present via the same selectors the pre-existing avatar-grid tests use (proves the swap, not just coincidental pass-through).
    - Test (new): `personas.editor.speechSectionTitle`'s en-US locale VALUE (read via `fs.readFileSync` + `JSON.parse`, mirroring `hcp-editor-tabs.test.tsx`'s `readLocaleJson` helper) contains the word "output" (case-insensitive) -- guards the VMODE-02 "Speech output" retitle without hardcoding the exact translated string across all locales.
  </behavior>
  <action>
    In `persona-editor.tsx`: replace the `import { AVATAR_CHARACTERS, getAvatarInitials } from "@/data/avatar-characters"` and `import { cn } from "@/lib/utils"` lines with `import { AvatarCharacterGallery } from "@/components/admin/avatar-character-gallery"` (delete the other two imports entirely once their only call sites -- listed in `<interfaces>` above -- are removed). Replace the private `PERSONA_VOICE_LOCALES`/`FLAGS`/`LOCALE_LABEL_KEY` const declarations (lines 37-55) with `import { SUPPORTED_VOICE_LOCALES, LOCALE_FLAGS, LOCALE_LABEL_KEY } from "@/lib/voice-constants"` and rename every in-file reference from `PERSONA_VOICE_LOCALES`/`FLAGS` to `SUPPORTED_VOICE_LOCALES`/`LOCALE_FLAGS` (the `LOCALE_LABEL_KEY` name is unchanged). Delete the `AvatarGridItem` type, the `avatarFilter`/`setAvatarFilter` state, the `filteredAvatarItems` memo, the `failedThumbnailsRef` ref, and the `handleThumbnailError` callback -- none have any remaining call site once Card #2's markup is replaced.

    Replace Card #2's `<CardContent>` body (the filter-button row + `filteredAvatarItems.map` grid) with a single `<AvatarCharacterGallery character={form.character} style={form.style} onSelect={(c, s) => { updateField("character", c); updateField("style", s); }} />` call, keeping the existing `<CardHeader>`/`<CardTitle>` (`{t("personas.characterSectionTitle")}`) unchanged.

    In Card #3 ("Speech"), leave every functional line (Language select, Voice select, Greeting textarea, configured-locales badges) untouched aside from renaming `PERSONA_VOICE_LOCALES` -> `SUPPORTED_VOICE_LOCALES` and `FLAGS` -> `LOCALE_FLAGS` at their call sites. Do NOT change the `<CardTitle>`'s JSX (`{te("speechSectionTitle")}` stays exactly as-is) -- only the JSON translation VALUE for `personas.editor.speechSectionTitle` changes, in the locale files (next paragraph).

    In all 5 `admin.json` locale files, update the existing `personas.editor.speechSectionTitle` key's VALUE (do not rename the key) to a portal-style "Speech output" phrasing consistent with each locale's existing terminology (reuse the same wording already used for `voiceLive.playgroundSection.speechOutput` in that locale, since it is the same underlying concept): en-US `"Speech output"`, zh-CN `"语音输出"`, es-ES/es-MX/es-US `"Salida de voz"`.

    In `persona-editor.test.tsx`: no changes required to the existing avatar-grid or speech-section test bodies (per the `<behavior>` block, they should pass unmodified against the swapped-in component) -- add the 2 new tests described above. If any existing avatar-grid test's selector turns out to depend on a DOM wrapper element that only the page's own markup provided (not the extracted component), adjust that single selector minimally rather than rewriting the test's intent.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/admin/persona-editor.test.tsx && npx tsc -b</automated>
  </verify>
  <done>persona-editor.tsx's Character & Avatar card renders via the shared AvatarCharacterGallery component with zero local gallery-state duplication; its locale constants come from the shared voice-constants.ts module; the Speech card's translated title reads as "Speech output" (or locale-equivalent) in all 5 locales; all persona-editor tests plus tsc -b pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extend persona E2E coverage for the shared gallery</name>
  <files>frontend/e2e/admin-avatar-personas.spec.ts</files>
  <action>
    Add a new `test(...)` to the existing `test.describe("Admin Avatar Personas — CRUD workflow", ...)` block in `admin-avatar-personas.spec.ts` (reuse the file's existing admin-session/navigation setup and the `createPersonaViaUi`-style helper pattern already present, per the file's current structure): create a persona via the editor, explicitly asserting the avatar gallery's filter buttons are visible before selecting a character (proving the shared `AvatarCharacterGallery` rendered, not just that a button with the character's name exists somewhere), select a non-default character/style combination distinct from the seeded default, save, then reload the edit page (`page.reload()` or re-navigate to the same `/admin/avatar-personas/:id/edit` URL) and assert the same character/style selection is still reflected as "selected" in the gallery (e.g. via the ring/border class or by re-locating the same button and checking an `aria-pressed`/selected-state attribute if one exists -- otherwise assert via the `AvatarView` preview's `data-avatar-character`/`data-avatar-style` attributes, which the file's existing tests already use at lines ~257-259). Clean up the created persona in the test's teardown following the file's existing deletion pattern (see the `test.afterAll`/inline delete calls already present for personas created during the suite).
  </action>
  <verify>
    <automated>cd frontend && npx playwright test e2e/admin-avatar-personas.spec.ts</automated>
  </verify>
  <done>A new E2E test proves the shared AvatarCharacterGallery renders in the persona editor and that a gallery selection survives a save + reload, without disturbing any existing test in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser -> `POST/PUT /api/v1/admin/avatar-personas` | Unchanged from Phases 36/37 -- this plan does not touch the persona API or its validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-38-06 | Tampering | `character`/`style` values produced by the swapped-in `AvatarCharacterGallery` | accept | Identical value domain to the pre-existing inline gallery (same `AVATAR_CHARACTERS` source, same `onSelect` contract); persona create/update validation (Phases 36/37, unmodified by this plan) already constrains these fields server-side -- no new surface introduced by the UI refactor |
</threat_model>

<verification>
1. `cd frontend && npx vitest run` -- full suite passes, including updated `persona-editor.test.tsx`.
2. `cd frontend && npx tsc -b` -- clean, no unused-locals from the deleted inline-gallery state/imports.
3. `cd frontend && npx playwright test e2e/admin-avatar-personas.spec.ts` -- passes, including the new gallery-persistence test.
4. Manual spot-check: open `/admin/avatar-personas/:id/edit`; confirm the Character & Avatar card looks visually identical to before (filter buttons + 4-col thumbnail grid) and the Speech card's title reads "Speech output".
</verification>

<success_criteria>
- Persona editor's avatar gallery is the shared `AvatarCharacterGallery` component, not a duplicated inline implementation.
- Persona editor's locale constants (`SUPPORTED_VOICE_LOCALES`/`LOCALE_FLAGS`/`LOCALE_LABEL_KEY`) come from `voice-constants.ts`, matching the HCP editor.
- Speech card presents as "Speech output" (or locale-equivalent) while preserving exact per-locale `voiceMap`/greeting semantics and the "(use default)" fallback.
- No backend files touched; no language-filtering or model-deployment persistence added (PEDIT-06/PEDIT-03 stay deferred).
- Full frontend unit suite + `tsc -b` + extended E2E spec pass.
</success_criteria>

<output>
After completion, create `.planning/phases/38-voice-mode-config-foundry-portal/38-03-SUMMARY.md`
</output>
