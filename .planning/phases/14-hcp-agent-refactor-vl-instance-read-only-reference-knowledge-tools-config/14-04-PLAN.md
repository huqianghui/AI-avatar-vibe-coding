---
phase: 14-hcp-agent-refactor-vl-instance-read-only-reference-knowledge-tools-config
plan: 04
type: execute
wave: 3
depends_on: ["14-01", "14-02", "14-03"]
files_modified:
  - backend/tests/test_voice_live_management.py
  - frontend/src/components/admin/vl-instance-dialog.test.tsx
  - frontend/src/components/admin/voice-avatar-tab.test.tsx
autonomous: false
requirements: [HCP-14-06]
must_haves:
  truths:
    - "Backend unassign endpoint test passes (POST /voice-live/instances/unassign returns 200)"
    - "Frontend VlInstanceDialog renders with 5 sections and handles create/edit"
    - "Frontend VoiceAvatarTab renders read-only preview when instance is assigned"
    - "Frontend build succeeds (npm run build)"
    - "Backend ruff check passes"
  artifacts:
    - path: "backend/tests/test_voice_live_management.py"
      provides: "Backend tests for unassign endpoint"
      contains: "test_unassign"
    - path: "frontend/src/components/admin/vl-instance-dialog.test.tsx"
      provides: "VlInstanceDialog component test"
      contains: "VlInstanceDialog"
    - path: "frontend/src/components/admin/voice-avatar-tab.test.tsx"
      provides: "VoiceAvatarTab read-only mode test"
      contains: "VoiceAvatarTab"
  key_links:
    - from: "backend/tests/test_voice_live_management.py"
      to: "backend/app/api/voice_live.py"
      via: "ASGI test client POST /api/v1/voice-live/instances/unassign"
      pattern: "voice-live/instances/unassign"
---

<objective>
Add backend and frontend tests for Phase 14 changes, then run full build verification to confirm everything works.

Purpose: Ensure the unassign endpoint, VlInstanceDialog, and simplified VoiceAvatarTab all function correctly. Final build verification catches any TypeScript, lint, or test regressions.

Output: Test files, passing CI checks
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-hcp-agent-refactor-vl-instance-read-only-reference-knowledge-tools-config/14-CONTEXT.md
@.planning/phases/14-hcp-agent-refactor-vl-instance-read-only-reference-knowledge-tools-config/14-01-SUMMARY.md
@.planning/phases/14-hcp-agent-refactor-vl-instance-read-only-reference-knowledge-tools-config/14-02-SUMMARY.md
@.planning/phases/14-hcp-agent-refactor-vl-instance-read-only-reference-knowledge-tools-config/14-03-SUMMARY.md

<interfaces>
From backend/app/api/voice_live.py (from Plan 01):
```python
@router.post("/instances/unassign", status_code=200)
async def unassign_instance_from_hcp(
    data: VoiceLiveInstanceUnassign,
    db: AsyncSession = Depends(get_db),
    user: User = Depends(get_current_user),
) -> dict:
```

From backend/tests/conftest.py (existing test patterns):
```python
# Uses httpx ASGITransport for testing
# auth_headers fixture provides JWT bearer token
# test_db provides in-memory SQLite session
```

From frontend test patterns:
```typescript
// vitest + @testing-library/react
// vi.mock() for hooks
// render() + screen.getByText/getByRole assertions
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend unassign endpoint test</name>
  <read_first>
    - backend/tests/test_voice_live_management.py (if exists -- extend it; if not, create)
    - backend/tests/conftest.py (test fixtures: test_db, auth_headers, client pattern)
    - backend/tests/test_hcp_agent_sync_integration.py (example of HCP + voice live test patterns)
    - backend/app/api/voice_live.py (the unassign endpoint to test)
    - backend/app/services/voice_live_instance_service.py (service functions)
  </read_first>
  <files>backend/tests/test_voice_live_management.py</files>
  <action>
Add tests to `backend/tests/test_voice_live_management.py` (create if not exists). If the file already exists, ADD the new test functions -- do not replace existing content.

**Test fixtures needed (use existing conftest patterns):**
- `client` (httpx AsyncClient with ASGITransport)
- `auth_headers` (JWT bearer token headers for admin user)
- A helper to create an HCP profile and a VL Instance in the test DB

**Tests to add:**

1. `test_unassign_instance_from_hcp`:
   - Create a VL Instance via POST /voice-live/instances
   - Create an HCP Profile via POST /hcp-profiles
   - Assign instance to HCP via POST /voice-live/instances/{id}/assign
   - Unassign via POST /voice-live/instances/unassign with body `{"hcp_profile_id": hcp_id}`
   - Assert response 200
   - Assert response body `{"hcp_profile_id": hcp_id, "voice_live_instance_id": null}`
   - GET the HCP profile and verify voice_live_instance_id is None/null

2. `test_unassign_nonexistent_hcp`:
   - POST /voice-live/instances/unassign with body `{"hcp_profile_id": "nonexistent-id"}`
   - Assert response 404

3. `test_unassign_already_unassigned_hcp`:
   - Create an HCP Profile (no VL Instance assigned)
   - POST /voice-live/instances/unassign with body `{"hcp_profile_id": hcp_id}`
   - Assert response 200 (idempotent -- setting null to null is fine)

Follow existing test patterns:
- Use `@pytest.mark.asyncio` decorator
- Use `async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as client:`
- Use `auth_headers` for authentication
- All API calls go through `/api/v1/voice-live/instances/...` path

Run `cd backend && ruff check .` and `ruff format --check .` to verify lint passes.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend && python3 -m pytest tests/test_voice_live_management.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_voice_live_management.py contains `test_unassign_instance_from_hcp`
    - backend/tests/test_voice_live_management.py contains `test_unassign_nonexistent_hcp`
    - All tests in the file pass (pytest exits 0)
    - `ruff check backend/tests/test_voice_live_management.py` passes
  </acceptance_criteria>
  <done>Backend tests for unassign endpoint all pass. Covers happy path, nonexistent HCP, and idempotent unassign.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend component tests + full build verification</name>
  <read_first>
    - frontend/src/components/admin/vl-instance-dialog.tsx (component to test)
    - frontend/src/components/admin/voice-avatar-tab.tsx (component to test)
    - frontend/src/components/voice/voice-session.test.tsx (existing test pattern for component tests with vi.mock)
    - frontend/src/hooks/use-voice-live.test.ts (existing test pattern for hook mocking)
  </read_first>
  <files>
    frontend/src/components/admin/vl-instance-dialog.test.tsx
    frontend/src/components/admin/voice-avatar-tab.test.tsx
  </files>
  <action>
1. Create `frontend/src/components/admin/vl-instance-dialog.test.tsx`:

```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";
import { render, screen } from "@testing-library/react";
// Mock hooks before importing component
vi.mock("@/hooks/use-voice-live-instances", () => ({
  useCreateVoiceLiveInstance: () => ({ mutate: vi.fn(), isPending: false }),
  useUpdateVoiceLiveInstance: () => ({ mutate: vi.fn(), isPending: false }),
}));
vi.mock("react-i18next", () => ({
  useTranslation: () => ({ t: (key: string) => key }),
}));
```

Tests:
- `it("renders create mode with empty form")`: Render with `open={true}` and no `instance` prop. Assert dialog title contains "voiceLive.createInstance". Assert name input is empty.
- `it("renders edit mode with pre-filled values")`: Render with `open={true}` and an `instance` prop (mock VoiceLiveInstance). Assert dialog title contains "voiceLive.editInstance". Assert name input has instance name.
- `it("renders all 5 form sections")`: Render open dialog. Assert presence of section headings: vlDialogModelSection, vlDialogVoiceSection, vlDialogAvatarSection, vlDialogConversationSection, vlDialogAgentSection.
- `it("does not render when closed")`: Render with `open={false}`. Assert no dialog content visible.

2. Create `frontend/src/components/admin/voice-avatar-tab.test.tsx`:

```typescript
import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/react";
```

Mock `@/hooks/use-voice-live-instances` to return a list of instances. Mock `react-hook-form` UseFormReturn. Mock `react-i18next`. Mock `react-router-dom` useNavigate.

Tests:
- `it("shows read-only preview when instance is assigned")`: Provide form with `voice_live_instance_id` set to a valid ID. Mock instances list includes that instance. Assert "voiceLive.configPreview" text is present. Assert instance name/model appears.
- `it("shows empty state when no instance assigned")`: Provide form with `voice_live_instance_id` as null. Assert "voiceLive.noInstanceAssigned" text is present. Assert "voiceLive.goToVlManagement" button/link is present.
- `it("does not render voice editing controls")`: Assert absence of temperature slider, voice name dropdown, avatar grid. File should NOT find elements with label "voiceAva" or "voiceAndrew".

3. Run full build verification:
```bash
cd frontend && npm run build
cd ../backend && ruff check . && ruff format --check .
```
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx vitest run src/components/admin/vl-instance-dialog.test.tsx src/components/admin/voice-avatar-tab.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/admin/vl-instance-dialog.test.tsx contains at least 3 test cases
    - frontend/src/components/admin/voice-avatar-tab.test.tsx contains at least 3 test cases
    - All vitest tests pass (exit 0)
    - `npm run build` succeeds (exit 0)
    - `ruff check .` passes in backend directory
  </acceptance_criteria>
  <done>Frontend component tests pass for VlInstanceDialog and VoiceAvatarTab. Full frontend build succeeds. Backend lint passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of Phase 14 complete flow</name>
  <files></files>
  <action>
Phase 14 is now complete. All automated tests pass and builds succeed. Human verification is needed to confirm:
- VL Management page CRUD works with the rich dialog
- HCP Voice Tab shows read-only preview
- Knowledge and Tools placeholder tabs are visible
- Avatar CDN thumbnails render correctly

Steps:
1. Start backend: `cd backend && uvicorn app.main:app --reload --port 8000`
2. Start frontend: `cd frontend && npm run dev`
3. Login as admin
4. Navigate to Voice Live Management (/admin/voice-live):
   - Click "New Instance" -- verify dialog shows 5 sections (Model, Voice, Avatar, Conversation, Agent)
   - Create an instance -- verify avatar CDN thumbnails display in the avatar grid
   - Edit an existing instance -- verify values are pre-filled
   - Delete an instance with no HCPs -- should succeed
   - Try deleting an instance with HCPs -- should show 409 conflict toast
5. Navigate to HCP Profiles then Edit an HCP:
   - Verify 4 tabs: Profile, Voice and Avatar, Knowledge, Tools
   - Click Voice and Avatar tab: should show instance selector + read-only preview (if assigned) or empty state (if not)
   - Assign a VL Instance from dropdown -- verify preview updates to show instance config
   - Click "Manage in Voice Live" -- should navigate to /admin/voice-live
   - Click Knowledge tab -- should show placeholder
   - Click Tools tab -- should show placeholder
  </action>
  <verify>Human verifies the above steps work correctly</verify>
  <done>Human confirms VL Management CRUD works, HCP Voice Tab is read-only with instance selector, Knowledge/Tools placeholders are visible, and avatar thumbnails render from CDN.</done>
</task>

</tasks>

<verification>
- Backend: `pytest tests/test_voice_live_management.py -x -v` passes
- Frontend: `vitest run` for component tests passes
- Frontend: `npm run build` succeeds
- Backend: `ruff check .` passes
- Visual: VL Management page CRUD works, HCP Voice Tab is read-only
</verification>

<success_criteria>
1. Backend unassign tests pass (happy path + error cases)
2. Frontend VlInstanceDialog test confirms 5 sections render
3. Frontend VoiceAvatarTab test confirms read-only mode
4. Frontend build succeeds
5. Backend lint passes
6. Human-verified: VL Management CRUD works, HCP Voice Tab is read-only, Knowledge/Tools placeholders visible
</success_criteria>

<output>
After completion, create `.planning/phases/14-hcp-agent-refactor-vl-instance-read-only-reference-knowledge-tools-config/14-04-SUMMARY.md`
</output>
