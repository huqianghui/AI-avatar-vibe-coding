---
phase: 14-hcp-agent-refactor-vl-instance-read-only-reference-knowledge-tools-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/voice_live.py
  - backend/app/schemas/voice_live_instance.py
  - frontend/src/api/voice-live.ts
  - frontend/src/hooks/use-voice-live-instances.ts
  - frontend/src/types/voice-live.ts
  - frontend/public/locales/en-US/admin.json
  - frontend/public/locales/zh-CN/admin.json
autonomous: true
requirements: [HCP-14-01, HCP-14-06]
must_haves:
  truths:
    - "POST /api/v1/voice-live/instances/unassign returns 200 and clears voice_live_instance_id on HCP profile"
    - "Frontend unassignVoiceLiveInstance API function exists and calls the unassign endpoint"
    - "useUnassignVoiceLiveInstance hook exists and invalidates both instance and hcp-profiles query caches"
    - "All new i18n keys exist in both en-US and zh-CN admin.json"
  artifacts:
    - path: "backend/app/api/voice_live.py"
      provides: "POST /voice-live/instances/unassign endpoint"
      contains: "unassign_instance_from_hcp"
    - path: "frontend/src/api/voice-live.ts"
      provides: "unassignVoiceLiveInstance API function"
      contains: "unassignVoiceLiveInstance"
    - path: "frontend/src/hooks/use-voice-live-instances.ts"
      provides: "useUnassignVoiceLiveInstance mutation hook"
      contains: "useUnassignVoiceLiveInstance"
  key_links:
    - from: "frontend/src/hooks/use-voice-live-instances.ts"
      to: "frontend/src/api/voice-live.ts"
      via: "import unassignVoiceLiveInstance"
      pattern: "unassignVoiceLiveInstance"
    - from: "backend/app/api/voice_live.py"
      to: "backend/app/services/voice_live_instance_service.py"
      via: "voice_live_instance_service.unassign_from_hcp"
      pattern: "unassign_from_hcp"
---

<objective>
Add the backend unassign endpoint, extend frontend API client and hooks with unassign support, add VoiceLiveInstanceUnassign schema, and add all new i18n keys required by Phase 14.

Purpose: Provides the data layer and i18n foundation that Plans 02 and 03 depend on. The unassign capability enables HCP Voice Tab to remove VL Instance association. The i18n keys enable both the rewritten VL Management page and the simplified HCP Voice Tab.

Output: Backend unassign endpoint, frontend API/hook, i18n keys in en-US and zh-CN
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-hcp-agent-refactor-vl-instance-read-only-reference-knowledge-tools-config/14-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/app/services/voice_live_instance_service.py (already exists):
```python
async def unassign_from_hcp(db: AsyncSession, hcp_profile_id: str) -> HcpProfile:
    """Remove Voice Live Instance association from an HCP Profile."""
```

From backend/app/schemas/voice_live_instance.py (existing assign schema):
```python
class VoiceLiveInstanceAssign(BaseModel):
    """Assign a Voice Live Instance to an HCP Profile."""
    hcp_profile_id: str
```

From frontend/src/types/voice-live.ts (existing instance types):
```typescript
export interface VoiceLiveInstance { ... }
export interface VoiceLiveInstanceCreate { ... }
export type VoiceLiveInstanceUpdate = Partial<VoiceLiveInstanceCreate>;
export interface VoiceLiveInstanceAssign { hcp_profile_id: string; }
```

From frontend/src/hooks/use-voice-live-instances.ts (existing hooks):
```typescript
export function useAssignVoiceLiveInstance() { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend unassign endpoint + schema</name>
  <read_first>
    - backend/app/api/voice_live.py (current routes — see where to add unassign)
    - backend/app/services/voice_live_instance_service.py (unassign_from_hcp already implemented)
    - backend/app/schemas/voice_live_instance.py (VoiceLiveInstanceAssign pattern to follow)
    - backend/app/schemas/hcp_profile.py (HcpProfileOut fields for response)
  </read_first>
  <files>
    backend/app/api/voice_live.py
    backend/app/schemas/voice_live_instance.py
  </files>
  <action>
1. In `backend/app/schemas/voice_live_instance.py`, add a new schema class:

```python
class VoiceLiveInstanceUnassign(BaseModel):
    """Unassign Voice Live Instance from an HCP Profile."""
    hcp_profile_id: str
```

2. In `backend/app/api/voice_live.py`, add the unassign endpoint AFTER the existing assign endpoint (line ~202) and BEFORE the Avatar section comment:

```python
@router.post("/instances/unassign", status_code=200)
async def unassign_instance_from_hcp(
    data: VoiceLiveInstanceUnassign,
    db: AsyncSession = Depends(get_db),
    user: User = Depends(get_current_user),
) -> dict:
    """Remove Voice Live Instance association from an HCP Profile."""
    profile = await voice_live_instance_service.unassign_from_hcp(db, data.hcp_profile_id)
    return {"hcp_profile_id": profile.id, "voice_live_instance_id": None}
```

3. Add the import of VoiceLiveInstanceUnassign to the existing import block from `app.schemas.voice_live_instance`.

IMPORTANT: Place the `/instances/unassign` route BEFORE the `/{instance_id}` parameterized routes to follow FastAPI static-before-parameterized rule (CLAUDE.md Gotcha #3). The route should go between the `list_voice_live_instances` (GET /instances) and `get_voice_live_instance` (GET /instances/{instance_id}) routes.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend && python3 -c "from app.api.voice_live import router; routes = [r.path for r in router.routes]; assert '/instances/unassign' in routes, f'Missing route. Found: {routes}'; print('OK: unassign route exists')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/api/voice_live.py contains `async def unassign_instance_from_hcp(`
    - backend/app/api/voice_live.py contains `VoiceLiveInstanceUnassign` in import from app.schemas.voice_live_instance
    - backend/app/schemas/voice_live_instance.py contains `class VoiceLiveInstanceUnassign(BaseModel):`
    - The `/instances/unassign` route appears BEFORE any `/{instance_id}` route in the file
  </acceptance_criteria>
  <done>POST /voice-live/instances/unassign endpoint exists, calls unassign_from_hcp service, returns {hcp_profile_id, voice_live_instance_id: null}</done>
</task>

<task type="auto">
  <name>Task 2: Frontend API/hook extension + i18n keys for Phase 14</name>
  <read_first>
    - frontend/src/api/voice-live.ts (existing API functions)
    - frontend/src/hooks/use-voice-live-instances.ts (existing hooks pattern)
    - frontend/src/types/voice-live.ts (existing types)
    - frontend/public/locales/en-US/admin.json (existing i18n keys)
    - frontend/public/locales/zh-CN/admin.json (existing i18n keys)
  </read_first>
  <files>
    frontend/src/api/voice-live.ts
    frontend/src/hooks/use-voice-live-instances.ts
    frontend/src/types/voice-live.ts
    frontend/public/locales/en-US/admin.json
    frontend/public/locales/zh-CN/admin.json
  </files>
  <action>
1. In `frontend/src/api/voice-live.ts`, add after the existing `assignVoiceLiveInstance` function:

```typescript
export async function unassignVoiceLiveInstance(
  hcpProfileId: string,
): Promise<{ hcp_profile_id: string; voice_live_instance_id: null }> {
  const response = await apiClient.post<{ hcp_profile_id: string; voice_live_instance_id: null }>(
    "/voice-live/instances/unassign",
    { hcp_profile_id: hcpProfileId },
  );
  return response.data;
}
```

2. In `frontend/src/hooks/use-voice-live-instances.ts`, add import of `unassignVoiceLiveInstance` from `@/api/voice-live` and add hook:

```typescript
export function useUnassignVoiceLiveInstance() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (hcpProfileId: string) => unassignVoiceLiveInstance(hcpProfileId),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
      queryClient.invalidateQueries({ queryKey: ["hcp-profiles"] });
    },
  });
}
```

3. In `frontend/src/types/voice-live.ts`, add after VoiceLiveInstanceAssign interface:

```typescript
export interface VoiceLiveInstanceUnassign {
  hcp_profile_id: string;
}
```

4. In `frontend/public/locales/en-US/admin.json`, add these keys inside the existing `"voiceLive"` object (after "migrateHint"):

```json
"readOnlyTitle": "Voice & Avatar Configuration",
"readOnlyDescription": "Voice/avatar settings come from the assigned Voice Live Instance. Edit in Voice Live Management.",
"assignedInstance": "Assigned Instance",
"noInstanceAssigned": "No Voice Live Instance assigned",
"goToVlManagement": "Manage in Voice Live",
"removeInstance": "Remove Assignment",
"removeInstanceConfirm": "Remove Voice Live Instance \"{{name}}\" from this HCP? The HCP will fall back to inline settings.",
"removeInstanceSuccess": "Instance assignment removed",
"instanceAssigned": "Instance assigned to HCP",
"configPreview": "Configuration Preview",
"previewModel": "Model",
"previewVoice": "Voice",
"previewAvatar": "Avatar",
"previewTurnDetection": "Turn Detection",
"previewLanguage": "Language",
"previewTemperature": "Temperature",
"vlDialogModelSection": "Generative AI Model",
"vlDialogVoiceSection": "Voice Settings",
"vlDialogAvatarSection": "Avatar Settings",
"vlDialogConversationSection": "Conversation Parameters",
"vlDialogAgentSection": "Agent Instructions",
"vlDialogTemperature": "Temperature",
"vlDialogTurnDetection": "Turn Detection",
"vlDialogNoiseSuppression": "Noise Suppression",
"vlDialogEchoCancellation": "Echo Cancellation",
"vlDialogEouDetection": "End-of-Utterance Detection",
"vlDialogRecognitionLanguage": "Recognition Language",
"vlDialogAgentInstructions": "Agent Instructions Override",
"vlDialogAgentInstructionsHint": "Optional. Override default agent instructions for all HCPs using this instance.",
"tabKnowledge": "Knowledge",
"tabTools": "Tools",
"knowledgePlaceholderTitle": "Knowledge Base",
"knowledgePlaceholderBody": "Knowledge base configuration (product materials, clinical references) will be available in a future update.",
"toolsPlaceholderTitle": "Function Call Tools",
"toolsPlaceholderBody": "Function Call tool configuration will be available in a future update.",
"assignToHcp": "Assign to HCP",
"assignSuccess": "Instance assigned to HCP",
"assignToHcpPlaceholder": "Select an HCP profile..."
```

5. In `frontend/public/locales/zh-CN/admin.json`, add matching keys inside the `"voiceLive"` object:

```json
"readOnlyTitle": "语音 & 数字人配置",
"readOnlyDescription": "语音/数字人设置来自已分配的 Voice Live 实例。请在 Voice Live 管理页编辑。",
"assignedInstance": "已分配实例",
"noInstanceAssigned": "未分配 Voice Live 实例",
"goToVlManagement": "前往 Voice Live 管理",
"removeInstance": "移除分配",
"removeInstanceConfirm": "移除 HCP 的 Voice Live 实例 \"{{name}}\"？该 HCP 将回退到内联设置。",
"removeInstanceSuccess": "已移除实例分配",
"instanceAssigned": "实例已分配给 HCP",
"configPreview": "配置预览",
"previewModel": "模型",
"previewVoice": "语音",
"previewAvatar": "数字人",
"previewTurnDetection": "轮次检测",
"previewLanguage": "识别语言",
"previewTemperature": "温度",
"vlDialogModelSection": "生成式 AI 模型",
"vlDialogVoiceSection": "语音设置",
"vlDialogAvatarSection": "数字人设置",
"vlDialogConversationSection": "对话参数",
"vlDialogAgentSection": "Agent 指令",
"vlDialogTemperature": "温度",
"vlDialogTurnDetection": "轮次检测",
"vlDialogNoiseSuppression": "噪音抑制",
"vlDialogEchoCancellation": "回声消除",
"vlDialogEouDetection": "语句结束检测",
"vlDialogRecognitionLanguage": "识别语言",
"vlDialogAgentInstructions": "Agent 指令覆盖",
"vlDialogAgentInstructionsHint": "可选。为使用此实例的所有 HCP 覆盖默认 Agent 指令。",
"tabKnowledge": "知识库",
"tabTools": "工具",
"knowledgePlaceholderTitle": "知识库",
"knowledgePlaceholderBody": "知识库配置（产品资料、临床参考文献）将在后续更新中提供。",
"toolsPlaceholderTitle": "Function Call 工具",
"toolsPlaceholderBody": "Function Call 工具配置将在后续更新中提供。",
"assignToHcp": "分配给 HCP",
"assignSuccess": "实例已分配给 HCP",
"assignToHcpPlaceholder": "选择 HCP..."
```
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc -b --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/api/voice-live.ts contains `export async function unassignVoiceLiveInstance(`
    - frontend/src/hooks/use-voice-live-instances.ts contains `export function useUnassignVoiceLiveInstance(`
    - frontend/src/types/voice-live.ts contains `export interface VoiceLiveInstanceUnassign`
    - frontend/public/locales/en-US/admin.json contains `"readOnlyTitle":`
    - frontend/public/locales/en-US/admin.json contains `"tabKnowledge":`
    - frontend/public/locales/en-US/admin.json contains `"vlDialogModelSection":`
    - frontend/public/locales/zh-CN/admin.json contains `"readOnlyTitle":`
    - frontend/public/locales/zh-CN/admin.json contains `"tabKnowledge":`
    - TypeScript compilation passes (npx tsc -b --noEmit exits 0)
  </acceptance_criteria>
  <done>Frontend unassign API function, hook, types, and all Phase 14 i18n keys exist in both locales. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- Backend: `python3 -c "from app.api.voice_live import router"` succeeds
- Frontend: `npx tsc -b --noEmit` passes
- i18n: en-US and zh-CN admin.json both have voiceLive.readOnlyTitle, voiceLive.tabKnowledge, voiceLive.vlDialogModelSection
</verification>

<success_criteria>
1. POST /voice-live/instances/unassign endpoint works and calls existing unassign_from_hcp service
2. Frontend API client has unassignVoiceLiveInstance function
3. Frontend hook useUnassignVoiceLiveInstance exists and invalidates correct caches
4. All new i18n keys present in both en-US and zh-CN
5. TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-hcp-agent-refactor-vl-instance-read-only-reference-knowledge-tools-config/14-01-SUMMARY.md`
</output>
