---
phase: 34-spanish-es-i18n
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - frontend/public/locales/es-ES/admin.json
  - frontend/public/locales/es-MX/admin.json
  - frontend/public/locales/es-US/admin.json
  - frontend/public/locales/es-ES/voice.json
  - frontend/public/locales/es-MX/voice.json
  - frontend/public/locales/es-US/voice.json
  - frontend/public/locales/es-ES/avatar.json
  - frontend/public/locales/es-MX/avatar.json
  - frontend/public/locales/es-US/avatar.json
  - frontend/src/i18n/untranslated-whitelist.ts
autonomous: true
requirements: [LANG-01]

must_haves:
  truths:
    - "Admin voice-map UI (built later in 34-08/34-09) can render every label/button/helper string in es-ES/es-MX/es-US with no missing-key fallback to English"
    - "Avatar page and voice-transcript UI can render every string in es-ES/es-MX/es-US with no missing-key fallback to English"
  artifacts:
    - path: "frontend/public/locales/es-ES/admin.json"
      provides: "full es-ES mirror of en-US/admin.json, identical key structure"
    - path: "frontend/public/locales/es-MX/voice.json"
      provides: "full es-MX mirror of en-US/voice.json, identical key structure"
    - path: "frontend/public/locales/es-US/avatar.json"
      provides: "full es-US mirror of en-US/avatar.json, identical key structure"
  key_links:
    - from: "frontend/src/i18n/locale-parity.test.ts"
      to: "frontend/public/locales/{es-ES,es-MX,es-US}/{admin,voice,avatar}.json"
      via: "describe.each(NAMESPACES) parity check, now exercised for these 3 namespaces"
      pattern: "admin\\.json|voice\\.json|avatar\\.json"
---

<objective>
Translate the `admin`, `voice`, and `avatar` namespaces into es-ES/es-MX/es-US (D-01/D-02). These 3 namespaces back the admin voice-map UI built in 34-08/34-09 and the avatar page/voice-transcript UI verified in 34-10 — landing them now (Wave 2) lets those later plans consume complete translations without a namespace-translation dependency blocking them.

Purpose: keep translation-batch work isolated from the parity-test/whitelist/switcher "contract" plan (34-01) and from the other namespace batches (34-03, 34-04) so all three batches can execute in parallel (same wave, disjoint files).
Output: 9 new locale JSON files (3 namespaces x 3 es-* locales), each parity-verified against en-US.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-spanish-es-i18n/34-CONTEXT.md
@.planning/phases/34-spanish-es-i18n/34-UI-SPEC.md

<interfaces>
This plan depends on 34-01's `frontend/src/i18n/locale-parity.test.ts` (already committed by the time this plan runs) and `frontend/src/i18n/untranslated-whitelist.ts`. Do not recreate either file — only APPEND new whitelist entries if you discover a genuinely identical brand-name/proper-noun leaf value (never a whole-namespace exemption). The parity test enumerates namespaces via `fs.readdirSync` on `frontend/public/locales/en-US/` — it will automatically pick up `admin.json`/`voice.json`/`avatar.json` once the es-* mirrors exist, no test-file change needed here.

Translation rules (D-02, locked, identical to 34-01 Task 3 step 3):
- Register: formal `usted` throughout all 3 variants (professional context) — never `tú`/`vosotros`.
- Vocabulary: es-ES uses Iberian word choices where a term has a distinct Iberian form (e.g. "ordenador"); es-MX/es-US use Latin-American word choices (e.g. "computadora"). Voice/avatar/admin copy is unlikely to contain such terms, but check each string.
- Preserve every `{{var}}` interpolation placeholder verbatim (same variable name) — the parity test's interpolation-token check will fail loudly if a placeholder is renamed or dropped.
- Mirror the exact JSON key structure/nesting of the en-US source file — the parity test's key-set check compares recursively.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Translate admin.json into es-ES/es-MX/es-US</name>
  <files>frontend/public/locales/es-ES/admin.json, frontend/public/locales/es-MX/admin.json, frontend/public/locales/es-US/admin.json, frontend/src/i18n/untranslated-whitelist.ts</files>
  <read_first>frontend/public/locales/en-US/admin.json (full file, source of truth for keys/structure), frontend/src/i18n/untranslated-whitelist.ts</read_first>
  <behavior>
    - Test: `locale-parity.test.ts -t "admin"` key-set/empty-value/interpolation/untranslated checks all pass for admin.json across all 5 locales
  </behavior>
  <action>
Read `frontend/public/locales/en-US/admin.json` in full. Create `frontend/public/locales/es-ES/admin.json`, `frontend/public/locales/es-MX/admin.json`, `frontend/public/locales/es-US/admin.json`, each mirroring the exact key structure with every string leaf value translated per the register/vocabulary rules above. Every `{{var}}` placeholder must appear verbatim in every translated value that contains it in en-US.

If any leaf value is a brand name, product name, or proper noun that is legitimately identical across en-US and all 3 es-* locales, append its `"admin.<dotted.key.path>"` entry to `frontend/src/i18n/untranslated-whitelist.ts`'s `UNTRANSLATED_WHITELIST` array (do not remove or reorder existing entries; keep the array under 15 total entries per the guardrail — if adding would exceed 15, re-review whether the value should instead be genuinely translated).
</action>
  <acceptance_criteria>
- `python3 -c "import json; en=json.load(open('frontend/public/locales/en-US/admin.json')); [assert_eq := (lambda l: set(json.load(open(f'frontend/public/locales/{l}/admin.json')).keys())==set(en.keys()))(l) for l in ['es-ES','es-MX','es-US']]"` — top-level key sets match (adjust to a plain loop if `assert_eq` walrus trick errors; the check is: all 3 es-* files parse as valid JSON with the same top-level key set as en-US)
- `cd frontend && npx vitest run src/i18n/locale-parity.test.ts -t "admin"` — 0 failures
</acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/i18n/locale-parity.test.ts -t "admin"</automated>
  </verify>
  <done>es-ES/es-MX/es-US admin.json exist, parse as valid JSON, mirror en-US's key structure exactly, and pass all 4 locale-parity checks for the "admin" namespace.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Translate voice.json and avatar.json into es-ES/es-MX/es-US</name>
  <files>frontend/public/locales/es-ES/voice.json, frontend/public/locales/es-MX/voice.json, frontend/public/locales/es-US/voice.json, frontend/public/locales/es-ES/avatar.json, frontend/public/locales/es-MX/avatar.json, frontend/public/locales/es-US/avatar.json, frontend/src/i18n/untranslated-whitelist.ts</files>
  <read_first>frontend/public/locales/en-US/voice.json, frontend/public/locales/en-US/avatar.json, frontend/src/i18n/untranslated-whitelist.ts</read_first>
  <behavior>
    - Test: `locale-parity.test.ts -t "voice"` all checks pass across all 5 locales
    - Test: `locale-parity.test.ts -t "avatar"` all checks pass across all 5 locales
  </behavior>
  <action>
Read both `frontend/public/locales/en-US/voice.json` and `frontend/public/locales/en-US/avatar.json` in full. Create the 6 es-* mirror files (3 locales x 2 namespaces) following the same rules as Task 1: exact key structure, formal `usted`, Iberian vocabulary for es-ES vs Latin-American for es-MX/es-US, every `{{var}}` placeholder preserved verbatim.

These two namespaces are the ones the avatar page (`avatar-page.tsx`), voice transcript component, and mic-permission dialog read from (per 34-CONTEXT.md's Reusable Assets) — pay particular attention to any string containing a placeholder like `{{sourceCount}}` or `{{speaker}}`, since those feed directly into the LANG-01 success criterion of "no missing-key fallback to English" on the live avatar page (verified later by 34-05's E2E and 34-10's E2E).

Append any legitimately-identical brand/proper-noun leaf keys to `untranslated-whitelist.ts` as in Task 1 (shared file — if Task 1 already ran in this same plan execution, append after its entries; do not duplicate the file's header comment).
</action>
  <acceptance_criteria>
- `cd frontend && npx vitest run src/i18n/locale-parity.test.ts -t "voice"` — 0 failures
- `cd frontend && npx vitest run src/i18n/locale-parity.test.ts -t "avatar"` — 0 failures
</acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/i18n/locale-parity.test.ts -t "voice" -t "avatar"</automated>
  </verify>
  <done>es-ES/es-MX/es-US voice.json and avatar.json exist, mirror en-US's structure exactly, preserve all interpolation placeholders, and pass all locale-parity checks for both namespaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser client -> static locale JSON | Same as 34-01 — static asset fetch, no user input reflected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-34-03 | Information Disclosure | New admin.json/voice.json/avatar.json es-* files served publicly | accept | UI copy only, no secrets — same exposure class as existing en-US/zh-CN files |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/i18n/locale-parity.test.ts -t "admin" -t "voice" -t "avatar"` — all green
- `cd frontend && npx tsc -b` — no type errors (JSON files are not compiled, but confirms no accidental `.ts` breakage)
</verification>

<success_criteria>
- admin.json, voice.json, avatar.json fully translated and parity-verified in es-ES/es-MX/es-US
- No missing-key fallback risk for these 3 namespaces once the switcher is used to select any es-* locale
</success_criteria>

<output>
After completion, create `.planning/phases/34-spanish-es-i18n/34-02-SUMMARY.md`
</output>
