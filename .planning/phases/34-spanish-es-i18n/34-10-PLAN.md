---
phase: 34-spanish-es-i18n
plan: 10
type: execute
wave: 6
depends_on: ["34-06", "34-07"]
files_modified:
  - frontend/e2e/anonymous-avatar-voice-es.spec.ts
autonomous: true
requirements: [LANG-02]

must_haves:
  truths:
    - "For each of es-ES/es-MX/es-US, a full anonymous-avatar-voice E2E flow (mocked WebRTC transport) reaches the connected state using that locale's default neural voice in the mocked session response"
    - "The entire phase's test surface (backend pytest, frontend vitest, tsc, ruff, Playwright E2E) is green in one final regression pass"
  artifacts:
    - path: "frontend/e2e/anonymous-avatar-voice-es.spec.ts"
      provides: "3 parametrized E2E cases (es-ES/es-MX/es-US) reusing the existing mock-transport helpers from anonymous-avatar-voice.spec.ts"
  key_links:
    - from: "frontend/e2e/anonymous-avatar-voice-es.spec.ts"
      to: "frontend/e2e/anonymous-avatar-voice.spec.ts"
      via: "shared mockSession/mockWebrtcSessionSuccess/installGrantedMic/installFakeWebrtcTransport helpers"
      pattern: "installFakeWebrtcTransport|mockWebrtcSessionSuccess"
---

<objective>
Close out LANG-02 with Playwright E2E proof that the mocked voice-session negotiation path works for all 3 es-* locales (D-11: mid-session locale switch rebuilding the session is the accepted MVP behavior, already implemented by existing `avatar-page.tsx` code per 34-CONTEXT.md — this plan verifies the es-* path specifically, it does not add new reconnection logic), then run the full project regression suite as this phase's final gate.

Purpose: this is the last plan of Phase 34 — after this plan's regression task is green, both LANG-01 and LANG-02 are fully implemented, unit-tested, and E2E-verified per CLAUDE.md's top-priority rule, ready for the phase's final commit + push.
Output: a new es-* voice E2E spec file, and a clean full-suite regression run.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-spanish-es-i18n/34-VALIDATION.md

<interfaces>
`frontend/e2e/anonymous-avatar-voice.spec.ts` (full 229-line file, confirmed by direct read) — the exact mock pattern this plan's new spec must reuse verbatim (import, do not reimplement):
- `mockSession(page)` — stubs the session-token endpoint
- `mockWebrtcSessionSuccess(page)` — stubs the `/public/avatar/webrtc-session` response; for the new es-* spec, this mock's stubbed response body's voice name must reflect `DEFAULT_PUBLIC_VOICE_BY_LOCALE[locale]` from 34-06 (es-ES-ElviraNeural / es-MX-DaliaNeural / es-US-PalomaNeural) rather than the existing default's en-US value — check whether `mockWebrtcSessionSuccess` accepts a parameter for this, or whether it needs a locale-aware variant added to the shared helpers file it lives in
- `installDeniedMic(page)` / `installGrantedMic(page)` — mic-permission mocking
- `installFakeWebrtcTransport(page)` — fake `RTCPeerConnection`/`WebSocket` implementing the offer/answer + `rtc.call.sdp.create`/`rtc.call.sdp.created` handshake
- Success assertion pattern: `page.waitForEvent("console", { predicate: (msg) => msg.text().includes("Remote description set successfully") })` and `page.getByTestId("audio-orb")` visible

34-VALIDATION.md's Wave 0 requirement explicitly lists `frontend/e2e/anonymous-avatar-voice-es.spec.ts` as required — this task satisfies that Wave 0 item retroactively (it was deferred to this final wave since it depends on 34-06's locale-aware voice fallback and 34-07's locale-forwarding fix both being in place first).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright E2E — es-* voice session negotiation (mocked)</name>
  <files>frontend/e2e/anonymous-avatar-voice-es.spec.ts</files>
  <read_first>frontend/e2e/anonymous-avatar-voice.spec.ts, frontend/src/components/shared/language-switcher.tsx</read_first>
  <action>
Create `frontend/e2e/anonymous-avatar-voice-es.spec.ts`, importing the shared mock helpers from `anonymous-avatar-voice.spec.ts`'s helper module (if helpers are exported from a separate `e2e/helpers/` file, import from there; if they are defined inline in that spec file, either extract them to a shared helper file first or duplicate only the minimal subset needed — prefer extraction if the helpers are not already exported, to avoid duplicating the offer/answer handshake fake).

For each of es-ES, es-MX, es-US, write a `test()` that:
1. Navigates to `/`.
2. Uses the language switcher to select the target locale (click trigger, click the matching dropdown item by its native-language label text, same interaction as 34-05's `language-switcher-es.spec.ts`).
3. Calls `mockSession(page)` and a locale-aware variant of `mockWebrtcSessionSuccess(page, { locale })` that stubs the webrtc-session response with `voice.name` set to that locale's `DEFAULT_PUBLIC_VOICE_BY_LOCALE` value (es-ES-ElviraNeural / es-MX-DaliaNeural / es-US-PalomaNeural) — add this `locale` parameter to the shared helper if it does not already accept one, defaulting to the existing en-US behavior when omitted so the original `anonymous-avatar-voice.spec.ts` file needs no changes.
4. Calls `installGrantedMic(page)` and `installFakeWebrtcTransport(page)`.
5. Triggers the avatar voice connection flow the same way the existing spec does (locate the entry-point button/action from the existing spec's setup steps).
6. Asserts the same success signal used by the existing spec: the "Remote description set successfully" console message fires and `page.getByTestId("audio-orb")` becomes visible.
7. Additionally asserts (via a `page.route` interception check or by inspecting the last webrtc-session request body captured during step 3's mock setup) that the outgoing `WebrtcSessionRequest.locale` field equals the target es-* locale — proving the frontend actually forwarded the switched locale, not just that the mock happened to return an es-* voice name regardless of what was requested.
</action>
  <acceptance_criteria>
- `frontend/e2e/anonymous-avatar-voice-es.spec.ts` exists with exactly 3 test cases (es-ES, es-MX, es-US)
- `cd frontend && npm run test:e2e -- anonymous-avatar-voice-es.spec.ts` — all 3 tests pass, 0 failures
</acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test:e2e -- anonymous-avatar-voice-es.spec.ts</automated>
  </verify>
  <done>Mocked E2E proves that switching to each es-* locale and initiating a voice session reaches the connected state, with the correct locale forwarded in the session request and the correct default voice reflected in the mocked response.</done>
</task>

<task type="auto">
  <name>Task 2: Full phase regression gate</name>
  <files></files>
  <read_first>.planning/phases/34-spanish-es-i18n/34-VALIDATION.md</read_first>
  <action>
Run the complete regression suite for Phase 34, in this order, fixing any failure before proceeding to the next command (do not skip a failing step):
1. `cd backend && ruff check . && ruff format --check .`
2. `cd backend && pytest -v` (full suite, `--cov-fail-under=89` enforced via `pyproject.toml` — if coverage drops below 89% due to new uncovered branches introduced anywhere in Phase 34's backend changes, add the missing test case(s) rather than lowering the threshold)
3. `cd frontend && npx tsc -b`
4. `cd frontend && npm test` (full vitest suite, including `locale-parity.test.ts` unfiltered)
5. `cd frontend && npm run build`
6. `cd frontend && npm run test:e2e` (full Playwright suite, including both new es-* specs from 34-05 and this plan, plus all pre-existing specs — confirming no regression to en-US/zh-CN flows)

If any step fails, diagnose and fix the root cause in the relevant Phase 34 file (do not modify pre-existing Phase 33-or-earlier code unless a Phase 34 change is the direct cause of the regression) and re-run that step and all subsequent steps.
</action>
  <acceptance_criteria>
- All 6 commands above exit 0 with no failing tests, no lint errors, no type errors, no coverage regression below 89%
</acceptance_criteria>
  <verify>
    <automated>cd backend && ruff check . && ruff format --check . && pytest -v && cd ../frontend && npx tsc -b && npm test && npm run build && npm run test:e2e</automated>
  </verify>
  <done>Every automated check across backend and frontend passes cleanly — Phase 34 (LANG-01 + LANG-02) is fully implemented, tested, and ready for final commit + push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A — verification-only plan | This plan adds test coverage and runs regression checks; it introduces no new production code path beyond the E2E spec file itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-34-14 | N/A | N/A | accept | No new trust boundary introduced by this plan; all threats already covered by 34-01 through 34-09's registers |
</threat_model>

<verification>
- `cd frontend && npm run test:e2e -- anonymous-avatar-voice-es.spec.ts` — green
- Full regression suite (Task 2's 6 commands) — all green
</verification>

<success_criteria>
- LANG-02 fully satisfied: es-* neural voice sessions negotiate correctly (mocked E2E-proven), anonymous text-chat locale forwarding fixed, refusal templates complete, admin voice_map editable end-to-end
- Entire Phase 34 (LANG-01 + LANG-02) passes its full automated regression suite with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/34-spanish-es-i18n/34-10-SUMMARY.md`
</output>
