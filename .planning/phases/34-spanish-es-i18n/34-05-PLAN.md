---
phase: 34-spanish-es-i18n
plan: 05
type: execute
wave: 3
depends_on: ["34-02", "34-03", "34-04"]
files_modified:
  - frontend/src/pages/admin/settings.tsx
  - frontend/src/pages/admin/settings.test.tsx
  - frontend/e2e/language-switcher-es.spec.ts
autonomous: true
requirements: [LANG-01]

must_haves:
  truths:
    - "The full 16-namespace x 5-locale parity suite is green with zero exceptions"
    - "A human/CI run of Playwright can switch the live app to es-ES, es-MX, and es-US and observe translated UI text with no English fallback"
    - "The admin settings page's secondary 2-option language Select is now a 5-option Select, consistent with the switcher"
  artifacts:
    - path: "frontend/e2e/language-switcher-es.spec.ts"
      provides: "E2E coverage: switch to es-ES/es-MX/es-US, assert translated text renders, assert no i18next missing-key console warning"
    - path: "frontend/src/pages/admin/settings.tsx"
      provides: "5-option defaultLanguage Select (was 2-option), per UI-SPEC Concern #1 resolved in-scope"
  key_links:
    - from: "frontend/e2e/language-switcher-es.spec.ts"
      to: "frontend/src/components/shared/language-switcher.tsx"
      via: "Playwright click on the switcher trigger + dropdown item"
      pattern: "getByRole\\(.button.*switch language|getByText\\(.Español"
---

<objective>
Close out LANG-01: run the full parity suite as a hard gate, add Playwright E2E proof that switching to each es-* locale renders real translated text on the live app (not just JSON-level parity), and resolve the UI-SPEC's flagged Concern #1 by extending `settings.tsx`'s secondary hardcoded language Select from 2 to 5 options for consistency.

Purpose: this is the mandatory "100% unit test + Playwright E2E green + commit + push" gate that CLAUDE.md's top-priority rule requires before LANG-02 (voice) work may begin — no LANG-02 plan may start until this plan's tasks are all green.
Output: full parity suite proof, a passing es-* Playwright spec, and a consistent 5-option settings.tsx Select.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-spanish-es-i18n/34-VALIDATION.md
@.planning/phases/34-spanish-es-i18n/34-UI-SPEC.md

<interfaces>
Current `frontend/src/pages/admin/settings.tsx` (per prior investigation, lines ~50-57): a `defaultLanguage` local state + a `<Select>` with exactly 2 `<SelectItem value="zh-CN">{tc("lang.zhCN")}</SelectItem>` / `<SelectItem value="en-US">{tc("lang.enUS")}</SelectItem>` entries inside a "Language Settings" Card. This Select is NOT wired to any runtime `i18n.changeLanguage` call (it is a static/mock "org default language" setting per UI-SPEC Concern #1) — extending it to 5 options is a pure UI-consistency fix, not a functional/logic change. Use the same `tc(...)` translation-function call already in scope, with keys `lang.esES`/`lang.esMX`/`lang.esUS` (already populated in `common.json` by 34-01 Task 3).

Existing E2E mock pattern to reuse (from `frontend/e2e/anonymous-avatar-voice.spec.ts`, read in full during research): `mockSession(page)` helper for session-token stubbing. For this plan's new spec, no avatar-session mocking is needed — it purely exercises the switcher + i18next-http-backend's real fetch of `/locales/{{lng}}/{{ns}}.json` (served by the dev/preview server, no network mock required since these are real static files now that 34-01..04 created them).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run and fix the full parity suite as a hard LANG-01 gate</name>
  <files>frontend/src/i18n/locale-parity.test.ts, frontend/src/i18n/untranslated-whitelist.ts</files>
  <read_first>frontend/src/i18n/locale-parity.test.ts, frontend/src/i18n/untranslated-whitelist.ts</read_first>
  <action>
Run `cd frontend && npx vitest run src/i18n/locale-parity.test.ts` (full, unfiltered — all 16 namespaces x 5 locales). This aggregates every namespace file created across 34-01 through 34-04.

If any test fails:
- Key-set mismatch → open the offending namespace's es-* file and en-US file side by side, add/remove keys to match en-US's structure exactly.
- Empty-value failure → fill in the missing translation for that specific key.
- Interpolation-token mismatch → fix the placeholder name in the es-* value to match en-US's `{{var}}` name exactly (do not just delete the placeholder).
- Untranslated-value failure → either provide a genuine translation, or if the value is a legitimate brand/proper-noun exact match, add `"{namespace}.{key}"` to `UNTRANSLATED_WHITELIST` (only if the array stays at or under 15 total entries after adding; if it would exceed 15, translate the value instead).
- Whitelist-guardrail failure (length > 15) → review every entry for genuine justification; remove any that should instead be translated.

Do not weaken any assertion in `locale-parity.test.ts` itself to make it pass — fix the underlying locale JSON files or the whitelist, never the test logic.
</action>
  <acceptance_criteria>
- `cd frontend && npx vitest run src/i18n/locale-parity.test.ts` — exits 0, 0 failed tests, all 16 namespaces x 5 locales covered
- `grep -c '^\s*"' frontend/src/i18n/untranslated-whitelist.ts` reflects <= 15 whitelist string entries (excluding comments)
</acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/i18n/locale-parity.test.ts</automated>
  </verify>
  <done>Full parity suite green with zero failures across the entire 16-namespace x 5-locale matrix; whitelist stays within the 15-entry guardrail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend settings.tsx language Select to 5 options + update its test</name>
  <files>frontend/src/pages/admin/settings.tsx, frontend/src/pages/admin/settings.test.tsx</files>
  <read_first>frontend/src/pages/admin/settings.tsx, frontend/src/pages/admin/settings.test.tsx</read_first>
  <behavior>
    - Test: the Language Settings Select renders 5 `SelectItem`s with values `zh-CN`, `en-US`, `es-ES`, `es-MX`, `es-US`
  </behavior>
  <action>
In `frontend/src/pages/admin/settings.tsx`, locate the "Language Settings" Card's `<Select>` (2 existing `<SelectItem>` entries for `zh-CN`/`en-US`). Append 3 more `<SelectItem>` entries using the same `tc(...)` call pattern already used for the first 2:
```tsx
<SelectItem value="es-ES">{tc("lang.esES")}</SelectItem>
<SelectItem value="es-MX">{tc("lang.esMX")}</SelectItem>
<SelectItem value="es-US">{tc("lang.esUS")}</SelectItem>
```
Do not add any new state, handler, or i18n.changeLanguage wiring — this Select remains the existing static/mock org-default-language setting (UI-SPEC Concern #1 explicitly scoped this as a trivial consistency addition, not a functional change).

In `frontend/src/pages/admin/settings.test.tsx`, update or add an assertion confirming all 5 `SelectItem` values are present (mirror whatever existing test pattern already checks the 2 current options — if the test opens the Select and queries option text, add 3 more `getByText`/`getByRole("option")` assertions for the es-* labels).
</action>
  <acceptance_criteria>
- `grep -c 'SelectItem value="' frontend/src/pages/admin/settings.tsx` includes at least 5 occurrences within the Language Settings Select block
- `cd frontend && npx vitest run src/pages/admin/settings.test.tsx` — all tests pass, 0 failures
</acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/admin/settings.test.tsx</automated>
  </verify>
  <done>settings.tsx's Language Settings Select has 5 options matching the switcher's 5 locales; settings.test.tsx asserts all 5 are present and passes.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright E2E — switch to each es-* locale, assert translated UI</name>
  <files>frontend/e2e/language-switcher-es.spec.ts</files>
  <read_first>frontend/e2e/anonymous-avatar-voice.spec.ts (mock/setup pattern reference only), frontend/src/components/shared/language-switcher.tsx</read_first>
  <action>
Create `frontend/e2e/language-switcher-es.spec.ts` with one `test()` per es-* locale (es-ES, es-MX, es-US), each:
1. Navigate to `/` (the app's public/avatar landing route — reuse whatever base route the existing `anonymous-avatar-voice.spec.ts` navigates to first, before any session-specific mocking).
2. Click the language-switcher trigger button (`getByRole("button", { name: /switch language/i })` — matches the existing component's aria-label/accessible name).
3. Click the target locale's dropdown item — locate it by its rendered native-language label text (e.g. `getByText("Español (España)")` for es-ES, `getByText("Español (México)")` for es-MX, `getByText("Español (EE. UU.)")` for es-US) since real i18next translations are loaded in E2E (unlike the mocked unit test).
4. Assert `localStorage.getItem("i18nextLng")` equals the target locale code (via `page.evaluate(() => localStorage.getItem("i18nextLng"))`).
5. Assert at least one piece of translated UI chrome text is visible post-switch (e.g. the switcher's own re-rendered trigger, or a known static label from `common.json`/`avatar.json` that differs from its en-US source) — pick a string that is genuinely different across locales (do not use a `lang.*` identity label, since those are intentionally identical per the fixed-identity-label convention).
6. Assert there is no browser console message matching `/missingKey/i` (i18next's built-in missing-key warning signature) throughout the whole flow — attach a `page.on("console", ...)` listener before step 1 and assert it never fired that pattern.

Use `test.describe.each` or 3 separate `test()` blocks (whichever matches this file's sibling `.spec.ts` conventions in `frontend/e2e/`) — no new Playwright config or fixture file needed.
</action>
  <acceptance_criteria>
- `frontend/e2e/language-switcher-es.spec.ts` exists and exports 3 test cases (one per es-* locale)
- `cd frontend && npm run test:e2e -- language-switcher-es.spec.ts` — all 3 tests pass, 0 failures, 0 missingKey console warnings observed
</acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test:e2e -- language-switcher-es.spec.ts</automated>
  </verify>
  <done>Playwright proves, against the real running app, that switching to es-ES/es-MX/es-US persists the locale, renders genuinely-translated UI text, and never triggers an i18next missing-key console warning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser client -> localStorage `i18nextLng` | Same as 34-01 T-34-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-34-06 | Tampering | settings.tsx defaultLanguage Select (mock/static, no backend write path) | accept | No backend mutation occurs from this control in this phase; purely cosmetic org-setting placeholder, no security surface introduced |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/i18n/locale-parity.test.ts src/pages/admin/settings.test.tsx` — all green
- `cd frontend && npm run test:e2e -- language-switcher-es.spec.ts` — all green
- `cd frontend && npx tsc -b` — no type errors
</verification>

<success_criteria>
- LANG-01 is fully satisfied: 16-namespace x 5-locale automated key-parity check green, switcher UI E2E-proven for all 3 es-* variants, no missing-key fallback observed
- This plan's completion is the CLAUDE.md-mandated checkpoint: commit + push before any LANG-02 plan (34-06 onward) begins
</success_criteria>

<output>
After completion, create `.planning/phases/34-spanish-es-i18n/34-05-SUMMARY.md`
</output>
