---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ".claude/settings.local.json"
  - ".claude/commands/gsd/**"
  - ".claude/get-shit-done/**"
  - ".claude/agents/**"
  - ".planning/**"
  - "docs/**"
  - "wiki/**"
  - "infra/azure/**"
  - "README.md"
  - "CLAUDE.md"
  - ".continue-here.md"
  - "frontend/index.html"
  - "frontend/package.json"
  - "frontend/package-lock.json"
  - "frontend/src/stores/theme-store.ts"
  - "frontend/src/stores/theme-store.test.ts"
  - "backend/pyproject.toml"
  - "backend/.env.example"
  - "backend/app/config.py"
  - "backend/app/api/internal_openai_proxy.py"
  - "backend/app/services/prompt_optimizer_client.py"
autonomous: true
requirements: []
must_haves:
  truths:
    - "No git-tracked file contains the old repository name string 'AI-Coach-vibe-coding'"
    - "No file under .planning/ contains the bare product name 'AI Coach'"
    - "README.md, CLAUDE.md, and .planning/PROJECT.md describe the product as 'AI Avatar Platform' with the new avatar positioning (anonymous mode, personalized/CRM mode, Spanish support, source-separated UI)"
    - "frontend/package.json, frontend/package-lock.json, and backend/pyproject.toml use the 'ai-avatar-*' package name prefix instead of 'ai-coach-*'"
    - "frontend/index.html title and theme-store.ts localStorage keys use 'ai-avatar' instead of 'ai-coach'"
    - "Backend passes ruff check + format check + smoke pytest; frontend passes tsc -b + npm run build after the rename"
  artifacts:
    - path: "README.md"
      provides: "AI Avatar Platform title and rewritten positioning section"
    - path: "CLAUDE.md"
      provides: "AI Avatar Platform header, AI Avatar Domain Rules, rewritten Project block"
    - path: ".planning/PROJECT.md"
      provides: "Rewritten What This Is / Core Value for avatar positioning"
    - path: "frontend/index.html"
      provides: "New title + renamed localStorage keys"
    - path: "frontend/src/stores/theme-store.ts"
      provides: "Renamed THEME_STORAGE_KEY / ACCENT_STORAGE_KEY"
    - path: "backend/pyproject.toml"
      provides: "Renamed package name ai-avatar-backend"
  key_links:
    - from: "frontend/index.html inline bootstrap script"
      to: "frontend/src/stores/theme-store.ts"
      via: "matching localStorage keys"
      pattern: "ai-avatar-(theme|accent)"
    - from: "backend/pyproject.toml [project] name"
      to: "backend/pyproject.toml [project.optional-dependencies] all group self-reference"
      via: "literal package name string"
      pattern: "ai-avatar-backend\\[dev,postgresql,voice\\]"
---

<objective>
Finish the project rename from "AI-Coach-vibe-coding" to "AI-avatar-vibe-coding": eliminate every remaining reference to the old repository name/path across the tracked repo, rewrite the product positioning (CLAUDE.md, README.md, .planning/PROJECT.md, frontend title) to "AI Avatar Platform" reflecting the new avatar requirements (anonymous mode, personalized/CRM mode, Spanish support, source-separated UI), rename the `ai-coach-*` package/config identifiers to `ai-avatar-*`, and verify nothing broke.

Purpose: The working directory has already been physically renamed/copied to `AI-avatar-vibe-coding`, but ~300 tracked files still reference the old name (hardcoded absolute paths in `.claude/`, historical `.planning/` docs, infra config, package names). Leaving these stale breaks GSD tooling (which reads absolute paths) and misrepresents the product going forward. This is prep work before building the new avatar features.
Output: All tracked files free of the old repo-name token; README/CLAUDE/PROJECT.md carry the new avatar positioning; package/config names use `ai-avatar-*`; backend + frontend build/test green.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260731-q3c-ai-coach-vibe-coding-ai-avatar-vibe-codi/260731-q3c-CONTEXT.md
</context>

<scope_note>
**IMPORTANT — deliberate scope boundary (per CONTEXT.md decisions):**

This plan does NOT rename the bare product name "AI Coach" everywhere in the repo — only in: (a) files under `.planning/**` (decision: "全部替换"), and (b) the four specific files called out for positioning rewrite: `README.md`, `CLAUDE.md`, `.planning/PROJECT.md`, `frontend/index.html`. It intentionally leaves "AI Coach" branding untouched in `docs/**`, `wiki/**`, frontend UI components (`user-layout.tsx`, `admin-layout.tsx`, `auth-layout.tsx`, locale JSON files under `frontend/public/locales/`), and their associated tests/E2E specs — none of these were named in CONTEXT.md decisions, and several have hardcoded test assertions (`admin-layout.test.tsx`, `auth-layout.test.tsx`, `user-layout.test.tsx`, `frontend/e2e/health.spec.ts`) that would break if the brand text changed. Do not "fix" these as a drive-by — that is a separate, larger UI rebrand not authorized by this task.

The hyphenated repository-name token `AI-Coach-vibe-coding` (and its use as an absolute path segment) IS replaced everywhere per CONTEXT.md decision ("必须修复：`.claude/` GSD 工具链中硬编码的绝对路径 ... 否则 GSD 命令会读取旧目录").
</scope_note>

<tasks>

<task type="auto">
  <name>Task 1: Mechanical bulk replace of the repo name/path token across the whole repo</name>
  <files>~300 git-tracked text files (see files_modified glob roots); no single file list is enumerable — see action for exact selection command</files>
  <action>
Run this from the repo root (`/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding`). Do NOT touch anything under `/Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding` (the old directory) — all commands below operate only on `git ls-files` output from the current (new) working tree, which cannot resolve to the old directory.

**Pass 1 — repo-wide: replace the hyphenated repo-name token everywhere it appears (paths, docs, config, `.claude/` hardcoded absolute paths, `.planning/**` history).** `grep -Il` skips binary files automatically (no need to special-case `.pen` or images), and `git ls-files` naturally excludes `node_modules/`, `.venv/`, `.omc/`, `.nyc_output/`, and `.claude/worktrees/` (all gitignored/untracked):

```bash
cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding
while IFS= read -r f; do
  sed -i '' 's/AI-Coach-vibe-coding/AI-avatar-vibe-coding/g' "$f"
done < <(git ls-files | xargs grep -Il 'AI-Coach-vibe-coding' 2>/dev/null)
```

**Pass 2 — `.planning/**` only: replace the bare product name "AI Coach Platform" first (longer match), then remaining bare "AI Coach" (shorter match).** Order matters — doing the longer phrase first avoids producing "AI Avatar Platform Platform":

```bash
cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding
while IFS= read -r f; do
  sed -i '' 's/AI Coach Platform/AI Avatar Platform/g' "$f"
done < <(git ls-files -- .planning | xargs grep -Il 'AI Coach Platform' 2>/dev/null)

while IFS= read -r f; do
  sed -i '' 's/AI Coach/AI Avatar/g' "$f"
done < <(git ls-files -- .planning | xargs grep -Il 'AI Coach' 2>/dev/null)
```

This pass intentionally covers every `.planning/phases/**`, `.planning/quick/**` (including this plan's own CONTEXT.md/PLAN.md, per CONTEXT.md decision: "叙述性文字中的旧路径也替换"), `.planning/codebase/**`, ROADMAP.md, STATE.md, REQUIREMENTS.md, etc. Do not manually curate exceptions — the decision was "全部替换" (replace all).

After both passes, `.planning/PROJECT.md` line 1 will read `# AI Avatar Platform — BeiGene` (Task 2 rewrites the body paragraphs below it).
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding && R1=$(git ls-files | xargs grep -l "AI-Coach-vibe-coding" 2>/dev/null | wc -l | tr -d ' ') && R2=$(git ls-files -- .planning | xargs grep -l "AI Coach" 2>/dev/null | wc -l | tr -d ' ') && echo "remaining_repo_token=$R1 remaining_planning_ai_coach=$R2" && test "$R1" = "0" && test "$R2" = "0"</automated>
  </verify>
  <done>Zero tracked files contain "AI-Coach-vibe-coding"; zero files under .planning/ contain "AI Coach".</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite product positioning and rename ai-coach-* identifiers to ai-avatar-*</name>
  <files>README.md, CLAUDE.md, .planning/PROJECT.md, frontend/index.html, frontend/src/stores/theme-store.ts, frontend/src/stores/theme-store.test.ts, frontend/package.json, frontend/package-lock.json, backend/pyproject.toml, backend/.env.example, backend/app/config.py, backend/app/api/internal_openai_proxy.py, backend/app/services/prompt_optimizer_client.py, infra/azure/main.bicep, infra/azure/scripts/build-and-push.ps1</files>
  <action>
Perform these edits in order. Use the Edit tool with exact old_string/new_string matches for the prose rewrites (items 1-3), and simple sed one-liners for the mechanical key/value renames (items 4-15).

**1. `CLAUDE.md`** — three edits:
- Header: `# CLAUDE.md — AI Coach Platform Engineering Handbook` → `# CLAUDE.md — AI Avatar Platform Engineering Handbook`
- Replace the entire `## AI Coach Domain Rules` section (the heading plus its 6 numbered rules, located right after the Gotcha List table and before `## CI Pipeline Overview`) with:
  ```markdown
  ## AI Avatar Domain Rules

  1. **Avatar Sessions** have a lifecycle: `created → active → ended`, tagged as `anonymous` or `personalized`
  2. **Anonymous mode** requires no login — avatar answers are grounded in official website content indexed into Azure AI Foundry IQ
  3. **Personalized mode** requires login — avatar responses are shaped by CRM-derived user profile data (POC: Excel-based user-profile mapping table, no live CRM integration)
  4. **User preferences** are injected via system prompt / prompt template at session start, keyed by `user_id`; preference extraction is backend-driven or manually tagged (deep memory mechanisms deferred post-POC)
  5. **Multi-language** support is mandatory across UI, voice, and avatar output for Chinese, English, and Spanish
  6. **Source separation** — the digital human's spoken/text response and the underlying source document/page link must render as separate UI elements, never merged into one bubble
  7. **All avatar interactions** must be auditable (traceable knowledge source per response)
  ```
- Replace the content between `<!-- GSD:project-start source:PROJECT.md -->` and `<!-- GSD:project-end -->` with:
  ```markdown
  ## Project

  **AI Avatar Platform**

  An AI-powered digital human (avatar) platform built on Azure PaaS. It offers two interaction modes: an anonymous, no-login mode where the avatar answers visitor questions grounded in official website content indexed into Azure AI Foundry IQ, and a personalized, login-required mode where the avatar responds using CRM-derived user profile data (POC: Excel-based mapping, no live CRM integration) and remembers user preferences injected via system prompt/template. The platform supports Chinese, English, and Spanish, with a clean UI that shows only the digital human plus source document links — voice content and document display are kept visually separate.

  **Core Value:** Visitors and logged-in users get instant, accurate, multi-language answers from a digital human grounded in trusted knowledge sources — anonymous users draw from public site content, logged-in users get personalized answers shaped by their own profile and preferences.

  ### Constraints

  - **Cloud**: Azure PaaS only (no AWS) — Azure OpenAI, Speech, Avatar, AI Foundry (incl. Foundry IQ), PostgreSQL
  - **i18n**: Must support Chinese + English + Spanish
  - **Auth**: Anonymous mode requires no login; personalized mode requires login and CRM-derived profile lookup (POC: Excel mapping, no live CRM integration)
  - **Memory**: User preference injection via system prompt/template keyed by user id; deep automated memory extraction deferred post-POC (backend extraction or manual tagging only)
  - **Budget**: Azure AI Avatar is premium — implement as configurable option, fall back to Azure Speech TTS
  - **Frontend**: Must be responsive — same app works on desktop, tablet, mobile, and Teams Tab
  ```
  Keep the `<!-- GSD:project-start source:PROJECT.md -->` / `<!-- GSD:project-end -->` comment markers themselves unchanged.

**2. `README.md`** — three edits:
- `# AI Coach Platform` → `# AI Avatar Platform`
- The subtitle block right below it (`<strong>AI-Powered Medical Representative Training Platform</strong><br/>` / `<em>面向医药代表的 AI 智能教练平台 — BeiGene (百济神州)</em>`) → replace both lines with `<strong>AI-Powered Digital Human Avatar Platform</strong><br/>` and `<em>基于 Azure AI 的智能数字人 Avatar 平台 — 官网问答 + 登录个性化知识助手</em>`
- Replace the `## What is AI Coach?` section (heading + the two paragraphs under it, ending right before `## Highlight: Voice Live + Digital Human Agent`) with:
  ```markdown
  ## What is AI Avatar Platform?

  AI Avatar Platform 是一个基于 **Azure AI Services** 的智能数字人问答平台，通过 **实时语音对话 + 数字人形象 (Avatar)** 提供两种交互模式：

  - **匿名模式**（无需登录）：数字人基于官方网站内容知识（已索引至 Azure AI Foundry IQ）回答访客问题
  - **登录模式**（个性化）：数字人基于 CRM 关联的用户画像回答问题（POC 阶段以 Excel 对应关系表模拟，不做真实 CRM 集成），并记住用户偏好（通过 system prompt / prompt template 按 userid 注入）
  - **多语言**：支持中文、英文、西班牙语
  - **清爽 UI**：仅展示数字人形象与来源文档链接，语音内容与文档展示分离呈现

  **核心价值**：访客和登录用户都能获得即时、准确、多语言的数字人问答体验 — 匿名用户基于公开官网知识，登录用户则获得基于个人画像与偏好的个性化回答。
  ```
  Leave everything from `## Highlight: Voice Live + Digital Human Agent` onward untouched (out of scope — full feature-list rewrite is a separate future task).

**3. `.planning/PROJECT.md`** — replace the `## What This Is` and `## Core Value` sections (between the title line and `## Requirements`) with:
  ```markdown
  ## What This Is

  An AI-powered digital human (avatar) platform for BeiGene (百济神州), built on Azure PaaS. It provides an anonymous, no-login mode where the avatar answers questions grounded in official website content indexed into Azure AI Foundry IQ, and a personalized, login-required mode where the avatar responds using CRM-derived user profile data (POC: Excel-based mapping, no live CRM integration) and remembers user preferences injected via system prompt/template. Supports Chinese, English, and Spanish, with a clean UI showing only the digital human plus source document links.

  ## Core Value

  Visitors and logged-in users get instant, accurate, multi-language answers from a digital human grounded in trusted knowledge sources — anonymous users draw from public site content, logged-in users get personalized answers shaped by their own profile and preferences.
  ```
  Leave `## Requirements` and everything below untouched (Task 1 already fixed the title line to `# AI Avatar Platform — BeiGene`).

**4. `frontend/index.html`** — three line edits:
- `<title>AI Coach Platform</title>` → `<title>AI Avatar Platform</title>`
- `localStorage.getItem('ai-coach-theme')` → `localStorage.getItem('ai-avatar-theme')`
- `localStorage.getItem('ai-coach-accent')` → `localStorage.getItem('ai-avatar-accent')`

**5. `frontend/src/stores/theme-store.ts`** — rename the two storage-key constants:
- `const THEME_STORAGE_KEY = "ai-coach-theme";` → `const THEME_STORAGE_KEY = "ai-avatar-theme";`
- `const ACCENT_STORAGE_KEY = "ai-coach-accent";` → `const ACCENT_STORAGE_KEY = "ai-avatar-accent";`
No backward-compat fallback needed — this is a POC per CONTEXT.md decision ("无需兼容旧 key").

**6. `frontend/src/stores/theme-store.test.ts`** — update the two matching assertions so the test suite stays in sync with #5:
- `expect(localStorage.getItem("ai-coach-theme")).toBe("dark");` → `expect(localStorage.getItem("ai-avatar-theme")).toBe("dark");`
- `expect(localStorage.getItem("ai-coach-accent")).toBe("teal");` → `expect(localStorage.getItem("ai-avatar-accent")).toBe("teal");`

**7. `frontend/package.json`** — `"name": "ai-coach-frontend"` → `"name": "ai-avatar-frontend"`

**8. `frontend/package-lock.json`** — same token appears exactly twice (root `name` + root package `name`); run:
  ```bash
  sed -i '' 's/ai-coach-frontend/ai-avatar-frontend/g' frontend/package-lock.json
  ```

**9. `backend/pyproject.toml`** — two sed passes:
  ```bash
  sed -i '' 's/ai-coach-backend/ai-avatar-backend/g' backend/pyproject.toml
  sed -i '' 's/AI Coach Training Platform - Backend API/AI Avatar Platform - Backend API/' backend/pyproject.toml
  ```
  (The first sed catches both the `[project] name = "ai-coach-backend"` line and the `all = ["ai-coach-backend[dev,postgresql,voice]"]` self-reference — verify both changed.)

**10. `backend/.env.example`** — `APP_NAME=AI Coach Platform` → `APP_NAME=AI Avatar Platform`

**11. `backend/app/config.py`** — `app_name: str = "AI Coach Platform"` → `app_name: str = "AI Avatar Platform"`

**12. `backend/app/api/internal_openai_proxy.py`** — `"owned_by": "ai-coach"` → `"owned_by": "ai-avatar"`

**13. `backend/app/services/prompt_optimizer_client.py`** — `"clientInfo": {"name": "ai-coach-backend", "version": "0.1.0"}` → `"clientInfo": {"name": "ai-avatar-backend", "version": "0.1.0"}`

**14. `infra/azure/main.bicep`** — the resource tag `project: 'ai-coach'` (inside the tags block — NOT the `githubRepo` param default, which Task 1's Pass 1 already fixed since it is the hyphenated repo-name token) → `project: 'ai-avatar'`

**15. `infra/azure/scripts/build-and-push.ps1`** — rename the local container image tag variables (build-script-local names, not live Azure resource identifiers, so safe to rename):
  ```bash
  sed -i '' 's/ai-coach-backend/ai-avatar-backend/; s/ai-coach-frontend/ai-avatar-frontend/' infra/azure/scripts/build-and-push.ps1
  ```

**Explicitly do NOT rename:** `AZURE_RESOURCE_GROUP` values (`ai-coach-public-rg`, `ai-coach-private-rg`) in `infra/azure/environments/*.json` and their mentions in `infra/azure/README.md` / `infra/azure/docs/operations.md` — these identify real, potentially already-deployed Azure resource groups. Renaming the config string without actually renaming (or re-creating) the live Azure resource group would break deployments. This is out of scope for a text rename; flag it to the user in the SUMMARY if they want an actual infra migration.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding && for f in README.md CLAUDE.md .planning/PROJECT.md frontend/index.html; do grep -q "AI Avatar Platform" "$f" || { echo "FAIL missing new name: $f"; exit 1; }; done && grep -q "ai-avatar-frontend" frontend/package.json && grep -q "ai-avatar-frontend" frontend/package-lock.json && grep -q "ai-avatar-backend" backend/pyproject.toml && grep -q "ai-avatar-theme" frontend/src/stores/theme-store.ts && grep -q "ai-avatar-theme" frontend/index.html && grep -q "ai-avatar-theme" frontend/src/stores/theme-store.test.ts && grep -q "ai-avatar" infra/azure/main.bicep && grep -q "ai-avatar-backend" infra/azure/scripts/build-and-push.ps1 && echo OK</automated>
  </verify>
  <done>README.md/CLAUDE.md/.planning/PROJECT.md/frontend/index.html show "AI Avatar Platform"; frontend/backend package names, theme-store storage keys, and the two backend literal identifiers all use ai-avatar-*; infra bicep tag and build script image names renamed; AZURE_RESOURCE_GROUP values deliberately left unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Verify nothing broke — lint, smoke tests, type-check, build</name>
  <files>none (verification only, no source files modified unless a failure requires a targeted fix)</files>
  <action>
Run backend and frontend verification in sequence; fix any failures found before considering this plan done.

**Backend:**
```bash
cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend
ruff check .
ruff format --check .
python -m pytest tests/test_health.py tests/test_prompt_optimizer_client.py tests/test_prompts_management_api.py tests/test_prompts_optimize_api.py tests/test_config_api.py -q
```
This is a smoke subset (per CONTEXT.md decision: "后端 ruff check + pytest 冒烟") covering the files whose literals changed (prompt_optimizer_client.py clientInfo name, config.py app_name) plus the health/config API surface — not the full suite, since no business logic changed, only identifiers and prose.

**Frontend:**
```bash
cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend
npx tsc -b
npx vitest run src/stores/theme-store.test.ts
npm run build
```

If any command fails, inspect and fix the specific file it points to — do not re-run the bulk sed passes from Task 1/2.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend && ruff check . && ruff format --check . && python -m pytest tests/test_health.py tests/test_prompt_optimizer_client.py tests/test_prompts_management_api.py tests/test_prompts_optimize_api.py tests/test_config_api.py -q && cd ../frontend && npx tsc -b && npx vitest run src/stores/theme-store.test.ts && npm run build</automated>
  </verify>
  <done>Backend ruff check/format pass, smoke pytest subset passes; frontend tsc -b, theme-store vitest run, and npm run build all pass with zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None introduced | This is a text/identifier rename with no new code paths, inputs, or trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Information Disclosure | Bulk sed across ~300 tracked files | accept | All changes are cosmetic string renames (repo name, product name, package identifiers) in a private dev repo; `.env` files are gitignored and out of `git ls-files` scope, so no secrets are touched |
| T-quick-02 | Tampering | Renamed backend/frontend package identifiers | accept | Identifiers (`ai-avatar-backend`, `ai-avatar-frontend`) are internal labels only (pyproject/package.json `name`, OpenAI-proxy `owned_by`, MCP `clientInfo.name`) — no external consumer depends on the old value |
</threat_model>

<verification>
- Task 1 verify passes: zero remaining "AI-Coach-vibe-coding" tokens repo-wide, zero remaining "AI Coach" under .planning/
- Task 2 verify passes: README.md/CLAUDE.md/.planning/PROJECT.md/frontend/index.html all show "AI Avatar Platform"; all ai-coach-* identifiers renamed to ai-avatar-*
- Task 3 verify passes: backend ruff + smoke pytest green; frontend tsc -b + theme-store vitest + npm run build green
- Manual spot-check: `git diff --stat` shows only expected files touched (no accidental edits inside docs/**, wiki/**, or frontend UI component/locale files per the scope_note)
</verification>

<success_criteria>
- Zero tracked files reference the old repository name "AI-Coach-vibe-coding"
- .claude/settings.local.json and all GSD tooling files use the new absolute path — GSD commands will read the new (not old) directory
- README.md, CLAUDE.md, and .planning/PROJECT.md present "AI Avatar Platform" with the new avatar positioning (anonymous mode, personalized/CRM mode, Spanish support, source-separated UI)
- frontend/package.json, package-lock.json, backend/pyproject.toml, and theme-store localStorage keys use ai-avatar-* naming
- Backend lint/smoke-test and frontend typecheck/build all pass after the rename
- "AI Coach" branding in docs/**, wiki/**, and frontend UI components/locales is deliberately left untouched (documented boundary, not an oversight)
</success_criteria>

<output>
After completion, create `.planning/quick/260731-q3c-ai-coach-vibe-coding-ai-avatar-vibe-codi/260731-q3c-SUMMARY.md`
</output>
