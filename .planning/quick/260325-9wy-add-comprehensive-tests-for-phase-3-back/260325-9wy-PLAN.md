---
phase: quick
plan: 260325-9wy
type: execute
wave: 1
depends_on: []
files_modified:
  # Backend test files (new + modified)
  - backend/tests/test_sessions_api_extended.py
  - backend/tests/test_scoring_api_extended.py
  - backend/tests/test_schemas_rubric_validation.py
  # Frontend vitest files (new)
  - frontend/src/hooks/use-rubrics.test.tsx
  - frontend/src/hooks/use-reports.test.tsx
  - frontend/src/api/rubrics.test.ts
  - frontend/src/api/reports.test.ts
  - frontend/src/components/admin/rubric-table.test.tsx
  - frontend/src/components/admin/rubric-editor.test.tsx
  - frontend/src/components/scoring/report-section.test.tsx
  - frontend/src/pages/admin/scoring-rubrics.test.tsx
  - frontend/src/pages/user/session-history.test.tsx
  # E2E files (new)
  - frontend/e2e/admin-rubrics.spec.ts
  - frontend/e2e/session-history.spec.ts
autonomous: true
must_haves:
  truths:
    - "Backend pytest coverage is 95%+ overall with all Phase 3 files at 95%+"
    - "Frontend vitest tests exist for all Phase 3 hooks, API clients, components, and pages"
    - "E2E Playwright specs exist for admin rubric CRUD and user session history"
    - "All tests pass (pytest, vitest run, and playwright test compile)"
  artifacts:
    - path: "backend/tests/test_sessions_api_extended.py"
      provides: "Tests for sessions API uncovered lines (SSE message, end, messages, report, suggestions)"
    - path: "backend/tests/test_scoring_api_extended.py"
      provides: "Tests for scoring API router lines 23, 39-40, 55-58"
    - path: "backend/tests/test_schemas_rubric_validation.py"
      provides: "Tests for scoring_rubric.py schema validation edge cases"
    - path: "frontend/src/hooks/use-rubrics.test.tsx"
      provides: "Tests for all 4 rubric hooks"
    - path: "frontend/src/hooks/use-reports.test.tsx"
      provides: "Tests for report and suggestion hooks"
    - path: "frontend/src/pages/admin/scoring-rubrics.test.tsx"
      provides: "Tests for admin scoring rubrics page"
    - path: "frontend/src/pages/user/session-history.test.tsx"
      provides: "Tests for session history page"
    - path: "frontend/e2e/admin-rubrics.spec.ts"
      provides: "E2E test for rubric management flow"
    - path: "frontend/e2e/session-history.spec.ts"
      provides: "E2E test for session history page"
  key_links:
    - from: "backend/tests/test_sessions_api_extended.py"
      to: "backend/app/api/sessions.py"
      via: "HTTP client calls to session endpoints"
      pattern: "client\\.(get|post).*sessions"
    - from: "backend/tests/test_scoring_api_extended.py"
      to: "backend/app/api/scoring.py"
      via: "HTTP client calls to scoring endpoints"
      pattern: "client\\.(get|post).*scoring"
---

<objective>
Add comprehensive tests for Phase 3 (Scoring & Assessment) to achieve 95%+ backend coverage across all files and full frontend test coverage for Phase 3 components.

Purpose: The backend is at 96% overall but several Phase 3 files are below 95% (sessions.py at 76%, scoring.py at 70%, scoring_rubric.py at 91%). Frontend Phase 3 components (rubric CRUD pages, session history, report sections) have zero vitest unit tests. E2E coverage is missing for rubric admin and session history flows.

Output: New test files that bring backend coverage to 95%+ per-file for Phase 3 code, vitest tests for all Phase 3 frontend components/hooks/API clients, and E2E specs for rubric CRUD and session history.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/tests/conftest.py
@backend/tests/test_scoring_api.py
@backend/tests/test_sessions_api.py
@backend/app/api/sessions.py
@backend/app/api/scoring.py
@backend/app/schemas/scoring_rubric.py
@frontend/src/hooks/use-scoring.test.tsx
@frontend/src/pages/user/scoring-feedback.test.tsx
@frontend/e2e/scoring-feedback.spec.ts
@frontend/e2e/coverage-helper.ts
@frontend/e2e/auth.setup.ts
@frontend/vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend pytest coverage — close gaps in sessions, scoring, schemas</name>
  <files>
    backend/tests/test_sessions_api_extended.py
    backend/tests/test_scoring_api_extended.py
    backend/tests/test_schemas_rubric_validation.py
  </files>
  <action>
Create three new backend test files to cover the uncovered lines in Phase 3 backend code. Follow existing test patterns in conftest.py (in-memory SQLite, TestSessionLocal, httpx AsyncClient).

**1. `test_sessions_api_extended.py`** — Cover the 24% gap in `app/api/sessions.py`:
- Test `POST /api/v1/sessions/` (line 42: create session) — create a session, assert 201 with SessionResponse shape
- Test `GET /api/v1/sessions/` (line 54: list sessions) — list user's sessions with pagination, assert PaginatedResponse shape
- Test `GET /api/v1/sessions/active` (lines 64-71: active session) — test both when active session exists and when none exists (expect 404 with NO_ACTIVE_SESSION code)
- Test `GET /api/v1/sessions/{id}` (line 82: get session) — get specific session, assert response shape
- Test `POST /api/v1/sessions/{id}/message` (lines 95-113: SSE message) — test sending message to completed session returns 409 SESSION_CLOSED. For the SSE flow, test with a mock adapter registered in the registry: verify the EventSourceResponse returns text/hint/done events
- Test `POST /api/v1/sessions/{id}/end` (line 199: end session) — end an in_progress session, assert status changes to completed
- Test `GET /api/v1/sessions/{id}/messages` (lines 214-215: get messages) — get messages for a session, assert list of MessageResponse
- Test `GET /api/v1/sessions/{id}/report` (lines 227-228: get report) — get report for a scored session, assert SessionReport shape
- Test `GET /api/v1/sessions/{id}/suggestions` (lines 239-247: get suggestions) — get suggestions for a session, assert list of SuggestionResponse

Use the same `_setup_*` helper pattern from test_scoring_api.py. Create a complete session setup helper that creates user, HCP profile, scenario (with key_messages JSON, scoring_weights JSON), and a coaching session with messages.

**2. `test_scoring_api_extended.py`** — Cover the 30% gap in `app/api/scoring.py`:
- Test `GET /api/v1/scoring/history` (line 23) — get score history for user, test with 0 scores and with multiple scored sessions
- Test `POST /api/v1/scoring/sessions/{id}/score` (lines 39-40) — additional test: verify the response includes all expected fields (overall_score, details with 5 dimensions, passed, feedback_summary, session_id)
- Test `GET /api/v1/scoring/sessions/{id}/score` (lines 55-58) — test the NotFoundException path when session has no score

**3. `test_schemas_rubric_validation.py`** — Cover lines 33-34, 58-59, 85 in `app/schemas/scoring_rubric.py`:
- Test `RubricCreate` with dimensions that do NOT sum to 100 — expect ValidationError with message containing "must sum to 100"
- Test `RubricCreate` with dimensions that DO sum to 100 — expect success
- Test `RubricUpdate` with dimensions that do NOT sum to 100 — expect ValidationError
- Test `RubricUpdate` with dimensions=None — expect success (no validation triggered)
- Test `RubricResponse.parse_dimensions_json` with a JSON string input — expect it to parse correctly (line 85)
- Test `RubricResponse.parse_dimensions_json` with a list input — expect passthrough

All tests must be async (pytest-asyncio). Use `from tests.conftest import TestSessionLocal` for direct DB access in setup helpers.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/backend && python3 -m pytest tests/test_sessions_api_extended.py tests/test_scoring_api_extended.py tests/test_schemas_rubric_validation.py -v && python3 -m pytest tests/ --cov=app --cov-report=term-missing -q 2>&1 | grep -E "(scoring|sessions|rubric|TOTAL)" | grep -v "100%"</automated>
  </verify>
  <done>
    All new backend tests pass. Coverage for app/api/sessions.py >= 90%, app/api/scoring.py >= 95%, app/schemas/scoring_rubric.py >= 95%. Overall backend coverage remains >= 95%.
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend vitest tests for Phase 3 hooks, API clients, components, pages</name>
  <files>
    frontend/src/hooks/use-rubrics.test.tsx
    frontend/src/hooks/use-reports.test.tsx
    frontend/src/api/rubrics.test.ts
    frontend/src/api/reports.test.ts
    frontend/src/components/admin/rubric-table.test.tsx
    frontend/src/components/admin/rubric-editor.test.tsx
    frontend/src/components/scoring/report-section.test.tsx
    frontend/src/pages/admin/scoring-rubrics.test.tsx
    frontend/src/pages/user/session-history.test.tsx
  </files>
  <action>
Create vitest unit tests for all Phase 3 frontend code. Follow the established patterns from `use-scoring.test.tsx` (for hooks) and `scoring-feedback.test.tsx` (for pages).

**API client tests** (`api/rubrics.test.ts`, `api/reports.test.ts`):
Follow pattern from `api/api-clients.test.ts`. Mock `@/api/client` (the axios instance). Test each exported function calls the correct HTTP method and URL path. Verify params are passed through. Verify response data is returned.

- `rubrics.test.ts`: Test `getRubrics()` calls GET `/scoring/rubrics`, test `getRubrics({scenario_type: "f2f"})` passes params, test `createRubric(payload)` calls POST, test `updateRubric(id, payload)` calls PUT `/scoring/rubrics/{id}`, test `deleteRubric(id)` calls DELETE
- `reports.test.ts`: Test `getSessionReport(sessionId)` calls GET `/scoring/sessions/{sessionId}/report`, test `getSessionSuggestions(sessionId)` calls GET `/scoring/sessions/{sessionId}/suggestions`

**Hook tests** (`hooks/use-rubrics.test.tsx`, `hooks/use-reports.test.tsx`):
Follow pattern from `use-scoring.test.tsx`. Use `vi.mock("@/api/rubrics")` and `vi.mock("@/api/reports")`. Wrap hooks in QueryClientProvider with retry:false. Test query hooks fetch data, handle errors, and respect enabled flag. Test mutation hooks call API and invalidate queries.

- `use-rubrics.test.tsx`: Test `useRubrics()` fetches rubrics, `useRubrics({scenario_type: "f2f"})` passes params, `useCreateRubric()` mutation invalidates "rubrics" query key, `useUpdateRubric()` mutation invalidates, `useDeleteRubric()` mutation invalidates
- `use-reports.test.tsx`: Test `useSessionReport("sess1")` fetches report, `useSessionReport(undefined)` does not fetch, `useSessionSuggestions("sess1")` fetches suggestions, `useSessionSuggestions(undefined)` does not fetch

**Component tests** (`rubric-table.test.tsx`, `rubric-editor.test.tsx`, `report-section.test.tsx`):
Use `@testing-library/react` render + screen. Mock child dependencies and i18next with `vi.mock("react-i18next", ...)` returning `t: (key) => key`.

- `rubric-table.test.tsx`: Render with mock rubric data, verify table rows appear with rubric names, verify edit/delete buttons exist
- `rubric-editor.test.tsx`: Render editor dialog in create mode (no initial data), verify form fields for name/description/scenario_type exist, verify dimension weight inputs exist, verify submit button calls onSave
- `report-section.test.tsx`: Render with mock report data (strengths, weaknesses, quotes, improvements), verify sections render with headings and content

**Page tests** (`scoring-rubrics.test.tsx`, `session-history.test.tsx`):
Follow pattern from `scoring-feedback.test.tsx`. Mock hooks, react-router-dom, react-i18next.

- `scoring-rubrics.test.tsx`: Mock `useRubrics` returning mock data, verify page renders rubric table, verify "Create Rubric" button exists, test loading state, test empty state
- `session-history.test.tsx`: Mock `useScoreHistory` returning mock history data, verify page renders history list/table with session items, test loading state, test empty state, verify score trend display

Each test file should have at least 3-5 test cases covering: normal render, loading state, error/empty state, and user interactions where applicable.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx vitest run --reporter=verbose src/hooks/use-rubrics.test.tsx src/hooks/use-reports.test.tsx src/api/rubrics.test.ts src/api/reports.test.ts src/components/admin/rubric-table.test.tsx src/components/admin/rubric-editor.test.tsx src/components/scoring/report-section.test.tsx src/pages/admin/scoring-rubrics.test.tsx src/pages/user/session-history.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>
    All 9 new vitest test files pass. Each file has 3+ test cases. Phase 3 hooks, API clients, components, and pages have unit test coverage.
  </done>
</task>

<task type="auto">
  <name>Task 3: E2E Playwright specs for admin rubric CRUD and session history</name>
  <files>
    frontend/e2e/admin-rubrics.spec.ts
    frontend/e2e/session-history.spec.ts
  </files>
  <action>
Create two new Playwright E2E spec files following the existing patterns (import from `./coverage-helper`, use `.auth/admin.json` or `.auth/user.json` storageState, use `page.goto()`, assert with `expect`).

**1. `admin-rubrics.spec.ts`** — Admin rubric CRUD flow:
- Use `test.use({ storageState: join(authDir, "admin.json") })` for admin auth
- Test: "rubrics page loads and shows heading" — navigate to `/admin/scoring-rubrics`, verify page heading is visible
- Test: "rubrics page shows rubric table or empty state" — look for table rows or empty state text
- Test: "create rubric button exists" — verify a create/add button is visible
- Test: "rubric table shows default rubric if seeded" — check for the seeded default F2F rubric name in the table
- Test: "can open create rubric dialog" — click create button, verify dialog/form appears with name input field
- Keep tests defensive (check counts before asserting visibility) like existing E2E patterns, since data depends on seed state

**2. `session-history.spec.ts`** — User session history flow:
- Use `test.use({ storageState: join(authDir, "user.json") })` for user auth
- Test: "history page loads" — navigate to `/user/history`, verify page renders (heading visible)
- Test: "history page shows session list or empty state" — look for session items or empty state message
- Test: "history page displays score information when sessions exist" — look for score numbers or chart elements
- Test: "history page has navigation back to dashboard" — verify a link/button to dashboard exists

Both files should:
- Import `test, expect` from `./coverage-helper` (not `@playwright/test`) for coverage collection
- Use `dirname/join/fileURLToPath` pattern for auth dir path
- Use defensive assertions (check count > 0 before asserting visibility) since E2E data state may vary
- Keep timeouts reasonable (use `page.waitForTimeout(2000)` only when needed for data loading)
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-avatar-vibe-coding/frontend && npx tsc --noEmit -p e2e/tsconfig.json 2>/dev/null || npx tsc --noEmit --esModuleInterop --module nodenext --moduleResolution nodenext e2e/admin-rubrics.spec.ts e2e/session-history.spec.ts 2>/dev/null; echo "E2E specs compile check complete"</automated>
  </verify>
  <done>
    Two new E2E spec files exist and are syntactically valid TypeScript. admin-rubrics.spec.ts covers rubric CRUD page navigation and UI elements. session-history.spec.ts covers history page navigation and content display. Both follow project E2E patterns with coverage collection.
  </done>
</task>

</tasks>

<verification>
1. Backend: `cd backend && python3 -m pytest tests/ --cov=app --cov-report=term-missing -q` shows overall >= 95% and Phase 3 files (sessions.py, scoring.py, scoring_rubric.py) improved
2. Frontend vitest: `cd frontend && npx vitest run` passes all tests including 9 new Phase 3 test files
3. Frontend E2E: `cd frontend && npx tsc -b` type-checks pass (E2E files compile)
4. No regressions: existing tests continue to pass
</verification>

<success_criteria>
- Backend pytest overall coverage >= 95% (currently 96%, should maintain or improve)
- Backend app/api/scoring.py coverage >= 95% (currently 70%)
- Backend app/api/sessions.py coverage >= 85% (currently 76%, SSE internals are hard to fully cover)
- Backend app/schemas/scoring_rubric.py coverage >= 95% (currently 91%)
- 9 new frontend vitest test files, each with 3+ passing tests
- 2 new E2E Playwright spec files covering rubric admin and session history
- All existing tests continue to pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/quick/260325-9wy-add-comprehensive-tests-for-phase-3-back/260325-9wy-SUMMARY.md`
</output>
